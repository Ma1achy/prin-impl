# Principia build requirements — one entry per normative obligation in the corpus.
#
# id         REQ-<AREA>-<n>; AREA is one of DEC ENC CHART INT EVT PAY GEN SCHED REF RENDER COL GUI TOOL VAL PERF SYS.
#            Numbered at extraction in milestone order, then corpus order; later additions take the next free
#            number in the area. Ids are permanent: never renumber; a retired requirement keeps its id.
# kind       absent for an ordinary obligation. `calibration` (R-71): a value the corpus doesn't give; the task proposes
#            it with its evidence, a reviewer checks it, the human confirms it at the milestone gate, and it is
#            recorded in decisions.md. `definition` (R-72): a definition the corpus doesn't give; the task that needs
#            it writes it into the docs, and the physics reviewer reviews it before merge.
# statement  one sentence, normative ("must").
# source     file + exact section heading (as printed by plan/tools/sections.py), one or more; a ruling is cited
#            as decisions.md + its heading. The docs stay the authority: where a requirement and its source
#            disagree, the source wins and the requirement is the bug.
# verify     method (unit test | property test | golden image | numerical gate | benchmark | review checklist |
#            GUI screenshot) and what is asserted.
# rulings    the R-n it depends on.
# milestone  the earliest milestone whose exit gate includes it (plan/MILESTONES.md); it stays green after.
# rq         open REVIEW_QUEUE entries it waits on, if any.
# retired    set instead of deleting: the reason (usually a ruling). A retired requirement is in no gate.
#
# Edited with plan/tools/reqio.py (patches) or by hand. Checked by plan/tools/coverage.py (plan/coverage.md).
# Extracted at step 7, checkpoint A; updated for the checkpoint-A rulings R-70 to R-96.

- id: REQ-DEC-001
  area: DEC
  statement: "Decode must be the factorised D = D_mass × D_cfg × D_mom over blocks z[0:2] configuration, z[2:6] momentum, z[6:8] mass, evaluated in the order mass → config → momentum → derived invariants."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: unit test
    detail: "each block depends only on its z slice (and earlier blocks); call order asserted"
  rulings: []
  milestone: M2
- id: REQ-DEC-002
  area: DEC
  statement: "Canonicalise must be the single function that quotients a physical state onto the section (CoM at origin, ρ̃ on +x, λ̃_y ≥ −δ_λ under the R-82 mirror tie, I = 1), used by both the decode path and encode."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/design/principia_core_design.md
    section: 5. Canonicalisation is the one seam to (m, r, p)
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: property test
    detail: "on random physical states the output satisfies all four section conditions and canonicalise is idempotent"
  rulings:
  - R-82
  milestone: M2
- id: REQ-DEC-003
  area: DEC
  statement: "The latent vector must be ordered z[0:2] = configuration controls (z_α, z_β), z[2:6] = momentum controls (z_q0..z_q3), z[6:8] = mass controls (z_μ1, z_μ2), and this block ordering must be fixed once and held."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2 — The decoder
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_chart_reference.md
    section: 1. The 8D latent chart — the reference coordinate system
  verify:
    method: unit test
    detail: "decode a z with only one block non-zero at a time and assert only the corresponding block's physical outputs move (config: α,β; momentum: p_ρ,p_λ; mass: m)"
  rulings: []
  milestone: M2
- id: REQ-DEC-004
  area: DEC
  statement: "Decode must run in the order mass → configuration → momentum → derived invariants, with every derived quantity (E, L_z, K) computed after the blocks it depends on."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2 — The decoder
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  verify:
    method: review checklist
    detail: "decode source calls D_mass before D_cfg before D_mom before invariant evaluation; invariant-momentum construction reads the already-decoded masses and configuration"
  rulings: []
  milestone: M2
- id: REQ-DEC-005
  area: DEC
  statement: "The integrator must have exactly one input type, the decoder's canonical (mᵢ, rᵢ, pᵢ) in the dimensionless system G = M = I = 1 whichever decoder (full or linearised x₀ + J_D·δ) produced it, must be stateless, and must never learn which chart produced it."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2 — The decoder
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Design axioms (the six that must survive contact with a code agent)"
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: docs/design/principia_chart_reference.md
    section: Chart reference — the maths, for implementation
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_core_design.md
    section: 5. Canonicalisation is the one seam to (m, r, p)
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  verify:
    method: review checklist
    detail: "the integrator entry point takes only (m, r, p); no chart id or chart parameter is reachable from integrator code; identity suite 5.1–5.4 run on the kernel's actual input"
  rulings: []
  milestone: M2
- id: REQ-DEC-006
  area: DEC
  statement: "Masses must always be produced by the decode, SimUniforms.m[3] must be advisory only, and every chart must declare requires_per_pixel_mass (true for the ternary mass plot, the Burrau family and mixed-axis charts with a mass axis), under which the integrator must not read SimUniforms.m[3]."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  verify:
    method: unit test
    detail: "for each chart with requires_per_pixel_mass, set SimUniforms.m[3] to garbage and assert per-pixel decoded masses and outcomes are unchanged"
  rulings: []
  milestone: M2
- id: REQ-DEC-007
  area: DEC
  statement: "Link selection and chart type must be baked (monomorphised Rust variants), z₀, q₁, q₂ and slice values must be uniforms, and DECODE_MODE (full vs linearised) must be a per-quad workgroup-uniform flag."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Design axioms (the six that must survive contact with a code agent)"
  verify:
    method: review checklist
    detail: "kernel has one monomorphised variant per chart/link set; no runtime axis-kind interpreter; changing z₀/q/slice does not recompile"
  rulings: []
  milestone: M2
- id: REQ-DEC-008
  area: DEC
  statement: "Invalid decodes must be tagged, never dropped: DEGENERATE(reason) must be a decode-time terminal label with a narrow, enumerated cause set, and every pixel must receive a label."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_chart_reference.md
    section: 0.7 Degeneracy — every pixel gets a label
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "the DEGENERATE reason enum is closed; fuzz z/(u,v) on every chart and assert every pixel carries an outcome label, none NaN, none missing"
  rulings:
  - R-72
  milestone: M2
  note: "The full DEGENERATE reason list and its map onto payload §2's decode_failed detail codes: REQ-DEC-038 (definition)."
- id: REQ-DEC-009
  area: DEC
  statement: "The chart constants must be μ_max = 5, q_max = 2, α_min = 0, ε_μ = ε_z = ε_q = 10⁻⁶, δ_λ = 10⁻¹² (the mirror tie on λ̃_y) and ε_w = 10⁻¹⁰ (default, bounding the squared seed norm ‖w⁽²⁾‖²_m), held as registry data, with the formulae written in the symbols."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3. The maths
  - file: docs/design/principia_chart_reference.md
    section: 0.1 Masses
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Three hard requirements on any registered link
  - file: docs/design/principia_dd_encode.md
    section: 2. Consolidated contract
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 3 — Block inverses (closed forms, with their forward mates)"
  - file: docs/design/principia_chart_reference.md
    section: 2.2 Deterministic momentum construction
  - file: decisions.md
    section: "R-10 — μ_max and q_max are settled *(closes RQ-8, amends R-5)*"
  - file: decisions.md
    section: R-21 — `α_min = 0` *(CD-1)*
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: unit test
    detail: "assert the registry values equal the stated constants; grep that the decode/encode formulae reference the named constants rather than literals"
  rulings:
  - R-5
  - R-10
  - R-21
  - R-82
  milestone: M2
  note: "R-5 put the constants on the decision sheet; R-10 and R-21 settled them; R-82 fixes δ_λ's variable (λ̃ = √μ_λ·λ) and ε_w's bound on the squared norm."
- id: REQ-DEC-010
  area: DEC
  statement: "The mass block must decode as μₖ = μ_max·tanh(z_μk) for k = 1,2 and (m₀, m₁, m₂) = softmax(0, μ₁, μ₂), with body 0 the softmax reference, so that z_μ = 0 gives equal masses."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.1 Mass
  - file: docs/design/principia_chart_reference.md
    section: 0.1 Masses
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Link functions — the mass-simplex / logit question
  - file: decisions.md
    section: "R-10 — μ_max and q_max are settled *(closes RQ-8, amends R-5)*"
  verify:
    method: unit test
    detail: "z_μ = 0 → (⅓,⅓,⅓); Burrau point μ₁ = log(b/c), μ₂ = log(a/c) → masses (c,b,a)/(a+b+c); Σmᵢ = 1 for fuzzed z"
  rulings:
  - R-10
  milestone: M2
- id: REQ-DEC-011
  area: DEC
  statement: "The mass block must emit DEGENERATE(M01_TINY) if and only if M₀₁ = m₀ + m₁ < ε, and nothing else in the mass block may tag degenerate."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.1 Mass
  - file: docs/design/principia_chart_reference.md
    section: 0.1 Masses
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: unit test
    detail: "decoder test 4: fuzz z_μ including saturation; tag fires exactly on M₀₁ < ε; outputs tagged, never NaN, never dropped"
  rulings:
  - R-71
  milestone: M2
  note: "Value of ε: REQ-DEC-039 (calibration)."
- id: REQ-DEC-012
  area: DEC
  statement: "The configuration block must use the canonical-frame hyperspherical decode ρ̃ = cos α·(1,0), λ̃ = sin α·(cos β, sin β), R̃ = 1, with α = α_min + (π/2 − 2α_min)·σ(z_α) at α_min = 0 (i.e. α = (π/2)σ(z_α)) and β = π·σ(z_β)."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/design/principia_chart_reference.md
    section: 0.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2 — The decoder
  - file: decisions.md
    section: R-21 — `α_min = 0` *(CD-1)*
  verify:
    method: unit test
    detail: "z = 0 → α = π/4, β = π/2, ‖ρ̃‖ = ‖λ̃‖; ‖ρ̃‖² + ‖λ̃‖² = 1 for fuzzed z"
  rulings:
  - R-21
  milestone: M2
- id: REQ-DEC-013
  area: DEC
  statement: "Positions must be reconstructed by unweighting (ρ = ρ̃/√μ_ρ, λ = λ̃/√μ_λ, μ_ρ = m₀m₁/M₀₁, μ_λ = m₂M₀₁) and r₀₁ = −m₂λ, r₂ = M₀₁λ, r₀ = r₀₁ − (m₁/M₀₁)ρ, r₁ = r₀₁ + (m₀/M₀₁)ρ, giving CoM = 0 identically and I = 1."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/design/principia_chart_reference.md
    section: 0.2 Configuration — hyperspherical mass-weighted Jacobi
  verify:
    method: unit test
    detail: "assert r₁ − r₀ = ρ, total CoM = 0 and I = Σmᵢ‖rᵢ‖² = 1 on fuzzed z; decoder test 6: recompute ρ̃, λ̃ from the emitted (m, rᵢ) and recover (α, β, R̃ = 1) to tolerance"
  rulings: []
  milestone: M2
- id: REQ-DEC-014
  area: DEC
  statement: "Both α-poles must be represented rather than excised (no α_min cap), and a configuration at an exact pole must yield a deterministic canonical value (atan2(0,0) = 0) rather than a NaN, with the pole degeneracies fenced by the collision detector, conditioning readout and saturation flags."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/design/principia_chart_reference.md
    section: 0.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: decisions.md
    section: R-21 — `α_min = 0` *(CD-1)*
  - file: decisions.md
    section: "R-133 — The seven checkpoint-B interpretations are accepted *(closes RQ-111)*"
  verify:
    method: unit test
    detail: "ingest configs with ‖λ̃‖ = 0 and ‖ρ̃‖ = 0 exactly: no NaN, deterministic output identical on repeat and across backends, `lookup_clamped` set (the encode clamp flag, not the payload's `saturated` bit; R-133)"
  rulings:
  - R-21
  - R-133
  milestone: M2
  note: "R-133: R-21's 'SAT flags' is read as the encode clamp flag lookup_clamped (inverse_encode Part 3–4, dd_encode §3.5)."
- id: REQ-DEC-015
  area: DEC
  statement: "The decode must honour the α orientation ‖ρ̃‖ = cos α, so small α is a large inner-pair separation and α → π/2 is a tight inner binary with a distant third body."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/design/principia_chart_reference.md
    section: 0.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "decoder test 9: α → 0 ⇒ ‖r₁ − r₀‖ maximal and ‖λ‖ minimal; monotone crossover at π/4"
  rulings: []
  milestone: M2
- id: REQ-DEC-016
  area: DEC
  statement: "Every rigid operation of the canonicaliser C (the rotation R(−φ), φ = atan2(ρ_y, ρ_x), and the mirror diag(1,−1)) must be applied to the full phase-space state — every rᵢ and every pᵢ — never to the configuration alone."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.3 Canonicalise
  - file: docs/design/principia_dd_decoder.md
    section: 3.4 Momentum
  - file: docs/design/principia_chart_reference.md
    section: 0.4 Canonicalisation `C`
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/design/principia_dd_encode.md
    section: 3.2 Canonicalisation `C` — the exact ordered procedure
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  verify:
    method: property test
    detail: "T3 with the half-mirror catch: random-gauge x → E → D equals C(x) with |L_z| sign consistent with lookup_mirrored; a config-only mirror must fail this check"
  rulings: []
  milestone: M2
- id: REQ-DEC-017
  area: DEC
  statement: "The mirror must be the one test in encode's frame — mirror iff λ̃_y < −δ_λ, with λ̃ = √μ_λ·λ and δ_λ = 10⁻¹² — and |λ̃_y| ≤ δ_λ must resolve to no mirror, deterministically, on every backend."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.3 Canonicalise
  - file: docs/design/principia_chart_reference.md
    section: 0.4 Canonicalisation `C`
  - file: docs/design/principia_dd_encode.md
    section: 3.2 Canonicalisation `C` — the exact ordered procedure
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: property test
    detail: "states with λ̃_y at exactly −δ_λ, at +δ_λ and straddling both: no mirror for |λ̃_y| ≤ δ_λ, mirror just below −δ_λ; the same output twice and on CPU-f64 and GPU-f32 builds; the test reads λ̃_y, not λ_y; T1 sweep includes deadband-straddling rotations"
  rulings:
  - R-82
  milestone: M2
- id: REQ-DEC-018
  area: DEC
  statement: "The canonicaliser must be implemented for charts that bypass the canonical-frame decode, and for canonical-frame decodes it must be a no-op (rotation angle ≈ 0, mirror not taken) away from the λ̃_y = 0 seam."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.3 Canonicalise
  - file: docs/design/principia_chart_reference.md
    section: 0.4 Canonicalisation `C`
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: unit test
    detail: "decoder test 5: apply C to canonical-frame decodes of fuzzed z, assert unchanged; at the seam the R-82 tie (|λ̃_y| ≤ δ_λ → no mirror) gives the same result twice"
  rulings:
  - R-82
  milestone: M2
- id: REQ-DEC-019
  area: DEC
  statement: "The free momentum block must decode qₖ = q_max·(2σ(z_qk) − 1), p_ρ = (q₀, q₁), p_λ = (q₂, q₃), and p₀ = −p_ρ − (m₀/M₀₁)p_λ, p₁ = p_ρ − (m₁/M₀₁)p_λ, p₂ = p_λ, with no √μ mass unweighting of the momenta."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.4 Momentum
  - file: docs/design/principia_chart_reference.md
    section: 0.3 Momentum — free Jacobi momenta
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2 — The decoder
  - file: decisions.md
    section: R-28 — Momentum decode has no mass unweighting *(CD-8)*
  - file: decisions.md
    section: "R-10 — μ_max and q_max are settled *(closes RQ-8, amends R-5)*"
  verify:
    method: unit test
    detail: "Σpᵢ = 0 to machine precision on fuzzed z (catches the crossed m₀/m₁ factors); z_q = 0 gives rest; |qₖ| ≤ q_max"
  rulings:
  - R-10
  - R-28
  milestone: M2
- id: REQ-DEC-020
  area: DEC
  statement: "The invariant-momentum construction must follow chart reference §2.2 exactly: v⁽ᴸ⁾ = (L_z/I)·J rᵢ, the seed family (ρ̇,λ̇) = (ρ,0) then (0,λ), (Jρ,0), (0,Jλ) converted to particle velocities, CoM drift and angular momentum projected out, mass-weighted normalisation, then a = √(2(K* − L_z²/2I)), vᵢ = vᵢ⁽ᴸ⁾ + a·wᵢ, pᵢ = mᵢvᵢ."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.4 Momentum
  - file: docs/design/principia_chart_reference.md
    section: 2.2 Deterministic momentum construction
  - file: docs/design/principia_chart_reference.md
    section: 2. Invariant-momentum charts `(Lz, E)` and `(Lz, K)`
  verify:
    method: property test
    detail: "over random (u,v) and a grid of feasible (L_z, K*) at fixed config, including near K* → K_min: Σpᵢ = 0, L_z(p) = L_z target and K(p) = K* to machine precision (decoder test 8; chart reference §5.2)"
  rulings: []
  milestone: M2
- id: REQ-DEC-021
  area: DEC
  statement: "Seed selection must take, among the seeds with ‖w⁽²⁾‖²_m > ε_w, the one with the largest ‖w⁽²⁾‖_m, breaking ties by seed order ((ρ,0), (0,λ), (Jρ,0), (0,Jλ)), and must emit DEGENERATE only when no seed qualifies."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.4 Momentum
  - file: docs/design/principia_chart_reference.md
    section: 2.2 Deterministic momentum construction
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "a fixture where several seeds qualify selects the largest-norm one, not the first; an exact-tie fixture selects the earlier seed; forcing the primary seed to degenerate (ρ = 0) selects a fallback, not DEGENERATE; DEGENERATE fires only where no seed has ‖w⁽²⁾‖²_m > ε_w"
  rulings:
  - R-72
  - R-82
  milestone: M2
  note: "The DEGENERATE reason this emits: REQ-DEC-038 (definition)."
- id: REQ-DEC-022
  area: DEC
  statement: "An invariant-momentum pixel with K* < L_z²/2I must be tagged as infeasible (terminal, labelled), exactly on that inequality, and never dropped or NaN."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.4 Momentum
  - file: docs/design/principia_chart_reference.md
    section: 2.2 Deterministic momentum construction
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The four axis kinds — a closed set
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "decoder test 8: infeasible pairs refused/tagged exactly on K* < L_z²/2; the feasibility comparison is branch-identical across backends"
  rulings:
  - R-72
  milestone: M2
  note: "The label (state and detail): REQ-DEC-040 (definition)."
- id: REQ-DEC-023
  area: DEC
  statement: "The scale gauge must be rᵢ ← rᵢ/ℓ, pᵢ ← √ℓ·pᵢ with ℓ = √I, a no-op when R̃ = 1 was enforced."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.5 Scale gauge
  - file: docs/design/principia_chart_reference.md
    section: 0.5 Scale gauge
  verify:
    method: unit test
    detail: "decoder test 10: applied to canonical output it is the identity; applied to a scaled input it yields I = 1 and transforms E, L_z per the encode contract's table"
  rulings: []
  milestone: M2
- id: REQ-DEC-024
  area: DEC
  statement: "Energy normalisation must be kept as specified (pᵢ ← η_E·pᵢ, η_E = √((E* − U)/K₀), feasibility E* ≥ U), allowed only on the free-Jacobi-momentum and latent charts, and switched off by an explicit Option/flag, never by E* = 0."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "3.7 Energy normalisation (optional; keep-or-drop is decision B9)"
  - file: docs/design/principia_chart_reference.md
    section: "0.6 Energy normalisation — optional, and forbidden on some charts"
  - file: docs/design/principia_dd_decoder.md
    section: 6. Deferred / flagged
  - file: decisions.md
    section: R-25 — `η_E` is kept, with an explicit off switch *(CD-5)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  verify:
    method: unit test
    detail: "with E* set, decoded energy equals E*; with the option None, momenta are untouched; E* = 0 is treated as a real target (energy set to zero), not as off"
  rulings:
  - R-25
  - R-72
  - R-124
  milestone: M2
  note: "R-25 overrides dd_decoder §6's 'keep-or-drop is B9' and the 'non-zero E*' phrasing; applied now (R-124): the refusal covers every Some(E*), including Some(0). The behaviour at K₀ = 0 (the rest start) and on a failed E* ≥ U check: REQ-DEC-041 (definition)."
- id: REQ-DEC-025
  area: DEC
  statement: "Every decode, for every chart, must satisfy Σmᵢ = 1, total CoM = 0, Σpᵢ = 0 and I = 1 to precision-appropriate tolerance."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: docs/design/principia_chart_reference.md
    section: 0.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: docs/design/principia_chart_reference.md
    section: 0.3 Momentum — free Jacobi momenta
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  verify:
    method: property test
    detail: "decoder test 1 and chart reference §5.2: fuzz z and random (u,v) for every chart; Σpᵢ = 0 to machine precision; I = 1 after the canonical-frame decode"
  rulings: []
  milestone: M2
- id: REQ-DEC-026
  area: DEC
  statement: "The decode blocks must not couple except through the join: perturbing one block's controls must leave the other blocks' intermediate values bit-identical."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  verify:
    method: property test
    detail: "decoder test 2: perturb z_μ, assert (α, β) and (p_ρ, p_λ) bit-identical; likewise for each block"
  rulings: []
  milestone: M2
- id: REQ-DEC-027
  area: DEC
  statement: "For all finite z, including saturation, the decode must respect its ranges α ∈ (0, π/2), β ∈ [0, π], |qₖ| ≤ q_max, mapping signed chart coordinates onto each physical axis's actual domain."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "Per-axis signedness: \"if it makes sense for that axis\""
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 3 — Charts
  verify:
    method: property test
    detail: "decoder test 3 over fuzzed z including ±large values; mass outputs stay in the open simplex"
  rulings: []
  milestone: M2
- id: REQ-DEC-028
  area: DEC
  statement: "The chart parametrisation must stay open on `[0,1]` with endpoints excluded, with no endpoint clamping and no artificial guards against approaching degenerate configurations."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "Degenerate configs: open & unguarded, approach as a limit"
  verify:
    method: review checklist
    detail: "decoder has no endpoint-landing or exclusion guard; degenerate configs are only approached as limits"
  rulings: []
  milestone: M2
- id: REQ-DEC-029
  area: DEC
  statement: "Decode must not apply a per-body momentum cap with CoM re-enforcement; q_max alone bounds the Jacobi momenta, clamped and flagged at the boundary."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 8.2 A per-body momentum cap with CoM re-enforcement
  - file: decisions.md
    section: R-11 — The per-body momentum cap is rejected *(closes RQ-9)*
  verify:
    method: property test
    detail: "T2 round trip encode(decode(z)) = z over random z including the q_max boundary; assert no per-body cap exists in decode and clamped samples carry the flag."
  rulings:
  - R-10
  - R-11
  milestone: M2
- id: REQ-DEC-030
  area: DEC
  statement: Momentum decode must not unweight by mass.
  source:
  - file: decisions.md
    section: R-28 — Momentum decode has no mass unweighting *(CD-8)*
  verify:
    method: unit test
    detail: "Decode fixture with unequal masses matches the dd_decoder §3.4 formula without a mass-unweighting step."
  rulings:
  - R-28
  milestone: M2
- id: REQ-DEC-031
  area: DEC
  statement: "Each QuadRequest must carry the quad's centre and half-width computed on the CPU in f64 and passed as f32, and the GPU must compute sample positions as u = centre + half·(2t − 1) with t = (i + 0.5)/N, never from the quad's min/max bounds."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  - file: docs/design/principia_deep_zoom.md
    section: 1. Quad-local coordinates — UV precision
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  verify:
    method: numerical gate
    detail: "at depth 30, the N samples of a quad decode to N distinct f32 positions matching the f64 reference within one f32 ulp of h; min/max bounds are not read by the kernel"
  rulings: []
  milestone: M5
- id: REQ-DEC-032
  area: DEC
  statement: "CPU quad centres/half-widths and the GPU quad-local sample coordinate must share the Y-up UV convention, with the single flip v = 1 − frag_coord.y/H at the framebuffer seam, so ICs are not mirrored within a quad."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  - file: docs/design/principia_deep_zoom.md
    section: 1. Quad-local coordinates — UV precision
  verify:
    method: unit test
    detail: "an asymmetric IC fixture decodes to the same IC at a given UV on CPU and GPU; exported image orientation matches the reference"
  rulings: []
  milestone: M5
- id: REQ-DEC-033
  area: DEC
  statement: "Sample collapse on the full nonlinear decoder — adjacent samples giving bitwise-identical ICs — must trigger DECODE_SWITCHOVER to the linearised decoder with refinement continuing, never a refinement stop."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  - file: docs/design/principia_deep_zoom.md
    section: 4. Sliding depth bound
  - file: decisions.md
    section: R-90 — The decoder switchover trigger *(closes RQ-41)*
  verify:
    method: unit test
    detail: "force bitwise-identical adjacent ICs on the full decoder (depth ~22, and at shallow depth with tiny q₁,q₂); assert DECODE_MODE flips to the linearised path and the quad keeps splitting"
  rulings:
  - R-90
  milestone: M6
- id: REQ-DEC-034
  area: DEC
  statement: "The linearised decode must replace the decode with x₀ + J_D·δ, where J_D is central differences of the full chart→IC composite in f64, chart-agnostic with no per-axis-kind special-casing, and |det J_D| must double as the measure weight."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/design/principia_chart_reference.md
    section: 3.3 The chart map
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/design/principia_deep_zoom.md
    section: Couplings to the contract
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "decoder test 11: at quad centre x₀ + J_D·0 equals the full decode exactly; at half-width the error vs full decode shrinks ∝ h² across depths; identical behaviour for an affine chart and the exponential-map shape-sphere chart"
  rulings:
  - R-71
  milestone: M6
  note: "Slope tolerance of the h² fit: REQ-DEC-042 (calibration)."
- id: REQ-DEC-035
  area: DEC
  statement: "Global/nonlinear precision must live in f64 on the CPU (quad bounds, centre/half-width, x₀, J_D, scheduling), the GPU must do only local f32 arithmetic, and the pixel inspector must witness a single trajectory in f64 on the CPU."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  verify:
    method: review checklist
    detail: "CPU quadtree types are f64; GPU uniforms are f32 c, h, x₀, J_D; inspector integrates in f64"
  rulings: []
  milestone: M6
- id: REQ-DEC-036
  area: DEC
  statement: "The linearised decoder must compute x₀ = D(c_u, c_v) and J_D by central differences in f64 on the CPU per deep quad, and the GPU must evaluate x = x₀ + J_D·δ without running the nonlinear decode pipeline."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-154 — REQ-DEC-036 is verified over a depth sweep at M5 *(closes RQ-124)*"
  verify:
    method: numerical gate
    detail: "over a depth sweep, linear vs full decode agree to O(h²) (R-154; the check at the switchover depth is REQ-DEC-037's); the linear path distinguishes adjacent samples to depth ≥ 50"
  rulings:
  - R-113
  - R-154
  milestone: M5
  note: "R-113 (RQ-99 M5-2, option a): moved to M5 (TASK-M5-04); the switchover (REQ-DEC-033/037) and the error-fit tests (REQ-DEC-034, REQ-DEC-042) stay in M6."
- id: REQ-DEC-037
  area: DEC
  statement: "The switch to the linearised decoder must be signalled by QuadRequest DECODE_MODE and must fire once `quad.collapsed` is set (the per-quad flag set when the full decoder's adjacent samples give bitwise-identical ICs) or when depth exceeds ℓ_switch = 20 (an upper bound; lowering's SWITCH = ℓ_switch), whichever comes first, with the same SimState, reduction and render on either side."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  - file: decisions.md
    section: R-90 — The decoder switchover trigger *(closes RQ-41)*
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  - file: decisions.md
    section: "R-154 — REQ-DEC-036 is verified over a depth sweep at M5 *(closes RQ-124)*"
  verify:
    method: unit test
    detail: "quads at depth 20/21 with distinct adjacent ICs carry FULL/LIN; a shallow tiny-q quad whose adjacent ICs are bitwise identical switches early; collapse is detected by bitwise IC comparison, not by the energy-drift diagnostic; lowering's SWITCH constant equals ℓ_switch; at the switchover depth, linear vs full decode agree to O(h²) (REQ-DEC-036's check, moved here by R-154)"
  rulings:
  - R-90
  - R-154
  milestone: M6
- id: REQ-DEC-038
  area: DEC
  kind: definition
  statement: "The full DEGENERATE reason list and its map onto payload §2's decode_failed detail codes (0 non-finite, 1 degenerate configuration, 2 invalid mass construction, 3 other) must be defined in dd_decoder §2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_chart_reference.md
    section: 0.7 Degeneracy — every pixel gets a label
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "dd_decoder §2 lists every DEGENERATE reason (M01_TINY, the no-qualifying-seed case, and any other) with its decode_failed detail code; payload §2 agrees; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-DEC-039
  area: DEC
  kind: calibration
  statement: "The M01_TINY ε (DEGENERATE fires iff M₀₁ < ε) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 3.1 Mass
  - file: docs/design/principia_chart_reference.md
    section: 0.1 Masses
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states whether ε is one of ε_μ, ε_z, ε_q, ε_w or a new constant, with the M₀₁ range reachable at μ_max = 5 as evidence; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-DEC-040
  area: DEC
  kind: definition
  statement: "The label (state and detail) of an infeasible invariant-chart pixel (K* < K_min) must be defined in chart_reference §2.2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 2.2 Deterministic momentum construction
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "chart_reference §2.2 names the state and detail an infeasible pixel carries, consistent with payload §2's enum; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-DEC-041
  area: DEC
  kind: definition
  statement: "η_E's behaviour at K₀ = 0 (the rest start) and on a failed E* ≥ U feasibility check must be defined in dd_decoder §3.7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "3.7 Energy normalisation (optional; keep-or-drop is decision B9)"
  - file: docs/design/principia_chart_reference.md
    section: "0.6 Energy normalisation — optional, and forbidden on some charts"
  - file: decisions.md
    section: R-25 — `η_E` is kept, with an explicit off switch *(CD-5)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "dd_decoder §3.7 states the outcome for K₀ = 0 and for E* < U (label or refusal); physics reviewer approved"
  rulings:
  - R-25
  - R-72
  milestone: M2
- id: REQ-DEC-042
  area: DEC
  kind: calibration
  statement: "dd_decoder test 11's slope tolerance on the ∝ h² error fit of the linearised decode must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal fits the log-log slope across depths for an affine and a curve chart and states the tolerance about 2; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M6
- id: REQ-DEC-043
  area: DEC
  kind: calibration
  statement: "dd_decoder test 12's f32-eps scale factor for cross-backend agreement on continuous decode values must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures f64-vs-f32 decode differences on the golden IC and fuzzed interiors and states the multiple of f32 eps that covers them; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-DEC-044
  area: DEC
  kind: calibration
  statement: "The per-precision tolerances of dd_decoder unit tests 1 (the identities Σmᵢ = 1, CoM = 0, Σpᵢ = 0, I = 1, \"to precision-appropriate tolerance\"), 6 (the reconstruction inverse) and 8 (the invariant construction's L_z and K) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states each tolerance in f64 and f32 with the measured residuals over fuzzed z that it covers; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-ENC-001
  area: ENC
  statement: "Encode's invariant fibre point must be the chart's own forward construction (encode reuses decode), and its tolerances must be stated in physical units."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  verify:
    method: property test
    detail: "decode(encode(x)) equals canonicalise(x) within a physical-unit tolerance on random states"
  rulings: []
  milestone: M2
- id: REQ-ENC-002
  area: ENC
  statement: "Encode must be E = (block/chart inverses) ∘ C, the quotient map of an arbitrary — including foreign — physical state onto the decode's canonical section (CoM at origin, zero total momentum, ρ̃ on +x, λ̃_y ≥ −δ_λ under the R-82 mirror tie, I = 1), and must be the one path for entered states (lock re-centre, physical IC entry, Burrau (m,n) entry, invariant-pair lookup, literature import, T-tests, and the ROUNDTRIP preset view, which recomputes encode(decode(z)) fragment-side), not for clicked pixels."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/design/principia_dd_encode.md
    section: 1. What it is
  - file: docs/design/principia_dd_encode.md
    section: 2. Consolidated contract
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 7 — Ground-truth ingestion (the validation programme's demand)"
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 0. The ladder — the organising abstraction
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_core_design.md
    section: 5. Canonicalisation is the one seam to (m, r, p)
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: review checklist
    detail: "all listed host-side consumers call the one encode entry point; the ROUNDTRIP view is a fragment preset, not a kernel mode (R-75); clicked-pixel locks do not call encode"
  rulings:
  - R-75
  - R-82
  milestone: M2
- id: REQ-ENC-003
  area: ENC
  statement: "Encode must be gauge-invariant (T1): E(g·x) = E(x) for every g = (translation a, boost u, rotation θ, scale s, mirror Π) acting as rᵢ' = s·R(θ)·Π·rᵢ + a, pᵢ' = s^(−1/2)·R(θ)·Π·pᵢ + mᵢ·u."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 8 — Round-trip contracts and the new debug view
  - file: docs/design/principia_dd_encode.md
    section: "3.3 The gauge group action, explicit (this is what T1 sweeps)"
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: property test
    detail: "encode test 2: random g per §3.3 including deadband-straddling rotations (λ̃_y within ±δ_λ); outputs equal after float tolerance; the mirror tie |λ̃_y| ≤ δ_λ gives no mirror, identically twice"
  rulings:
  - R-82
  milestone: M2
- id: REQ-ENC-004
  area: ENC
  statement: "Encode must be a right inverse on the interior (T2), asserted per chart in physical units as ‖D(z) − D(E(D(z)))‖_phys ≤ ε_phys over interior z, with clamp-adjacent samples asserting lookup_clamped instead of a tight residual."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 4 — Conditioning: assert in physical units, never in z"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 8 — Round-trip contracts and the new debug view
  - file: docs/design/principia_dd_encode.md
    section: 3.5 Conditioning (numbers, and the tolerance rule)
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: property test
    detail: encode test 1 / decoder test 7 per chart
  rulings:
  - R-71
  milestone: M2
  note: "Value of ε_phys: REQ-ENC-024 (calibration)."
- id: REQ-ENC-005
  area: ENC
  statement: "Encode must be a left inverse modulo gauge (T3): D(E(x)) = C(x), with shape angles and masses matching, E matching after the recorded rescale, and |L_z| matching with sign consistent with lookup_mirrored."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 8 — Round-trip contracts and the new debug view
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  verify:
    method: property test
    detail: "encode test 3: random scale, orientation, offset and parity; a config-only mirror fails the L_z check"
  rulings: []
  milestone: M2
- id: REQ-ENC-006
  area: ENC
  statement: "The scale step must set λ = I_in^(−1/2), r ← r/√I_in, p ← I_in^(1/4)·p, record λ, surface lookup_rescaled carrying λ, and transform E_canon = √I_in·E_in, L_z,canon = L_z,in/I_in^(1/4), t_canon = t_in/I_in^(3/4), with timing outputs convertible back by I_in^(3/4)."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 2 — The scale gauge gap (missing from the original inverse policy)"
  - file: docs/design/principia_dd_encode.md
    section: 3.2 Canonicalisation `C` — the exact ordered procedure
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "encode test 4: Burrau state at 2× scale → I_in = 4·I_canon, λ = 1/2, E_canon = 2·E_in, timing factor I_in^(3/4); assert the whole row"
  rulings: []
  milestone: M2
- id: REQ-ENC-007
  area: ENC
  statement: "The block inverses must be the closed forms of inverse-encode Part 3: μₖ = log(mₖ/m₀), z_μk = artanh(μₖ/μ_max) with |μₖ| ≤ (1−ε_μ)μ_max; α = atan2(‖λ̃‖, ‖ρ̃‖), β = atan2(λ̃_y, λ̃_x) ∈ [0, π], s_α = 2α/π, s_β = β/π, z = logit(s) with s clamped to [ε_z, 1−ε_z]; p_λ = p₂, p_ρ = p₁ + (m₁/M₀₁)p₂, s = ½(q/q_max + 1), z = logit(clamp(s, ε_q, 1−ε_q)), with |qₖ| > q_max clamped and flagged lookup_clamped."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 3 — Block inverses (closed forms, with their forward mates)"
  - file: docs/design/principia_dd_encode.md
    section: 3.1 Forward ↔ inverse pairing table — closures verified
  - file: decisions.md
    section: "R-10 — μ_max and q_max are settled *(closes RQ-8, amends R-5)*"
  - file: decisions.md
    section: R-21 — `α_min = 0` *(CD-1)*
  verify:
    method: unit test
    detail: "substitute forward into inverse for fuzzed z: each block recovers z to tolerance; |q| > q_max input sets lookup_clamped"
  rulings:
  - R-10
  - R-21
  milestone: M2
- id: REQ-ENC-008
  area: ENC
  statement: "Encode into the active chart must dispatch by axis kind: kind 1 reads the component exactly; kind 2 applies the block inverse plus the axis's own residual convention; kind 3 evaluates L_z(x), E(x), K(x) directly for chart coordinates; annotation axes have nothing to invert; otherwise encode falls back to latent z, which is always available."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: docs/design/principia_dd_encode.md
    section: 3.2 Canonicalisation `C` — the exact ordered procedure
  verify:
    method: unit test
    detail: "round-trip a state through each axis kind; a state not aligned with any chart lands in latent z"
  rulings: []
  milestone: M2
- id: REQ-ENC-009
  area: ENC
  statement: "For invariant charts, the chart-coords → latent direction must reuse decode: run the chart's own forward construction at the current frozen configuration, then free-momentum-invert the constructed p; smallest-latent-norm may be used only as the documented fallback where no construction exists."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: docs/design/principia_dd_encode.md
    section: 3.1 Forward ↔ inverse pairing table — closures verified
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  verify:
    method: unit test
    detail: "enter (L_z, E) numerically vs click the corresponding pixel → identical z to precision"
  rulings: []
  milestone: M2
- id: REQ-ENC-010
  area: ENC
  statement: "Encode must check invariant feasibility (|L_z| ≤ √(2I(E−U)), K ≥ L_z²/2I) before construction, route infeasible pairs through project / clamp / reject with the flag surfaced (never silent, never NaN), and emit the same DEGENERATE label as the pixel path when no seed has ‖w⁽²⁾‖²_m > ε_w."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "encode test 6: (L_z, E) with K* < L_z²/2 is projected/clamped/rejected and flagged; degenerate-seed config returns the pixel path's DEGENERATE"
  rulings:
  - R-72
  - R-82
  milestone: M2
  note: "The labels themselves: REQ-DEC-038 and REQ-DEC-040 (definition)."
- id: REQ-ENC-011
  area: ENC
  statement: "On-curve Burrau encode must recover the acute angle from the canonical config, set ν(θ) = sec θ − tan θ, and verify the mass coupling μ₁ = log(b/c), μ₂ = log(a/c)."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  - file: docs/design/principia_dd_encode.md
    section: 3.1 Forward ↔ inverse pairing table — closures verified
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "encode test 5: physical (3,4,5) → masses (5,4,3)/12, right angle recovered, ν matches; decode back → same triangle to ε_phys"
  rulings:
  - R-72
  milestone: M2
  note: "What encode does with an input at θ > π/4 on the folded acute-angle chart: REQ-ENC-022 (definition)."
- id: REQ-ENC-012
  area: ENC
  statement: "Lookup must support the exact-IC form ((m,n), a triple, a raw z, or physical (mᵢ, rᵢ, pᵢ) → encode into the latent chart → lock) and the partial-specification form (fill masses equal unless the chart implies Burrau, Burrau geometry = current locked ν or ν = 1/2, momentum = rest, latent = 0, then set the slice basis to span the unspecified DOF)."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  verify:
    method: unit test
    detail: "enter each exact form and a partial spec; assert the resulting lock and basis"
  rulings: []
  milestone: M2
- id: REQ-ENC-013
  area: ENC
  statement: "Lookup and lock must validate in order (1) hypercube bounds, (2) chart feasibility — (L_z,E) parabola, (L_z,K) K ≥ 0 and K ≥ L_z²/2I, mass simplex mᵢ > 0 with Σ = 1 and interior buffer ε_m, shape sphere θ ∈ [0, 2π) and φ ∈ [0, π] with no polar buffer, Burrau ν ∈ (0,1), mixed charts both axes — (3) decode sanity (masses positive, CoM and total momentum zero within tolerance), and on failure prefer project, then clamp (lookup_clamped, showing moved coordinates), then reject naming the failed constraint only when the alternatives would give a qualitatively different IC."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: decisions.md
    section: "R-13 — Lookup has no coincident-bodies rejection *(closes RQ-11)*"
  - file: decisions.md
    section: "R-12 — The shape-sphere chart map stays as the markdown has it *(closes RQ-10)*"
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "one fixture per layer and per failure mode; bodies within r_coll are not rejected"
  rulings:
  - R-12
  - R-13
  - R-14
  - R-71
  - R-72
  milestone: M2
  note: "The space the hypercube layer checks: REQ-ENC-023 (definition); the decode-sanity tolerance: REQ-ENC-027 (calibration)."
- id: REQ-ENC-014
  area: ENC
  statement: "Encode must report everything it discards: lookup_rescaled, lookup_mirrored and lookup_clamped must each fire if and only if their operation did."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  verify:
    method: unit test
    detail: "canonical inputs raise no notice; each operation forced individually raises exactly its notice"
  rulings: []
  milestone: M2
- id: REQ-ENC-015
  area: ENC
  statement: "Canonicalisation must run in the order 1a subtract CoM → 0 rescale (I computed in the CoM frame) → 1b remove the boost (pᵢ ← pᵢ − mᵢP_tot/M) → 2 rotate ρ̃ to +x → 3 mirror iff λ̃_y < −δ_λ, pinned in the shared source so both precisions run the identical sequence."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 3.2 Canonicalisation `C` — the exact ordered procedure
  - file: docs/design/principia_dd_encode.md
    section: 6. Deferred / flagged
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 2 — The scale gauge gap (missing from the original inverse policy)"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: decisions.md
    section: "R-23 — Encode order: CoM subtraction precedes `I` *(CD-3)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: decisions.md
    section: R-82 — One mirror test, one seed rule *(closes RQ-33)*
  verify:
    method: unit test
    detail: "feed an off-CoM, boosted, scaled, rotated state; assert I is computed after CoM subtraction and the result equals the canonical representative on both backends"
  rulings:
  - R-23
  - R-82
  milestone: M2
  note: "R-23's order is now the normative numbering of inverse_encode_contract Part 6 and dd_encode §3.2; R-82 fixes the mirror test."
- id: REQ-ENC-016
  area: ENC
  statement: "An exactly coincident inner pair (ρ̃ = 0) must not be represented and must be caught with lookup_clamped (not a separate coincident-bodies rejection), taking the same branch at both precisions."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 3.2 Canonicalisation `C` — the exact ordered procedure
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: decisions.md
    section: "R-13 — Lookup has no coincident-bodies rejection *(closes RQ-11)*"
  verify:
    method: unit test
    detail: "encode test 9: ρ → 0 inputs hit the step-2 guard with the lookup_clamped reason on CPU-f64 and GPU-f32"
  rulings:
  - R-13
  milestone: M2
  note: R-13 overrides dd_encode §3.2 / test 9's 'reject' wording
- id: REQ-ENC-017
  area: ENC
  statement: "Off-curve Burrau encode must project by ν* = argmin_ν ‖n(α,β) − n(α_γ(ν), β_γ(ν))‖² + ‖m − m_γ(ν)‖² (shape-sphere chordal ⊕ mass-simplex Euclidean, unit weights) with a Brent/golden-section-grade 1-D minimiser, report d(ν*), and fall back to latent z beyond tolerance."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 3.4 Curve projection (off-curve input) — metric pinned
  - file: docs/design/principia_dd_encode.md
    section: 6. Deferred / flagged
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  - file: decisions.md
    section: R-24 — The encode projection metric is confirmed *(CD-4)*
  verify:
    method: unit test
    detail: "encode test 7: on-curve input — argmin and closed form agree on ν; off-curve — d(ν*) reported, latent fallback taken beyond tolerance and still satisfies T2"
  rulings:
  - R-24
  milestone: M2
  note: "R-24 confirms the metric the docs mark 'confirm/veto' and fills inverse-encode's unspecified dist"
- id: REQ-ENC-018
  area: ENC
  statement: "Encode must satisfy the T1/T2/T3 guarantees and report everything it discards."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: unit test
    detail: "encode results carry a discarded-quantities report; T1–T3 tests (defined in inverse_encode_contract) pass"
  rulings: []
  milestone: M2
  note: "T1–T3 are defined in principia_inverse_encode_contract.md, not in these files"
- id: REQ-ENC-019
  area: ENC
  statement: "Lookup must have no coincident-bodies rejection branch: a looked-up IC takes the same path as any pixel, and exactly coincident bodies are caught by the range check as lookup_clamped."
  source:
  - file: decisions.md
    section: "R-13 — Lookup has no coincident-bodies rejection *(closes RQ-11)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "Lookup fixtures: exactly coincident → lookup_clamped; bodies within r_coll pass lookup unrejected; no separate rejection branch."
  rulings:
  - R-13
  - R-113
  milestone: M2
  note: "R-113 (RQ-95): the t = 0 collision label at dispatch is a separate M3 requirement, closed with REQ-EVT-002."
- id: REQ-ENC-020
  area: ENC
  statement: "The encode projection metric must be shape-sphere chordal ⊕ mass-simplex Euclidean with unit weights."
  source:
  - file: decisions.md
    section: R-24 — The encode projection metric is confirmed *(CD-4)*
  verify:
    method: unit test
    detail: "Off-curve Burrau encode fixture: the projected point minimises the stated metric (compare to brute-force search)."
  rulings:
  - R-24
  milestone: M2
  note: Recorded; applied before off-curve Burrau encode.
- id: REQ-ENC-021
  area: ENC
  statement: "Past a conditioning threshold the Inspector must switch from chart-encode to direct-physical-inject and flag it, and for non-degenerate configs it must run both paths and report their difference."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Degeneracy routing
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: unit test
    detail: "a near-degenerate IC flips to direct inject with a flag; random non-degenerate ICs show agreeing paths"
  rulings:
  - R-71
  milestone: M8
  note: "The conditioning threshold and the two-path agreement tolerance: REQ-ENC-028 (calibration)."
- id: REQ-ENC-022
  area: ENC
  kind: definition
  statement: "What encode does with an input at θ > π/4 on the folded acute-angle Burrau chart (relabel, fall back to latent z, or refuse) must be defined in dd_encode §3.1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 3.1 Forward ↔ inverse pairing table — closures verified
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: decisions.md
    section: "R-27 — Both Burrau charts are kept, each labelled with its quotient *(CD-7)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "dd_encode §3.1 states the θ > π/4 behaviour and the flag it raises; physics reviewer approved"
  rulings:
  - R-27
  - R-72
  milestone: M2
- id: REQ-ENC-023
  area: ENC
  kind: definition
  statement: "The space in which lookup's hypercube layer checks bounds (s = σ(z), the clamp space, or other) must be defined in inverse_encode_contract's chart-aware validation by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "layer 1 names its space and bounds consistently with z ∈ ℝ⁸ passing through σ/tanh; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-ENC-024
  area: ENC
  kind: calibration
  statement: "ε_phys, the physical-units round-trip tolerance of T2, must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 4 — Conditioning: assert in physical units, never in z"
  - file: docs/design/principia_dd_encode.md
    section: 3.5 Conditioning (numbers, and the tolerance rule)
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures the physical-unit round-trip residual over interior z per chart at f64 and f32 and sets ε_phys above it; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-ENC-025
  area: ENC
  kind: definition
  statement: "The conditioning number κ(z) must be defined in inverse_encode_contract Part 4 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 4 — Conditioning: assert in physical units, never in z"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 1 — What encode is: a quotient map onto a section"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 7 — Ground-truth ingestion (the validation programme's demand)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 4 gives κ(z) as a formula (building on d logit/ds = 1/(s(1−s))) that bounds the T2 residual; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-ENC-026
  area: ENC
  kind: calibration
  statement: "The curve-projection distance tolerance beyond which off-curve Burrau encode falls back to latent z must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 3.4 Curve projection (off-curve input) — metric pinned
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal gives d(ν*) distributions for on-curve and off-curve fixtures under the R-24 metric; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-ENC-027
  area: ENC
  kind: calibration
  statement: "The decode-sanity tolerance on CoM at the origin and total momentum zero in lookup's validation layer 3 must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures CoM and Σp residuals of decode at f64 and f32 over fuzzed z and sets the tolerance above them; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-ENC-028
  area: ENC
  kind: calibration
  statement: "The Inspector's degeneracy-routing conditioning threshold (past which chart-encode flips to direct-physical-inject) and its two-path agreement tolerance must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Degeneracy routing
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures κ and the chart-encode vs direct-inject difference over near-degenerate and random ICs and states both values; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M8
- id: REQ-ENC-029
  area: ENC
  kind: definition
  statement: "The physical-units norm ‖·‖_phys of the round-trip residual (which of the masses, positions and momenta enter, with what weighting and whether absolute or relative) must be defined in inverse_encode_contract Part 4 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 4 — Conditioning: assert in physical units, never in z"
  - file: docs/design/principia_dd_encode.md
    section: 3.5 Conditioning (numbers, and the tolerance rule)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 4 gives ‖·‖_phys over (m, r, p) on the canonical section, used unchanged by T2, the ingestion residual and the ROUNDTRIP preset; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-ENC-030
  area: ENC
  kind: definition
  statement: "The metric in which lookup projects a failed input to \"the nearest valid point in the chart's feasible region\" and the lock re-centres \"to the nearest feasible point\", and the criterion by which projecting or clamping \"would give a qualitatively different IC\" (so lookup rejects) must be defined in inverse_encode_contract's chart-aware validation by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the projection metric per feasibility constraint (the (L_z, E) parabola at least) and the reject criterion; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-ENC-031
  area: ENC
  kind: definition
  statement: "Which two directions the slice basis spans after a partial-specification lookup that leaves more than two degrees of freedom unspecified must be defined in inverse_encode_contract Part 6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 6 — The canonical inverse policy, completed
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 6 gives the deterministic rule choosing the two spanned directions (and what happens to the rest); physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-ENC-032
  area: ENC
  kind: definition
  statement: "The canonicalisation conditioning number the Inspector shows live (the measure that grows without bound as ρ → 0 and as R → 0 and is finite elsewhere) must be defined in ic_inspector_scratchpad's degeneracy-routing section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Degeneracy routing
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 4 — Conditioning: assert in physical units, never in z"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the conditioning number as a formula of the physical configuration, shows it diverges exactly at ρ → 0 and R → 0, and states its relation to κ(z) (REQ-ENC-025); physics reviewer approved"
  rulings:
  - R-72
  milestone: M8
- id: REQ-ENC-033
  area: ENC
  statement: "A looked-up IC with bodies within r_coll must reach dispatch and be labelled `collision`, `t_end_step = 0`."
  source:
  - file: decisions.md
    section: "R-13 — Lookup has no coincident-bodies rejection *(closes RQ-11)*"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "lookup fixture with bodies within r_coll → dispatched → state = collision, t_end_step = 0"
  rulings:
  - R-13
  - R-113
  milestone: M3
  note: Split from REQ-ENC-019 (R-113, RQ-95).
- id: REQ-CHART-001
  area: CHART
  statement: "Each chart must lower to a monomorphised Rust variant (the chart a type parameter the compiler specialises); there must never be a runtime axis-kind interpreter or runtime chart-type switch on the compute side, and adding a chart must mean a lowering rule plus a kernel type."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_core_design.md
    section: "2. Authoring layer lowers to specialised kernels — by monomorphisation"
  verify:
    method: review checklist
    detail: "the compute kernel contains no runtime dispatch on axis kind or chart id; every chart is a generic instantiation"
  rulings: []
  milestone: M2
- id: REQ-CHART-002
  area: CHART
  statement: "Shape-sphere hemisphere redundancy ((θ, φ) ∼ (θ, π − φ)) must be expressed through system_image = n-to-1 with n = 2 (2-to-1 over the φ hemispheres, which decode to the same system, R-141) carrying its fold, and the chart must render one hemisphere and say so, or render both and flag the redundancy."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Part 1 — The physics: what the 8D actually is"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/design/principia_chart_reference.md
    section: 3.3 The chart map
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-104 — The new `system_image` value is `DoubleCover` *(closes RQ-64)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-141 — The shape sphere is 2-to-1 over its φ hemispheres *(closes RQ-71, corrects R-104)*"
  verify:
    method: golden image
    detail: "the shape-sphere render carries either one labelled hemisphere or both with a redundancy flag; a descriptor unit test reports n-to-1 with n = 2 (R-141)"
  rulings:
  - R-14
  - R-59
  - R-104
  - R-113
  - R-141
  milestone: M2
  note: "R-59 D5: has_redundant_hemisphere is replaced by system_image (inverse_encode_contract's flag table and chart_reference §3.3 now say so); R-104 names the shape sphere's value DoubleCover. R-113 (RQ-95): verified at M2 by golden image; the hemisphere toggle is a separate M8 GUI requirement."
- id: REQ-CHART-003
  area: CHART
  statement: "A chart must be a map Φ : [0,1]² → Y followed by the shared decoder D and canonicaliser C, with the affine slice z(s,t) = z₀ + (2s−1)q₁ + (2t−1)q₂ as one implementation of Φ — its scale living in q (zoom a common scale on q₁, q₂, no per-axis factors) — and nonlinear charts using the general Φ."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 3 — Charts
  - file: docs/design/principia_chart_reference.md
    section: Chart reference — the maths, for implementation
  - file: docs/design/principia_chart_reference.md
    section: 1.1 Affine slices — the "no tilt" case
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "The three coordinate spaces (they nest; each is right for its job)"
  - file: docs/contracts/principia_canonical_spec.md
    section: "4. Charts & navigation *(authoritative: `chart_decoder_contract`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 0. The ladder — the organising abstraction
  - file: decisions.md
    section: R-83 — The slice scale lives in q *(closes RQ-34)*
  verify:
    method: unit test
    detail: "affine chart: z(½,½) = z₀ and corners equal z₀ ± q₁ ± q₂; zoom scales q₁ and q₂ by one common factor; the chart has no s_u, s_v parameters"
  rulings:
  - R-83
  milestone: M2
- id: REQ-CHART-004
  area: CHART
  statement: "Chart axes must be one of the closed set of four kinds (raw latent, derived-in-block, cross-block invariant, coupled curve), authored CPU-side and lowered by monomorphisation to a specialised decode+map per chart, never interpreted at runtime."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The four axis kinds — a closed set
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Design axioms (the six that must survive contact with a code agent)"
  - file: docs/contracts/principia_canonical_spec.md
    section: "4. Charts & navigation *(authoritative: `chart_decoder_contract`)*"
  - file: docs/design/principia_core_design.md
    section: "2. Authoring layer lowers to specialised kernels — by monomorphisation"
  verify:
    method: review checklist
    detail: "no runtime axis-kind dispatch in the kernel; each chart is a type parameter of the kernel"
  rulings: []
  milestone: M2
- id: REQ-CHART-005
  area: CHART
  statement: "A derived-in-block (kind 2) axis must declare and label its residual convention for the leftover within-block DOF, and encode must use the same convention as the forward axis."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The four axis kinds — a closed set
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 5 — Inverse by axis kind (completing the four kinds)
  verify:
    method: unit test
    detail: "encode 'm₀ = 0.4' under hold-m₁:m₂ and hold-m₁=m₂: different z, each round-trips only under its own convention"
  rulings: []
  milestone: M2
- id: REQ-CHART-006
  area: CHART
  statement: "A cross-block invariant (kind 3) axis must sit downstream of every block it depends on, bring its feasibility boundary with infeasible pixels tagged, and use a joint solve when two coupled invariants are swept."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The four axis kinds — a closed set
  verify:
    method: unit test
    detail: "the validator refuses an invariant axis whose dependency set is not upstream; (L_z,E) solves jointly"
  rulings: []
  milestone: M2
- id: REQ-CHART-007
  area: CHART
  statement: "A coupled-curve (kind 4) axis must carry its embed map γ and tangent γ', and tilt on it must require a lock (evaluated at the centre)."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The four axis kinds — a closed set
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Directions are axis kinds — the unifying rule
  verify:
    method: unit test
    detail: "attempting to tilt a curve axis without a lock is refused; with a lock the tilt uses γ'(ν₀)"
  rulings: []
  milestone: M2
- id: REQ-CHART-008
  area: CHART
  statement: "A mixed-axis chart must let each axis state which latent coordinate or derived quantity it maps and its warp from [0,1], fix the other six by a stated convention, compose as Φ_mixed(u,v) = D(z(u,v)) with no new GPU logic, and inherit both axes' feasibility constraints."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The four axis kinds — a closed set
  - file: docs/design/principia_chart_reference.md
    section: 1.3 Mixed-axis charts
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  verify:
    method: unit test
    detail: "(α, p_ρ) chart decodes through the shared decoder; feasibility is the conjunction of both axes' checks"
  rulings: []
  milestone: M2
- id: REQ-CHART-009
  area: CHART
  statement: "The named compound directions (mass perturbation away from Burrau toward equal masses, energy increase at fixed L_z, and the Burrau-to-unconstrained morph) must be predefined q vectors usable as slice directions or tilt targets, evaluated at and recomputed with the chart centre."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Directions are axis kinds — the unifying rule
  - file: docs/design/principia_chart_reference.md
    section: 4.6 The central hypothesis
  verify:
    method: unit test
    detail: "direction library returns the three vectors; moving the centre changes the energy-at-fixed-L_z vector; the morph supports the §4.6 persistence test"
  rulings: []
  milestone: M2
- id: REQ-CHART-010
  area: CHART
  statement: "When a tilt or excursion carries the lock's neighbourhood outside an invariant chart's feasible region, those pixels must be tagged (never dropped), the condition must be detected, and the lock re-centred to the nearest feasible point or switched to a chart where it is valid."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The lock (projective microscope)
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "tilt a locked (L_z, E) view past the parabola; infeasible pixels tagged; lock validation reports and offers re-centre"
  rulings:
  - R-72
  milestone: M2
  note: "The label infeasible pixels carry: REQ-DEC-040 (definition)."
- id: REQ-CHART-011
  area: CHART
  statement: "The well-posedness validator must confirm, from each axis's block-touch set, conventions, invariant/curve status and the slice, that all 8 DOF are determined exactly once (annotation axes counting none) and refuse any chart that leaves a block under-determined or double-writes one."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/contracts/principia_canonical_spec.md
    section: "4. Charts & navigation *(authoritative: `chart_decoder_contract`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  verify:
    method: unit test
    detail: "fixtures: 2 swept + 6 frozen accepted; 1 swept + annotation + 7 frozen accepted; a double-written block and an under-determined block each refused"
  rulings: []
  milestone: M2
- id: REQ-CHART-012
  area: CHART
  statement: "Every chart axis must carry the metadata block-touch set, annotation-only, invariant (sector solved into, dependency set, conserved_along_flow — true for E and L_z, false for K), residual convention and curve (embed and tangent)."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  verify:
    method: review checklist
    detail: "axis descriptor struct has all fields; E and L_z set conserved_along_flow, K does not"
  rulings: []
  milestone: M2
- id: REQ-CHART-013
  area: CHART
  statement: "A chart with an annotation axis must be labelled as a 1-swept family rendered in 2D and support per-value statistics along the swept axis only, never 2D density claims across the annotation axis; the decoder must ignore the annotation axis."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  verify:
    method: unit test
    detail: "Euclid plane: varying m at fixed ν gives identical ICs; quantitative 2D-density queries on it are refused"
  rulings: []
  milestone: M2
- id: REQ-CHART-014
  area: CHART
  statement: "Every chart must declare the per-chart descriptors system_image (bijective, n-to-1 with its fold, DoubleCover — covers each shape twice, as two labelled systems — or ray-degenerate), has_feasibility_boundary, coupling (flagging same-block axis collisions for kind-4 charts) and annotation."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: decisions.md
    section: "R-27 — Both Burrau charts are kept, each labelled with its quotient *(CD-7)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-104 — The new `system_image` value is `DoubleCover` *(closes RQ-64)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-141 — The shape sphere is 2-to-1 over its φ hemispheres *(closes RQ-71, corrects R-104)*"
  - file: decisions.md
    section: "R-157 — `DoubleCover` stays, for the full-range Burrau chart *(closes RQ-127, amends R-141)*"
  verify:
    method: unit test
    detail: "each registered chart returns the descriptors; the shape sphere is n-to-1 with n = 2 (R-141); the full-range Burrau (Euclid) chart is DoubleCover (R-157)"
  rulings:
  - R-27
  - R-72
  - R-104
  - R-113
  - R-141
  - R-157
  milestone: M2
  note: "R-27 adds the value for 'covers each shape twice, as two labelled systems' and corrects the stale 'ray-degenerate' description of the Euclid plane; R-104 names it DoubleCover (chart_decoder_contract Part 5 lists four values). R-113 (RQ-95): the int (m,n) lattice's 'bijective' check is in REQ-CHART-037's verify (M4)."
- id: REQ-CHART-015
  area: CHART
  statement: "D and C must be shared and written once, so adding a chart means adding only a Φ, and no chart may carry its own decoder, integrator or renderer."
  source:
  - file: docs/design/principia_chart_reference.md
    section: Chart reference — the maths, for implementation
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  verify:
    method: review checklist
    detail: "no chart module contains mass/config/momentum decode formulae or integrator code; Burrau charts go through the shared decoder"
  rulings: []
  milestone: M2
- id: REQ-CHART-016
  area: CHART
  statement: "An oblique latent slice must record the orthonormal (q₁, q₂) pair used, and raw tilts must replace rather than compose, with the chart constructor as the commit mechanism."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 1.1 Affine slices — the "no tilt" case
  verify:
    method: unit test
    detail: "oblique slice's exported state contains q₁, q₂; applying two raw tilts yields the second, not the composition"
  rulings: []
  milestone: M2
- id: REQ-CHART-017
  area: CHART
  statement: "The invariant charts (L_z, E) and (L_z, K) must fix geometry and mass and map the unit square onto the feasible interior via the warp K(t) = K_max·t^γ_K (K_max > 0, γ_K ≥ 1), E(t) = U + K(t), L_max(t) = √(2·I·K(t)), L_z(s,t) = (2s−1)·L_max(t) (K* = K(t) directly on (L_z, K))."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 2. Invariant-momentum charts `(Lz, E)` and `(Lz, K)`
  - file: docs/design/principia_chart_reference.md
    section: 2.1 Feasibility, and the warp that makes every pixel valid
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: property test
    detail: "no (u,v) in [0,1]² produces K* < K_min under the warp (chart reference §5.2)"
  rulings:
  - R-71
  milestone: M2
  note: "Defaults for K_max and γ_K: REQ-CHART-044 (calibration)."
- id: REQ-CHART-018
  area: CHART
  statement: "The shape-sphere forward map must be shape_vec with u = ‖ρ̃‖² − ‖λ̃‖², v = 2ρ̃·λ̃, w = 2(ρ̃ ∧ λ̃) (standard positive cross), n = (u, v, w)/I, holding masses and momenta fixed."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3. Shape-sphere chart `(θ, φ)`
  - file: docs/design/principia_chart_reference.md
    section: 3.1 Forward map (already implemented as `shape_vec`)
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Part 1 — The physics: what the 8D actually is"
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  verify:
    method: unit test
    detail: "L⁺ (bodies 0→1→2 anticlockwise equilateral) → w = +1; collision of 0 and 1 → n = (−1, 0, 0); cross-check n against the IC Inspector's JS on random ICs"
  rulings:
  - R-14
  milestone: M2
- id: REQ-CHART-019
  area: CHART
  statement: "The shape-sphere chart must state which chart map it uses; the spherical map must be θ = 2π·s (azimuth, horizontal), φ = π·(1 − t) (polar from +w, vertical, L⁺ at the top), n = (sin φ cos θ, sin φ sin θ, cos φ), over the full φ ∈ [0, π] with no polar buffer; the exponential map n = cos(r)·n₀ + sin(r)·d/‖d‖ about an orthonormal frame (n₀, e₁, e₂) must be available as the nonlinear chart."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3. Shape-sphere chart `(θ, φ)`
  - file: docs/design/principia_chart_reference.md
    section: 3.3 The chart map
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: "R-12 — The shape-sphere chart map stays as the markdown has it *(closes RQ-10)*"
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-117 — The lowering appendix's shape-sphere row uses (θ, φ) *(closes RQ-85)*"
  verify:
    method: unit test
    detail: "(s,t) = (·, 1) → L⁺ (0,0,+1); (s,t) = (·,0) → L⁻; φ spans [0, π] with no excised cap; exp-map at d = 0 returns n₀"
  rulings:
  - R-12
  - R-14
  - R-117
  milestone: M2
- id: REQ-CHART-020
  area: CHART
  statement: "The shape-sphere inverse must be the closed form a = I(1+n₀)/2, b = I(1−n₀)/2, p = I·n₁/2, q = I·n₂/2, ρ̃ = √a(cos φ_f, sin φ_f), λ̃ = √b(cos ψ, sin ψ), ψ = φ_f + atan2(q, p), then unweighted and reconstructed per §0.2."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3.2 Inverse — closed form
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-178 — The shape-sphere round trip composes Φ alone *(closes A3)*"
  - file: decisions.md
    section: R-181 — The shape-sphere round-trip bound *(closes T1)*
  verify:
    method: numerical gate
    detail: "Φ alone (R-178): shape_vec(Φ(u,v)) == n(u,v) over random masses and shapes on both hemispheres, max |Δn| ≤ 1e−13 absolute (REQ-CHART-053, R-181); the canonical fold (Φ then C) checked separately on the upper hemisphere (R-141)"
  rulings:
  - R-14
  - R-178
  - R-181
  milestone: M2
- id: REQ-CHART-021
  area: CHART
  statement: "The Burrau family must use Euclid's parametrisation (a = m²−n², b = 2mn, c = m²+n² for coprime m > n > 0, m − n odd), right angle at the origin normalised by hypotenuse with each mass equal to its opposite side (r₀ = 0, r₁ = (a/c, 0), r₂ = (0, b/c); m = (c, b, a)/(a+b+c)), and state whether the normalised or historical (3,4,5)-mass convention is in use."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.1 The discrete family — Euclid's parametrisation
  - file: docs/design/principia_chart_reference.md
    section: 4.2 Positions and masses — Burrau convention
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  verify:
    method: unit test
    detail: "(m,n) = (2,1) gives (3,4,5) and masses (5,4,3)/12; the §4.1 table rows ((3,2)→(5,12,13), (4,1)→(15,8,17), (4,3)→(7,24,25)) reproduce; Jacobi ρ = (a/c, 0), λ = (−ab/(c(b+c)), b/c)"
  rulings:
  - R-22
  milestone: M2
- id: REQ-CHART-022
  area: CHART
  statement: "The continuous Burrau axis must use ν = n/m ∈ (0,1) with a ∝ 1 − ν², b ∝ 2ν, c ∝ 1 + ν², θ(ν) = arctan((1 − ν²)/(2ν)), everything varying smoothly with ν, and the (ν, K) chart must take K from §2's construction."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.3 Continuous interpolation — the `ν` axis
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  verify:
    method: unit test
    detail: "positions, masses and Jacobi vectors are continuous in ν; primitive-triple ν values reproduce the discrete family"
  rulings: []
  milestone: M2
- id: REQ-CHART-023
  area: CHART
  statement: "The Euclid plane must map m(u) = m_min(m_max/m_min)^u (default [1, 32]) and ν(v) linearly on [ν_min, ν_max] (default [1/32, 31/32]), decode from ν alone with Burrau masses and rest, treat m as an annotation axis, and overlay primitive triples where m, n ∈ ℤ, m > n, gcd(m,n) = 1, m − n odd."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  verify:
    method: unit test
    detail: "varying u at fixed v gives identical ICs; overlay marks exactly the primitive-triple lattice points"
  rulings: []
  milestone: M2
- id: REQ-CHART-024
  area: CHART
  statement: "The acute-angle Burrau charts must use θ ∈ (0, π/4], ν(θ) = sec θ − tan θ, and Φ_θ,K(u,v) = (ν(θ(u)), m_Burrau(ν(θ(u))), (L_z = 0, K(v))) with θ(u) linear on [θ_min, θ_max] and K(v) = K_max·v^γ_K, with the variants Φ_θ,L_z (fixed K, swept L_z) and Φ_θ,δm."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: unit test
    detail: ν(θ) inverts θ(ν) on (0, π/4]; L_z = 0 on Φ_θ,K
  rulings:
  - R-71
  milestone: M2
  note: "θ_min and θ_max: REQ-CHART-045 (calibration); Φ_θ,L_z's fixed K and L_z range: REQ-CHART-046 (calibration)."
- id: REQ-CHART-025
  area: CHART
  statement: "Both Burrau shape charts — the folded acute-angle chart (shape only) and the full-range Euclid chart (shape × labelling) — must be kept, each labelled via system_image with the quotient it covers (the full-range chart DoubleCover, replacing has_redundant_hemisphere), and shape fractions must not be taken from the full-range chart."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: decisions.md
    section: "R-27 — Both Burrau charts are kept, each labelled with its quotient *(CD-7)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-104 — The new `system_image` value is `DoubleCover` *(closes RQ-64)*"
  - file: decisions.md
    section: "R-157 — `DoubleCover` stays, for the full-range Burrau chart *(closes RQ-127, amends R-141)*"
  verify:
    method: review checklist
    detail: "both charts registered; system_image values differ and are shown in the chart label; fraction statistics refuse the full-range chart"
  rulings:
  - R-27
  - R-59
  - R-72
  - R-104
  - R-157
  milestone: M2
  note: "R-27 settles 'pending change 2, open' (canonical_spec §11 records it as ruled; chart_reference §4.5's paragraph still calls it open). Applied before any Burrau statistic. R-104 names the value DoubleCover (chart_reference §4.5 now says so)."
- id: REQ-CHART-026
  area: CHART
  statement: "The ternary mass plot must fix geometry at ν₀ and map x = u, y = (1−u)v, m₀ = 1−x−y, m₁ = x, m₂ = y, shrunk toward the barycentre by ε_m (default 10⁻⁴) as m ← (1 − 3ε_m)m + ε_m(1,1,1), with rest momenta and the Burrau point (c,b,a)/(a+b+c) as an overlay marker."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: docs/design/principia_chart_reference.md
    section: 4.4 Relaxations — each gives another chart
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  verify:
    method: unit test
    detail: "every pixel's masses are ≥ ε_m and sum to 1; the Burrau marker lands at the Burrau masses"
  rulings:
  - R-22
  milestone: M2
- id: REQ-CHART-027
  area: CHART
  statement: "The δm bifurcation strip must blend masses as m(u,v) = (1 − v)·m_Burrau(ν(θ(u))) + v·m_target (default equal masses) with rest momenta."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  verify:
    method: unit test
    detail: v = 0 row equals Burrau masses; v = 1 row equals m_target
  rulings: []
  milestone: M2
- id: REQ-CHART-028
  area: CHART
  statement: "Every chart must implement the Chart trait with map<F: Float>(u: F, v: F) -> ChartOut<F> generic over the float type (lowering Part 2), forbids_energy_normalisation(), name() (written in every dump header) and validate(u, v) -> ValidationResult, which stays CPU-side in f64."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: R-26 — Every chart declares its domain function *(CD-6)*
  - file: decisions.md
    section: R-118 — Φ is generic over the float type *(closes RQ-86)*
  - file: decisions.md
    section: R-140 — The four readings are accepted *(closes RQ-118)*
  verify:
    method: review checklist
    detail: "trait definition contains the four methods; map is generic over the float type and is instantiated at f32 by the kernel and at f64 on the CPU; validate takes f64 and runs on the CPU; each chart implements validate; dumps carry the chart name in their header"
  rulings:
  - R-26
  - R-118
  - R-140
  milestone: M2
  note: "R-26 adds validate to the trait (the doc's trait lacks it). R-118: chart_reference §5.1 reads map<F: Float>."
- id: REQ-CHART-029
  area: CHART
  statement: "Charts must be implemented in the order Latent {z0, q1, q2}, ShapeSphere {n0, e1, e2, I, phase}, Burrau {nu_range, k_range}, InvariantLE / InvariantLK, MassSimplex."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  verify:
    method: review checklist
    detail: build plan orders chart milestones as listed
  rulings: []
  milestone: M2
- id: REQ-CHART-030
  area: CHART
  statement: "The axis-aligned latent slice with q₁ = ê_α, q₂ = ê_β must equal a direct (α, β) sweep."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: docs/design/principia_chart_reference.md
    section: 1.1 Affine slices — the "no tilt" case
  verify:
    method: unit test
    detail: compare decoded IC per pixel between the two constructions
  rulings: []
  milestone: M2
- id: REQ-CHART-031
  area: CHART
  statement: "Every chart must declare forbids_energy_normalisation (true for (L_z, E) and (L_z, K)), and the validation pass must refuse, in code, any configuration combining such a chart with an energy-normalisation override."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "3.7 Energy normalisation (optional; keep-or-drop is decision B9)"
  - file: docs/design/principia_chart_reference.md
    section: "0.6 Energy normalisation — optional, and forbidden on some charts"
  - file: docs/design/principia_chart_reference.md
    section: 2. Invariant-momentum charts `(Lz, E)` and `(Lz, K)`
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: R-25 — `η_E` is kept, with an explicit off switch *(CD-5)*
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  verify:
    method: unit test
    detail: "a config combining (L_z, E) with an E* override is refused and a test asserts the refusal; same for (L_z, K)"
  rulings:
  - R-25
  - R-124
  milestone: M2
  note: "R-25 (applied now, R-124): the override is the explicit Option being Some — including E* = 0; the docs' 'non-zero E*' is conformed."
- id: REQ-CHART-032
  area: CHART
  statement: "Every link-registry entry must ship forward, inverse, log-det Jacobian, ε clamps and a sampling note, covering generation-root §3.9's table (simplex, bounded σ, bounded tanh, positive, symmetric, unbounded)."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: "3.9 The link registry (consolidated from chart contract Part 2.5)"
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Links carry a measure
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Integrity: the link is part of the experiment"
  verify:
    method: unit test
    detail: "registry enumerates the six links, each with all five members"
  rulings: []
  milestone: M2
- id: REQ-CHART-033
  area: CHART
  statement: "Decode and encode must consume only the link functions and inverses generated from the link registry, never inline transcendentals or re-derive inverses; link ids must be recorded in provenance, and a link swap must swap both directions."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: docs/design/principia_dd_encode.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: "T2 round-trips via registry inverses; swapping a link changes provenance and swaps the decode and encode directions together"
  rulings:
  - R-113
  milestone: M2
  note: "R-113 (RQ-95): 'a link swap changes the sim key, recompiles and re-integrates from t = 0' is a separate M4 requirement."
- id: REQ-CHART-034
  area: CHART
  statement: "Each link must satisfy: constraint preservation for all inputs incl. saturation; inverse round-trip within ε-clamp tolerance in physical units; analytic log-det matching a numeric Jacobian; C¹ continuity."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Three hard requirements on any registered link
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  verify:
    method: property test
    detail: "per link, fuzz across the domain for (a)–(d) of generation-root §5 test 8"
  rulings: []
  milestone: M2
- id: REQ-CHART-035
  area: CHART
  statement: "A chart with a physical-quantity axis must be nonlinear: pixels map through Φ, then the decoder."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 03 Chart builder
  verify:
    method: unit test
    detail: "adding a physical-quantity axis sets the chart kind to nonlinear and the decode path calls Φ"
  rulings: []
  milestone: M2
- id: REQ-CHART-036
  area: CHART
  statement: "The shape-sphere convention must be u = ‖ρ̃‖² − ‖λ̃‖², v = 2ρ̃·λ̃, w = 2(ρ̃ ∧ λ̃), n = (u, v, w)/I, θ the azimuth in (u, v) on the horizontal axis (0..2π) and φ the polar angle from +w on the vertical axis (0..π, L⁺ at top), with no polar buffer."
  source:
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-12 — The shape-sphere chart map stays as the markdown has it *(closes RQ-10)*"
  verify:
    method: unit test
    detail: "Numeric landmark tests: BC₀₁ → (−1, 0, 0), L⁺ → (0, 0, +1), all binary collisions at w = 0, equal masses 120° apart; cross-check n against the IC Inspector's JS on random ICs."
  rulings:
  - R-12
  - R-14
  milestone: M2
  note: "R-14 corrects R-12's premise; R-12's 'no polar buffer' stands."
- id: REQ-CHART-037
  area: CHART
  statement: "Each chart in the lowering appendix must lower to its stated Φ map, extra uniforms and flags (latent affine slice with its scale in q, shape sphere by chart_reference §3.3's (θ, φ) map with system_image n-to-1, n = 2 (R-141), (L_z,E)/(L_z,K) invariant charts writing tagged payloads for infeasible pixels in-kernel, ternary mass, Euclid ν plane annotation axis, θ×K strip requiring lock for curve tilt, Burrau integer lattice per-cell dispatch, Anosova physical-frame through canonicalise), all ending in canonicalise → (m,r,p) → wrapper(occupant)."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Appendix — worked enumeration of the current chart set
  - file: decisions.md
    section: R-26 — Every chart declares its domain function *(CD-6)*
  - file: decisions.md
    section: "R-27 — Both Burrau charts are kept, each labelled with its quotient *(CD-7)*"
  - file: decisions.md
    section: R-83 — The slice scale lives in q *(closes RQ-34)*
  - file: decisions.md
    section: "R-104 — The new `system_image` value is `DoubleCover` *(closes RQ-64)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-117 — The lowering appendix's shape-sphere row uses (θ, φ) *(closes RQ-85)*"
  - file: decisions.md
    section: "R-141 — The shape sphere is 2-to-1 over its φ hemispheres *(closes RQ-71, corrects R-104)*"
  verify:
    method: review checklist
    detail: "one row per chart checked against the chart registry and kernel variants; the shape-sphere row lowers (s,t) → (θ,φ) → n → (ρ̃, λ̃) (R-117); the int (m,n) lattice's system_image is bijective (R-113)"
  rulings:
  - R-26
  - R-27
  - R-83
  - R-104
  - R-113
  - R-117
  - R-141
  milestone: M4
  note: "R-104: the appendix's shape-sphere row names DoubleCover (2-to-1 over the φ hemispheres). R-117: that row is conformed to R-14's (θ, φ) map."
- id: REQ-CHART-038
  area: CHART
  statement: "Every navigation gesture must be a CPU-side edit of the one chart uniform (z₀, q₁, q₂) — pan/slice edit z₀, zoom/tilt edit the basis, the lock pins the centre — with non-raw slice/tilt directions taken as tangents at the centre, and the GPU must have no locked mode, view mode, camera object or second code path."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "4. Charts & navigation *(authoritative: `chart_decoder_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Part 4 — Navigation is chart construction (pan, slice, zoom, tilt, lock)"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Design axioms (the six that must survive contact with a code agent)"
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  verify:
    method: unit test
    detail: "each gesture produces only a diff of (z₀, q₁, q₂); the kernel has no mode flag"
  rulings:
  - R-69
  - R-92
  milestone: M5
  note: "The lock is chart construction, held in SimConfig and undoable (R-69; systems_architecture §6 now says so). Which gestures re-integrate and which re-address is R-92's: the slice plane is on the sim key."
- id: REQ-CHART-039
  area: CHART
  statement: "Validation must be CPU-side through each chart's validate(u, v) -> ValidationResult so the GPU never sees an invalid IC, and the quadtree, lookup/lock and the legend must query the same function (the quadtree to skip quads entirely outside the feasible region)."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: decisions.md
    section: R-26 — Every chart declares its domain function *(CD-6)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "quads above the (L_z, E) parabola are skipped by the scheduler; lookup and legend call validate"
  rulings:
  - R-26
  - R-72
  milestone: M5
  note: "Recorded; applied before the invariant charts and quad-skip. What a skipped quad's pixels carry: REQ-CHART-041 (definition); the scheduler's quad-skip rule, including quads partly inside the chart's domain: REQ-SCHED-083 (definition)."
- id: REQ-CHART-040
  area: CHART
  statement: "Each chart-builder axis must have a kind: a latent direction (any normalised mix of the eight z components, with a normalise button showing |q|), a physical quantity (energy, L_z, virial ratio, mass ratio…, with a range and what is held fixed per the kind-2 residual convention), or a Burrau dimension (Euclid (m, n) → the primitive triple, with the neighbourhood swept)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 03 Chart builder
  verify:
    method: GUI screenshot
    detail: "screenshot of each axis kind against 03_chartbuilder.png; normalise sets |q| = 1"
  rulings: []
  milestone: M8
- id: REQ-CHART-041
  area: CHART
  kind: definition
  statement: "What the pixels of a quad skipped as entirely outside the chart's feasible region carry must be defined in inverse_encode_contract's chart-aware validation by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: docs/design/principia_chart_reference.md
    section: 0.7 Degeneracy — every pixel gets a label
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the label a skipped quad's pixels carry, so that no pixel is unlabelled (chart_reference §0.7); physics reviewer approved"
  rulings:
  - R-72
  milestone: M5
- id: REQ-CHART-042
  area: CHART
  kind: definition
  statement: "The name of R-27's added system_image value ('covers each shape twice, as two labelled systems') must be defined in chart_decoder_contract Part 5 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: decisions.md
    section: "R-27 — Both Burrau charts are kept, each labelled with its quotient *(CD-7)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "chart_decoder_contract Part 5 lists the value beside bijective, n-to-1 and ray-degenerate; the full-range Burrau chart uses it; physics reviewer approved"
  rulings:
  - R-27
  - R-72
  milestone: M2
  retired: "R-104: the value is named DoubleCover, and chart_decoder_contract Part 5 and lowering's shape-sphere row now say so; carried by REQ-CHART-014."
- id: REQ-CHART-043
  area: CHART
  kind: definition
  statement: "The variants of R-26's ValidationResult (returned by validate(u, v)) must be defined in chart_reference §5.1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: R-26 — Every chart declares its domain function *(CD-6)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "chart_reference §5.1 lists the ValidationResult variants, covering project / clamp / reject and the failed constraint; physics reviewer approved"
  rulings:
  - R-26
  - R-72
  milestone: M2
- id: REQ-CHART-044
  area: CHART
  kind: calibration
  statement: "The invariant-chart warp defaults K_max (> 0) and γ_K (≥ 1) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 2.1 Feasibility, and the warp that makes every pixel valid
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal shows the (L_z,E) and (L_z,K) charts at the proposed defaults covering the feasible interior with usable resolution near K_min; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-CHART-045
  area: CHART
  kind: calibration
  statement: "The acute-angle Burrau axis range θ_min and θ_max (θ(u) = θ_min + (θ_max − θ_min)u within (0, π/4]) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the range with renders of the θ×K strip at the proposed defaults; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-CHART-046
  area: CHART
  kind: calibration
  statement: "Φ_θ,L_z's fixed K and its L_z range must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 4.5 The Burrau-family chart maps
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states K and the L_z range, feasible under |L_z| ≤ √(2IK), with a render at the defaults; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-CHART-047
  area: CHART
  kind: definition
  statement: "The shape-sphere chart's equal-area projection (Mollweide or Hammer–Aitoff: its formula and inverse) and whether the projection is a chart map (on the sim key, re-integrating) or a display remap of the equirectangular render must be defined in chart_reference §3.3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3.3 The chart map
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section names the projection with its forward and inverse maps and states whether switching projection changes the sim key; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-CHART-048
  area: CHART
  kind: definition
  statement: "Where the nonlinear charts' held values come from — the shape sphere's `m_fixed` and `p_fixed` (and the frame `p_fixed` is expressed in relative to the fibre-phase rotation that canonicalisation undoes) and its fibre phase `φ_f`, and the invariant charts' fixed geometry and masses must be defined in chart_reference §2 and §3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3. Shape-sphere chart `(θ, φ)`
  - file: docs/design/principia_chart_reference.md
    section: 3.2 Inverse — closed form
  - file: docs/design/principia_chart_reference.md
    section: 2. Invariant-momentum charts `(Lz, E)` and `(Lz, K)`
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 5 — Well-posedness and the validation contract
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "each chart's held blocks are traced to the slice pin (z₀) or a named chart parameter, with p_fixed's frame and φ_f stated; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-CHART-049
  area: CHART
  kind: calibration
  statement: "The agreement tolerance of the shape-sphere normal `n` computed by `shape_vec` against the IC Inspector's reference implementation on random ICs must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3.1 Forward map (already implemented as `shape_vec`)
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the tolerance with the measured max difference over the fixture's random ICs; recorded in decisions.md"
  rulings:
  - R-14
  - R-71
  milestone: M2
- id: REQ-CHART-050
  area: CHART
  kind: definition
  statement: "The \"direct `(α, β)` sweep\" that the axis-aligned latent slice with q₁ = ê_α, q₂ = ê_β must equal (its (u, v) → (α, β) map, and so the equality asserted) must be defined in chart_reference §5.2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: docs/design/principia_chart_reference.md
    section: 0.2 Configuration — hyperspherical mass-weighted Jacobi
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the direct sweep's map and the equality the test asserts; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-CHART-051
  area: CHART
  kind: definition
  statement: "The latent vectors of the named compound directions — the energy-increase-at-fixed-L_z tangent (the curve it is tangent to, its pull-back through the momentum links and its normalisation), the mass-perturbation-away-from-Burrau direction's normalisation, and the Burrau-to-unconstrained morph's source and target directions must be defined in chart_decoder_contract Part 4 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Directions are axis kinds — the unifying rule
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives each direction as a unit latent vector evaluated at the chart centre; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-CHART-052
  area: CHART
  statement: "A link swap must change the sim key, recompile and re-integrate from t = 0."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "swap one link: the sim key and the payload signature change, the kernel variant is recompiled and the march restarts at t = 0"
  rulings:
  - R-113
  milestone: M4
  note: Split from REQ-CHART-033 (R-113, RQ-95).
- id: REQ-CHART-053
  area: CHART
  kind: calibration
  statement: "The shape-sphere round-trip bound (max |shape_vec(Φ(u,v)) − n(u,v)|) must be calibrated: provisionally 1e−13 absolute, proposed with the measured maximum as evidence by the task that builds the inverse, used by CI provisionally, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3.2 Inverse — closed form
  - file: decisions.md
    section: R-181 — The shape-sphere round-trip bound *(closes T1)*
  verify:
    method: review checklist
    detail: "the proposal states the bound with the measured max |Δn| and its margin; recorded in decisions.md"
  rulings:
  - R-71
  - R-181
  - R-182
  milestone: M2
- id: REQ-INT-001
  area: INT
  statement: "The unwrapped phase `θ̃` must be a running SimState accumulator that adds the principal-value delta of `atan2(n_v, n_u)` each step, with `orbit_count = ⌊|θ̃|/2π⌋` and `retrograde = sign(θ̃) < 0` derived at read."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: "3.7 The shape readout and winding (live, per macro-step — lockstep, ratified)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "on a synthetic n(t) path circulating the w axis, θ̃ accumulates with no 2π jumps, orbit_count matches the path's turn count and retrograde matches its direction"
  rulings:
  - R-113
  milestone: M1
  note: "R-113 (RQ-94): dd test 8 on a real circulating bounded orbit is a separate M3 requirement."
- id: REQ-INT-002
  area: INT
  statement: "The physics must use dimensionless units G = M = I = 1 throughout, with a single global horizon T."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  verify:
    method: unit test
    detail: decoded ICs have total mass 1 and I = 1; G is the constant 1
  rulings: []
  milestone: M2
- id: REQ-INT-003
  area: INT
  statement: "The shape-sphere landmarks must be computed from the shape map with the current masses, not hard-coded: `BC₀₁` at `(−1, 0, 0)`, `L⁺` (bodies 0→1→2 anticlockwise) at `(0, 0, +1)`, all binary collisions on `w = 0`, and the collision landmarks placed at their mass-weighted positions."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: "3.7 The shape readout and winding (live, per macro-step — lockstep, ratified)"
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-50 — The shape-sphere collision landmarks are mass-weighted *(CO-1 (a))*"
  - file: docs/design/principia_chart_reference.md
    section: 3.4 Landmarks at known fixed coordinates
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  verify:
    method: unit test
    detail: "dd test 9: BC₀₁ -> (−1,0,0) for any masses; L⁺ -> (0,0,+1); random masses -> all collisions at w = 0; equal masses -> azimuths 180°, 60°, 300°"
  rulings:
  - R-14
  - R-22
  - R-50
  - R-124
  milestone: M2
  note: "R-50 (applied now, R-124): the collision landmarks are mass-weighted; dd_integrator §3.7's 'B18 still open' is conformed. Decides audit B18 left open in R-14."
- id: REQ-INT-004
  area: INT
  statement: "The shared kernel must be generic over its real type so the CPU runs it at f64 by default and at double-double on the same source, while the GPU runs it at f32."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/design/principia_core_design.md
    section: "Principle: divergence is the observable"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: unit test
    detail: "the kernel instantiates at f32, f64 and double-double and the same golden-IC test runs on each"
  rulings: []
  milestone: M3
- id: REQ-INT-005
  area: INT
  statement: "Compute occupants (steppers, regularisations, and separate integration protocols such as reversibility's forward+reverse) must be build-time Rust variants only; there must be no runtime source compilation, no runtime interpreter and no runtime-authored custom compute occupant."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/design/principia_core_design.md
    section: "4. Integrate and colour are separate passes — and now separate *mechanisms*"
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: review checklist
    detail: "the occupant set is closed at build time; no code path compiles or interprets user-authored compute code"
  rulings: []
  milestone: M3
- id: REQ-INT-006
  area: INT
  statement: "Regularisation must be an occupant slot independent of the stepper — none / AZ / Heggie / logH — with Heggie global regularisation the default."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: docs/design/principia_dd_predictability_horizon.md
    section: The predictability horizon
  - file: decisions.md
    section: "R-161 ✱ — Heggie's default time transformation is the measured one, Eq. 22 at n = 3/2"
  verify:
    method: unit test
    detail: "each stepper composes with each regularisation; the default config selects Heggie, with Eq. 22 at n = 3/2 as its time transformation and Eq. 20 selectable (R-161)"
  rulings:
  - R-19
  - R-161
  milestone: M3
- id: REQ-INT-007
  area: INT
  statement: "Integration must use a fixed dt and a deterministic count-bound schedule (on a fixed tau-schedule for occupants that own their time mapping), never wall-clock, so the same spec re-renders the same result up to cross-backend f32 tolerance with branches exact."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: the same spec rendered twice gives bit-identical CPU output
  rulings:
  - R-19
  - R-113
  milestone: M3
  note: "R-113 (RQ-97): the branch-exact GPU arm is covered in M4 by REQ-VAL-059 (Tier L)."
- id: REQ-INT-008
  area: INT
  statement: "The integrator must be structured as one swappable occupant inside a fixed shared wrapper, where the wrapper alone owns the loop, adaptive substepping, COM projection, invariant monitoring, terminal detection and the state readout, identical regardless of which occupant is bound, and each occupant must carry a capability profile."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: The pixel's life = one ladder traversal
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: property test
    detail: "seam 4: swap occupants (Euler, KDK, Yoshida-4, Yoshida-6, RK4) on the same IC set; only the §3.2 arithmetic changes and the wrapper code path is shared (no occupant-specific branch in wrapper code)"
  rulings:
  - R-19
  milestone: M3
  note: "Under R-19 the occupant seam is ADVANCE (see REQ-INT-020 and REQ-INT-053); for an unregularised stepper ADVANCE is this loop. systems_architecture names the seam STEP(state, dt, params); R-19 makes ADVANCE(state, t_now, t_target, params) the occupant seam"
- id: REQ-INT-009
  area: INT
  statement: "Under ADVANCE the per-substep cadence (COM projection, invariant accumulation, terminal detection) must be passed into the occupant as a callback and invoked after every internal step, so an occupant cannot skip it."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: docs/contracts/principia_integrator_contract.md
    section: What this preserves, and what it costs
  - file: decisions.md
    section: R-19 — Change 8 landed in full *(closes RQ-17)*
  verify:
    method: property test
    detail: "instrument the callback: for every occupant, callback invocations == internal steps taken; an occupant that omits the call fails to compile or fails the test"
  rulings:
  - R-19
  milestone: M3
- id: REQ-INT-010
  area: INT
  statement: "After every STEP the wrapper must project out CoM position and total linear momentum with `R_com = Σ mᵢrᵢ/M`, `P_com = Σ pᵢ`, `rᵢ ← rᵢ − R_com`, `pᵢ ← pᵢ − (mᵢ/M)P_com` (or `vᵢ ← vᵢ − V_com` for a velocity occupant), with `M` computed once and cached."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: COM projection and invariant monitoring
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: docs/design/principia_dd_integrator.md
    section: "3.4 COM projection (per `STEP`; the policy is integrator contract Part 1)"
  verify:
    method: unit test
    detail: "after one STEP from a state with nonzero CoM offset/momentum, R_com and P_com are zero to rounding; M is not recomputed per step"
  rulings: []
  milestone: M3
- id: REQ-INT-011
  area: INT
  statement: "The wrapper must never project, rescale or otherwise restore energy or angular momentum; they are monitored only."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: COM projection and invariant monitoring
  - file: docs/design/principia_dd_integrator.md
    section: "3.4 COM projection (per `STEP`; the policy is integrator contract Part 1)"
  verify:
    method: review checklist
    detail: no code path modifies state to restore E or L_z
  rulings: []
  milestone: M3
- id: REQ-INT-012
  area: INT
  statement: "Before the loop the wrapper must store `E_0` and `L_{z,0}`, and after every STEP (post-projection) evaluate `E(t)` and `L_z(t)` and accumulate `ΔE_final`, `max|ΔE|`, `ΔL_z` final and `max|ΔL_z|`, with relative forms `δ_E = ΔE/max(ε_E, |E_0|)` and `δ_L = ΔL_z/max(ε_L, |L_{z,0}|)`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: COM projection and invariant monitoring
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: docs/design/principia_dd_integrator.md
    section: 3.5 Invariant monitoring (per `STEP`, post-projection)
  verify:
    method: unit test
    detail: "rest start (E_0 ≠ 0, L_z0 = 0): δ_L finite; a synthetic spike-then-recover trace yields max|ΔE| ≫ |ΔE_final| (dd test 7)"
  rulings: []
  milestone: M3
- id: REQ-INT-013
  area: INT
  statement: "Every occupant must advertise a capability profile `{order, force_evals, symplectic, reversible, owns_time_mapping}` (plus the re-registration field of Part 2b), and downstream features must read the profile rather than assume occupant properties."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2a — Widening the slot: `owns_time_mapping`, and the `advance` signature"
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "profile struct exists with these fields for every composed occupant; energy-drift interpretation, the reversibility diagnostic and total_substeps_log2/FTLE confidence all read it (grep for occupant-name special cases outside the profile)"
  rulings:
  - R-19
  - R-72
  milestone: M3
  note: "How order sets the meaning of the total_substeps_log2 complexity proxy and FTLE confidence: REQ-INT-078 (definition)."
- id: REQ-INT-014
  area: INT
  statement: "The occupant table must be provided with these profiles: Euler (order 1, 1 eval, not symplectic, not reversible, debug only), KDK (2, 1, symplectic, Potato–Low default), Yoshida-4 (4, 3, symplectic, Medium–High default), Yoshida-6 (6, 7, symplectic, Ultra–Extreme default), RK4 (4, 4, not symplectic, not reversible, validation cross-check) and Aarseth–Zare + RK4 (4, 4, not symplectic, not reversible, owns time mapping). Aarseth–Zare + time-transformed leapfrog stays in Part 2a as an allowed future occupant, not a v1 requirement (R-162, R-168)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  - file: docs/contracts/principia_integrator_contract.md
    section: The table gains two rows
  - file: decisions.md
    section: R-162 — The reversible occupant is logH's TTL time mode
  - file: decisions.md
    section: "R-168 — REQ-INT-014 covers the built occupants; Aarseth–Zare + TTL is an allowed future occupant *(follows R-162)*"
  verify:
    method: unit test
    detail: "assert each built occupant's advertised profile and default tier binding equals the Part 2 / Part 2a tables"
  rulings:
  - R-162
  - R-168
  milestone: M3
- id: REQ-INT-015
  area: INT
  statement: "The Euler occupant must be kept as a debug occupant only (profile `symplectic: false`), never offered as a production option."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  verify:
    method: review checklist
    detail: "Euler is absent from tier defaults and production occupant lists; present only in debug selection"
  rulings: []
  milestone: M3
- id: REQ-INT-016
  area: INT
  statement: "The kernel's driver layer must be written fresh to Part 2c's rules rather than ported from prin-rs, while the step/deriv/Hamiltonian physics layer is ported by transcription."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: The split
  - file: docs/contracts/principia_integrator_contract.md
    section: The equations and the step control (R-160, R-161)
  - file: decisions.md
    section: "R-159 — The prin-rs reference set is imported *(closes RQ-102 and RQ-103, with R-160 to R-167)*"
  - file: decisions.md
    section: "R-160 — The integrator equations are transcribed into integrator_contract Part 2b"
  verify:
    method: review checklist
    detail: "driver code has no prin-rs lineage violating rules 1-5; physics layer is a transcription with 0 Vec/Box/dyn/std, of integrator_contract Part 2b's equations (R-160), with docs/reference/prin-rs as reference, not authority (R-159)"
  rulings:
  - R-159
  - R-160
  milestone: M3
- id: REQ-INT-017
  area: INT
  statement: "Stepper (RK4, KDK, Yoshida-N) and regularisation must be two independent occupant slots, with the regularisation slot offering `none` (Cartesian, kept permanently as the independent control), Aarseth–Zare (re-registers at every sync boundary), Heggie 1974 global (no reference body) and logH (time transformation only)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2b — Regularisation is a SECOND swappable axis, not a property of the stepper"
  verify:
    method: unit test
    detail: "every stepper composes with every regularisation occupant; `none` is always available"
  rulings: []
  milestone: M3
- id: REQ-INT-018
  area: INT
  statement: "The regularisation's re-registration count must be visible downstream, as a `re_registrations: u32` profile field or at minimum a per-trajectory count in the payload."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: The profile gains a field
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-160 — The integrator equations are transcribed into integrator_contract Part 2b"
  verify:
    method: review checklist
    detail: "a consumer can read, per method or per trajectory, how many sync-boundary re-registrations occurred"
  rulings:
  - R-72
  - R-160
  milestone: M3
  note: "Profile field, payload count or both (and the payload placement, a ledger change): REQ-INT-073 (definition)."
- id: REQ-INT-019
  area: INT
  statement: "The system must never switch between regularisation occupants automatically; the choice must be exposed to the user, with a report of which occupant would have been better."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: One thing deliberately NOT decided
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "no code path selects an occupant from trajectory state; the occupant is a user/sim-key choice"
  rulings:
  - R-72
  milestone: M3
  note: "How 'which would have been better' is judged, when, and where it is reported: REQ-INT-074 (definition)."
- id: REQ-INT-020
  area: INT
  statement: "The occupant seam must be `ADVANCE(state, t_now, t_target, params) -> state'`, where the wrapper owns the target time and the occupant owns how it gets there; KDK/Yoshida must implement it as the fixed `N_sub` STEP loop and Aarseth–Zare by stepping in fictitious time `tau` until it lands on `t_target`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2a — Widening the slot: `owns_time_mapping`, and the `advance` signature"
  - file: decisions.md
    section: R-19 — Change 8 landed in full *(closes RQ-17)*
  verify:
    method: unit test
    detail: "for KDK/Yoshida, ADVANCE(t, t+dt_macro) equals N_sub STEP calls bit-for-bit; for AZ, ADVANCE ends with physical time exactly t_target"
  rulings:
  - R-19
  milestone: M3
- id: REQ-INT-021
  area: INT
  statement: "`owns_time_mapping` must be reported for the composed occupant (stepper × regularisation) and must be true whenever the regularisation is not `none`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2a — Widening the slot: `owns_time_mapping`, and the `advance` signature"
  - file: decisions.md
    section: R-19 — Change 8 landed in full *(closes RQ-17)*
  verify:
    method: unit test
    detail: "enumerate every stepper × regularisation pair; owns_time_mapping == (regularisation != none)"
  rulings:
  - R-19
  milestone: M3
- id: REQ-INT-022
  area: INT
  statement: "Each occupant must report `total_substeps` honestly, and a test must assert the reported count equals the steps actually taken, since the wrapper can no longer verify `N_sub` itself."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: What this preserves, and what it costs
  verify:
    method: property test
    detail: "compare the reported total_substeps with the number of per-substep callback invocations over fuzzed ICs for every occupant"
  rulings: []
  milestone: M3
- id: REQ-INT-023
  area: INT
  statement: "Each occupant's step schedule must be count-bound on a fixed schedule (fixed `dt` or fixed `tau`-schedule), never wall-clock."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: What this preserves, and what it costs
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: decisions.md
    section: R-19 — Change 8 landed in full *(closes RQ-17)*
  verify:
    method: review checklist
    detail: "no wall-clock or timer read on any path that affects the number or size of steps; AZ's schedule is a fixed tau-schedule"
  rulings:
  - R-19
  milestone: M3
- id: REQ-INT-024
  area: INT
  statement: "The Aarseth–Zare slot must be specified to accept a time-transformed leapfrog in Mikkola–Tanikawa form (not plain KDK, since Gamma is not separable), and while RK4 fills it the profile must advertise `symplectic: false`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: The table gains two rows
  - file: decisions.md
    section: R-162 — The reversible occupant is logH's TTL time mode
  verify:
    method: review checklist
    detail: "AZ+RK4 profile has symplectic=false and reversible=false; the stepper slot under AZ accepts a Mikkola–Tanikawa leapfrog occupant"
  rulings:
  - R-162
  milestone: M3
  note: "R-162: Aarseth–Zare with Mikkola–Tanikawa isn't required; the slot is specified, and the reversible occupant is logH's TTL time mode (REQ-VAL-044)."
- id: REQ-INT-025
  area: INT
  statement: "The occupant must read only the per-sample IC `(m, r, p)`, `dt` and `G`; the IC must come per sample from the decoder (never a uniform) and the integrator must never learn the chart's name."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/contracts/principia_integrator_contract.md
    section: What this preserves, and what it costs
  - file: docs/design/principia_dd_integrator.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: property test
    detail: "seam 3: every chart in the lowering appendix drives the same kernel unmodified; the occupant signature takes no chart identifier"
  rulings: []
  milestone: M3
- id: REQ-INT-026
  area: INT
  statement: "Wrapper loop parameters must be bound once per frame/tier as `SimUniforms` and take the Part 3 table's defaults and owners (`T_horizon` ∈ [50, 200] with default 50, `dt_macro` = max(10⁻³, T/65535), `N_max` default 64 user-adjustable, `r_sub` = 0.05, `gamma_sub` = 1.5, `r_coll` user-exposed, `tau` and the 0.4 escape window, `r_close` = 0.01, `eps_E` = `eps_L` = 10⁻⁶, `G` = `M_total` = 1)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  verify:
    method: unit test
    detail: "default SimUniforms match the table (T_horizon = 50, dt_macro = 10⁻³); dt_macro = T/65535 for T above 65.535"
  rulings:
  - R-29
  - R-113
  - R-132
  milestone: M3
  note: "`tau` has no default in the corpus; it is set by measurement (R-29: re-measure the gap), tracked in open-questions.md, change 11 (i). R-113 (RQ-98): the sim-key clause is REQ-SCHED-007's (M4). R-132: T default 50, dt_macro = max(10⁻³, T/65535) (REQ-INT-081)."
- id: REQ-INT-027
  area: INT
  statement: "The substepper must expose the live current `N_sub` per macro-step for the animated effort view."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  verify:
    method: unit test
    detail: "readout of N_sub per macro-step equals the bucket value used for that macro-step"
  rulings: []
  milestone: M3
- id: REQ-INT-028
  area: INT
  statement: "`N_sub` must be chosen by a generated comparison tree of `d²` against 64 build-time-frozen f32 thresholds `THR[n] = round_f32((r_sub/n^{2/3})²)`, with no runtime sqrt, div, pow, loop or dynamic index, and must equal the specification `clamp(⌈(r_sub/r_min)^{γ_sub}⌉, 1, N_max)`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/design/principia_dd_integrator.md
    section: 3.3 Substep law — and the determinism pin, made concrete
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: "fuzz d² across and straddling every bucket edge; N_sub bit-identical on CPU-f64 and CPU-f32 (0 forks); table generated offline in f64 libm and only the f32 output ships"
  rulings:
  - R-34
  - R-113
  milestone: M3
  note: "R-113 (RQ-97): the native-GPU leg is REQ-VAL-059's N_sub (M4); the browser leg is the M8 browser Tier-L requirement closed with REQ-VAL-116."
- id: REQ-INT-029
  area: INT
  statement: "The substep cap N_max must bound the substep count per macro-step without terminating: on N_sub == N_max the trajectory advances with the capped (under-resolved) step, identically on both pipelines, and the sticky saturated descriptor bit (bit 5) is set at that point and is never a state value."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 6 — Three boundaries stated honestly (deferred work lands here)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "force a near-singular encounter: N_sub hits N_max, march continues to a dynamical outcome, saturated bit set"
  rulings:
  - R-68
  - R-86
  - R-113
  milestone: M3
  note: "R-86: saturated ⟺ N_sub == N_max (generation-root §3.1 now says so). R-68: the substep cap default is 64. R-113 (RQ-97): 'CPU and GPU take the same capped step' is covered in M4 by REQ-VAL-059 and REQ-VAL-061."
- id: REQ-INT-030
  area: INT
  statement: "COM projection must be part of the deterministic step on the parity surface, executed with identical arithmetic and operation order on CPU and GPU, and the integrator must work in CoM-frame particle coordinates with Jacobi-to-particle conversion done once at decode."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: "stepOnce on shared states: the projected state is bit-identical across repeated CPU-f32 runs and follows the one written operation order; no Jacobi conversion inside the step"
  rulings:
  - R-113
  milestone: M3
  note: "R-113 (RQ-97): the CPU-f32 vs GPU-f32 bit-for-bit match on Metal is covered in M4 by REQ-VAL-061 (one step)."
- id: REQ-INT-031
  area: INT
  statement: "Branch-cut crossing detection for the free-group word must run at every accepted substep, order simultaneous crossings by crossing fraction `τ = d₀/(d₀−d₁)` ascending with fixed cut-ID priority only as a tie-break, and count an endpoint on a cut iff it goes negative → non-negative, identically on CPU and GPU."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier B — integer-exact *given the same branch decisions* (integer & packed fields)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "synthetic double crossing in one substep orders by τ; equal τ uses cut-ID; endpoint exactly on the cut follows the half-open rule"
  rulings:
  - R-72
  - R-113
  milestone: M3
  note: "The τ-sort, the tie tolerance and the sign convention: REQ-INT-079 (definition). R-113 (RQ-97): 'word identical CPU/GPU' is covered in M4 by REQ-VAL-072 (the word's integer arithmetic)."
- id: REQ-INT-032
  area: INT
  statement: "The reversibility diagnostic must either replay the recorded macro-step + substep schedule exactly in reverse or bind a fixed-step reversible occupant with COM projection disabled, and must be available only for occupants whose profile has `reversible: true`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 6 — Three boundaries stated honestly (deferred work lands here)"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  verify:
    method: review checklist
    detail: "reversibility diagnostic refuses an occupant with reversible=false; implementation uses one of the two named routes"
  rulings: []
  milestone: M3
  note: Deferred work per Part 6 / dd §6.
- id: REQ-INT-033
  area: INT
  statement: "v1 must handle close approaches by substepping capped at `N_max` with the `saturated` flag, with Kustaanheimo–Stiefel regularisation deferred to v2."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 6 — Three boundaries stated honestly (deferred work lands here)"
  - file: docs/design/principia_dd_integrator.md
    section: 6. Deferred / flagged
  verify:
    method: review checklist
    detail: "no KS state representation in v1; saturated regions continue to their outcome"
  rulings: []
  milestone: M3
- id: REQ-INT-034
  area: INT
  statement: "On termination a sample must latch: its state stops advancing, its accumulators freeze and its state enum takes its terminal value."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/design/principia_dd_integrator.md
    section: "3.7 The shape readout and winding (live, per macro-step — lockstep, ratified)"
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  verify:
    method: unit test
    detail: "run synthetic samples to collision and to escape: the loop ends with done set at the terminating step; every SimState word is bit-identical on all subsequent dispatches; state == its terminal value"
  rulings:
  - R-95
  - R-103
  milestone: M3
  note: "For escape (R-95, R-103): state and t_end latch at the firing step, time averages freeze at t_esc, and in production done is set and the loop ends. The post-escape march for pitfalls §2.4's checks runs only in the validation harness, on its own state; the payload never sees it."
- id: REQ-INT-035
  area: INT
  statement: "Every sample must be a full SimState with its own Benettin shadow, and a Benettin shadow must never be included in the colour/spread sample pool."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_integrator.md
    section: 3.8 Co-computations (tier-gated, ride the forward pass)
  verify:
    method: unit test
    detail: "dd test 11: assert structurally that shadows are excluded from the colour/spread pool"
  rulings: []
  milestone: M3
- id: REQ-INT-036
  area: INT
  statement: "The force must be `aᵢ = Σ_{j≠i} mⱼ(rⱼ − rᵢ)/‖rⱼ − rᵢ‖³` with `G = 1` over the three pairs, sharing the `‖·‖³` computation with `r_min`."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.1 Force (G = 1)
  verify:
    method: unit test
    detail: "force on a known 3-body state matches the hand-computed value to f64 rounding; total force sums to zero"
  rulings: []
  milestone: M3
- id: REQ-INT-037
  area: INT
  statement: "The occupants must implement exactly the update rules and coefficients of dd_integrator §3.2: Euler, KDK, Yoshida-4 (`w₁ = 1/(2 − 2^{1/3})`, `w₀ = −2^{1/3}w₁`), Yoshida-6 solution A, and the standard RK4 tableau on `(r, p)`."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.2 Occupants
  verify:
    method: unit test
    detail: "coefficient constants equal §3.2 to all printed digits; w₀ + 2w₁ = 1; Yoshida-6 w₀ = 1 − 2(w₁+w₂+w₃)"
  rulings: []
  milestone: M3
- id: REQ-INT-038
  area: INT
  statement: "The horizon test must be an integer macro-step counter against `horizon_steps = ⌈T/dt_macro⌉`, never accumulated float time."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.3 Substep law — and the determinism pin, made concrete
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  verify:
    method: review checklist
    detail: "no `t += dt` feeding the t >= T branch; horizon compare is on an integer counter"
  rulings: []
  milestone: M3
- id: REQ-INT-039
  area: INT
  statement: "Each marching trajectory (the base, each Benettin shadow, each ensemble copy and its shadow) must be CoM-projected, monitored and detected independently every step; a shadow must not inherit the base's projection."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: "3.4 COM projection (per `STEP`; the policy is integrator contract Part 1)"
  verify:
    method: unit test
    detail: "run base+shadow with deliberately different roundoff; each has its own R_com computed; FTLE unchanged vs a reference that projects independently"
  rulings: []
  milestone: M3
- id: REQ-INT-040
  area: INT
  statement: "The drift suspect predicates must be read-time predicates over the stored drift latches (no stored suspect bits): energy-suspect on relative δE, L_z-suspect on absolute ΔL_z, and consumers must read the occupant's `symplectic` flag before interpreting drift as dynamics."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.5 Invariant monitoring (per `STEP`, post-projection)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  verify:
    method: unit test
    detail: "payload has no suspect bits; predicate evaluated at read; Euler + energy-drift view lights SUSPECT_ENERGY everywhere"
  rulings: []
  milestone: M3
- id: REQ-INT-041
  area: INT
  statement: "The diffusion slope must be a Welford streaming slope per macro-step: the time-only part derived in closed form (n = step_count, mean_t = (n+1)h/2, C_tt = h²n(n²−1)/12), the per-sample part updating mean_y and C_ty in two f32 accumulators with the old-mean time deviation δ_t = 0.5·n·h, never as stored (t, y) points or naive raw sums, and the slope C_ty/C_tt(n) read as a sentinel for n < 2."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.5 Invariant monitoring (per `STEP`, post-projection)
  - file: decisions.md
    section: "R-17 — The diffusion sentinel uses the streaming slope *(closes RQ-15)*"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 4. Welford diffusion (streaming regression)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Stability metrics are just running accumulators (they get simpler)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "G. Cross-check views (certify a *seam*, not a field — integration tests with a display)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "streaming slope equals least-squares slope of y on t for random series within f32 tolerance; n=0,1 returns the sentinel; no shared mutable global in the dispatch"
  rulings:
  - R-17
  - R-72
  milestone: M3
  note: "What the per-sample y is: REQ-PAY-073 (definition)."
- id: REQ-INT-042
  area: INT
  statement: "The shape vector `n = (u, v, w)/I` with `u = ‖ρ̃‖² − ‖λ̃‖²`, `v = 2ρ̃·λ̃`, `w = 2(ρ̃ ∧ λ̃)` from mass-weighted Jacobi coordinates must be derived live each macro-step from the current state, with no checkpoint array stored."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: "3.7 The shape readout and winding (live, per macro-step — lockstep, ratified)"
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  verify:
    method: unit test
    detail: "dd test 9: ‖n‖ = 1; equilateral -> n_w = ±1; collinear -> n_w = 0; n agrees with the IC Inspector's shapePoint on 5,000 random ICs to f64 round-off after the mirror fold"
  rulings:
  - R-14
  milestone: M3
- id: REQ-INT-043
  area: INT
  statement: "The Benettin FTLE must use one shadow renormalised every `n_renorm` steps (`S += log(δ/δ₀)`, shadow reset to `x + δ₀·δ⃗/δ`), and at read or termination finalise the partial interval `S_final = S + log(δ_current/δ₀)` then `λ = S_final/(step_count·dt_macro)`, with `completed_renorms = step_count / renorm_interval_steps` derived."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.8 Co-computations (tier-gated, ride the forward pass)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "dd test 10: halving δ₀ and doubling n_renorm leaves λ_T unchanged within tolerance; λ_T ≈ 0 on Kepler-embedded orbit, large on Burrau; reading between renorm boundaries is not biased low"
  rulings:
  - R-71
  - R-72
  milestone: M3
  note: "‖δ₀‖ and n_renorm: REQ-INT-076 (calibration); δ₀'s direction: REQ-INT-077 (definition)."
- id: REQ-INT-044
  area: INT
  statement: "The Levi-Civita inverse must use the conditioned branch, not the unstable u0 = sqrt((|rho| + rho.x)/2) form, so accuracy does not depend on absolute orientation."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: "7.3 The f32 horizon is falsified — and the cause is AZ, not precision"
  verify:
    method: numerical gate
    detail: "at rho angle 179.9°: f32 relative error ≈ 5.96e-8 (f32 eps) and f64 ≈ 1e-16, vs 2.2e-2 / 3.5e-9 for the unstable form"
  rulings:
  - R-35
  milestone: M3
  note: applies to the AZ occupant
- id: REQ-INT-045
  area: INT
  statement: "Threshold and suspect decisions must use the live f32 measurement, never an unpacked f16 latch."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  verify:
    method: review checklist
    detail: "every control-flow comparison on drift/d_min reads the live f32 value; grep the kernel for unpack2x16float feeding a branch"
  rulings: []
  milestone: M3
- id: REQ-INT-046
  area: INT
  statement: "Branch-cut crossing detection must run at every accepted adaptive substep, unless the topology contract proves that each tested segment holds at most one crossing."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  verify:
    method: unit test
    detail: "a fixture with a cross-and-recross inside one macro-step yields both crossings"
  rulings: []
  milestone: M3
- id: REQ-INT-047
  area: INT
  statement: "Crossings within one segment must be ordered by increasing `τ = d₀ / (d₀ − d₁)`, with a fixed cut-ID priority used only as a tie-break when `τ` values are equal within a defined tolerance."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "fixture segment crossing both cuts near their shared endpoint: symbols appended in τ order"
  rulings:
  - R-72
  milestone: M3
  note: "The tie tolerance (with the τ-sort and sign convention): REQ-INT-079 (definition)."
- id: REQ-INT-048
  area: INT
  statement: "A crossing must be counted iff the signed distance goes from negative to non-negative (`d < 0` then `d ≥ 0`), the reverse transition being the other direction, identically on CPU and GPU."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: 1. Generator ↔ branch-cut convention
  - file: decisions.md
    section: R-150 — REQ-INT-048's GPU arm leaves M3 *(closes RQ-120)*
  verify:
    method: unit test
    detail: "endpoints exactly on a cut: no duplicate or missed crossing; the GPU arm is REQ-VAL-072's at M4 (R-150)"
  rulings:
  - R-150
  milestone: M3
- id: REQ-INT-049
  area: INT
  statement: "The Aarseth–Zare occupant's final fictitious-time step in each sync sub-interval must be shrunk to land exactly on the boundary: `dtau_eff = min(dtau, max(dt_left − t_so_far, 0)/(A·B))`."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 0.1 The mechanism, and the fix — measured, not proposed
  - file: docs/design/principia_dd_validation_orbits.md
    section: 5. Immediate action
  verify:
    method: numerical gate
    detail: "figure-eight closure after one period: ≤ 4.232e-09 at eta = 0.001 (vs 2.818e-03 without the fix); convergence better than first order"
  rulings: []
  milestone: M3
- id: REQ-INT-050
  area: INT
  statement: "A regularisation-free occupant must be kept permanently, sharing no coordinate machinery with the others, as the arm that adjudicates when regularised occupants disagree."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.3 The control that found it
  - file: decisions.md
    section: R-164 — +0.305 and −0.082 are correlations, not controls
  verify:
    method: unit test
    detail: "the occupant registry includes an unregularised (leapfrog) occupant sharing no coordinate machinery with the others; no gate rests on the FTLE–drift Spearman correlations (+0.305 leapfrog, −0.082 AZ, a null against its shifted control), which are prior findings, not controls (R-164)"
  rulings:
  - R-164
  milestone: M3
  note: "pitfall: \"4.3 The control that found it\" — 4.3 The control that found it."
- id: REQ-INT-051
  area: INT
  statement: "The default regularisation must be Heggie global regularisation, which never re-registers the state through a reference-body chart."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.4 The remedy, and its own falsification test
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.1 The finding
  - file: docs/read_first/principia_INDEX.md
    section: The evidence base — where settled defaults were measured
  - file: docs/design/principia_dd_integrator.md
    section: Drill-down — the Integrator (Physics rung)
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2b — Regularisation is a SECOND swappable axis, not a property of the stepper"
  - file: decisions.md
    section: "R-161 ✱ — Heggie's default time transformation is the measured one, Eq. 22 at n = 3/2"
  - file: decisions.md
    section: R-165 — The 32-case figure is 3915 → 74
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: numerical gate
    detail: "Re-run the Heggie vs AZ comparison on the 32-case matrix (fixtures/case_matrix.toml): Heggie must win on the recorded cases (31 of 32; err>10, error_ratio > 10, 3915 → 74 at prin-rs 8600d45, the original run at 70cfbc4 giving 3916 → 73, R-165) with AZ winning only on 'far'; the default occupant config names Heggie, and the re-run confirms its default time transformation, Eq. 22 at n = 3/2 (R-161)."
  rulings:
  - R-161
  - R-165
  - R-166
  milestone: M3
  note: "pitfall: \"4.4 The remedy, and its own falsification test\"; pitfall: \"4.1 The finding\" — 4. RE-REGISTRATION COUNT — the mechanism that had to be found twice."
- id: REQ-INT-052
  area: INT
  statement: "The predictive step limit must be on by default, and the magenta non-finite pixels and the wedges must both stay removed as two distinct defects."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 8. TWO ARTEFACTS ARE NOT ONE DEFECT
  - file: docs/read_first/principia_INDEX.md
    section: The evidence base — where settled defaults were measured
  - file: decisions.md
    section: R-163 — The wedge ablation is re-run on Heggie
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: numerical gate
    detail: "Ablation on config_stability (fixtures/slices.toml) with prin-rs wedge_census.rs's three switches (none / dtau only / clamp only / limit only / all three), re-run on Heggie with Aarseth–Zare kept for comparison (R-163), wedge density being at least 25% pale pixels in a 9×9 window at 1024²: the shipping config must give non-finite = 0 and wedge density ≈ 0.0001 (the limit-only / all-three rows), and both columns are reported separately."
  rulings:
  - R-163
  - R-166
  milestone: M3
  note: "pitfall: \"8. TWO ARTEFACTS ARE NOT ONE DEFECT\" — 8. TWO ARTEFACTS ARE NOT ONE DEFECT. INDEX: the predictive step limit, not the dtau fix, removed the wedges."
- id: REQ-INT-053
  area: INT
  statement: "ADVANCE(state, t_now, t_target, params) must be the occupant seam, with regularisation a separate axis, owns_time_mapping reported for the composed occupant (true whenever regularisation is not none), and the per-substep cadence passed in as a callback."
  source:
  - file: decisions.md
    section: R-19 — Change 8 landed in full *(closes RQ-17)*
  verify:
    method: unit test
    detail: "For each stepper × regularisation composition assert owns_time_mapping == (regularisation != none); the cadence callback is invoked once per substep."
  rulings:
  - R-19
  milestone: M3
- id: REQ-INT-054
  area: INT
  statement: Law 18's substep count bound must be on a fixed τ-schedule.
  source:
  - file: decisions.md
    section: R-19 — Change 8 landed in full *(closes RQ-17)*
  verify:
    method: property test
    detail: "Fuzz ICs; the substep count per sync interval never exceeds the Law 18 bound computed on the fixed τ-schedule."
  rulings:
  - R-19
  milestone: M3
- id: REQ-INT-055
  area: INT
  statement: The inspector's CPU reference integration must be RK45.
  source:
  - file: decisions.md
    section: "R-33 — The independent convergence reference is Brutus-style *(IE-5, amended)*"
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  verify:
    method: review checklist
    detail: computeIC's inspector path uses the RK45 integrator in f64
  rulings:
  - R-33
  milestone: M3
  note: "R-33: 'RK45 stays the inspector's reference.' trajectory_viewing calls computeIC the Precision-ring path."
- id: REQ-INT-056
  area: INT
  statement: "The Yoshida-6 coefficients must be checked against Yoshida (1990) Table 1 solution A, and the w₂ < 0 label fixed, before Yoshida-6 enters the shared source."
  source:
  - file: decisions.md
    section: "R-57 — The Yoshida-6 coefficients are checked against Yoshida (1990), and the `w₂ < 0` label fixed *(TO-1)*"
  - file: docs/design/principia_dd_integrator.md
    section: 3.2 Occupants
  - file: docs/design/principia_dd_integrator.md
    section: 6. Deferred / flagged
  - file: open-questions.md
    section: Audit section C — transcription checks
  verify:
    method: unit test
    detail: "Coefficients in the shared source equal the published digits; composition order test (6th-order convergence slope on a Kepler fixture)."
  rulings:
  - R-57
  milestone: M3
  note: "R-57 also fixes the 'w₂ < 0' label in integrator contract Part 6 (the negative Yoshida-6 weight is w₁; for Yoshida-4 it is w₀) — a doc fix, not a build obligation."
- id: REQ-INT-057
  area: INT
  statement: "The wrapper's branch decisions made from the current state (substep bucket N_sub, cap decision, collision, escape, horizon, SIM_FAILED and terminal) must be bit-identical across the CPU and every GPU backend on identical inputs, per step, while continuous values (positions, momenta, energy, trajectory, and the outcome class they decide near a chaotic boundary) are left free to diverge and are never reconciled, and labels on chaotic trajectories may differ across precisions and are reported, not suppressed."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/design/principia_core_design.md
    section: "Principle: divergence is the observable"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/notes/principia_gpu_determinism_note.md
    section: "Why branches are special (continuous divergence is fine; branch divergence is not)"
  - file: docs/contracts/principia_parity_contract.md
    section: Tier L — exact, no tolerance (the bulk of the suite)
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  verify:
    method: property test
    detail: "identical per-step inputs (fuzzed states) dispatched on GPU-f32 and CPU-f64: branch decisions bit-exact; no code adjusts continuous values toward agreement; no test asserts label equality along a chaotic trajectory"
  rulings:
  - R-34
  - R-84
  milestone: M4
  note: "R-84: parity_contract governs — branch decisions are identical on identical inputs, per step; labels on chaotic trajectories may differ across precisions (Tier L / Tier B)."
- id: REQ-INT-058
  area: INT
  statement: "Kernel code must follow the GPU compile-discipline checklist: comparison-only branches, no non-finite literals, constant/unrolled indices, flag-in-while-condition loops with zero breaks, clamp in f32 before any float→int cast, and explicit fma."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: review checklist
    detail: "naga validation passes; a lint over the kernel finds no break, no f32::INFINITY/NaN literal, no dynamic index, no unclamped float→int cast"
  rulings: []
  milestone: M4
- id: REQ-INT-059
  area: INT
  statement: "Terminal exit from the integration loops must be via a `done` flag, set by detect_terminal on collision or escape, tested in each `while` loop condition, with zero `break` statements and no counted `for` loop with a mid-body exit."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: review checklist
    detail: "grep the kernel: no `break` in march loops, no `for s in 0..N_sub`; plus a GPU dispatch of 100 macro-steps in one dispatch through SPIR-V->MSL (Metal) and on lavapipe (R-110) matches the CPU branch words"
  rulings:
  - R-103
  - R-110
  - R-113
  milestone: M4
  note: "R-103: in production, done is set when escape fires and the loop ends (integrator_contract Part 1's pseudocode now says so); the §2.4 post-escape march lives in the validation harness. R-113 (RQ-97): the SPIR-V->WGSL-> browser-compiler leg is the M8 browser Tier-L requirement."
- id: REQ-INT-060
  area: INT
  statement: "No precision-dependent constant (a value that differs between the f32 and f64 instantiations) may appear in a control-flow decision; a tolerance that must scale with precision must be applied where the value is used, never where the branch is taken."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Rules the new kernel must hold by construction
  verify:
    method: review checklist
    detail: "audit every branch condition in the kernel: no `Real`-dependent threshold (e.g. prin-rs SYNC_EPS / LAND_EPS_REL)"
  rulings: []
  milestone: M4
- id: REQ-INT-061
  area: INT
  statement: "Every branch decision in the kernel must depend only on comparisons against compile-time constants, integer counters and single-rounded arithmetic, never on a runtime transcendental (`pow`, `sqrt`, `div`)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Rules the new kernel must hold by construction
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/design/principia_dd_integrator.md
    section: 3.3 Substep law — and the determinism pin, made concrete
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The one-line law
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier B — integer-exact *given the same branch decisions* (integer & packed fields)"
  verify:
    method: review checklist
    detail: "enumerate all branch inputs; none is fed by runtime pow/sqrt/div; the spike control (runtime ceil((r_sub/sqrt(d2))^1.5)) is absent"
  rulings: []
  milestone: M4
- id: REQ-INT-062
  area: INT
  statement: "Every march loop in the kernel must have a bound (no bare `loop {`)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Rules the new kernel must hold by construction
  verify:
    method: review checklist
    detail: "grep kernel for `loop {`; every loop has a count bound usable by the watchdog and dispatch chunking"
  rulings: []
  milestone: M4
- id: REQ-INT-063
  area: INT
  statement: "Min/max folds in the kernel must be seeded from the first element, and no non-finite literal (e.g. `f32::INFINITY`) may appear in kernel source."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Rules the new kernel must hold by construction
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  verify:
    method: unit test
    detail: "naga validation of the compiled kernel passes (no infinite float literal); an all-unusable input folds to a defined value, not 0 or inf"
  rulings: []
  milestone: M4
- id: REQ-INT-064
  area: INT
  statement: "Every multi-op arithmetic expression feeding a branch input — including `d²`, the windowed `|Δn̂|` and `E_rel` — must use explicit `fma` in a fixed written form, and these branch inputs must be enumerated in the source rather than relying on a backend contraction switch."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: And FMA contraction is a live question
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier B — integer-exact *given the same branch decisions* (integer & packed fields)"
  - file: decisions.md
    section: "R-34 — FMA: explicit fma at every branch input, enumerated *(IE-6)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  verify:
    method: review checklist
    detail: "an enumerated list of branch inputs exists; each multi-op input is written with explicit fma; no reliance on per-backend FP-contraction settings"
  rulings:
  - R-34
  milestone: M4
  note: "R-34 (recorded; applied before the parity gate on a second backend) settles Part 2c's open 'check whether contraction is controllable per backend' in favour of explicit fma everywhere. Recorded; applied before the parity gate on a second backend."
- id: REQ-INT-065
  area: INT
  statement: "Occupant selection and the `FTLE_ENABLED` co-computation selection must select baked kernel variants, never runtime uniforms, and be on the sim key, while the ensemble must not be a baked variant: `copy_index` must be a uniform and each copy the same kernel dispatched again."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: decisions.md
    section: R-102 — The ensemble isn't a baked variant *(closes RQ-62)*
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  verify:
    method: review checklist
    detail: "no runtime uniform branches on occupant or FTLE; each (occupant, FTLE) combination is a distinct compiled variant; E = 0 and E = 3 use the same pipeline, the copies differing only in the copy_index uniform"
  rulings:
  - R-89
  - R-102
  - R-124
  milestone: M4
  note: "R-124: systems_architecture §5.5 is conformed to R-102 (copies dispatched again, not looped)."
- id: REQ-INT-066
  area: INT
  statement: "The `d²` fed to the substep bucket must be position-quantised: positions cast to f32, then a lone subtract, a lone multiply and an explicitly written `fma(dy, dy, dx*dx)`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/design/principia_dd_integrator.md
    section: 3.3 Substep law — and the determinism pin, made concrete
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  verify:
    method: property test
    detail: "d² computed on every backend is bit-identical for fuzzed position pairs"
  rulings: []
  milestone: M4
- id: REQ-INT-067
  area: INT
  statement: "Any float-to-int cast in the kernel must be preceded by a clamp in f32 into the target range."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  - file: docs/design/principia_dd_integrator.md
    section: 3.3 Substep law — and the determinism pin, made concrete
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  verify:
    method: review checklist
    detail: "every float->int conversion (bucket index etc.) is preceded by an explicit f32 clamp; no out-of-range OpConvertFToU is reachable"
  rulings: []
  milestone: M4
- id: REQ-INT-068
  area: INT
  statement: "`N_max` must be a sim-key tunable used with the same value by both pipelines."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  verify:
    method: unit test
    detail: "change N_max -> sim-key change; parity harness passes N_max identically to both instantiations"
  rulings: []
  milestone: M4
- id: REQ-INT-069
  area: INT
  statement: "The Benettin shadow co-computation must be gated by a baked tier flag within the one forward kernel, and each ensemble copy must be the same kernel dispatched again with copy_index as a uniform (at E = 0, simply not dispatched)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 8 — Errata against the older design docs
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  - file: decisions.md
    section: R-102 — The ensemble isn't a baked variant *(closes RQ-62)*
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  verify:
    method: review checklist
    detail: "FTLE-off variant contains no shadow state; no ensemble kernel variant exists; E = 0 dispatches no copies"
  rulings:
  - R-102
  - R-124
  milestone: M4
  note: "R-124: systems_architecture §5.5 is conformed to R-102 (copies dispatched again, not looped)."
- id: REQ-INT-070
  area: INT
  statement: "The kernel must take the chart description (z₀, basis vectors, chart id + params) as a uniform and per sample map → z → decode → (m, r, p) → integrate in-kernel, never from a pre-decoded IC buffer, keeping per-frame CPU cost O(1)."
  source:
  - file: docs/design/principia_core_design.md
    section: 3. Kernel eats chart params, decodes in-kernel
  verify:
    method: review checklist
    detail: "no IC buffer exists; per-frame CPU work is a uniform write and dispatch"
  rulings: []
  milestone: M4
- id: REQ-INT-071
  area: INT
  statement: "Kernel loops over a small fixed iteration count (such as the three body pairs) must use constant indices or unrolled pairs, never runtime-indexed arrays that inject implicit bounds-check exits."
  source:
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  verify:
    method: unit test
    detail: "GPU vs CPU Hamiltonian on a fixed state sums all three body pairs (e.g. H = -1.732 not -0.577 on the spike state)"
  rulings: []
  milestone: M4
- id: REQ-INT-072
  area: INT
  statement: "The ensemble co-computation must compute, over each footprint's E+1 samples at resolve, outcome entropy H = −Σ pᵢ log pᵢ and spread σ²_T = (1/(E+1)) Σ ‖Φ_T(x₀⁽ᵏ⁾) − x̄(T)‖²."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.8 Co-computations (tier-gated, ride the forward pass)
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "dd test 11: deep basin-interior pixel gives H = 0 and tiny σ²; boundary-straddling pixel gives H > 0"
  rulings:
  - R-80
  - R-113
  milestone: M5
  note: "R-113 (RQ-98): the copy offsets (copy 0 un-jittered, copies 1..E at centred, footprint-scaled Halton points) are a separate M4 requirement."
- id: REQ-INT-073
  area: INT
  kind: definition
  statement: "Where the re-registration count lives — a re_registrations profile field, a per-trajectory payload count, or both, with its payload placement as a ledger change — must be defined in integrator_contract Part 2b by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: The profile gains a field
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 2b states the choice; any payload field appears in the ledger with a schema-version change; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-INT-074
  area: INT
  kind: definition
  statement: "How the report of which regularisation occupant would have been better is judged, when it runs and where it is reported must be defined in integrator_contract Part 2b by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: One thing deliberately NOT decided
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 2b states the judging criterion, the trigger and the report location, without any automatic switching; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-INT-075
  area: INT
  kind: definition
  statement: "The treatment of the r_coll / N_max coupling — surfaced in the UI, or very small r_coll raising N_max at a cost the quality controller accounts for — must be defined in integrator_contract Part 7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 7 states which of the two options holds and how it is exposed; physics reviewer approved"
  rulings:
  - R-72
  milestone: M5
- id: REQ-INT-076
  area: INT
  kind: calibration
  statement: "The Benettin shadow's initial separation ‖δ₀‖ and the renormalisation interval n_renorm must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.8 Co-computations (tier-gated, ride the forward pass)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows dd test 10 (halving δ₀ and doubling n_renorm leaves λ_T unchanged) at the proposed values at f32 and f64; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-INT-077
  area: INT
  kind: definition
  statement: "The direction of the Benettin shadow's initial offset δ₀ must be defined in dd_integrator §3.8 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.8 Co-computations (tier-gated, ride the forward pass)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§3.8 fixes δ₀'s direction deterministically (same on CPU and GPU); physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-INT-078
  area: INT
  kind: definition
  statement: "How an occupant's order sets the meaning of the total_substeps_log2 complexity proxy and FTLE confidence must be defined in integrator_contract Part 2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 2 gives the mapping from profile order to the proxy's interpretation and to FTLE confidence; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-INT-079
  area: INT
  kind: definition
  statement: "The τ tie rule — the τ-sort, the tolerance within which τ values count as equal, and the sign convention — must be defined in the integrator contract by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the integrator contract states the sort, the tie tolerance (a comparison-only branch, identical on CPU and GPU) and the sign convention; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-INT-080
  area: INT
  kind: calibration
  statement: "The regularised occupants' sync schedule — the number of sync boundaries n_sync over the horizon and Aarseth–Zare's step factor eta in dtau = eta·dt_left/(A·B) — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 0.1 The mechanism, and the fix — measured, not proposed
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2a — Widening the slot: `owns_time_mapping`, and the `advance` signature"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal gives n_sync and eta per regularised occupant (declared by the occupant, on the sim key with its config), with the figure-eight closure and drift evidence across the eta / n_sync ladder of dd_validation_orbits §0.1; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-INT-081
  area: INT
  statement: "The horizon T_horizon must default to 50 within integrator_contract's [50, 200], and dt_macro must be max(10⁻³, T/65535), so the dispatch-enforced ⌈T/dt_macro⌉ ≤ 65535 holds over the whole range and the u16 step indices stay exact (R-86)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  - file: decisions.md
    section: "R-156 — The plan-pass readings are accepted *(closes RQ-126)*"
  verify:
    method: unit test
    detail: "default T_horizon = 50 with dt_macro = 10⁻³; at T = 200, dt_macro = 200/65535 and ⌈T/dt_macro⌉ = 65535; no T in [50, 200] is refused at dispatch"
  rulings:
  - R-71
  - R-132
  - R-156
  milestone: M3
  note: "R-132 rules the values the R-71 calibration asked for; nothing here stays calibrated."
- id: REQ-INT-082
  area: INT
  statement: "The unwrapped phase θ̃ accumulated over a real march must have no 2π jumps, with the derived orbit_count and retrograde matching the orbit."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: "3.7 The shape readout and winding (live, per macro-step — lockstep, ratified)"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "dd test 8: on a circulating bounded orbit θ̃ has no 2π jumps, orbit_count matches a hand count, retrograde matches the L_z sign"
  rulings:
  - R-113
  milestone: M3
  note: Split from REQ-INT-001 (R-113, RQ-94).
- id: REQ-INT-083
  area: INT
  statement: "Each footprint's E+1 ensemble samples must be copy_index 0..E, copy 0 the un-jittered centre and copies 1..E at Halton (2,3) points 1..E centred (minus ½) and scaled to the footprint."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.8 Co-computations (tier-gated, ride the forward pass)
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "copy 0's offset is exactly zero; copy k's offset equals (Halton_k − ½) scaled to the footprint for k = 1..E"
  rulings:
  - R-80
  - R-113
  milestone: M4
  note: Split from REQ-INT-072 (R-113, RQ-98).
- id: REQ-INT-084
  area: INT
  kind: definition
  statement: "integrator_contract Part 2b must carry the Heggie, logH and TTL equations of motion, their time transformations and their step control, and the predictive step limit (dτ ≤ f·d_min / (|v_rel|_max·A·B), f = 0.02), transcribed from the prin-rs reference set with citations (Heggie 1974; Mikkola & Tanikawa 1999; Preto & Tremaine 1999), with Heggie's default time transformation Eq. 22 at n = 3/2 and Eq. 20 selectable; physics-reviewed and confirmed at the M3 gate."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: The equations and the step control (R-160, R-161)
  - file: decisions.md
    section: "R-160 — The integrator equations are transcribed into integrator_contract Part 2b"
  - file: decisions.md
    section: "R-161 ✱ — Heggie's default time transformation is the measured one, Eq. 22 at n = 3/2"
  - file: decisions.md
    section: R-162 — The reversible occupant is logH's TTL time mode
  - file: decisions.md
    section: "R-159 — The prin-rs reference set is imported *(closes RQ-102 and RQ-103, with R-160 to R-167)*"
  verify:
    method: review checklist
    detail: "Part 2b gives the equations, time transformations and step control for Heggie, logH and TTL and the predictive step limit, each cited to its paper and to the prin-rs file it was transcribed from; the physics reviewer approved; the human confirmed it at the M3 gate"
  rulings:
  - R-159
  - R-160
  - R-161
  - R-162
  milestone: M3
  note: "Transcribed from docs/reference/prin-rs (reference, not authority, R-159); the TTL verdict (built, validated, loses on accuracy) is recorded as a prior finding (R-162)."
- id: REQ-EVT-001
  area: EVT
  statement: "Every pixel must get a labelled output — escape, collision or bounded (reaching the horizon is bounded), sim_failed or decode_failed, or RUNNING while in flight — with no timeout state, and substep-cap saturation a confidence flag, never a terminal."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  verify:
    method: property test
    detail: "fuzzed ICs: every sample's state is one of the six labels; saturated samples still reach a dynamical outcome"
  rulings: []
  milestone: M3
- id: REQ-EVT-002
  area: EVT
  statement: "At dispatch, before the first step, the terminal detectors must be evaluated on the decoded IC: an IC already within `r_coll` is `collision` with `t_end_step = 0`, there must be no t = 0 escape outcome, and `decode_failed` must be used only when the decoder fails to produce a valid physical IC."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: decisions.md
    section: R-60 — No t = 0 escape outcome *(closes RQ-19)*
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  verify:
    method: unit test
    detail: "IC inside r_coll -> collision, t_end_step 0; IC already escaping -> not escape at t=0; non-finite IC -> decode_failed"
  rulings:
  - R-29
  - R-60
  milestone: M3
  note: "R-60: no step-0 escape; an IC already escaping is classified when its window completes (generation-root §3.1 now says so)."
- id: REQ-EVT-003
  area: EVT
  statement: "Collision must be detected every STEP by counting the pairs with `‖rᵢ − rⱼ‖² < r_coll²` (squared, against the sim-key constant): exactly one pair gives `collision` with `detail` = that pair id (pair `k` is the side opposite body `k`), and two or more give `collision` with `detail = 3` (triple collision, terminal and non-continuable)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  verify:
    method: unit test
    detail: "dd test 6: head-on pair -> collision with correct 0-based pair id; two pairs below r_coll in one step -> detail = 3, never a binary label"
  rulings:
  - R-22
  milestone: M3
- id: REQ-EVT-004
  area: EVT
  statement: "The squared minimum pair separation must be computed once per step and shared by the substep bucket, the collision test and `d_min`; `d_min` over all three pairs must be the primary stored quantity and `r_coll` a recorded parameter carried on every output."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  verify:
    method: review checklist
    detail: "one min-separation computation per step; every output/provenance record carries r_coll"
  rulings: []
  milestone: M3
- id: REQ-EVT-005
  area: EVT
  statement: "Escape must be detected post-step on the projected state as `|Δn̂| over a window < tau AND E_rel > 0`, with `E_rel = ½|Δv|² − (M_pair + m_b)/d` about the centre of mass of the other two (total mass), the window 0.4 time units (provisional) sampled at macro-step boundaries for unregularised occupants and at sync boundaries for regularised ones, and the escaper recorded as the body with `E_rel > 0` and the largest `d`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: decisions.md
    section: "R-61 — The escaper's separation is its distance to the other two's centre of mass *(amends R-29)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_simstate_payload.md
    section: VERTICAL-SLICE ADDITIONS
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  verify:
    method: unit test
    detail: "dd test 5: synthetic hyperbolic ejection fires once |Δn̂| < tau with E_rel > 0; grazing near-escape never fires; settled bound hierarchy (E_rel < 0) never fires; escaper id = largest-d body"
  rulings:
  - R-29
  - R-60
  - R-61
  - R-95
  milestone: M3
- id: REQ-EVT-006
  area: EVT
  statement: "Triple ejection (ionisation) must be reported as `escape` with `detail = 3`, gated on all three pairwise relative energies positive, all three separations growing and total `E > 0`, evaluated with the escape rule's settling."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: decisions.md
    section: "R-32 — The ionisation gate is pairwise-unbound, separating and total `E > 0`, with settling *(IE-4 (c))*"
  verify:
    method: unit test
    detail: "a synthetic three-way unbound E > 0 state fires escape detail=3; a state with one bound pair does not"
  rulings:
  - R-32
  milestone: M3
  note: "Doc text says the gate's definition pass is open; R-32 (recorded; applied before outcome data at scale) settles it. Recorded; applied before outcome data at scale."
- id: REQ-EVT-007
  area: EVT
  statement: "The `state` enum must hold the physical outcomes `escape`/`bounded`/`collision` and the lifecycle states `sim_failed` (non-finite during integration; payload untrustworthy except `state`) and `decode_failed`, mutually exclusive; there must be no `timeout` state and no `MAX_SUBSTEPS` terminal (reaching `T` is `bounded`)."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 4 — Determinism, and the substep as the subtle seam
  verify:
    method: unit test
    detail: "horizon reached without event -> bounded; NaN injected mid-march -> sim_failed; substep cap -> not terminal"
  rulings: []
  milestone: M3
- id: REQ-EVT-008
  area: EVT
  statement: "The detectors must be wrapper machinery that reads the same state regardless of which occupant produced it."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  verify:
    method: review checklist
    detail: "detector code lives in the wrapper module and takes only state + params"
  rulings: []
  milestone: M3
- id: REQ-EVT-009
  area: EVT
  statement: "A decoded IC with r_min(0) < r_coll must be labelled COLLISION_T0(pair) with t_event = 0, including looked-up ICs, which take the same path as any pixel."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 0.7 Degeneracy — every pixel gets a label
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Chart-aware validation
  - file: decisions.md
    section: "R-13 — Lookup has no coincident-bodies rejection *(closes RQ-11)*"
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  - file: decisions.md
    section: R-60 — No t = 0 escape outcome *(closes RQ-19)*
  verify:
    method: unit test
    detail: "decode an IC with a pair inside r_coll; assert COLLISION_T0 with the correct opposite-side pair id and t_event = 0; the same IC entered via lookup gives the same label"
  rulings:
  - R-13
  - R-22
  - R-60
  milestone: M3
- id: REQ-EVT-010
  area: EVT
  statement: "Once escape fires, `state` must read escape, `t_end` must be fixed and, in production, `done` must be set so the loop ends, with pitfalls §2.4's three checks run only in the validation harness on its own state; collision must stay terminal."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: decisions.md
    section: "R-31 — Escape doesn't terminate until pitfalls §2.4's three checks pass *(IE-3)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  verify:
    method: unit test
    detail: "escape-fired fixture: state reads escape, t_end_step is latched at the firing step and the production loop ends there (done set); a collision stops the march; the harness's §2.4 march leaves the payload byte-identical"
  rulings:
  - R-31
  - R-95
  - R-103
  milestone: M3
  note: "R-31 settles the doc's 'whether escape terminates integration is open'; R-95 says what state holds after escape fires; R-103 ends the production loop at escape and moves the §2.4 checks to the validation harness (integrator_contract Parts 1 and 7, pitfalls §2.4 now say so). pitfall: \"2.4 Should it terminate?\" — collision is a singularity, not a heuristic."
- id: REQ-EVT-011
  area: EVT
  statement: "The escape settling test must use the windowed `|Δn̂|` (needing `n̂` from one window earlier), kept distinct from `closure_min` (distance to the initial shape)."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: VERTICAL-SLICE ADDITIONS
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  verify:
    method: review checklist
    detail: the settling test does not read closure_min
  rulings:
  - R-29
  - R-37
  - R-59
  milestone: M3
  note: "R-59 D2 corrected the conflation. Where the lagged-n̂ register is held is tracked in open-questions.md, change 11 (ii)."
- id: REQ-EVT-012
  area: EVT
  statement: "The patchwork must not recur: no displayed per-pixel quantity may be frozen at a per-pixel termination time while it is still moving, so a rendered field must never be a stitch of termination-time strata."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: "1. THE PATCHWORK — the longest-running one, and the most instructive"
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.4 The actual bug
  - file: docs/read_first/principia_01_pitfalls.md
    section: "1.3 What settled it — a controlled experiment, not an argument"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: golden image
    detail: "Controlled experiment of §1.3 on the config slice, config_stability (fixtures/slices.toml; R-166): render once integrating every pixel to t_max and once with the shipping termination behaviour, one variable changed; the two images must show the same continuous ribbons, with no domes, tents, wedges or clean seams (per-pixel diff within the golden tolerance); also histogram t_end of escape-terminated pixels to confirm the field is not ~32 discrete strata."
  rulings:
  - R-31
  - R-71
  - R-166
  milestone: M3
  note: "pitfall: \"1. THE PATCHWORK — the longest-running one, and the most instructive\"; pitfall: \"1.4 The actual bug\"; pitfall: \"1.3 What settled it — a controlled experiment, not an argument\" — 1. §1.4 records stop_on_event freezing shape_vec at ~32 sync-boundary t_end values (99.4% of pixels frozen vs 0.15% in the reference). The golden tolerance: REQ-EVT-023 (calibration)."
- id: REQ-EVT-013
  area: EVT
  statement: "Escape must not be declared on transiently positive two-body energy mid-encounter: the pre-fix two-condition gate (receding ∧ relative energy > 0, with no persistence guard) must not recur, and escapes that fire must not re-bind."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.5 The root cause underneath the root cause
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  verify:
    method: numerical gate
    detail: "The 0-of-895 test: take every trajectory that fires escape on a fixture slice, integrate +1, +2, +3, +4 and +8 sync boundaries (2–3x the firing time) past firing with the original discretisation, and assert none re-bind; plus a unit fixture of a close encounter where E_rel goes briefly positive while receding and closure has not settled: escape must not fire."
  rulings:
  - R-29
  - R-31
  - R-103
  milestone: M3
  note: "pitfall: \"1.5 The root cause underneath the root cause\"; pitfall: \"2.4 Should it terminate?\" — 1.5 The root cause underneath the root cause (no distance gate; 0 of 895 still unbound; escape fraction overstated 5.8x). R-31 records prin-rs's check 1 as passed; the regression test still guards this build. The integration past firing runs in the validation harness on its own state (R-103)."
- id: REQ-EVT-014
  area: EVT
  statement: "Under the escape criterion, toggling stop_on_escape must give near-identical images, because only converged trajectories may be frozen."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.6 The fix — see §2 for the criterion
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: decisions.md
    section: "R-148 — The validation harness renders the \"off\" image *(closes RQ-78)*"
  verify:
    method: golden image
    detail: "Render the same slice with stop_on_escape on (the production march) and off (the validation harness's own march, continued past escape; R-148); assert the per-pixel colour difference is within the golden tolerance and no seam structure appears in the difference image."
  rulings:
  - R-29
  - R-31
  - R-71
  - R-95
  - R-148
  milestone: M3
  note: "pitfall: \"1.6 The fix — see §2 for the criterion\" — 1.6 The fix — see §2 for the criterion. Lesson: the patchwork was caused by stopping while the displayed quantity was still moving. The golden tolerance: REQ-EVT-023 (calibration); after escape fires, time averages freeze at t_esc (R-95; REQ-PAY-058)."
- id: REQ-EVT-015
  area: EVT
  statement: "Escape must be declared iff |Δn̂| over the window < tau AND E_rel > 0 — two conditions only, with no receding test, no distance gate r_esc and no persistence constant k_esc."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.1 Escape is a limit, not a threshold
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2. THE ESCAPE CRITERION — what replaced it, and why
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: open-questions.md
    section: Pending-changes register
  verify:
    method: unit test
    detail: "Fixtures: settled hierarchical escaper (fires); bound hierarchy with settled shape but E_rel < 0 (does not fire — closure alone is only 82.8% precise); mid-encounter flicker with E_rel > 0 but |Δn̂| ≥ tau (does not fire — energy alone flickers); assert the detector has no r_esc/receding/k_esc inputs (stale three-gate remnants, R-59 D1, absent)."
  rulings:
  - R-29
  - R-59
  milestone: M3
  note: "pitfall: \"2.2 The criterion\"; pitfall: \"2.1 Escape is a limit, not a threshold\"; pitfall: \"2. THE ESCAPE CRITERION — what replaced it, and why\" — 2. THE ESCAPE CRITERION — closure measures the approach to a limit in the quotient space (§2.1); receding and d > r_esc add literally nothing (§2.2)."
- id: REQ-EVT-016
  area: EVT
  statement: "E_rel must be computed as ½|Δv|² − (M_pair + m_b)/d, the escaper b's two-body energy about the centre of mass of the other two using the total mass, never the M_pair-only form."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  verify:
    method: unit test
    detail: "Hand-computed fixture with unequal masses where the M_pair-only form gives E_rel > 0 and the total-mass form gives E_rel < 0; assert the detector returns the total-mass value and does not fire (regression against the prin-rs physics error)."
  rulings:
  - R-29
  milestone: M3
  note: "pitfall: \"2.2 The criterion\" — 2.2 The criterion — prin-rs's M_pair-only E_rel biases toward escape."
- id: REQ-EVT-017
  area: EVT
  statement: "The |Δn̂| closure window must be 0.4 time units (provisional per R-29), sampled at macro-step boundaries for unregularised occupants and at sync boundaries for regularised ones."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  verify:
    method: unit test
    detail: "for an unregularised occupant (KDK/Yoshida) the detector compares n̂ at the current macro-step boundary with n̂ held from 0.4 time units earlier on the macro-step grid; for a regularised occupant (AZ, Heggie, logH) the same on the sync grid, for several n_sync values"
  rulings:
  - R-29
  - R-95
  milestone: M3
  note: "pitfall: \"2.2 The criterion\" — the window length is marked provisional in R-29; a change must go through a ruling. R-95 fixes the sampling grid."
- id: REQ-EVT-018
  area: EVT
  statement: "The escaper must be the body with E_rel > 0 and the largest distance d to the centre of mass of the other two — the same d as in E_rel."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: decisions.md
    section: "R-61 — The escaper's separation is its distance to the other two's centre of mass *(amends R-29)*"
  verify:
    method: unit test
    detail: "Fixture where two bodies have E_rel > 0 and differ in d; assert the one with larger d (to the other two's CoM) is reported as escaper and in detail."
  rulings:
  - R-29
  - R-61
  milestone: M3
  note: "pitfall: \"2.2 The criterion\""
- id: REQ-EVT-019
  area: EVT
  statement: "The escape criterion must err late rather than wrong: it must not trade precision for early firing, since t_end is a stored, reported quantity."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.3 The 96.3% recall is the right failure direction
  verify:
    method: numerical gate
    detail: "On the re-validation set, count escape fires whose ground truth is bound (false positives writing wrong t_end); gate at the measured precision (pre-R-29: 100.0%); a criterion variant that fires earlier with any false positives fails."
  rulings:
  - R-29
  milestone: M3
  note: "pitfall: \"2.3 The 96.3% recall is the right failure direction\" — 2.3 The 96.3% recall is the right failure direction — a criterion firing at t = 1.5 with 0.5% false positives writes wrong timestamps permanently."
- id: REQ-EVT-020
  area: EVT
  statement: "Terminal precedence must be: sim_failed first; then whichever event came first in time, with a same-time tie going to collision; triple ejection as a detail of escape, with triple collision and ionisation placed explicitly; then running; then bounded — identically on CPU and GPU in the shared source."
  source:
  - file: decisions.md
    section: R-30 — Event precedence is by time *(IE-2)*
  - file: decisions.md
    section: "R-6 — The event priority order goes to the decision sheet *(closes RQ-4)*"
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: docs/design/principia_dd_integrator.md
    section: 6. Deferred / flagged
  - file: docs/contracts/principia_parity_contract.md
    section: Tier L — exact, no tolerance (the bulk of the suite)
  verify:
    method: unit test
    detail: "Fixtures: escape before collision → escape; collision before escape → collision; same-step tie → collision; sim_failed with any other event → sim_failed; triple ejection reported as escape detail; until the label writer lands the R-6 pin (collision beats escape) holds."
  rulings:
  - R-6
  - R-30
  - R-32
  milestone: M3
  note: "R-30 (recorded; applied with the label writer) replaces dd_integrator §3.6's static SIM_FAILED > COLLISION > ESCAPE > BOUNDED order; until the label writer lands, R-6 says the §3.6 pin stands. Triple collision and ionisation placement: payload §2 and dd_integrator :171."
- id: REQ-EVT-021
  area: EVT
  statement: "Collision must be decided by the comparison d² < r_coll², with d² position-quantised (f32 positions, lone sub/mul, explicit fma)."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  verify:
    method: property test
    detail: "fuzz pairs near r_coll on CPU and GPU; collision decision bit-identical"
  rulings:
  - R-34
  milestone: M4
- id: REQ-EVT-022
  area: EVT
  statement: "`r_coll` must be a user-exposed sim-key parameter (default `1e-3` of `R`) with the same value on both pipelines, and changing it must invalidate and re-boot affected payloads."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  verify:
    method: unit test
    detail: "change r_coll -> sim-key change -> invalidate + re-boot; parity harness passes the same r_coll to both sides"
  rulings: []
  milestone: M5
- id: REQ-EVT-023
  area: EVT
  kind: calibration
  statement: "The pitfall §1 golden tolerances — stop_on_escape on vs off giving near-identical images, and the patchwork controlled-experiment diff — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.6 The fix — see §2 for the criterion
  - file: docs/read_first/principia_01_pitfalls.md
    section: "1.3 What settled it — a controlled experiment, not an argument"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-148 — The validation harness renders the \"off\" image *(closes RQ-78)*"
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: numerical gate
    detail: "the proposal measures per-pixel differences on the config slice, config_stability (fixtures/slices.toml; R-166), for both experiments and states the tolerance, with a seam-structure check; recorded in decisions.md"
  rulings:
  - R-71
  - R-148
  - R-166
  milestone: M3
- id: REQ-EVT-024
  area: EVT
  statement: "The escape detector must use a provisional settling threshold tau = 1e-3 (prin-rs CLOSURE_TAU, src/outcome.rs:263 at 8600d45), stated in TASK-M3-12 and inside the recorded range 7.04e-05 … 2.70e-02, until the confirmed tau (REQ-EVT-025) replaces it."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-173 — Tau is split into a provisional and a confirmed value *(closes C2, C3, S3, G6)*"
  verify:
    method: unit test
    detail: "the detector's tau is 1e-3, lies inside 7.04e-05 … 2.70e-02, and its definition cites its source and marks it provisional"
  rulings:
  - R-29
  - R-71
  - R-173
  milestone: M3
- id: REQ-EVT-025
  area: EVT
  kind: calibration
  statement: "The confirmed escape settling threshold tau (the |Δn̂| closure bound over the escape window) must be calibrated inside REQ-VAL-051's re-measured gap: proposed with its evidence by the task that measures the gap, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md; it replaces the provisional 1e-3."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-173 — Tau is split into a provisional and a confirmed value *(closes C2, C3, S3, G6)*"
  verify:
    method: review checklist
    detail: "the proposal places tau inside the re-measured gap and shows outcomes unchanged across its middle; recorded in decisions.md"
  rulings:
  - R-29
  - R-71
  - R-173
  milestone: M3
- id: REQ-EVT-026
  area: EVT
  kind: definition
  statement: "The escape detector's seven synthetic fixtures (hyperbolic ejection, grazing near-escape, settled bound hierarchy, mid-encounter flicker, unequal-mass E_rel, two-candidate escaper, three-way unbound with its one-bound-pair control) must be defined with every parameter and expected outcome, and reviewed by the physics reviewer."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-182 — Escape fixtures are defined; proposed tolerances are provisional in CI *(closes T4, T5)*"
  verify:
    method: review checklist
    detail: "TASK-M3-12's fixture definitions give masses, positions, velocities, t_end and the expected outcome for each; the physics reviewer checks each E_rel and energy claim"
  rulings:
  - R-72
  - R-182
  milestone: M3
- id: REQ-PAY-001
  area: PAY
  statement: "ICDescriptor must be 64 B (its 12 f32 fields plus explicit padding, with E₀ derived as K_0 + V_0 and not stored), and the sample descriptor must use bits 0–9 (bits 8–9 `last_symbol`) with bits 10–15 reserved."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "size_of::<ICDescriptor>() == 64 with the padding a declared member and no stored E₀ field; descriptor bits 10–15 zero"
  rulings:
  - R-86
  - R-113
  milestone: M0
  note: "R-113 (RQ-93 M0-9, option b): QuadReduction's sizing is a separate M5 requirement, closed with REQ-PAY-077."
- id: REQ-PAY-002
  area: PAY
  statement: "The SimState, ICDescriptor and RenderQuad field sets and packing must be generated from the generation-root ledger as the single source of truth, matching the fields listed in render contract Part 1 (live r,p in CoM frame, one tier-gated Benettin shadow, Benettin S, unwrapped theta, Welford mean_y/C_ty, E_0/Lz_0 f32, d_min/dE_max/dLz_max f16 packed with the descriptor, times as exact u16 macro-step indices, exact total_substeps u32, sample_descriptor u32; ICDescriptor's 12 fields; RenderQuad's 9 fields)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: decisions.md
    section: R-62 — `rho_mag` and `lambda_mag` *(amends R-22)*
  verify:
    method: unit test
    detail: "generated Rust and WGSL struct layouts compared field-by-field against the ledger tables; the Part 1 field lists are a subset check"
  rulings:
  - R-22
  - R-36
  - R-62
  milestone: M0
  note: "ICDescriptor field names are 0-based per R-22 and rho_mag/lambda_mag per R-62."
- id: REQ-PAY-003
  area: PAY
  statement: "The SimState hot per-sample footprint must be recomputed from the ledger and recorded rather than taken from the stated 136 B (FTLE-on) / 88 B (FTLE-off) figures, and SimState must be 8-byte aligned."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  - file: decisions.md
    section: "R-40 — Memory tiers and the quality device key off `eps` *(PL-5)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  - file: decisions.md
    section: "R-73 — Apply the whole ruling-follow-up checklist now *(closes RQ-56)*"
  verify:
    method: unit test
    detail: "assert align_of::<SimState>() == 8 and record size_of for the FTLE-on/off variants; the recorded widths replace 136/88 wherever they are quoted"
  rulings:
  - R-40
  - R-59
  - R-73
  milestone: M0
  note: "R-40 / R-59 D6: payload §7 records the recomputed 144 / 96 B, and the corpus now quotes them (R-73)."
- id: REQ-PAY-004
  area: PAY
  statement: "The sample_descriptor must pack state in bits 0–2 (0 escape, 1 bounded, 2 collision, 3 running, 4 sim_failed, 5 decode_failed), detail in bits 3–4, saturated in bit 5, dmin_pair in bits 6–7 and last_symbol in bits 8–9, with bits 10–15 reserved, occupying the low 16 bits of packed_a."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "generated sd_* accessors extract exactly these bit ranges; pack/unpack round trip over all 1024 values of bits 0–9; bits 10–15 decode as zero"
  rulings:
  - R-22
  - R-86
  milestone: M0
  note: "dmin_pair and detail pair ids follow R-22 (pair k names the side opposite body k)."
- id: REQ-PAY-005
  area: PAY
  statement: "No trajectory may be stored: per-sample persistent state must be fixed-size, O(1) in `t`, with the shape `n` derived each step."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  verify:
    method: review checklist
    detail: no time-indexed per-sample buffer exists
  rulings: []
  milestone: M0
- id: REQ-PAY-006
  area: PAY
  statement: "`QuadReduction` must contain only fixed-size scalars (never a history), sized from its member list and then aligned; the ~80 B figure is not a cap."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.7 `QuadReduction` — completed ledger
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: review checklist
    detail: no array member grows with time or sample count
  rulings:
  - R-72
  - R-113
  milestone: M5
  note: "Member order, packing and final size: REQ-PAY-077 (definition). R-113 (RQ-93 M0-9, option b): moved to M5 with the QuadReduction ledger."
- id: REQ-PAY-007
  area: PAY
  statement: "Only the word may be split out of the hot struct: every other `SimState` field, including the closure fields, must stay co-resident in the single hot `SimState`."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  verify:
    method: review checklist
    detail: "the generated layout has exactly two payload buffers (SimState + word); closure_min/closure_step are SimState members, not a parallel buffer"
  rulings: []
  milestone: M0
- id: REQ-PAY-008
  area: PAY
  statement: "The stored `SimState` layout must match payload §1's struct exactly — fields, order, types and bit offsets — with `SimStateFTLE` 144 B and `SimStateBase` (no `r_sh`, `p_sh`) 96 B, both 8-byte aligned with no further padding."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 7. Memory
  - file: decisions.md
    section: "R-73 — Apply the whole ruling-follow-up checklist now *(closes RQ-56)*"
  verify:
    method: unit test
    detail: "assert size_of/align_of of both generated Rust variants (144/8, 96/8) and every field offset against the §1 table"
  rulings:
  - R-40
  - R-59
  - R-73
  milestone: M0
  note: "R-59 D6 / R-40 retire the 136/88 B widths; the corpus now quotes 144 / 96 B (R-73)."
- id: REQ-PAY-009
  area: PAY
  statement: "The closure fields must be present unconditionally in both stored variants (not tier-gated), `closure_min` must be f32 (not f16), and the free `_reserved` u16 must not be spent without re-checking the alignment argument."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  verify:
    method: review checklist
    detail: "both variants carry closure_min: f32 and closure_step: u16; any ledger change touching _reserved includes a size/alignment recheck"
  rulings: []
  milestone: M0
- id: REQ-PAY-010
  area: PAY
  statement: "Phase state, shadow, the accumulators `S`, `theta`, `mean_y`, `C_ty` and the drift references `E_0`, `Lz_0` must be stored as f32, and bf16 must not be used anywhere in the payload."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_simstate_payload.md
    section: 4. Welford diffusion (streaming regression)
  verify:
    method: unit test
    detail: "assert the ledger types of these fields are f32 and no bf16 type appears in the generated artefacts"
  rulings: []
  milestone: M0
- id: REQ-PAY-011
  area: PAY
  statement: "f16-packed scalars must use the core WGSL `pack2x16float`/`unpack2x16float` (and matching Rust), must not require the `shader-f16` feature, and must not be tier-gated."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-186 — GitHub-hosted runners first; no self-hosted runner *(amends R-110, R-169, R-174)*"
  verify:
    method: unit test
    detail: "run the fragment unpack self-test on an adapter without shader-f16; results match the Rust unpack; in CI on every commit on GitHub-hosted macos-15 (Metal) and on lavapipe (R-110, R-186)"
  rulings:
  - R-110
  - R-186
  milestone: M0
- id: REQ-PAY-012
  area: PAY
  statement: "`packed_a`, `packed_b` and `times` must follow payload §2's bit diagrams: descriptor in `packed_a` bits 0–15 and `d_min` f16 in 16–31; `dE_max` f16 low and `dLz_max` f16 high in `packed_b`; `t_end_step` u16 low and `t_dmin_step` u16 high in `times`."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`packed_a` (u32)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`packed_b` (u32)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`times` (u32)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  verify:
    method: unit test
    detail: "pack known values and assert raw u32 bit patterns and the generated accessors (`pa_d_min`, `pb_dE_max`, `pb_dLz_max`, `tm_*`)"
  rulings: []
  milestone: M0
- id: REQ-PAY-013
  area: PAY
  statement: "The `sample_descriptor` bit layout must match payload §2's table: `state` 0–2, `detail` 3–4, `saturated` 5, `dmin_pair` 6–7, `last_symbol` 8–9, bits 10–15 reserved."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: "3. The ledger (the exact content — two generators must emit identical bits)"
  - file: decisions.md
    section: "R-70 — Where two docs conflict, the later consolidated doc wins"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "assert the generated extract/insert offsets and widths for every field against the table"
  rulings:
  - R-36
  - R-70
  - R-86
  milestone: M0
- id: REQ-PAY-014
  area: PAY
  statement: "`state` must be the mutually exclusive enum 0 escape, 1 bounded, 2 collision, 3 running, 4 sim_failed, 5 decode_failed; codes 6–7 are reserved and a binary decoder must treat an unknown non-running state as finished and untrusted."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  verify:
    method: unit test
    detail: decode codes 0–7; 6 and 7 report finished and untrusted
  rulings: []
  milestone: M0
- id: REQ-PAY-015
  area: PAY
  statement: "Descriptor bits 10–15 (and the word's reserved space) must be reserved, decode as zero and never be opportunistically reused; any new use must be a ledger edit that changes the schema version."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 6. Deferred / flagged
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  verify:
    method: unit test
    detail: "pack every field at max value; assert bits 10–15 are zero; the static disjointness test lists them as reserved"
  rulings:
  - R-36
  milestone: M0
- id: REQ-PAY-016
  area: PAY
  statement: "The continuation table must be exactly payload §3's frozen table (`inverse = [1,0,3,2]`, `cont_symbol[0..2]` as listed, each an involution so `predecessor_symbol == cont_symbol`), generated from the Rust layout definition identically into the Rust kernel/host and the WGSL fragment side."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  verify:
    method: unit test
    detail: "assert the generated Rust and WGSL tables equal the frozen arrays; assert involution and that no continuation equals inverse(prev)"
  rulings:
  - R-63
  milestone: M0
- id: REQ-PAY-017
  area: PAY
  statement: "The payload width must be a function of the Real type, never hardcoded to f32."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.1 Extended and arbitrary precision
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.7 The rule for this section
  verify:
    method: unit test
    detail: "Instantiate the payload for f32 and f64 (and a DoubleF64 stub row); assert widths derive from size_of::<Real>() and the layout is generated per precision."
  rulings: []
  milestone: M0
  note: The one 'DECIDE NOW, COSTS NOTHING' item in §7 (per §7.7).
- id: REQ-PAY-018
  area: PAY
  statement: "The descriptor pack/unpack round-trip parity check (roundtrip_ctl) must be able to fail on a contaminated value in any bit, including bits the unpack masks off."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN
  verify:
    method: property test
    detail: "Flip each bit (0..all) of a packed descriptor in turn; roundtrip_ctl must fail for every bit, including bits 0–1 that from_bits masks off (the recorded pass on a contaminated value must now fail)."
  rulings: []
  milestone: M0
  note: "pitfall: \"9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN\" — 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN."
- id: REQ-PAY-019
  area: PAY
  statement: "spread_event must be stored as an f16 per the ledger, and any agreement value must be derived from it on the fly; no ensemble_outcome_agreement field may exist."
  source:
  - file: decisions.md
    section: "R-18 — The agreement value is `spread_event` *(closes RQ-16)*"
  verify:
    method: unit test
    detail: "Generated payload has spread_event: f16 and no ensemble_outcome_agreement; the agreement view computes from spread_event."
  rulings:
  - R-18
  milestone: M0
- id: REQ-PAY-020
  area: PAY
  statement: "The ICDescriptor Jacobi-vector magnitude fields must be named rho_mag and lambda_mag."
  source:
  - file: decisions.md
    section: R-62 — `rho_mag` and `lambda_mag` *(amends R-22)*
  verify:
    method: unit test
    detail: "Generated ICDescriptor has fields rho_mag and lambda_mag and no rho0_mag/rho1_mag."
  rulings:
  - R-22
  - R-62
  milestone: M0
- id: REQ-PAY-021
  area: PAY
  statement: "FTLE (S_final/(n·dt) with the partial renorm interval finalised, never plain S/t), ensemble spread (at resolve), the substep-log proxy (countLeadingZeros on the exact total_substeps u32), the shape vector n(t) and drift must be derived at read, not stored as fields."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Stability metrics are just running accumulators (they get simpler)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  verify:
    method: unit test
    detail: "the ledger has no ftle/spread/log-proxy/n fields; the derived ftle equals a reference computed with the finalised renorm"
  rulings: []
  milestone: M1
- id: REQ-PAY-022
  area: PAY
  statement: "The read-side SimState type must be fixed across all tiers: a tier-absent (derived) feature reads NaN at unpack and is never stored, and validity must be decided by has_<feature> compile-time consts plus descriptor predicates (sd_is_failed, ftle_valid), never isnan()."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: Render swapping is unaffected — the struct is the boundary
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: unit test
    detail: "with FTLE baked out, ftle reads NaN and ftle_valid is false; a grep finds no isnan() in validity logic"
  rulings:
  - R-79
  milestone: M1
- id: REQ-PAY-023
  area: PAY
  statement: "The free-group word must use mixed-radix log2(3) packing (76 symbols in 121 bits over x,y,z and .w[0:24]) with a 7-bit length at .w[25:31], truncation signalled only by the length sentinel 127 (no flag bit), decoded sequentially; the reduced crossing count must be treated as valid only when not truncated."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  verify:
    method: unit test
    detail: "encode/decode random words up to 76 symbols round-trip; length 127 ⇒ fgw_truncated; the retained-prefix helper clamps 127 → 76"
  rulings: []
  milestone: M1
- id: REQ-PAY-024
  area: PAY
  statement: "Storage must never hold NaN — a blown-up sample stores the defined failed-state values, never the non-finite values that failed it — and validity must be answered by `has_<feature>` consts and descriptor predicates, never `isnan`."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: review checklist
    detail: "grep generated WGSL/Rust for isnan in validity logic; every stored value of synthetic and forced-failure samples is finite"
  rulings:
  - R-79
  milestone: M1
- id: REQ-PAY-025
  area: PAY
  statement: "The per-sample payload must be two physical buffers — the hot `SimState` buffer and a separate cold word buffer holding one `vec4<u32>` (16 B) free-group word per sample — and the word must never be stored inline in `SimState`."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  verify:
    method: unit test
    detail: "assert the generated `SimState` types contain no `vec4<u32>`/word member and the word buffer element is exactly 16 B"
  rulings: []
  milestone: M1
- id: REQ-PAY-026
  area: PAY
  statement: "FTLE-on and FTLE-off must be two monomorphised stored Rust variants (`SimStateFTLE`, `SimStateBase`), while the fragment read side must be one unified `SimState` type in which a tier-absent feature reads as a NaN sentinel and is announced by a baked `has_<feature>` const."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  verify:
    method: unit test
    detail: "compile the WGSL read type for both tiers: identical struct shape; with has_ftle=false the ftle accessor returns NaN"
  rulings: []
  milestone: M1
- id: REQ-PAY-027
  area: PAY
  statement: "`total_substeps` must be an exact u32 running sum of `N_sub` that survives resume, and the log₂ complexity proxy must be derived at read as `select(0, 31 − countLeadingZeros(max(total,1)), total > 1)`, never stored."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: property test
    detail: "resume a march at random steps: total_substeps equals the uninterrupted run; the proxy equals ⌊log₂ total⌋ for totals ≥ 2 and 0 for totals 0 and 1, across 0…2³²−1"
  rulings:
  - R-86
  milestone: M1
- id: REQ-PAY-028
  area: PAY
  statement: "For a truncated word, every word-derived quantity (reduced crossing count, `last_symbol`, per-pair quantities) must be reported invalid, and no accessor may expose 127 as a crossing count."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: 4. Validity under truncation
  verify:
    method: unit test
    detail: "truncated fixture word: fgw_reduced_length_valid false, sd_last_symbol_valid false, retained prefix length 76"
  rulings: []
  milestone: M1
- id: REQ-PAY-029
  area: PAY
  statement: "Decode must run only at resolve/inspect and recover the reduced symbol sequence in O(length) by popping the base-3 tail, taking the residue as `d₀`, and replaying forward through the continuation table."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  verify:
    method: property test
    detail: "decode(append(s)) equals the freely reduced s for random streams up to 76 symbols"
  rulings: []
  milestone: M1
- id: REQ-PAY-030
  area: PAY
  statement: "The diffusion slope must be `C_ty / C_tt(n)` with `n` taken from the sample's own `t_end_step`, and for `n < 2` it must return the sentinel −1.0 without dividing by zero."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 4. Welford diffusion (streaming regression)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  verify:
    method: unit test
    detail: "n = 0, 1 → −1.0 and slope_valid false; a latched sample uses its own n, not the playhead"
  rulings:
  - R-17
  milestone: M1
  note: "Payload §4 offers \"NaN/sentinel or −1\"; the ledger fixes −1.0."
- id: REQ-PAY-031
  area: PAY
  statement: "Derived quantities (`ftle`, `diffusion_slope`, `orbit_count`, `retrograde`, current drifts, reduced crossing count, `total_substeps_log2`, the shape point `n`, the live substep count, suspect predicates) must be computed at read, and the removed fields (`arc_length_n`, peak substep, a stored `ftle_valid`, `encounter_count`, per-pair tallies, `dominant_pair`, `trajectory_stats`, `timeout`, per-sample ensemble spread) must not be stored."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  verify:
    method: review checklist
    detail: the generated ledger contains none of the removed fields
  rulings: []
  milestone: M1
- id: REQ-PAY-032
  area: PAY
  statement: "`ftle_valid` must be derived as FTLE tier on, state not failed, `n > 0` and `completed_renorms = step_count / renorm_interval_steps > 0`."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  verify:
    method: unit test
    detail: truth table over tier/state/n/renorms
  rulings: []
  milestone: M1
- id: REQ-PAY-033
  area: PAY
  statement: "Word accessors must include `fgw_length_raw`, `fgw_truncated`, `fgw_reduced_length`/`fgw_reduced_length_valid` and `fgw_retained_prefix_length` (clamping 127 to 76, for debug/export only), each accessor must have exactly one name (payload §6's), and the reduced crossing count must equal `fgw_reduced_length` when valid."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: What is already settled (in the payload spec, not here)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "accessor outputs for length 0, 1, 76 and 127; the generated unpack layer defines no second name for any accessor (e.g. no fgw_prefix_length)"
  rulings:
  - R-86
  milestone: M1
- id: REQ-PAY-034
  area: PAY
  statement: "The decode stage must write the ICDescriptor post-decode, pre-integration, with K₀ = Σ‖pᵢ‖²/2mᵢ, V₀ = −Σ mᵢmⱼ/‖rᵢ − rⱼ‖, virial_ratio = 2K₀/|V₀|, rho_mag, lambda_mag, ρ_ratio, ρ_angle and r_min_pair₀, with mass fields named m0, m1, m2, and E₀ = K₀ + V₀ derived at read, not stored."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "3.6 ICDescriptor derived quantities (decode-time, pre-integration)"
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_dd_decoder.md
    section: 6. Deferred / flagged
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  - file: decisions.md
    section: R-62 — `rho_mag` and `lambda_mag` *(amends R-22)*
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "golden IC (z = 0): assert every descriptor field in f64 and f32 against hand-computed values; field names match the ledger; the derived E₀ equals K_0 + V_0 and the struct has no E₀ field"
  rulings:
  - R-22
  - R-62
  - R-86
  milestone: M2
  note: "R-62 names the Jacobi magnitude fields rho_mag / lambda_mag; dd_decoder §3.6 says only 'ρ-magnitudes'"
- id: REQ-PAY-035
  area: PAY
  statement: "`ICDescriptor` must hold the 12 f32 fields `m0 m1 m2`, `q_mass`, `rho_mag`, `lambda_mag`, `rho_ratio` (log), `rho_angle` (cyclic), `K_0`, `V_0` (diverging), `virial_ratio`, `r_min_pair_0` (log), produced at decode, plus 16 B of explicit padding to 64 B, and must not store E₀."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.6 `ICDescriptor` (12 × f32)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "generated ICDescriptor field names/types/scales; the 16 B padding is a declared member; size is 64 B"
  rulings:
  - R-22
  - R-62
  - R-86
  milestone: M2
- id: REQ-PAY-036
  area: PAY
  statement: "The symbolic free-group word must live in a separate cold buffer (about 16 B per copy, mixed-radix, about 76 symbols, indexed identically to SimState), touched only on a branch-cut crossing and at resolve, never per step."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  verify:
    method: unit test
    detail: "the word buffer is a distinct binding with the same index; no per-step write to it outside a crossing"
  rulings: []
  milestone: M3
- id: REQ-PAY-037
  area: PAY
  statement: "`t_end_step` must store the completed macro-step count as an exact u16 (running: current, terminal: latched, initial: 0), and dispatch must refuse any `(T, dt_macro)` with `horizon_steps = ⌈T/dt_macro⌉ > 65535` and assert `horizon_steps × N_max ≤ 2³²−1`."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 5 — Units and the horizon (inherited scale gauge)
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`times` (u32)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "T/dt_macro giving 65536 steps -> refused at dispatch; t_end_step of a terminal sample equals the latched step count"
  rulings:
  - R-86
  milestone: M3
- id: REQ-PAY-038
  area: PAY
  statement: "The free_group_word must live in a separate word buffer indexed identically to samples (per copy), not inline in SimState, and must be read only at resolve and inspect, never in the march."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  verify:
    method: review checklist
    detail: "SimState has no word member; the word buffer is a separate binding indexed by sample index; the march kernel never binds or reads it"
  rulings: []
  milestone: M3
- id: REQ-PAY-039
  area: PAY
  statement: "Storage buffers must never hold NaN: a blown-up sample must store the defined failed-state values, diffusion must read the −1.0 sentinel when the streaming slope is invalid (n < 2), and the kernel-set state enum (sim_failed/decode_failed) and sticky saturated bit must be the primary invalid signals."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: decisions.md
    section: "R-17 — The diffusion sentinel uses the streaming slope *(closes RQ-15)*"
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: unit test
    detail: "scan every stored float in synthetic, forced-failure and real payloads: no NaN bit patterns; a sample with n<2 reads diffusion == -1.0"
  rulings:
  - R-17
  - R-79
  milestone: M3
- id: REQ-PAY-040
  area: PAY
  statement: "times must pack t_end_step in bits 0–15 and t_dmin_step in bits 16–31 as exact macro-step indices (the only format, with no Q0.16 fallback), with display fractions derived via a horizon_steps uniform, and dispatch must refuse a configuration with horizon_steps = ⌈T/dt_macro⌉ > 65535."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "round-trip t_end_step/t_dmin_step exactly; a config with ⌈T/dt_macro⌉ > 65535 is refused at dispatch"
  rulings:
  - R-86
  milestone: M3
- id: REQ-PAY-041
  area: PAY
  statement: "The f16 scalars must pack d_min in the high 16 bits of packed_a and dE_max (low) / dLz_max (high) in packed_b, read with unpack2x16float, and the pack side must clamp to the binary16 finite range first, packing 0.0 for failed states."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  verify:
    method: unit test
    detail: "pack values beyond ±65504 and failed-state samples; unpack yields finite clamped values and 0.0 respectively"
  rulings: []
  milestone: M3
- id: REQ-PAY-042
  area: PAY
  statement: "The word buffer must be mutated only when a branch-cut crossing is detected (never per step) and read only at resolve and inspect."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  verify:
    method: property test
    detail: "instrument word-buffer writes in a CPU march over fixture trajectories: write count == detected crossing count; no word reads in the march kernel's per-step path"
  rulings: []
  milestone: M3
- id: REQ-PAY-043
  area: PAY
  statement: "`closure_min` (f32) must be the running minimum of the shape-sphere distance `|n̂(t) − n̂(0)|` and `closure_step` (exact u16) the macro-step index of that minimum, updated as two registers latched in a local f32 during the march, with no array or history."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  verify:
    method: unit test
    detail: "on a fixture trajectory compare against an offline minimum over the stored shape path; assert closure_step equals the argmin step"
  rulings:
  - R-37
  - R-59
  milestone: M3
- id: REQ-PAY-044
  area: PAY
  statement: "`d_min`, `dE_max` and `dLz_max` must be monotone latches held in a local f32 during the march and packed to f16 only when persistent state is written (semantics (a))."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  verify:
    method: unit test
    detail: "resume a sample across many persist boundaries; assert the packed latch equals the f16 of a single f32 latch over the whole run"
  rulings: []
  milestone: M3
- id: REQ-PAY-045
  area: PAY
  statement: "Before packing to f16, valid-state values must be clamped to ±65504, and for `sim_failed`/`decode_failed` samples the `d_min`, `dE_max` and `dLz_max` halves must hold 0.0."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  verify:
    method: property test
    detail: "fuzz inputs incl. ±1e6, ±Inf: packed halves are finite and clamped; failed-state samples unpack to exactly 0.0"
  rulings: []
  milestone: M3
- id: REQ-PAY-046
  area: PAY
  statement: "`detail` must be a union keyed by `state` and written in the same operation as `state`: escape → body id 0–2 or 3 = triple ejection; collision → pair id 0–2 or 3 = triple collision; sim_failed → 0 NaN, 1 Inf/overflow, 2 non-finite derived, 3 reserved; decode_failed → 0 non-finite output, 1 degenerate configuration, 2 invalid mass construction, 3 other; meaningless for running and bounded."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  verify:
    method: unit test
    detail: "for each state, pack/unpack each detail code and assert the decoded meaning"
  rulings:
  - R-30
  milestone: M3
  note: "R-30 places triple ejection as a detail of escape, consistent with this map."
- id: REQ-PAY-047
  area: PAY
  statement: "`dmin_pair` must be the latched pair id (0–2) of the pair that achieved `d_min`, `3` = unset, and must not be derived from the word."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: What is already settled (in the payload spec, not here)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  verify:
    method: unit test
    detail: "on a fixture close encounter, dmin_pair equals the pair of minimum separation; initial value is 3"
  rulings:
  - R-22
  milestone: M3
- id: REQ-PAY-048
  area: PAY
  statement: "`last_symbol` (descriptor bits 8–9) must mirror the march's `prev` every time `prev` changes (push and cancellation-pop) via `set_last_symbol`, which preserves bits 0–7 and 16–31; it is a redundant cache that is valid only when the sidecar `length` is ≥ 1 and ≠ 127."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: property test
    detail: "random crossing sequences: whenever 1 ≤ length ≤ 76, last_symbol equals the final symbol of decode(W); other packed_a bits unchanged by every set"
  rulings:
  - R-86
  milestone: M3
- id: REQ-PAY-049
  area: PAY
  statement: "State 1 must mean `bounded` at horizon `T` (neither escape nor collision within `T`, `T` in the sim key), must keep the name `bounded`, and must absorb the old `timeout` state."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  verify:
    method: review checklist
    detail: "no `timeout` or `survived_horizon` state exists; horizon T is part of the sim key"
  rulings: []
  milestone: M3
- id: REQ-PAY-050
  area: PAY
  statement: "`dE_max = max_t |ΔE|`, `dLz_max = max_t |ΔL_z|` and `d_min = min_t |separation|` must be absolute monotone latches over the whole trajectory."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  verify:
    method: property test
    detail: "compare latches against offline max/min over a fixture trajectory (within f16 packing)"
  rulings: []
  milestone: M3
- id: REQ-PAY-051
  area: PAY
  statement: "`t_end_step` must be the exact count of completed macro-steps at all times (0 initially, advancing while running, latched at termination — for escape, at the step it fires, where the production loop ends), and `t_dmin_step` the exact absolute macro-step index of closest approach."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`times` (u32)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  verify:
    method: property test
    detail: "sample running and terminal states: t_end_step equals the step counter; t_dmin_step equals the argmin step"
  rulings:
  - R-95
  - R-103
  milestone: M3
  note: "R-103: the pitfalls §2.4 checks march on in the validation harness only, on their own state (payload `times` now says so)."
- id: REQ-PAY-052
  area: PAY
  statement: "The word must be the Horner mixed-radix integer `W₁ = d₀`, `W_{k+1} = 3·W_k + e_k` over 121 payload bits (low bits in `x`, then `y`, `z`, then `.w` bits 0–24), with `length` in `.w` bits 25–31 holding 0–76 for valid words and 127 meaning truncated, for a capacity of 76 symbols."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  verify:
    method: property test
    detail: "append random reduced 76-symbol words; assert packing matches the closed form and fits 121 bits; length field values"
  rulings: []
  milestone: M3
- id: REQ-PAY-053
  area: PAY
  statement: "Append must follow payload §3's algorithm: ignore crossings once truncated; first symbol sets `W`; the inverse of `prev` pops via div-mod and the reverse table (a pop to empty clears the word); a non-cancelling push at `length == 76` sets `length_raw = 127`; otherwise push `3W + e`. The length cap, not numeric overflow, is the truncation rule."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  - file: decisions.md
    section: "R-70 — Where two docs conflict, the later consolidated doc wins"
  verify:
    method: property test
    detail: "compare against a reference free-group reducer on random symbol streams, incl. 77-symbol words with small numeric W"
  rulings:
  - R-70
  - R-73
  milestone: M3
- id: REQ-PAY-054
  area: PAY
  statement: "The time-only moments must be derived in closed form (`mean_t = (n+1)h/2`, `C_tt(n) = h²·n(n²−1)/12`) or from a read-only prefix table for non-uniform schedules, never from a mutable shared global."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 4. Welford diffusion (streaming regression)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  verify:
    method: unit test
    detail: "closed form equals brute-force Σ(t_i − mean_t)² for n = 0..1000"
  rulings: []
  milestone: M3
- id: REQ-PAY-055
  area: PAY
  statement: "The shape point `n` must be computed from positions only (`shape(r)` mass-weighted, or `shape(r, masses)`), never from momenta."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  verify:
    method: review checklist
    detail: shape helper signature takes r (and masses), not p
  rulings: []
  milestone: M3
- id: REQ-PAY-056
  area: PAY
  statement: "SimState must carry closure_min: f32 and closure_step: u16 as ledger fields, where the running minimum of |n̂(t) − n̂(0)| starts only after the shape has first departed from n̂(0) by more than the threshold δ_dep, with departure latched per sample."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 6. The closure field — from validation to discovery
  - file: decisions.md
    section: "R-37 — `t_min` is a departure threshold on the shape sphere *(PL-2 (b))*"
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  verify:
    method: unit test
    detail: "a periodic orbit's closure_min ≈ 0 at closure_step ≈ period/dt_macro; closure before departure is ignored"
  rulings:
  - R-37
  - R-59
  milestone: M3
  note: "Where the per-sample departed bit lives is tracked in open-questions.md (R-37's entry)."
- id: REQ-PAY-057
  area: PAY
  statement: "Every stability metric must be a fixed-size O(1) accumulator updated in place each step, never a time-series."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Stability metrics are just running accumulators (they get simpler)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  verify:
    method: review checklist
    detail: "every accumulator field has a fixed size; no per-sample growable buffer exists"
  rulings: []
  milestone: M3
- id: REQ-PAY-058
  area: PAY
  statement: "Once escape fires, `state` must read escape and `t_end_step` must be fixed at the firing step, time averages (FTLE's S/T and the like) must freeze at t_esc and the production loop must end, and the post-escape march for the pitfalls §2.4 checks must run only in the validation harness on its own state, never writing the payload."
  source:
  - file: decisions.md
    section: "R-31 — Escape doesn't terminate until pitfalls §2.4's three checks pass *(IE-3)*"
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`times` (u32)"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 1 — The shape: a swappable `step()` slot inside a fixed wrapper"
  verify:
    method: unit test
    detail: "on a fixture escaper, the production loop ends at the firing step; state, t_end_step, S and the other stored accumulators and latches are unchanged by later dispatches; the derived FTLE equals S/T at t_esc; running the harness's §2.4 march leaves the payload buffer byte-identical"
  rulings:
  - R-31
  - R-95
  - R-103
  milestone: M3
- id: REQ-PAY-059
  area: PAY
  statement: "The n̂ from one window earlier must be held in a lagged register updated at the window's sampling points — macro-step boundaries for unregularised occupants, sync boundaries for regularised ones — and the payload/SimState widths must include it."
  source:
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: open-questions.md
    section: Pending-changes register
  verify:
    method: review checklist
    detail: "SimState layout table lists the lagged n̂ register and the payload §1 width totals are recomputed to include it; unit test that the register is updated only at the occupant's sampling points (macro-step or sync boundaries)"
  rulings:
  - R-29
  - R-95
  milestone: M3
  note: "Not yet in SimState; the register's placement is tracked in open-questions.md, change 11 (ii)."
- id: REQ-PAY-060
  area: PAY
  statement: "The engine must store no trajectory history or checkpoint array (state is O(1) in time); a GUI time scrub must set the display time and re-integrate progressively, never replay stored frames."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_temporal_architecture_note.md
    section: The decision
  - file: docs/design/principia_temporal_architecture_note.md
    section: What changes vs what doesn't
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Decisions — DECIDED (ratified in conversation; recorded here so they don't evaporate)"
  verify:
    method: property test
    detail: "GPU bytes resident stay constant as the playhead advances over a long march; scrubbing back re-integrates from t = 0"
  rulings:
  - R-66
  - R-73
  milestone: M5
- id: REQ-PAY-061
  area: PAY
  statement: "Ensemble spread must not be stored as a per-sample SimState field; it must be derived at the resolve stage from a footprint's E+1 samples, consumed live for display and aggregated into QuadReduction for the scheduler."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Stability metrics: FTLE and diffusion are SimState fields; spread is a resolve-stage reduction"
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Amendments this makes
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  verify:
    method: review checklist
    detail: "no spread field in SimState; the resolve pass computes spread from the E+1 grouped samples and writes it into QuadReduction"
  rulings:
  - R-80
  milestone: M5
- id: REQ-PAY-062
  area: PAY
  statement: "Every sample, base and each ensemble copy, must be the same full SimState struct computing every field including its own FTLE Benettin shadow; ensemble copies must be E additional samples per nominal sample, not extra fields inside one struct."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Uniform samples: every sample is a full, normal SimState"
  verify:
    method: review checklist
    detail: "one SimState type for nominal and copy samples; copies are extra array entries; each has its own shadow"
  rulings: []
  milestone: M5
  note: "The sampling note allows a partial-record optimisation only if profiling later shows it is needed."
- id: REQ-PAY-063
  area: PAY
  statement: "The payload and its compatibility signature must contain sim-key fields only; no scheduler state (cache age, compute order, priority, landing frame) may be carried into the payload."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 1 — The firewall: the scheduler is arbitrary about *what* it looks at, never about *what* it sees"
  verify:
    method: property test
    detail: "run two sessions with different camera paths/budgets over the same region; payloads of every common quad and their compatibility signatures are byte-identical"
  rulings: []
  milestone: M5
- id: REQ-PAY-064
  area: PAY
  statement: "QuadRequest.flags must be bit-packed exactly per Part 5's table (0 DECODE_MODE, 1 ENSEMBLE_ENABLED, 2 FTLE_ENABLED, 3 PREVIEW_MODE, 4 FULL_RETENTION, 5 COMPUTE_IC_DESCRIPTOR, 6–7 reserved), and the kernel's one debug mode (the bring-up mode) must never be a flag bit but a baked kernel variant."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  - file: decisions.md
    section: "R-39 — `FULL_RETENTION` keeps bit 4, owned by the measurement path *(PL-4, amended)*"
  - file: decisions.md
    section: "R-41 — `DEBUG_MODE` uses the baked variants, not flag bits *(RS-1 (b))*"
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  verify:
    method: unit test
    detail: "CPU and WGSL flag constants match the table; bits 6–7 unused by any code path"
  rulings:
  - R-39
  - R-41
  - R-75
  milestone: M5
- id: REQ-PAY-065
  area: PAY
  statement: "The temporal accumulators must be fixed-size and updated in place on the GPU each step, so the CPU sees scalars and never a time-series: the latching running max divergence held per footprint with the resident quad (not a QuadReduction member), feeding \"unresolved\" under the same eps, and the running mean divergence and write-once first-divergence time as diagnostics that are not split inputs, with no divergence-trend member."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  - file: decisions.md
    section: "R-91 — The temporal accumulators feed \"unresolved\" *(closes RQ-42)*"
  - file: docs/design/principia_dd_generation_root.md
    section: Temporal accumulators (scheduler Part 8)
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-142 — The latch is evaluated on the GPU; only its verdict returns *(closes RQ-72)*"
  verify:
    method: unit test
    detail: "accumulator size is constant in t; QuadReduction has no running_max_divergence or divergence_trend member; running max is monotone per footprint; first-divergence time never changes after first write; a footprint whose running max once exceeded eps still reads unresolved after its spread falls back below eps; the split's output ignores the diagnostics"
  rulings:
  - R-91
  - R-99
  - R-142
  milestone: M5
  note: "The latch's storage layout: see the definition requirement for the per-footprint latch's layout."
- id: REQ-PAY-066
  area: PAY
  statement: "`QuadReduction` identity must be the `level` (u8) only, with readback indexed positionally by the requested quad (no `id`)."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Identity
  verify:
    method: unit test
    detail: readback of k requested quads maps to them by position
  rulings: []
  milestone: M5
- id: REQ-PAY-067
  area: PAY
  statement: "`word_buffer[i]` must be the word of the sample whose state is `simstate_buffer[i]`, using the same per-copy `(quad, sample, copy)` → linear index map, so there are exactly `(E+1)` words per nominal sample (none for shadows)."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  verify:
    method: property test
    detail: "fuzz (quad, sample, copy, E) and assert both buffers resolve to the same index; word count per footprint == E+1"
  rulings: []
  milestone: M5
- id: REQ-PAY-068
  area: PAY
  statement: "There must be no per-sample 'is ensemble' tag: grouping must be structural (E+1 samples per footprint, copy_index 0..E), with copy_index 0 marking the un-jittered nominal and 1..E the copies, and ensemble on/off the `ENSEMBLE_ENABLED` quad dispatch flag, never a baked variant."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Stability metrics: FTLE and diffusion are SimState fields; spread is a resolve-stage reduction"
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  - file: decisions.md
    section: R-102 — The ensemble isn't a baked variant *(closes RQ-62)*
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  verify:
    method: review checklist
    detail: SimState has no ensemble tag field
  rulings:
  - R-80
  - R-102
  milestone: M5
- id: REQ-PAY-069
  area: PAY
  statement: "When DECODE_MODE is set, the reference IC x₀ and the Jacobian J_D must travel in a separate uniform buffer bound only for linear-path quads, not inside the QuadRequest."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  verify:
    method: unit test
    detail: "the QuadRequest struct has no x₀/J_D fields; the linear-path bind group binds the uniform and the full-decoder bind group does not"
  rulings: []
  milestone: M6
- id: REQ-PAY-070
  area: PAY
  kind: definition
  statement: "The symbolic-dynamics contract's §1 convention — which branch cuts are generators `a` and `b`, and the crossing-direction sign — must be transcribed with citations from the literature (Montgomery; Šuvakov–Dmitrašinović) by the task that needs it, matching the half-open convention, reviewed by the physics reviewer before merge and confirmed by the human at the M3 gate."
  source:
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: 1. Generator ↔ branch-cut convention
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: "Status: OPEN — specification required before per-pair quantities are trusted"
  - file: decisions.md
    section: "R-125 — The branch-cut convention is M3's, transcribed from the literature *(closes RQ-96)*"
  - file: decisions.md
    section: "R-156 — The plan-pass readings are accepted *(closes RQ-126)*"
  verify:
    method: review checklist
    detail: "contract §1 gives the a/b assignment and the sign with its citations and is cross-checked with the integrator's half-open crossing rule; REQ-VAL-043 reproduces the published braid classes with it; physics reviewer approved; confirmed at the M3 gate"
  rulings:
  - R-38
  - R-113
  - R-125
  - R-156
  milestone: M3
  note: "R-125: moved to M3 (TASK-M3-16), transcribed rather than defined under R-72. The punctured-sphere relation and the attribution algorithm (REQ-PAY-071/072) are v2."
- id: REQ-PAY-071
  area: PAY
  statement: "Before any per-pair view ships, the thrice-punctured-sphere relation expressing the third collision loop via `a` and `b` must be specified explicitly."
  source:
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: 2. The punctured-sphere relation (third pair)
  verify:
    method: review checklist
    detail: contract §2 written
  rulings:
  - R-38
  milestone: M6
  retired: "R-125: v2 — per-pair views are out of v1 (PL-3, R-38)"
- id: REQ-PAY-072
  area: PAY
  statement: "Before any per-pair view ships, a deterministic attribution algorithm must map a reduced word to `enc_01`, `enc_02`, `enc_12` and `dominant_pair = argmax`, reproducibly."
  source:
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: 3. Third-pair attribution algorithm
  verify:
    method: property test
    detail: same word → same tallies on CPU and GPU
  rulings:
  - R-38
  milestone: M6
  retired: "R-125: v2 — per-pair views are out of v1 (PL-3, R-38)"
- id: REQ-PAY-073
  area: PAY
  kind: definition
  statement: "The per-sample value `y` that the Welford diffusion regression fits against time must be defined in `principia_dd_simstate_payload.md` §4 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 4. Welford diffusion (streaming regression)
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "payload §4 states what y is per sample, as a formula over SimState fields; the Welford update and the diffusion views use that definition"
  rulings:
  - R-72
  milestone: M3
- id: REQ-PAY-074
  area: PAY
  kind: definition
  statement: "Where the precision-dependent closure floor (~1e-7 on the f32 GPU path, ~1e-16 on the f64 CPU path) is reported — readout, export, legend must be defined in `principia_dd_simstate_payload.md` (closure) by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the payload doc names each place the floor is reported; every closure display or export path shows the floor for its precision"
  rulings:
  - R-72
  milestone: M4
- id: REQ-PAY-075
  area: PAY
  kind: definition
  statement: "`QuadReduction`'s `class_histogram[N]` — its bin count N and its bin width, given that a u8 bin overflows at 256 samples must be defined in `principia_dd_generation_root.md` §3.7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.7 `QuadReduction` — completed ledger
  - file: docs/design/principia_dd_generation_root.md
    section: Outcome (all at joint `class ⊕ detail` grain)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "N matches the joint class ⊕ detail set; the bin width cannot overflow at the largest footprint count per quad (N² × (E+1))"
  rulings:
  - R-72
  milestone: M5
  note: "Bin width is a layout value; the task records it with the definition."
- id: REQ-PAY-076
  area: PAY
  kind: definition
  statement: "Which class fraction `outcome_impurity = 1 − max(class fraction)` takes (the joint class ⊕ detail grain or another) must be defined in `principia_dd_generation_root.md` §3.7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Outcome (all at joint `class ⊕ detail` grain)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the ledger row states the fraction; the impurity-mask cross-check (debug_tooling_plan §G) uses the same fraction"
  rulings:
  - R-72
  milestone: M5
- id: REQ-PAY-077
  area: PAY
  kind: definition
  statement: "`QuadReduction`'s member order, the packing of its 2-bit and 5-bit members and its final aligned size must be defined in `principia_dd_generation_root.md` §3.7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.7 `QuadReduction` — completed ledger
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the ledger lists member order, packed-member bit positions and the aligned size; the generated Rust and WGSL layouts match it"
  rulings:
  - R-72
  milestone: M5
- id: REQ-PAY-078
  area: PAY
  kind: calibration
  statement: "The time-reversal horizon of `roundtrip_error`, with its measured cost, must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the task shows roundtrip_error against horizon on measured phase-error cases (RC §7.19f) and the per-quad cost at the chosen horizon"
  rulings:
  - R-71
  milestone: M5
- id: REQ-PAY-079
  area: PAY
  kind: definition
  statement: "When `roundtrip_error` runs (which quads, and at which point of the march) must be defined in `principia_dd_generation_root.md` §3.7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the ledger row states when the time-reversal round trip is computed and how it reaches the reduction"
  rulings:
  - R-72
  milestone: M5
- id: REQ-PAY-080
  area: PAY
  kind: calibration
  statement: "The `error_ratio` threshold for departing from 1.0 (the boolean trust flag from which \"indeterminate\" is read) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: R-87 — `failed_fraction` is retired *(closes RQ-38)*
  verify:
    method: numerical gate
    detail: "the threshold separates exact-dynamics footprints from integration-error footprints on the measured cases (RC §7.19c), with its false-alarm rate reported"
  rulings:
  - R-71
  - R-87
  milestone: M6
- id: REQ-PAY-081
  area: PAY
  kind: definition
  statement: "How `error_ratio` enters `ensemble_spread` and \"unresolved\" (its role beside `spread_shape` and `spread_event`) must be defined in `principia_dd_generation_root.md` §3.7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  - file: docs/design/principia_dd_generation_root.md
    section: Open
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: R-87 — `failed_fraction` is retired *(closes RQ-38)*
  verify:
    method: review checklist
    detail: "the ledger replaces \"how error_ratio enters is OPEN\" with the rule, consistent with R-87 (indeterminate is read from error_ratio)"
  rulings:
  - R-72
  - R-87
  milestone: M6
- id: REQ-PAY-082
  area: PAY
  kind: calibration
  statement: "The refinement gate threshold and the integrator tolerance, as a pair, must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Open
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "a sweep over the pair (the ledger's eta = 0.005 and 0.02 cases included) showing the chosen pair reaches the trust bar"
  rulings:
  - R-71
  milestone: M6
- id: REQ-PAY-083
  area: PAY
  kind: calibration
  statement: "The word truncation-rate threshold that decides whether 76 symbols suffice must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "measured truncation rates over the validation fixtures and a representative survey against the proposed threshold"
  rulings:
  - R-71
  milestone: M3
- id: REQ-PAY-084
  area: PAY
  kind: calibration
  statement: "The crossing-distribution criterion that decides whether symbolic spread `S_word` is additive to outcome impurity must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the measured crossing distribution and the criterion applied to it"
  rulings:
  - R-71
  milestone: M6
- id: REQ-PAY-085
  area: PAY
  kind: calibration
  statement: "The FTLE-accuracy improvement that would switch the f32 shadow from absolute to displacement form must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "FTLE of both shadow forms against known-Lyapunov periodic orbits, and the improvement the switch requires"
  rulings:
  - R-71
  milestone: M4
- id: REQ-PAY-086
  area: PAY
  kind: definition
  statement: "The failed-state contents of a blown-up sample's f32 fields (phase state, shadow, accumulators, drift refs) must be defined in `principia_dd_simstate_payload.md` (the storage rules beside §1) by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: review checklist
    detail: "the payload doc lists the stored value of each f32 field for sim_failed and decode_failed samples; none is NaN; the failure path writes exactly those"
  rulings:
  - R-72
  - R-79
  milestone: M3
- id: REQ-PAY-087
  area: PAY
  kind: definition
  statement: "The payload layout as a function of the Real type — which SimState fields widen with Real and which keep their fixed width (the f16-packed latches, the packed u32 words, the u16 step indices), the resulting f64 layout, and the shape of the DoubleF64 stub row — must be defined in dd_simstate_payload §1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 1. `SimState` — the hot struct
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.1 Extended and arbitrary precision
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§1 states per field whether its width follows Real, gives the f64 layout's size and alignment and the DoubleF64 row; the generated layouts match it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M0
- id: REQ-PAY-088
  area: PAY
  kind: definition
  statement: "The formulas of the ICDescriptor fields `q_mass`, `rho_mag`, `lambda_mag`, `rho_ratio`, `rho_angle` and `r_min_pair_0` (including whether `rho_mag` and `lambda_mag` are ‖ρ‖, ‖λ‖ or the mass-weighted ‖ρ̃‖, ‖λ̃‖) must be defined in dd_decoder §3.6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "3.6 ICDescriptor derived quantities (decode-time, pre-integration)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.6 `ICDescriptor` (12 × f32)
  - file: decisions.md
    section: R-62 — `rho_mag` and `lambda_mag` *(amends R-22)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives each field's formula from (m, r, p) at decode, consistent with the ledger's presentation metadata (log, cyclic); physics reviewer approved"
  rulings:
  - R-62
  - R-72
  milestone: M2
- id: REQ-PAY-089
  area: PAY
  statement: "QuadReduction must be sized from its member list (about 80 B) and then aligned."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.7 `QuadReduction` — completed ledger
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "size_of::<QuadReduction>() equals the aligned sum of its member list as REQ-PAY-077 defines it; the generated Rust and WGSL sizes agree"
  rulings:
  - R-86
  - R-113
  milestone: M5
  note: Split from REQ-PAY-001 (R-113, RQ-93 M0-9 option b).
- id: REQ-GEN-001
  area: GEN
  statement: "The catalogue metadata of the `SimState` scalars must match generation-root §3.4's table (scale, range, sentinel), with `energy_drift` and `Lz_drift` on diverging scales."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/design/principia_dd_generation_root.md
    section: 6. Deferred / flagged
  verify:
    method: unit test
    detail: "catalogue entries for each §3.4 field; drift fields have scale=diverging"
  rulings: []
  milestone: M0
- id: REQ-GEN-002
  area: GEN
  statement: "Every ledger entry must carry the §3.8 metadata (name, location, type, scale, range, optional sentinel and tier gate, provenance, consumers), and a field without a complete entry must fail generation loudly, naming the field."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.8 Metadata schema (what every entry must carry)
  - file: docs/design/principia_dd_generation_root.md
    section: 1. What it is
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: docs/contracts/principia_render_contract.md
    section: Part 6 — The debug catalogue (first build target)
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_debug_tooling_plan.md
    section: H. Codegen self-test (the tooling that tests the tooling)
  verify:
    method: unit test
    detail: "delete one entry's `scale` → generation fails with that field's name"
  rulings: []
  milestone: M0
- id: REQ-GEN-003
  area: GEN
  statement: "Within each packed word fields must never overlap, all bits must be covered or explicitly reserved, and widths must fit ranges."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  verify:
    method: unit test
    detail: static disjointness/coverage check over the ledger
  rulings: []
  milestone: M0
- id: REQ-GEN-004
  area: GEN
  statement: "Pack∘unpack must be the identity per field, property-fuzzed over the full value range in Rust (host and kernel) and in a GPU self-test dispatch of the WGSL fragment unpack using the u32 `extractBits` overload."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-186 — GitHub-hosted runners first; no self-hosted runner *(amends R-110, R-169, R-174)*"
  verify:
    method: property test
    detail: "fuzz all fields incl. top bits set (catches i32 sign extension); the GPU self-test dispatch runs in CI on every commit on GitHub-hosted macos-15 (Metal) and on lavapipe (R-110, R-186)"
  rulings:
  - R-110
  - R-186
  milestone: M0
- id: REQ-GEN-005
  area: GEN
  statement: "f16 pairs must round-trip through `pack2x16float`/`unpack2x16float` within f16 epsilon."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  verify:
    method: property test
    detail: fuzz finite values in ±65504
  rulings: []
  milestone: M0
- id: REQ-GEN-006
  area: GEN
  statement: "`t_end_step`/`t_dmin_step` must round-trip exactly as u16 (no fixed point, no Q0.16), bit-identical CPU/GPU on identical inputs, and the derived display fraction (step / horizon_steps) must have exact endpoints."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "C. Payload field views — `times` (u32), f16-packed scalars & `free_group_word` (separate buffer)"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: property test
    detail: "fuzz 0..65535: exact round trip on CPU and GPU; the fraction is exactly 0 and 1 at the endpoints"
  rulings:
  - R-84
  - R-86
  milestone: M0
- id: REQ-GEN-007
  area: GEN
  statement: "The codegen self-test must check: pack∘unpack = id property-fuzzed per field in Rust and via a GPU self-test dispatch of the WGSL unpack; no field overlap, reserved bits explicit and widths fitting ranges; f16 pairs round-tripping; and detail decoding per state."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: H. Codegen self-test (the tooling that tests the tooling)
  verify:
    method: property test
    detail: fuzz per field on host and GPU; static layout checks pass
  rulings: []
  milestone: M0
- id: REQ-GEN-008
  area: GEN
  statement: "The payload schema version must be the content hash of the ledger, including the continuation table, so any ledger or continuation-table change changes the version automatically."
  source:
  - file: decisions.md
    section: R-36 — Schema version = content hash of the ledger *(PL-1)*
  - file: decisions.md
    section: "R-63 — The continuation table is hashed with the ledger *(confirms R-36's application)*"
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "Change one ledger row, then one continuation-table entry: the schema version changes each time; unchanged ledger gives a stable version."
  rulings:
  - R-22
  - R-36
  - R-63
  - R-113
  milestone: M0
  note: "R-113 (RQ-93 M0-12): the compatibility signature carrying the version is REQ-GEN-017 (M5)."
- id: REQ-GEN-009
  area: GEN
  statement: "Ledger-generated per-field debug shaders must mount into frag/debug/generated/ (or a scanned virtual directory) and surface through the same scan as hand-written debug shaders."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  verify:
    method: unit test
    detail: "after ledger codegen, each generated field view appears as a debug registry entry"
  rulings: []
  milestone: M1
- id: REQ-GEN-010
  area: GEN
  statement: "One source — the Rust layout definition transcribed from the ledger/payload doc — must generate the Rust kernel pack/unpack, the Rust host export decoder, the WGSL fragment unpack accessors and the WGSL debug catalogue; hand-duplication is forbidden and a hand edit to a generated file must be detected."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 1. What it is
  - file: docs/design/principia_dd_generation_root.md
    section: "Drill-down — the Generation Root (layout table + link registry)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "Drill-down — the SimState payload (consolidated authoritative spec)"
  - file: docs/design/principia_dd_generation_root.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/contracts/principia_render_contract.md
    section: Part 5 — Struct inspection SDK
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 2 — Two assembly mechanisms (the substrate split; the old \"one mechanism\" claim retires)"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/design/principia_systems_architecture.md
    section: 7. Hierarchy and dependency (the build DAG, abstract)
  - file: docs/design/principia_debug_tooling_plan.md
    section: Principle
  verify:
    method: unit test
    detail: "mutate one ledger entry → all four artefacts change together; edit a generated file → the generated-file guard fails"
  rulings:
  - R-36
  milestone: M1
- id: REQ-GEN-011
  area: GEN
  statement: "The debug catalogue must be exhaustive by construction: a new ledger field must appear in the picker automatically or generation must fail."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_generation_root.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/contracts/principia_render_contract.md
    section: Part 6 — The debug catalogue (first build target)
  - file: docs/design/principia_dd_colouring.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: unit test
    detail: "add a field with metadata → it appears in the generated catalogue"
  rulings: []
  milestone: M1
- id: REQ-GEN-012
  area: GEN
  statement: "The diffusion sentinel −1.0 must survive pack/unpack bit-exact and the catalogue must show it as its literal value on the ramp, never scale it (R-136)."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: decisions.md
    section: "R-136 — `debug_invalid(frag_xy)` draws the hatch; sentinels show their value *(closes RQ-114)*"
  verify:
    method: unit test
    detail: "sentinel round trip; catalogue render of −1.0 shows the literal value −1.0 on the ramp (R-136)"
  rulings:
  - R-17
  - R-136
  milestone: M1
- id: REQ-GEN-013
  area: GEN
  statement: "Links must be registry entries selected per block/control, restricted to links type-compatible with that block's codomain (simplex, bounded interval, half-line, symmetric cap, unbounded), with the defaults mass = softmax ∘ μ_max·tanh, config = sigmoid, free momentum = sigmoid."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Link functions — the mass-simplex / logit question
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Integrity: the link is part of the experiment"
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  verify:
    method: unit test
    detail: "registering a link whose codomain does not match the block's constraint type is rejected; each default resolves to the named entry"
  rulings: []
  milestone: M2
- id: REQ-GEN-014
  area: GEN
  statement: "The link registry must span genuinely different sampling densities (e.g. softmax vs an edge-reaching simplex map, t^γ at spread-apart γ, sigmoid vs a heavier-tailed bounded map) so a robustness sweep can test whether a finding survives a link swap."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Integrity: the link is part of the experiment"
  verify:
    method: review checklist
    detail: "for each block the registry holds at least two links whose recorded over/under-sampling regions differ"
  rulings: []
  milestone: M2
- id: REQ-GEN-015
  area: GEN
  statement: "Link functions and kernel pack/unpack must be generated Rust; fragment accessors and the catalogue must be generated WGSL flowing through the fragment assembler; link selection must be baked per block."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  verify:
    method: review checklist
    detail: inspect generator outputs per target
  rulings: []
  milestone: M2
- id: REQ-GEN-016
  area: GEN
  statement: "The integrator must write every field of the generation-root ledger per its metadata."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: golden image
    detail: "dd seam 5: each field's debug view is live and sane; sentinel conventions honoured"
  rulings: []
  milestone: M3
- id: REQ-GEN-017
  area: GEN
  statement: "The schema version must be the content hash of the canonicalised ledger, must be part of the payload compatibility signature, and any ledger change must invalidate every cached payload so that no stale-schema payload is ever served."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_generation_root.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: docs/design/principia_dd_generation_root.md
    section: 6. Deferred / flagged
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  verify:
    method: unit test
    detail: "flip one bit offset → hash and signature change; the cache test serves zero stale-schema payloads"
  rulings:
  - R-36
  milestone: M5
- id: REQ-GEN-018
  area: GEN
  statement: "|det J_D| must match the analytic link derivatives wherever closed forms exist."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: numerical gate
    detail: "compare numeric |det J_D| against the sum of registry log-det Jacobians at sampled points, per link"
  rulings: []
  milestone: M6
- id: REQ-GEN-019
  area: GEN
  statement: "Every registry entry must carry inputDomains: each node declares the domain of each input it maps from, inheriting the manifest's per-field domain by default, and a node that transforms its field must declare its own."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  - file: docs/design/principia_colour_composition.md
    section: 1.2 Family B — field-ramp  →  `vec3` or `f32`
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: decisions.md
    section: "R-53 — Node interfaces declare their input domains *(GU-2 (a))*"
  verify:
    method: unit test
    detail: "scan the registry: every entry has inputDomains; an untransformed node's domain equals the manifest [lo, hi] of its field"
  rulings:
  - R-53
  milestone: M7
  note: "R-53 is recorded against colour_composition §1.2 and §3, but those sections do not yet state it; the requirement follows the ruling."
- id: REQ-GEN-020
  area: GEN
  statement: "The fragment registry must be generated by scanning shaders/wgsl/frag/{colour, brightness, combiner, post, debug}, so adding a .wgsl file registers an occupant with no code edited, runtime-authored customs join the same way, and the fixed compositor passes are not scanned."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  verify:
    method: unit test
    detail: "drop a new colour .wgsl into frag/colour/: the rebuilt registry lists it; compositor/ files are absent"
  rulings: []
  milestone: M7
- id: REQ-GEN-021
  area: GEN
  statement: "Registry entries must be {id, slot, source, category, uniformSchema, inputDomains} keyed by directory, and an occupant must be valid only in its slot."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  verify:
    method: unit test
    detail: "a colour occupant is refused in the brightness slot; every entry carries all six fields"
  rulings: []
  milestone: M7
- id: REQ-GEN-022
  area: GEN
  statement: "The occupant tree must compile to deterministic readable WGSL with one named function per node of stable identity across recompiles, comments carrying node name and parameter values, parameters bound as uniforms (slider edits rebind, not recompile), and calls into one shared WGSL library."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 5. Codegen & eject
  verify:
    method: unit test
    detail: "compiling the same graph twice yields byte-identical WGSL; a slider change triggers no recompile"
  rulings: []
  milestone: M7
- id: REQ-GEN-023
  area: GEN
  statement: "Files under frag/debug/ must be tagged category: debug, the dev GUI showing them and a polished GUI hiding them by filtering on ViewUI.debugVisible."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  - file: docs/contracts/principia_gui_state_contract.md
    section: "7. What a replacement GUI must honour (the teardown contract)"
  verify:
    method: unit test
    detail: "debugVisible = false removes exactly the debug-tagged entries from the list"
  rulings: []
  milestone: M8
- id: REQ-GEN-024
  area: GEN
  kind: definition
  statement: "The §3.8 metadata entry of a field derived at read rather than stored (its `location` kind and the stored fields it derives from) and of a vector-valued field such as `n` (its type and component count, so the ledger knows it is a vector) must be defined in dd_generation_root §3.8 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.8 Metadata schema (what every entry must carry)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§3.8 gives a location kind for derived fields and a vector type; §3.4's derived fields and `n` have complete entries under it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M0
- id: REQ-GEN-025
  area: GEN
  kind: calibration
  statement: "The tolerances of generation-root registry test 8 (c) (analytic log-det against a numeric Jacobian) and (d) (continuity of the central-difference derivative, with its step) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the finite-difference step and both tolerances, with the measured discrepancy per registered link across its domain; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-GEN-026
  area: GEN
  kind: definition
  statement: "The alternative registry links REQ-GEN-014 needs — an edge-reaching simplex map, a heavier-tailed bounded map and any further simplex link — each with forward, inverse, log-det, ε clamps and sampling note must be defined in dd_generation_root §3.9 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: "3.9 The link registry (consolidated from chart contract Part 2.5)"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Integrity: the link is part of the experiment"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2.5 — Link functions & compactification (customisable)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§3.9 has at least two links per block, each row complete (forward, inverse, log-det, ε clamps, over/under-sampling note) and meeting the three hard requirements; physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-GEN-027
  area: GEN
  kind: definition
  statement: "The declaration format by which a fragment occupant's `.wgsl` source declares its `uniformSchema` and `inputDomains` (read by the scanner and by the assembler's schema-driven uniforms) must be defined in gui_state_contract §3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: decisions.md
    section: "R-53 — Node interfaces declare their input domains *(GU-2 (a))*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the declaration syntax for uniforms (name, type, default, range) and per-input domains, with the default inheriting the manifest's [lo, hi]; one worked example; physics reviewer approved"
  rulings:
  - R-53
  - R-72
  milestone: M1
- id: REQ-SCHED-001
  area: SCHED
  statement: "The quadtree must be built in three layers in order, each a working system: L0 flat grid (SimState, decoder, fragment contracts), L1 quad cache with ancestor fallback (FIFO queue, CPU tracks cache membership only, no sim data GPU→CPU), L2 adaptive refinement with the linearised-decoder uniforms."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: 3. Three-layer quadtree
  - file: decisions.md
    section: "R-179 — Sim data is per-sample payload; QuadReduction is allowed *(closes A4, T3)*"
  verify:
    method: review checklist
    detail: "milestone plan and code history show L0 → L1 → L2, with L1 returning no per-sample SimState payload GPU→CPU; QuadReduction, a reduction, is the one permitted automatic return (R-179, R-142)"
  rulings:
  - R-179
  milestone: M0
- id: REQ-SCHED-002
  area: SCHED
  statement: "A quad's SimState must be a pure function of (IC, sim key, playhead t): identical whether reached by continuous marching, catch-up after reveal, or re-boot after eviction, and independent of the frame on which it is computed."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 1 — The firewall: the scheduler is arbitrary about *what* it looks at, never about *what* it sees"
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_integrator.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  - file: docs/contracts/principia_integrator_contract.md
    section: What this preserves, and what it costs
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 3 — Cross-chart sharing: permitted, deferred"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 8. What drills down from here
  verify:
    method: property test
    detail: "for random ICs, compute the state at a fixed t via (a) continuous march, (b) off-loop catch-up after reveal, (c) evict + re-boot, (d) different visit orders / frame budgets; assert bit-identical SimState on the same backend"
  rulings: []
  milestone: M3
- id: REQ-SCHED-003
  area: SCHED
  statement: "The scheduler must never seed an IC's integration from a cached nearby state; every IC must integrate from its own decoded (m, r, p), never from a neighbour's endpoint or a parent quad's trajectory."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 1 — The firewall: the scheduler is arbitrary about *what* it looks at, never about *what* it sees"
  verify:
    method: review checklist
    detail: "the integration entry point takes only the decoded IC and sim key; grep/review shows no path passing neighbour or parent state into the march; covered additionally by the visit-order property test"
  rulings: []
  milestone: M3
- id: REQ-SCHED-004
  area: SCHED
  statement: "Time must be a live clock: a global playhead advances by fixed dt, every visible sample's SimState marches with it under a barrier over the live set, and the fragment stage colours the current state."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_temporal_architecture_note.md
    section: The decision
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  verify:
    method: unit test
    detail: "after k frames every live-set quad's step count equals the playhead's"
  rulings: []
  milestone: M4
- id: REQ-SCHED-005
  area: SCHED
  statement: "The bound occupant must be on the sim key, so switching integrator re-integrates, and the CPU and GPU must run the same occupant."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: The table gains two rows
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  verify:
    method: unit test
    detail: "switch occupant -> sim-key change -> payload invalidated and re-integrated; parity harness asserts both sides bind the same occupant"
  rulings: []
  milestone: M4
- id: REQ-SCHED-006
  area: SCHED
  statement: "Changing any render mode, stain-graph node or wire, uniform, palette or preset must never recompute sim data: no re-integration, no compute dispatch, sim-buffer hash unchanged."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  - file: docs/design/principia_colour_composition.md
    section: 0. Scope & membrane position
  - file: docs/design/principia_dd_colouring.md
    section: 1. What it is
  - file: docs/design/principia_dd_colouring.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: property test
    detail: "dd_colouring test 12 / seam 'render key': with the frame loop paused, cycle every render mode, stain-graph node, uniform and palette; assert zero compute dispatches and an unchanged sim-buffer hash"
  rulings:
  - R-64
  milestone: M4
- id: REQ-SCHED-007
  area: SCHED
  statement: "The sim buffers must be keyed by a sim key comprising chart id+params, the slice plane (z₀'s out-of-plane part, span{q₁, q₂}, the in-plane orientation), warps, link ids, integrator config, T, event thresholds, the quality tier's sim-key components (N, FTLE, word — not depth, not E) and the payload schema version (the content hash of the ledger), and a sim-key change must reset the march from t = 0."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  - file: decisions.md
    section: R-36 — Schema version = content hash of the ledger *(PL-1)*
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  verify:
    method: unit test
    detail: "changing each sim-key component changes the key and re-boots the state to t=0; changing E, MAX_REL_DEPTH, an in-plane pan or zoom, the lock or any render-key component does not change the sim key"
  rulings:
  - R-36
  - R-89
  - R-92
  milestone: M4
- id: REQ-SCHED-008
  area: SCHED
  statement: "The frame loop must never present mixed time: only the barrier-synced live set, all at one playhead t, may reach the screen, with checkerboard's bounded one-dt skew during playhead advance as the only exception."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "6. The single-playhead invariant, relaxed (the one architectural concession)"
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  verify:
    method: property test
    detail: "with checkerboard Off, every presented frame's live quads carry identical t; with checkerboard on, skew ≤ one dt and zero whenever the playhead is stationary"
  rulings: []
  milestone: M4
- id: REQ-SCHED-009
  area: SCHED
  statement: "The sim must advance in fixed dt steps decoupled from render frames (accumulate wall-clock, take fixed steps), with playback speed a dt-per-second setting, never tied to device frame rate."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Decisions — DECIDED (ratified in conversation; recorded here so they don't evaporate)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 5 — Determinism & reproducibility
  verify:
    method: property test
    detail: "run the same session at simulated 30 and 144 fps; the state at each sim step is bit-identical"
  rulings: []
  milestone: M4
- id: REQ-SCHED-010
  area: SCHED
  statement: Dispatch must hard-assert `horizon_steps × N_max ≤ 2³²−1`.
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  verify:
    method: unit test
    detail: a config violating the bound fails the assertion at dispatch
  rulings: []
  milestone: M4
- id: REQ-SCHED-011
  area: SCHED
  statement: "\"Stop marching\" must be gated on `is_finished` (not running), never on `is_resolved_outcome`, with `is_resolved_outcome` = state ≤ 2, `is_running` = 3 and `is_failed` = state ≥ 4 including reserved 6–7."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  verify:
    method: unit test
    detail: "a sim_failed sample is not re-dispatched; predicate truth table for codes 0–7"
  rulings: []
  milestone: M4
- id: REQ-SCHED-012
  area: SCHED
  statement: "The march must update each sample's state in place, with no double-buffering."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 7. Memory
  verify:
    method: review checklist
    detail: no ping-pong SimState copy exists
  rulings: []
  milestone: M4
- id: REQ-SCHED-013
  area: SCHED
  statement: "Default playback must reach t_end in one minute at 1×, with steps_per_frame = ⌈T / (60 × fps × dt_macro)⌉ and a speed multiplier of about 0.1×–10× on top."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Decisions — DECIDED (ratified in conversation; recorded here so they don't evaporate)"
  verify:
    method: unit test
    detail: "the formula yields the steps per frame; the multiplier range is clamped to 0.1–10"
  rulings: []
  milestone: M4
  note: "Whether `fps` in the formula is measured or nominal: REQ-SCHED-084 (definition)."
- id: REQ-SCHED-014
  area: SCHED
  statement: "Cache identity must be `(chart id + params, slice plane) + QuadID`, the slice plane being z₀'s out-of-plane part, span{q₁, q₂} and the in-plane orientation, never `QuadID` alone."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  verify:
    method: unit test
    detail: "same (z,tx,ty) under two charts or two slice planes (tilt, rotation, out-of-plane slice) → two distinct entries; an in-plane pan or zoom on the same plane keeps the identity"
  rulings:
  - R-92
  milestone: M5
- id: REQ-SCHED-015
  area: SCHED
  statement: "The payload compatibility signature must include chart/decode version, link ids, integrator occupant + config, horizon `T`, enabled metrics (tier flags), event thresholds, `copy_index` for an ensemble copy (the nominal's signature excluding E) and the schema hash, and lookup must require identity and signature match, with the entry's `t` as resume metadata."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "changing any listed component misses the cache; changing E leaves the nominal's signature unchanged"
  rulings:
  - R-36
  - R-89
  milestone: M5
- id: REQ-SCHED-016
  area: SCHED
  statement: "A cached `SimState` must be presentable only at its stored `t`; for a later playhead it must resume `t_cached → playhead`, and the resumed state must equal the never-evicted state."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 7 — The current-state cache (resume points, hard-capped)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  verify:
    method: property test
    detail: "evict at random t and resume: binary fields bit-identical to the uninterrupted run"
  rulings: []
  milestone: M5
- id: REQ-SCHED-017
  area: SCHED
  statement: "Each knob change must invalidate exactly its blast radius in caching Part 2's table (render/palette: nothing; colour params: bake only; in-plane pan, zoom and lock: nothing — they change which quad addresses are requested, never the addresses; slicing out of the plane, tilt, rotate and chart-mode switch: nothing invalidated, new identities integrating fresh; `T`, `dt_macro`, thresholds, eps floors, occupant, links, chart/decode version: all sim buffers; quality tier: only the affected sim-key components (N, FTLE, word), E invalidating nothing; transport: nothing; scheduler knobs, `render_scale`, lock-to-native: nothing)."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: Part 2 — What invalidates what (the dependency graph)
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-97 — Quad addresses live in the slice plane *(closes RQ-57)*"
  verify:
    method: property test
    detail: "per knob, assert which cache entries survive; a tilt adds new-identity entries and leaves the old ones valid; an E or transport change invalidates nothing"
  rulings:
  - R-69
  - R-89
  - R-92
  - R-96
  - R-97
  milestone: M5
  note: "R-69 makes lock a SimConfig (undoable) edit; that does not change its blast radius (it changes neither key, R-92)."
- id: REQ-SCHED-018
  area: SCHED
  statement: "v1 must key the cache on `(identity, signature)`; IC-keyed cross-chart sharing is deferred to v2."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 3 — Cross-chart sharing: permitted, deferred"
  verify:
    method: review checklist
    detail: no IC-keyed lookup in v1
  rulings: []
  milestone: M5
- id: REQ-SCHED-019
  area: SCHED
  statement: "The baseline cover, its visible ancestor chain and the backdrop identity's leaf cover must be evict-exempt (pinned as frozen states); everything else must evict by cost-weighted LRU whose cost includes `t_cached`."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 4 — Baseline-first: an absolute tier, not a priority weight"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 7 — The current-state cache (resume points, hard-capped)"
  verify:
    method: unit test
    detail: "under memory pressure pinned entries survive; deeper-t entries outlive shallower ones"
  rulings: []
  milestone: M5
  note: "The baseline cover's depth: REQ-SCHED-074 (calibration). The cost formula: REQ-SCHED-079 (definition)."
- id: REQ-SCHED-020
  area: SCHED
  statement: "While a gesture is active the scheduler must dispatch only the full-canvas coarse cover of the current identity in `PREVIEW_MODE` with complexity scoring suspended — the frame budget governing, ancestors showing so there are never blanks — and resume full machinery, including must-split above the screen floor, when the debounce fires."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  verify:
    method: property test
    detail: "during a synthetic drag, only preview coarse-cover jobs are dispatched"
  rulings:
  - R-108
  milestone: M5
  note: "Must-split above the floor is the at-rest target (R-108). The debounce interval: REQ-SCHED-075 (calibration)."
- id: REQ-SCHED-021
  area: SCHED
  statement: "Preview covers for abandoned identities must evict first, and jobs completing for a superseded epoch must be dropped or filed as flotsam, never painted."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  verify:
    method: unit test
    detail: "stale-epoch job result is not composited; flotsam is the first eviction victim"
  rulings: []
  milestone: M5
- id: REQ-SCHED-022
  area: SCHED
  statement: "Readbacks (the `QuadReduction` readback and the hover-trace readback) must be fire-and-forget with one in flight and latest wins, and `onSubmittedWorkDone` must never be awaited in the frame loop."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  verify:
    method: review checklist
    detail: grep the loop for awaited map/onSubmittedWorkDone
  rulings: []
  milestone: M5
- id: REQ-SCHED-023
  area: SCHED
  statement: "Scheduler work per frame must be time-budgeted inside the worker (N quads per frame, remainder deferred)."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: benchmark
    detail: "deep-zoom gesture landing with dozens of Jacobian quads: frame time stays within budget"
  rulings: []
  milestone: M5
  note: "The budget: REQ-SCHED-076 (calibration)."
- id: REQ-SCHED-024
  area: SCHED
  statement: "Deep zoom must keep global precision on the CPU in f64 (quad centre/half-width, x₀, J_D) and give the GPU only quad-local relative arithmetic x = x₀ + J_D·δ in f32, passed as per-quad uniforms."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/contracts/principia_render_contract.md
    section: Part 8 — Errata against the older design docs
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  verify:
    method: unit test
    detail: "per-quad uniforms carry c, h, x₀, J_D; kernel computes only x₀ + J_D·δ"
  rulings: []
  milestone: M5
- id: REQ-SCHED-025
  area: SCHED
  statement: "In-plane pan and zoom must only change which quad addresses are asked for — the addresses themselves are fixed in the slice plane's own frame, relative to the plane anchor — slicing out of the plane, tilting and rotating must request new identities whose quads integrate fresh, the lock must change neither key, and no navigation may invalidate a cached payload; revealed quads catch up to the playhead off-loop."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: The two keys as ladder geometry
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  - file: docs/contracts/principia_caching_contract.md
    section: Part 2 — What invalidates what (the dependency graph)
  - file: decisions.md
    section: "R-97 — Quad addresses live in the slice plane *(closes RQ-57)*"
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  verify:
    method: property test
    detail: "pan away and back: cached entries are still valid and no integration is dispatched for them; tilt away and back: the original plane's entries are still present and valid; no invalidation event fires"
  rulings:
  - R-92
  - R-97
  milestone: M5
- id: REQ-SCHED-026
  area: SCHED
  statement: "Changes to a link or an axis warp must trigger re-integration, while a change to colour compactification must only recolour the payload without re-running the integrator."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2.5 — Link functions & compactification (customisable)
  verify:
    method: unit test
    detail: "change colour compactification: no integration dispatch occurs; change a link or axis warp: affected quads are invalidated"
  rulings: []
  milestone: M5
- id: REQ-SCHED-027
  area: SCHED
  statement: "The integrator occupant and its config, including `owns_time_mapping`, must be part of the hashed compatibility signature, so a different occupant invalidates cached payloads."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: What this preserves, and what it costs
  verify:
    method: unit test
    detail: "change only the occupant (or the regularisation) -> compatibility hash changes -> payload re-integrated"
  rulings: []
  milestone: M5
- id: REQ-SCHED-028
  area: SCHED
  statement: "The scheduler must never reuse a neighbour's SimState as a stand-in for an unfinished quad, and ancestor fallback must remain display-only, never writing a child's payload."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 1 — The firewall: the scheduler is arbitrary about *what* it looks at, never about *what* it sees"
  verify:
    method: unit test
    detail: "with ancestor fallback active, assert the child's payload buffer is untouched until the child's own dispatch writes it"
  rulings: []
  milestone: M5
- id: REQ-SCHED-029
  area: SCHED
  statement: "Preview (PREVIEW_MODE, reduced horizon T) and refined payloads of the same quad must be distinct cache entries, and the cache must never serve a preview payload where full quality was asked, or vice versa."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 5 — Preview vs refine is a second sim key
  verify:
    method: unit test
    detail: "insert preview and refined payloads for one quad; assert distinct keys and that full-quality lookups miss on a preview-only entry and vice versa"
  rulings: []
  milestone: M5
- id: REQ-SCHED-030
  area: SCHED
  statement: "Sharpen-on-idle must be a fresh integration at full T that replaces the preview in what is displayed, never an in-place edit of the preview payload."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 5 — Preview vs refine is a second sim key
  verify:
    method: unit test
    detail: "after idle, the refined payload is a new cache entry and the preview entry's bytes are unchanged"
  rulings: []
  milestone: M5
- id: REQ-SCHED-031
  area: SCHED
  statement: "The measurement path (the Measure tool and matched N / 2N renders) must dispatch a uniform grid with QuadRequest.flags bit 4 FULL_RETENTION set, skipping reduction and retaining every per-sample result."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 2 — Refinement density is not probability density (enforced here, because this is where it would break)"
  - file: decisions.md
    section: "R-39 — `FULL_RETENTION` keeps bit 4, owned by the measurement path *(PL-4, amended)*"
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 10 Measure — a tool on the figure
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  verify:
    method: unit test
    detail: "a Measure-tool dispatch sets bit 4, uses no adaptive subdivision, and returns N² per-sample results per quad; interactive dispatches leave bit 4 clear"
  rulings:
  - R-39
  milestone: M5
- id: REQ-SCHED-032
  area: SCHED
  statement: "Eviction must be cost-weighted LRU with eviction resistance proportional to computeCostMs, so expensive quads resist eviction and cheap high-coherence quads are evicted first."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-120 — Eviction takes the lowest cost-weighted resistance first *(closes RQ-88)*"
  verify:
    method: unit test
    detail: "with two equally-old quads of different computeCostMs under cache pressure, the cheaper is evicted first"
  rulings:
  - R-120
  milestone: M5
  note: "The cost formula: REQ-SCHED-079 (definition)."
- id: REQ-SCHED-033
  area: SCHED
  statement: "The scheduler must skip subsequent passes for quads no longer visible, keep 2–4 jobs in flight, and discard stale results via an epoch counter so a result for a quad the camera has left is dropped, not written."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  verify:
    method: unit test
    detail: "pan away mid-dispatch; the returned result carries an old epoch and is not written to the cache; in-flight count never exceeds the configured limit in [2,4]"
  rulings: []
  milestone: M5
- id: REQ-SCHED-034
  area: SCHED
  statement: "Newly revealed or newly split quads must catch up off-loop (0→playhead or resume-point→playhead) in the background, shown via the moving fallback (blurred live ancestor), and a hard barrier must cover the live set only."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  verify:
    method: unit test
    detail: "a slow reveal never delays the live-set barrier; the revealed region shows the animating blurred ancestor until promotion"
  rulings: []
  milestone: M5
- id: REQ-SCHED-035
  area: SCHED
  statement: "Promotion of a catching-up quad (and the parent→children swap) must be gated on time-sync (quad.t == playhead at a barrier), never on completion, and must be atomic."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  verify:
    method: unit test
    detail: "children that finish early wait for the barrier with t == playhead; no frame presents a partial set of siblings"
  rulings: []
  milestone: M5
- id: REQ-SCHED-036
  area: SCHED
  statement: "Transport controls must behave as specified: pause freezes the playhead but not compute (reveals still catch up and promote), restart sets playhead→0 and discards state, loop auto-restarts at t_end, the GUI's clock must advance RenderState's playhead each frame through a SetField marked \"no history\", and changing transport must invalidate nothing (ViewUI state, not on the sim key)."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: docs/design/principia_temporal_architecture_note.md
    section: Transport controls (free from statelessness)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: docs/contracts/principia_caching_contract.md
    section: Part 2 — What invalidates what (the dependency graph)
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  - file: decisions.md
    section: "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
  verify:
    method: unit test
    detail: "pause then pan: new quads promote at the frozen t; restart zeroes t and clears live state; loop wraps at t_end; play/pause/speed/loop leave the sim key and every cache entry unchanged; playback adds no undo entry"
  rulings:
  - R-96
  - R-101
  milestone: M5
- id: REQ-SCHED-037
  area: SCHED
  statement: "Refinement must hand off live-to-live: the parent keeps marching and rendering while children catch up off-loop, and the parent→children swap happens atomically at a barrier with both at the same t."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Refinement must be a live-to-live handoff (or it looks broken)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  verify:
    method: unit test
    detail: "during a split the parent region never freezes (its t advances every frame) and the swap frame shows children at the parent's t"
  rulings: []
  milestone: M5
- id: REQ-SCHED-038
  area: SCHED
  statement: "Ensemble copies must be scheduler leaves: ENSEMBLE_ENABLED is checked only for nominal samples and copies always dispatch ensembles-off, bounding the count at E per nominal sample."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 9 — Ensemble / SSAA sampling (dispatch rules)
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "No recursion: structural uniformity ≠ role uniformity"
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Amendments this makes
  verify:
    method: unit test
    detail: "with ENSEMBLE_ENABLED and E = 3, a quad dispatches N²·(E+1) trajectories, never more"
  rulings: []
  milestone: M5
- id: REQ-SCHED-039
  area: SCHED
  statement: "A footprint's E+1 sample offsets must be copy 0 at the un-jittered centre and copies 1..E at Halton (2,3) points 1..E, centred (minus ½) and scaled to the footprint, indexed by copy_index 0..E, deterministic with time/frame and chart_id excluded and identical on CPU and GPU, with no per-cell rotation, so copy positions are fixed by the pixel alone."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 9 — Ensemble / SSAA sampling (dispatch rules)
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Ensemble copies ARE the SSAA samples
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  - file: decisions.md
    section: R-100 — No per-cell Halton rotation *(closes RQ-60)*
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The sampling pattern: deterministic Halton offsets"
  - file: decisions.md
    section: "R-147 — The R-97 to R-109 follow-ups are applied *(closes RQ-77)*"
  verify:
    method: unit test
    detail: "copy 0's offset is (0, 0); copies 1..E equal the reference Halton (2,3) points 1..E minus ½, scaled to the footprint, in every footprint; identical across frames and CPU/GPU"
  rulings:
  - R-80
  - R-100
  - R-147
  milestone: M5
- id: REQ-SCHED-040
  area: SCHED
  statement: "Quad addresses (depth, tx, ty) — (level, i, j) in the slice plane's own frame, relative to the plane anchor (z₀'s value at the last re-integrating event) — must be computed against the post-flip Y-up frame, and the CPU quadtree (f64 centres and half-widths) and the GPU quad uniforms must share the convention explicitly."
  source:
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "Code paths that must honour the single convention (each is a separate path)"
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "The three coordinate spaces (they nest; each is right for its job)"
  - file: decisions.md
    section: "R-97 — Quad addresses live in the slice plane *(closes RQ-57)*"
  - file: decisions.md
    section: R-100 — No per-cell Halton rotation *(closes RQ-60)*
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  verify:
    method: unit test
    detail: "CPU- and GPU-computed addresses agree for every quad; an in-plane pan or zoom leaves every resident quad's address unchanged; linearised decode within a quad is not mirrored relative to the full decode"
  rulings:
  - R-97
  - R-100
  milestone: M5
  note: "There is no hash seed (R-100). The frame's level-0 cell size: see the definition requirement for the slice-plane frame's scale."
- id: REQ-SCHED-041
  area: SCHED
  statement: "Ensemble copies must be full uniform `SimState`s at copy_index 1..E, offset to Halton (2,3) points 1..E centred and scaled to the footprint (copy_index 0 = the un-jittered nominal), must be scheduler leaves that never spawn their own ensemble, and must carry no per-sample ensemble tag."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  verify:
    method: unit test
    detail: "copy offsets equal the centred Halton(2,3) points 1..E; copy 0 is at the centre; copies never spawn ensembles"
  rulings:
  - R-80
  milestone: M5
- id: REQ-SCHED-042
  area: SCHED
  statement: "A `SimState` and its word must be allocated, evicted, recomputed, resumed and restored after device loss together, share one cache compatibility signature, and never desynchronise (index `i` in both buffers always describes the same trajectory at the same playhead)."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 0. The two buffers (split by access pattern)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  verify:
    method: property test
    detail: "random sequences of allocate/evict/resume/recompute/device-loss on a quad: after each, SimState[i].t_end_step and word[i] equal those of a never-evicted reference run"
  rulings: []
  milestone: M5
- id: REQ-SCHED-043
  area: SCHED
  statement: "Background work (prebake, export job, catch-up march) must run at lower priority than the frame loop and the rest of the system, be pre-emptible, and yield regularly."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: CPU
  verify:
    method: unit test
    detail: under a running export the frame loop keeps its budget
  rulings: []
  milestone: M5
- id: REQ-SCHED-044
  area: SCHED
  statement: "When the window loses focus or the tab is hidden, GPU work must stop (not throttle), and telemetry must record that backgrounding happened."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: GPU
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Collect everything relevant, in one file
  verify:
    method: unit test
    detail: hiding the tab halts dispatches; the record logs it
  rulings: []
  milestone: M5
- id: REQ-SCHED-045
  area: SCHED
  statement: "With the quad cache, panning must never blank: a cached ancestor must always show, and zoom must show blurry ancestors that sharpen as children finish."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: 3. Three-layer quadtree
  - file: decisions.md
    section: "R-133 — The seven checkpoint-B interpretations are accepted *(closes RQ-111)*"
  verify:
    method: property test
    detail: capture frames during a fast pan and zoom; no blank pixels
  rulings:
  - R-133
  milestone: M5
  note: "R-133: the check compares no layout, so no artboard is needed; verified as a property test."
- id: REQ-SCHED-046
  area: SCHED
  statement: "Changing a sim-key quality knob (N, FTLE, word) must invalidate cached quads and re-boot the march from t = 0, masked by the blur-stale backdrop, while changing E, depth (MAX_REL_DEPTH) or render_scale must not invalidate any payload."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "Consequence: quality changes are (mostly) sim-key changes"
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "changing N changes the sim-key hash and evicts; changing E, MAX_REL_DEPTH or render_scale leaves the hash and cache intact"
  rulings:
  - R-89
  milestone: M5
- id: REQ-SCHED-047
  area: SCHED
  statement: "The QuadReduction (coherence, impurity, spread, suspect fraction) must be the only automatic return and drive split/keep/merge, and the screen texture may feed only the backdrop's snapshot tier, never being read as data."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: The return paths
  verify:
    method: review checklist
    detail: "no code path reads the screen texture into data structures; scheduler inputs come only from QuadReduction"
  rulings: []
  milestone: M5
- id: REQ-SCHED-048
  area: SCHED
  statement: "The sim key must comprise chart id+params, the slice plane (z₀'s out-of-plane part, span{q₁, q₂}, the in-plane orientation), warps, link ids, occupant, T/dt/thresholds, the tier's sim-key components (N, FTLE, word — E is live, cached per copy_index) and the schema; the render key must comprise the stain graph (nodes, wires, sources), node params, overlays and palette/compaction; the playhead, in-plane pan and zoom and the lock must be in neither key."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: The two keys as ladder geometry
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  verify:
    method: unit test
    detail: "changing each listed component flips exactly the stated key; advancing the playhead, panning or zooming in-plane, toggling the lock or changing E flips neither"
  rulings:
  - R-36
  - R-64
  - R-89
  - R-92
  milestone: M5
- id: REQ-SCHED-049
  area: SCHED
  statement: "Cache keys must separate identity (which quad) from validity (computed how), and a preview must never satisfy a request for a refined result."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: unit test
    detail: "a preview-valid entry is not returned for a refined lookup of the same quad"
  rulings: []
  milestone: M5
- id: REQ-SCHED-050
  area: SCHED
  statement: "The viewport cache must hold only the current-t state of recent quads under a hard cap, evict by cost-weighted LRU including t_cached, and take its cap as a fraction of the VRAM budget detected by device characterisation, never a fixed constant, so it never grows into a history buffer."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: The one accepted cost (stated plainly)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Decisions — DECIDED (ratified in conversation; recorded here so they don't evaporate)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 7 — The current-state cache (resume points, hard-capped)"
  - file: docs/design/principia_quality_device_note.md
    section: What this subsystem resolves (two previously-open questions)
  verify:
    method: unit test
    detail: "cache bytes never exceed the cap; entries hold no past-t state; cap scales with the detected budget"
  rulings:
  - R-4
  - R-40
  milestone: M5
  note: "The cap is sized by the device-characterisation solve within the limits leg's hard ceiling (quality/device note; see REQ-PERF-055). The cost formula: REQ-SCHED-079 (definition)."
- id: REQ-SCHED-051
  area: SCHED
  statement: "The frame loop must barrier only over the live set; newly revealed or split quads must catch up 0 → playhead off-loop (background, non-blocking, generation-cancelled, reusing the refinement compute path) and be promoted atomically at a barrier only when quad.t == live_set.t."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: Still open (settle at implementation, or next edit pass)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  verify:
    method: property test
    detail: "a slow catching-up quad never delays a present; promotion only at a barrier with equal t; a cancelled generation never promotes"
  rulings: []
  milestone: M5
- id: REQ-SCHED-052
  area: SCHED
  statement: "Resolution must come from quad subdivision and anti-aliasing from the ensemble copies within each sample's IC-space footprint; the base N×N grid must not be used as a sub-pixel supersampler and one sample must never be fabricated into many pixels."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The core move: colour per sample, average last"
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  verify:
    method: review checklist
    detail: "no code path upsamples a sample across multiple pixels other than the display scale; AA only from ensemble copies"
  rulings:
  - R-88
  milestone: M5
  note: "R-88: below the screen floor the criterion may supersample where a footprint is unresolved; that extra density still comes from quad subdivision (capped by MAX_REL_DEPTH), never from the base grid."
- id: REQ-SCHED-053
  area: SCHED
  statement: "If the optional Cranley–Patterson rotation is used, it must hash the integer quad address (depth, tx, ty) and pixel index with an integer bit-mixing hash and convert to a fractional rotation only at the end, never hashing floats or screen position."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The sampling pattern: deterministic Halton offsets"
  - file: decisions.md
    section: "R-147 — The R-97 to R-109 follow-ups are applied *(closes RQ-77)*"
  verify:
    method: property test
    detail: "rotation value bit-identical CPU/GPU; unchanged under pan; changes when a quad splits"
  rulings:
  - R-147
  milestone: M5
  retired: "R-100: there is no per-cell Cranley–Patterson rotation; copy positions are fixed by the pixel alone."
- id: REQ-SCHED-054
  area: SCHED
  statement: "Tilting the 2-plane must request new identities that integrate fresh and must never invalidate the cache."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: The projective microscope — addressing, not computing
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  verify:
    method: unit test
    detail: "Tilt the plane and back; assert cached entries for the original plane are still present and valid and the tilted planes were added as new identities."
  rulings:
  - R-92
  milestone: M5
- id: REQ-SCHED-055
  area: SCHED
  statement: "The quadtree must be per-slice; an 8D BVH over the manifold must not be built."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: "8.1 An 8D BVH over the manifold, instead of a per-slice quadtree"
  - file: docs/read_first/principia_00_philosophy.md
    section: "8. Considered and rejected — with the numbers, so they are not reinvented"
  verify:
    method: review checklist
    detail: "Acceleration structure is the per-slice quadtree; no manifold-level hierarchy exists."
  rulings: []
  milestone: M5
  note: Rejected idea recorded with its cost.
- id: REQ-SCHED-056
  area: SCHED
  statement: "A refinement job must not be dispatched while the current view's baseline cover (the viewport-covering quads a few levels above camera depth) is missing."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 4 — Baseline-first: an absolute tier, not a priority weight"
  - file: docs/contracts/principia_caching_contract.md
    section: Part 8 — The composed guarantee
  verify:
    method: property test
    detail: "random navigations: assert no refinement dispatch precedes baseline completion"
  rulings: []
  milestone: M6
  note: "The number of levels: REQ-SCHED-074 (calibration)."
- id: REQ-SCHED-057
  area: SCHED
  statement: "Absolute zoom depth must not be capped by any constant; infinite zoom is the default and reachable depth must emerge only from the decode floor and the integration floor."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 3 — Depth: infinite zoom by default"
  - file: docs/contracts/principia_scheduler_contract.md
    section: The absolute floor is emergent
  - file: docs/design/principia_deep_zoom.md
    section: 4. Sliding depth bound
  verify:
    method: unit test
    detail: "drive a scripted zoom to depth > 30 with MAX_REL_DEPTH fixed; assert quads keep being scheduled below every fixed depth until a floor fires; review: no MAX_DEPTH-style absolute constant exists"
  rulings: []
  milestone: M6
- id: REQ-SCHED-058
  area: SCHED
  statement: "The split gate must be split(C) ⟺ (in_view(C) ∧ tile_size(C) > pixel_size) ∨ (policy_splits(C) ∧ ℓ < camera_depth + MAX_REL_DEPTH), a window sliding with the camera that caps every split beyond the screen floor (in view below it and off screen alike), never the absolute form ℓ < MAX_DEPTH."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`MAX_REL_DEPTH` (renamed from `MAX_DEPTH`)"
  - file: docs/design/principia_deep_zoom.md
    section: 4. Sliding depth bound
  - file: decisions.md
    section: "R-15 — `Policy::Tolerance` governs refinement *(closes RQ-13)*"
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  - file: decisions.md
    section: "R-98 — `MAX_REL_DEPTH` caps every split beyond the screen floor *(closes RQ-58)*"
  verify:
    method: unit test
    detail: "for camera depths 0, 10, 30 and MAX_REL_DEPTH = 4: an in-view quad above the screen floor splits whatever the policy says; below the floor a policy-splitting quad at ℓ = camera_depth + 3 splits and at ℓ = camera_depth + 4 does not; an off-screen policy-splitting quad at ℓ = camera_depth + 4 does not split"
  rulings:
  - R-15
  - R-88
  - R-98
  milestone: M6
- id: REQ-SCHED-059
  area: SCHED
  statement: "MAX_REL_DEPTH must not be on the sim key: lowering it must invalidate no payload, only stop scheduling deeper quads, leaving already-computed quads valid and cached."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`MAX_REL_DEPTH` (renamed from `MAX_DEPTH`)"
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "lower MAX_REL_DEPTH while zoomed; assert the sim-key hash is unchanged and no cached quad is evicted or invalidated"
  rulings:
  - R-89
  milestone: M6
- id: REQ-SCHED-060
  area: SCHED
  statement: "The screen-space floor (tile_size(quad, zoom) ≤ pixel_size) must be evaluated live against the current zoom, never cached as a quad fact and never terminal, so that zooming in regrows tiles above pixel size and in-view quads split again."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: 4. Sliding depth bound
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  verify:
    method: unit test
    detail: "screen-floor a quad, zoom in 2×; assert it is re-queued as refinable and splits with real new samples"
  rulings:
  - R-88
  milestone: M6
- id: REQ-SCHED-061
  area: SCHED
  statement: "In view, a quad above the screen-space floor must split at rest (during a gesture the frame budget governs and ancestors show); otherwise refinement must happen iff the policy splits and no cap or terminal floor has fired, with the boundaries in the order screen-space floor → MAX_REL_DEPTH (capping every split beyond the screen floor, off-screen policy splits included, MAX_REL_DEPTH ≥ the screen floor) → DECODE_SWITCHOVER (a switch, not a stop) → AT_F32_FLOOR on the linear path and the integration floor."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: 4. Sliding depth bound
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  - file: decisions.md
    section: "R-98 — `MAX_REL_DEPTH` caps every split beyond the screen floor *(closes RQ-58)*"
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  verify:
    method: unit test
    detail: "table-driven test over quads in each state asserts the split/stop/switch outcome per the hierarchy; at rest an in-view quad above the screen floor splits even when the policy keeps it; an off-screen quad is capped by MAX_REL_DEPTH; a MAX_REL_DEPTH below the screen floor is rejected"
  rulings:
  - R-15
  - R-88
  - R-98
  - R-108
  milestone: M6
- id: REQ-SCHED-062
  area: SCHED
  statement: "The full decoder must switch to the linearised decoder once `quad.collapsed` is set (its adjacent samples give bitwise-identical ICs) or at depth ℓ_switch = 20 (lowering's SWITCH), whichever comes first, and sample collapse on the linearised decoder must fire AT_F32_FLOOR, a terminal stop cached as a quad fact, so the response to sample collapse keys off which decoder is active."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: 4. Sliding depth bound
  - file: decisions.md
    section: R-90 — The decoder switchover trigger *(closes RQ-41)*
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`QuadRequest.flags`"
  verify:
    method: unit test
    detail: "force collapse with DECODE_MODE = 1: the quad is marked terminal (AT_F32_FLOOR) and never re-queued; the same symptom with DECODE_MODE = 0 switches instead; a non-collapsed quad at depth 21 decodes linearised and at depth 15 decodes full"
  rulings:
  - R-90
  milestone: M6
- id: REQ-SCHED-063
  area: SCHED
  statement: "A quad whose samples are substep-saturated and whose suspect_fraction stays high must be stopped at the integration floor: flagged floor-limited, not re-queued as refinable, while its samples keep their real (low-confidence) outcomes rather than being terminated."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: "Reserved: the integration floor"
  verify:
    method: unit test
    detail: "fixture quad with saturated samples above the suspect threshold: assert no split, floor-limited flag set, not re-queued, and per-sample outcomes unchanged"
  rulings: []
  milestone: M6
  note: "\"High\" and \"stays\": REQ-SCHED-077 (calibration)."
- id: REQ-SCHED-064
  area: SCHED
  statement: "Only the true floors (AT_F32_FLOOR, the integration floor) and Decision::Undetermined may make a quad terminal; every other Ready quad (kept by the policy, or screen-floored at this zoom) must stay ReadyRefinable and be revisited when budget frees or on zoom-in, and terminal quads must never be revisited."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: decisions.md
    section: "R-47 — `Undetermined` is reported, terminal and flagged, until a chart shows it non-zero *(RS-7)*"
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  verify:
    method: unit test
    detail: "lifecycle state machine test: each stop reason maps to terminal/refinable as stated; scheduler never dequeues a terminal quad"
  rulings:
  - R-47
  - R-88
  milestone: M6
  note: "Part 4 says only the precision/integration floors are terminal; R-47 adds Undetermined as terminal-and-flagged, and the ruling wins."
- id: REQ-SCHED-065
  area: SCHED
  statement: "Tile priority must be P_tile = w_v·P_visible + w_z·P_zoom + w_c·P_complexity + w_f·P_focus with defaults w_v = 10, w_z = 2, w_c = 3, w_f = 1, and the weights must be exposed in research mode."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  verify:
    method: unit test
    detail: "priority of fixture quads matches the formula at default weights; the research-mode settings expose all four weights"
  rulings:
  - R-44
  - R-55
  milestone: M6
  note: "How 'never compute off-screen' and 'offscreen → stop' reconcile with policy §0.1, and how order in view enters (R-44): REQ-SCHED-080 (definition). R-55 centres P_focus on the pointer; its decay: REQ-SCHED-081 (definition)."
- id: REQ-SCHED-066
  area: SCHED
  statement: "P_focus must be the inverse distance to the pointer while the pointer is in view and to the viewport centre otherwise, with a weight and a decay."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-55 — Cursor bias is `P_focus` centred on the pointer *(GU-4 (a))*"
  verify:
    method: unit test
    detail: "with pointer in view, the quad under the pointer gets maximal P_focus; with pointer out of view, the centre quad does"
  rulings:
  - R-55
  milestone: M6
  note: "The weight is w_f = 1 (scheduler Part 6's default). The decay: REQ-SCHED-081 (definition)."
- id: REQ-SCHED-067
  area: SCHED
  statement: "Relevance terms (P_visible, P_focus) must be computed relative to the camera or quad centre, using the centre-plus-half-width pattern, not in global UV."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: docs/design/principia_deep_zoom.md
    section: 1. Quad-local coordinates — UV precision
  - file: decisions.md
    section: "R-46 — Relevance is computed relative to the camera or quad centre *(RS-6)*"
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  verify:
    method: numerical gate
    detail: "at depth ≥ 40 (where the global-UV defect shows), P_visible and P_focus match an f64 reference ordering of quads exactly"
  rulings:
  - R-46
  milestone: M6
  note: "R-46 is recorded, applied before any deep-zoom demo; Part 6 does not yet state it. Recorded; applied before any deep-zoom demo."
- id: REQ-SCHED-068
  area: SCHED
  statement: "The frontier margin must be derived from the descent's refill rate, and it must be benchmarked against a plain widened margin (the baseline it must beat) and must not be a naive cull."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: docs/design/principia_dd_refinement_policy.md
    section: "0.1 In view, the camera decides depth and the criterion decides ORDER"
  - file: decisions.md
    section: "R-45 — The frontier margin is derived from the refill rate, and a widened margin is the baseline to beat *(RS-5)*"
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  verify:
    method: benchmark
    detail: "scripted camera paths: compare in-view quads and stall (px texel size) for naive cull, widened margin and refill-derived margin; refill-derived must beat widened margin, and naive cull (170 px, 4 in view) is the known failure"
  rulings:
  - R-45
  milestone: M6
  note: "The derivation: REQ-SCHED-082 (definition); the baseline's size: REQ-SCHED-078 (calibration)."
- id: REQ-SCHED-069
  area: SCHED
  statement: "Quad depth must satisfy ℓ < camera_depth + MAX_REL_DEPTH (the sliding cap on every split beyond the screen floor, off-screen policy splits included, MAX_REL_DEPTH ≥ the screen floor), at rest every in-view quad above the screen floor must be split, and quad-state transitions must be legal."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  - file: decisions.md
    section: "R-98 — `MAX_REL_DEPTH` caps every split beyond the screen floor *(closes RQ-58)*"
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  verify:
    method: property test
    detail: "fuzzed navigation never produces a quad beyond the sliding cap, an unsplit in-view quad above the screen floor at rest, or an illegal transition"
  rulings:
  - R-88
  - R-98
  - R-108
  milestone: M6
  note: "The legal transitions: REQ-TOOL-116 (definition)."
- id: REQ-SCHED-070
  area: SCHED
  statement: "The motion refinement-floor lever must let quads stop one or two levels above pixel size during an active pan/zoom and snap back to the pixel floor at rest, living in the scheduler's motion adaptation, not as a persistent tier setting."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  verify:
    method: unit test
    detail: "during a gesture leaves stop at tile ≈ 2–4 px; after rest they refine to ≤ 1 px"
  rulings:
  - R-108
  milestone: M6
  note: "R-108: must-split above the screen floor is the at-rest target, so this motion lever does not contradict it (memory_tiers §5 now says so)."
- id: REQ-SCHED-071
  area: SCHED
  statement: "The camera must be wired into scheduler priority, and scheduler Part 6 (P_complexity = 1 − coherence, never compute off-screen, offscreen → stop) must be reconciled with refinement policy §0.1, stating how order in view enters."
  source:
  - file: decisions.md
    section: "R-44 — The camera is wired; scheduler Part 6 is reconciled with policy §0.1 *(RS-4)*"
  verify:
    method: unit test
    detail: "Pan sequence regression: the quadtree/priority order changes with the camera (a byte-identical tree at every pan step fails, cf. philosophy §4.4)."
  rulings:
  - R-44
  milestone: M6
  note: "The reconciliation is not yet written in scheduler Part 6: REQ-SCHED-080 (definition)."
- id: REQ-SCHED-072
  area: SCHED
  statement: "The equirect bake cache must be keyed only on colour occupant + params, stay outside the quad cache and its eviction, and rebake on param change, debounced."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: Part 2 — What invalidates what (the dependency graph)
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  verify:
    method: unit test
    detail: "chart switch reuses the bake; param change triggers one debounced rebake"
  rulings: []
  milestone: M7
- id: REQ-SCHED-073
  area: SCHED
  statement: "Export must run the same frame loop in blocking mode — a hard barrier over all visible quads, fully caught up and refined with no fallback — capturing converged data only, while interactive playback runs the progressive mode, and the export job must never block the UI."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  verify:
    method: unit test
    detail: "every exported frame has all visible quads at the frame's t and refined; the UI thread stays responsive during export"
  rulings: []
  milestone: M8
- id: REQ-SCHED-074
  area: SCHED
  kind: calibration
  statement: "The baseline cover's depth (how many levels above camera depth the viewport-covering baseline quads sit) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 4 — Baseline-first: an absolute tier, not a priority weight"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the level count with its evidence: baseline-completion latency and viewport coverage measured on scripted deep-zoom navigations"
  rulings:
  - R-71
  milestone: M5
- id: REQ-SCHED-075
  area: SCHED
  kind: calibration
  statement: "The gesture debounce (the interval after the last gesture event before the at-rest regime resumes) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the interval with its evidence: scripted drag sequences showing no premature at-rest dispatch and the at-rest resume latency"
  rulings:
  - R-71
  milestone: M5
- id: REQ-SCHED-076
  area: SCHED
  kind: calibration
  statement: "The per-frame scheduler work budget (the time budget, or N quads per frame, the worker spends before deferring the rest) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the budget with its evidence: frame-time percentiles on the deep-zoom landing benchmark with dozens of Jacobian quads"
  rulings:
  - R-71
  milestone: M5
- id: REQ-SCHED-077
  area: SCHED
  kind: calibration
  statement: "The integration floor's suspect_fraction threshold (\"high\") and its persistence (\"stays\", with its unit, frames or levels) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: "Reserved: the integration floor"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "decisions.md records both with their evidence: suspect_fraction traces on substep-saturated near-collision fixtures, separating floor-limited quads from quads that resolve on refinement"
  rulings:
  - R-71
  milestone: M6
- id: REQ-SCHED-078
  area: SCHED
  kind: calibration
  statement: "The widened-margin baseline's size (the plain widened frontier margin the refill-derived margin must beat) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: decisions.md
    section: "R-45 — The frontier margin is derived from the refill rate, and a widened margin is the baseline to beat *(RS-5)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the margin size with its evidence: the scripted-camera-path benchmark of in-view quads and stall for the candidate sizes"
  rulings:
  - R-45
  - R-71
  milestone: M6
- id: REQ-SCHED-079
  area: SCHED
  kind: definition
  statement: "The cost-weighted LRU cost (how computeCostMs and t_cached combine into eviction resistance) must be defined in caching_contract Part 7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 7 — The current-state cache (resume points, hard-capped)"
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the cost formula; the eviction unit tests (cheaper-first, deeper-t outlives shallower) follow from it"
  rulings:
  - R-72
  milestone: M5
- id: REQ-SCHED-080
  area: SCHED
  kind: definition
  statement: "The reconciled priority rule (how scheduler Part 6's 'never compute off-screen' and 'offscreen → stop' reconcile with refinement policy §0.1's off-screen row, and how order in view enters priority) must be defined in scheduler_contract Part 6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: docs/design/principia_dd_refinement_policy.md
    section: "0.1 In view, the camera decides depth and the criterion decides ORDER"
  - file: decisions.md
    section: "R-44 — The camera is wired; scheduler Part 6 is reconciled with policy §0.1 *(RS-4)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 6 and policy §0.1 no longer disagree about off-screen quads; the doc says how order in view enters P_tile"
  rulings:
  - R-44
  - R-72
  milestone: M6
- id: REQ-SCHED-081
  area: SCHED
  kind: definition
  statement: "P_focus's decay law (how w_f·P_focus falls off with distance from the pointer or viewport centre) must be defined in scheduler_contract Part 6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-55 — Cursor bias is `P_focus` centred on the pointer *(GU-4 (a))*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the decay law; any constant it introduces is recorded as a calibration (R-71)"
  rulings:
  - R-55
  - R-72
  milestone: M6
- id: REQ-SCHED-082
  area: SCHED
  kind: definition
  statement: "The refill-rate frontier margin (the formula deriving the frontier margin from the descent's refill rate) must be defined in refinement_policy §7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: decisions.md
    section: "R-45 — The frontier margin is derived from the refill rate, and a widened margin is the baseline to beat *(RS-5)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the formula and its inputs; the frontier-margin benchmark uses it"
  rulings:
  - R-45
  - R-72
  milestone: M6
- id: REQ-SCHED-083
  area: SCHED
  kind: definition
  statement: "The quad-skip rule (which quads the scheduler skips from a chart's domain function, including quads partly inside the domain) must be defined in scheduler_contract by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: R-26 — Every chart declares its domain function *(CD-6)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the rule for quads wholly outside, wholly inside and partly inside the chart's domain"
  rulings:
  - R-26
  - R-72
  milestone: M5
  note: "What a skipped quad's pixels carry: REQ-CHART-041 (definition)."
- id: REQ-SCHED-084
  area: SCHED
  kind: definition
  statement: "The frame-cadence fps (whether fps in steps_per_frame = ⌈T / (60 × fps × dt_macro)⌉ is measured or nominal) must be defined in temporal_architecture_note's Decisions by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Decisions — DECIDED (ratified in conversation; recorded here so they don't evaporate)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc says which fps the formula reads, and the steps-per-frame unit test uses it"
  rulings:
  - R-72
  milestone: M4
- id: REQ-SCHED-085
  area: SCHED
  kind: definition
  statement: "How the optional Cranley–Patterson rotation composes with the centred Halton offsets (copies 1..E only; copy 0 stays at the centre) must be defined in the sampling/MSAA note's sampling-pattern section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The sampling pattern: deterministic Halton offsets"
  - file: decisions.md
    section: "R-147 — The R-97 to R-109 follow-ups are applied *(closes RQ-77)*"
  verify:
    method: review checklist
    detail: "the doc states the composition (order of rotate, wrap and centre, and the scale to the footprint); the offset unit test uses it"
  rulings:
  - R-72
  - R-80
  - R-147
  milestone: M5
  note: "Written by R-80's conforming edit to the sampling note, which names R-72 for it; not an RQ-51 item."
  retired: "R-100: there is no per-cell rotation, so there is no composition with the centring to define."
- id: REQ-SCHED-086
  area: SCHED
  kind: definition
  statement: "The scale of the slice plane's own frame — the level-0 cell size of the quad addresses (level, i, j), relative to the plane anchor — must be defined in deep_zoom §1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: The precision split (the CPU/GPU seam, decode side)
  - file: docs/design/principia_deep_zoom.md
    section: 1. Quad-local coordinates — UV precision
  - file: decisions.md
    section: "R-97 — Quad addresses live in the slice plane *(closes RQ-57)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "deep_zoom §1 states the level-0 cell size in the slice plane's frame and how the per-quad centre and half-width follow from it; the address unit test uses it; physics reviewer approved"
  rulings:
  - R-72
  - R-97
  milestone: M5
  note: "R-97 fixes the frame's origin (the plane anchor) but gives no scale; §1 still states a quad's span as ~2^−ℓ of the view's [0,1]² UV domain."
- id: REQ-SCHED-087
  area: SCHED
  kind: calibration
  statement: "The scheduler's in-flight job limit (within scheduler Part 6's 2–4 jobs) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal shows dispatch throughput and pan-away latency (stale results dropped) at 2, 3 and 4 jobs in flight; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M5
- id: REQ-SCHED-088
  area: SCHED
  kind: calibration
  statement: "The depth of the in-motion full-canvas coarse cover (how many levels above camera depth the gesture-time `PREVIEW_MODE` cover sits) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 4 — Baseline-first: an absolute tier, not a priority weight"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the level offset, whether it equals REQ-SCHED-074's baseline-cover depth, and its evidence: dispatches per gesture frame and canvas coverage on scripted tilt and slice drags; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M5
- id: REQ-SCHED-089
  area: SCHED
  kind: calibration
  statement: "The motion refinement floor's offset (one or two levels above pixel size, and whether the arbiter moves it between the two) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the offset (1, 2, or arbiter-moved between them) with evidence: frame time and live-sample count during scripted pans at each offset; recorded in decisions.md"
  rulings:
  - R-71
  - R-108
  milestone: M6
- id: REQ-SCHED-090
  area: SCHED
  kind: calibration
  statement: "The constants of P_focus's decay law (REQ-SCHED-081) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-55 — Cursor bias is `P_focus` centred on the pointer *(GU-4 (a))*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states each constant of the written decay law with evidence: dispatch order around the pointer on a scripted hover, and that visibility still dominates; recorded in decisions.md"
  rulings:
  - R-55
  - R-71
  milestone: M6
- id: REQ-REF-001
  area: REF
  statement: "`QuadReduction` outcome members must be `class_histogram` (u8 × N), `dominant_outcome` (5-bit `class ⊕ detail`), `outcome_impurity` (f16, 1 − max fraction) and `terminated_fraction` (f16), all at the joint `class ⊕ detail` grain, with dominant and impurity derived from the one histogram."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Outcome (all at joint `class ⊕ detail` grain)
  verify:
    method: unit test
    detail: "fixture footprints: dominant and impurity agree with the histogram; triple events at detail 3"
  rulings:
  - R-20
  milestone: M5
  note: "N and the bin width of class_histogram, and which class fraction outcome_impurity uses: REQ-PAY-075 and REQ-PAY-076 (definition)."
- id: REQ-REF-002
  area: REF
  statement: "`QuadReduction` spread members must be `spread_shape = spread(n̂)/2.0`, `spread_event = disagreement(class⊕detail)/(1 − 1/(E+1))`, `roundtrip_error` and a 2-bit `spread_winner`, with `ensemble_spread = max(spread_shape, spread_event)`."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  verify:
    method: unit test
    detail: hand-computed footprints
  rulings:
  - R-18
  milestone: M5
- id: REQ-REF-003
  area: REF
  statement: "`error_ratio` must be `sigma_E(t)/sigma_E(0)` computed with max-deviation (not MAD), NaN-safe, `max`-aggregated over footprints, and used as a boolean flag only."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  verify:
    method: unit test
    detail: "a copy at 2.9% energy drift among 8 yields a ratio ≫ 1 (≈204.8 in the measured case); exact dynamics → 1.0"
  rulings: []
  milestone: M5
  note: "The flag threshold and how error_ratio enters ensemble_spread: REQ-PAY-080 (calibration); REQ-PAY-081 (definition)."
- id: REQ-REF-004
  area: REF
  statement: "Every footprint must keep all `E+1` copies in its reductions: a copy that fails to integrate must raise spread, and no drift filter or \"failed\" exclusion may remove copies."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  verify:
    method: property test
    detail: "inject a non-finite copy: spread rises; copy count stays E+1"
  rulings: []
  milestone: M5
- id: REQ-REF-005
  area: REF
  statement: "Kinetic energy, total energy, `L_z`, `d_min`, FTLE spread, diffusion, `S_word` and `t_end` must not be ensemble-spread contributors."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Ensemble spread — the two bounded contributors
  verify:
    method: review checklist
    detail: the spread reduction reads only shape and event
  rulings: []
  milestone: M5
- id: REQ-REF-006
  area: REF
  statement: "`QuadReduction` validity members must be `suspect_fraction` (f16), `saturated_fraction` (f16), `valid_sample_count` (u16), `coherence` (f32), `escape_time_min`/`_max` (f16), `retrograde_fraction` (f16) and `mean_orbit_count_spread` (f16)."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Validity and diagnostics
  verify:
    method: unit test
    detail: generated layout member list and types
  rulings: []
  milestone: M5
- id: REQ-REF-007
  area: REF
  statement: "`spread_t_end` must not be included in `QuadReduction` until escape time is bisected inside the firing interval."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Conditional — not yet included
  verify:
    method: review checklist
    detail: no spread_t_end member in v1
  rulings: []
  milestone: M5
- id: REQ-REF-008
  area: REF
  statement: "`priority`, `cacheAge`, `lifecycle`, `computeCostMs` and `gentime` must be CPU quad metadata, not `QuadReduction` members, and parent–child disagreement must be computed on the CPU as the `alpha` estimator."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Not reduction fields
  verify:
    method: review checklist
    detail: none of these in the GPU struct
  rulings: []
  milestone: M5
- id: REQ-REF-009
  area: REF
  statement: "A quad whose every footprint is budget-exhausted with all copies unusable must be reported `Undetermined` (`footprint_undetermined`), detected from those conditions rather than from the spread statistic."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: VERTICAL-SLICE ADDITIONS
  verify:
    method: numerical gate
    detail: "starved near-field fixture (64/64 budget, 512/512 non-finite) → Undetermined, although its spread_median reads 4.58e-4"
  rulings:
  - R-47
  milestone: M5
- id: REQ-REF-010
  area: REF
  statement: "Ensemble-spread reductions must not use NaN-ignoring max (e.g. `f64::max`) in a way that silently returns the event spread when the shape spread is undetermined."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: VERTICAL-SLICE ADDITIONS
  verify:
    method: unit test
    detail: "shape spread NaN → reduction reports undetermined, not the event value"
  rulings: []
  milestone: M5
- id: REQ-REF-011
  area: REF
  statement: "The screen-space floor must be a view-relative refinement boundary, never terminal (in view, quads above it must split at rest — during a gesture the frame budget governs; below it the criterion may supersample; MAX_REL_DEPTH caps every split beyond it, off-screen policy splits included); the only terminal floors are the linear-decoder AT_F32_FLOOR and the integration floor, and the per-sample saturated flag must be non-terminal."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  - file: decisions.md
    section: "R-98 — `MAX_REL_DEPTH` caps every split beyond the screen floor *(closes RQ-58)*"
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  verify:
    method: unit test
    detail: "quad-state is terminal iff AT_F32_FLOOR or the integration floor is hit; zooming out un-stops screen-floored quads"
  rulings:
  - R-88
  - R-98
  - R-108
  milestone: M6
- id: REQ-REF-012
  area: REF
  statement: "The split/keep/merge decision must be Policy::Tolerance's, applied together with the MAX_REL_DEPTH gate, with an in-view quad above the screen floor splitting regardless at rest (during a gesture the frame budget governs) and the terminal guard checked before the policy."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 3 — Depth: infinite zoom by default"
  - file: decisions.md
    section: "R-15 — `Policy::Tolerance` governs refinement *(closes RQ-13)*"
  - file: docs/read_first/principia_INDEX.md
    section: The evidence base — where settled defaults were measured
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  - file: decisions.md
    section: "R-108 — Must-split above the floor is the at-rest target *(closes RQ-68)*"
  verify:
    method: unit test
    detail: "a terminal quad is never passed to the policy; a non-terminal in-view quad above the screen floor splits; otherwise a non-terminal quad splits iff policy_splits and the depth gate holds"
  rulings:
  - R-15
  - R-44
  - R-88
  - R-108
  milestone: M6
  note: "Part 6's 'offscreen → stop' guard against policy §0.1 (off screen the criterion decides depth): REQ-SCHED-080 (definition)."
- id: REQ-REF-013
  area: REF
  statement: "The split predicate must be evaluated live at the playhead during playback as Policy::Tolerance's one test under eps, counting disagreement in class (including RUNNING vs terminal), spread_shape > eps and a per-footprint latched running maximum that ever exceeded eps, a quad being unresolved if any of its footprints is, and symbolic spread S_word must not enter it."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  - file: decisions.md
    section: "R-91 — The temporal accumulators feed \"unresolved\" *(closes RQ-42)*"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  verify:
    method: unit test
    detail: "a quad whose samples diverge mid-flight and reconverge splits at the divergence time and stays split while resident; changing S_word alone never changes the split decision"
  rulings:
  - R-15
  - R-91
  - R-99
  milestone: M6
  note: "S_word is a diagnostic, not a split input (R-99; scheduler Part 8 now says so). Its metric: REQ-REF-042 (definition)."
- id: REQ-REF-014
  area: REF
  statement: "Symbolic spread `S_word` must operate on whole raw reduced words and must not depend on pair attribution."
  source:
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: Downstream consumers waiting on this contract
  verify:
    method: review checklist
    detail: S_word implementation uses no per-pair helper
  rulings:
  - R-38
  milestone: M6
  note: "The metric: REQ-REF-042 (definition)."
- id: REQ-REF-015
  area: REF
  statement: "`QuadReduction` refinement members must be `alpha_area` (f16), `alpha_energy` (f16) and `worst_energy_drift` (f16), with `alpha_area` the stop rule's exponent judged over two levels (policy §2), not the α-era \"scaling exponent of `ensemble_spread`\"."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  - file: decisions.md
    section: "R-42 — The `alpha_area` defects: tell empty from full by `n_unresolved`; refuse the floor on a negative exponent *(RS-2 (b)+(c))*"
  - file: decisions.md
    section: "R-73 — Apply the whole ruling-follow-up checklist now *(closes RQ-56)*"
  verify:
    method: unit test
    detail: member types; alpha_area computed per refinement_policy §2
  rulings:
  - R-42
  - R-59
  - R-73
  milestone: M6
- id: REQ-REF-016
  area: REF
  statement: "If `|alpha_energy − 1| > 0.05`, every exponent from that quad must be treated as untrustworthy."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  verify:
    method: unit test
    detail: a quad with alpha_energy = 1.1 is flagged
  rulings: []
  milestone: M6
- id: REQ-REF-017
  area: REF
  statement: "`alpha` must be used ordinally only, never to forecast the realised gain as `2^-alpha`."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  verify:
    method: review checklist
    detail: no code computes 2^-alpha as a gain forecast
  rulings: []
  milestone: M6
- id: REQ-REF-018
  area: REF
  statement: "Estimator failure must be read from `error_ratio` departing from 1.0, with the fallback being to acquire a third scale, and no `failed_fraction` may exist."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  - file: decisions.md
    section: R-87 — `failed_fraction` is retired *(closes RQ-38)*
  verify:
    method: unit test
    detail: "an ensemble whose error_ratio departs from 1.0 triggers third-scale acquisition; no failed_fraction field or reference exists"
  rulings:
  - R-87
  milestone: M6
  note: "The departure threshold: REQ-PAY-080 (calibration)."
- id: REQ-REF-019
  area: REF
  statement: "`running_max_divergence` (f32, max-updated, latching, feeding \"unresolved\" under eps) must be held per footprint with the resident quad, not as a QuadReduction member, `running_mean_divergence` and `first_divergence_t` (write-once, sentinel until crossed) must be f32 diagnostics that are not split inputs, and there must be no `divergence_trend` member."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Temporal accumulators (scheduler Part 8)
  - file: decisions.md
    section: "R-91 — The temporal accumulators feed \"unresolved\" *(closes RQ-42)*"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-142 — The latch is evaluated on the GPU; only its verdict returns *(closes RQ-72)*"
  verify:
    method: unit test
    detail: "diverge-then-reconverge fixture: the footprint's running max stays; first_divergence_t written once; the generated layout has no divergence_trend and QuadReduction has no running_max_divergence"
  rulings:
  - R-91
  - R-99
  - R-142
  milestone: M6
  note: "The latch's storage layout: see the definition requirement for the per-footprint latch's layout."
- id: REQ-REF-020
  area: REF
  statement: "In view, a quad must stop only when its texel is at or below one screen pixel: Keep ⟺ n_unresolved == 0 ∧ tile_size_px ≤ 1 and Floor ⟺ area floor fired ∧ tile_size_px ≤ 1, so the in-view tree is complete at screen resolution."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "0.1 In view, the camera decides depth and the criterion decides ORDER"
  verify:
    method: unit test
    detail: "an in-view quad with n_unresolved = 0 and tile_size_px = 2 splits; at tile_size_px = 1 it keeps"
  rulings: []
  milestone: M6
- id: REQ-REF-021
  area: REF
  statement: "Off-screen quads must read as adequate at query time (adequacy met, stored nowhere), so off screen the criterion alone decides depth, capped by MAX_REL_DEPTH."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "0.1 In view, the camera decides depth and the criterion decides ORDER"
  - file: decisions.md
    section: "R-98 — `MAX_REL_DEPTH` caps every split beyond the screen floor *(closes RQ-58)*"
  - file: docs/contracts/principia_scheduler_contract.md
    section: "`MAX_REL_DEPTH` (renamed from `MAX_DEPTH`)"
  verify:
    method: unit test
    detail: "an off-screen quad with tile_size_px ≫ 1 and n_unresolved = 0 keeps; an unresolved off-screen quad at ℓ = camera_depth + MAX_REL_DEPTH does not split; no adequacy field is persisted on the quad"
  rulings:
  - R-98
  milestone: M6
  note: "How this reconciles with scheduler Part 6's 'never compute off-screen': REQ-SCHED-080 (definition)."
- id: REQ-REF-022
  area: REF
  statement: "A quad must split iff any footprint is unresolved, where unresolved(f) ⟺ spread_shape(f) > eps ∨ its own latched running maximum of spread_shape ever exceeded eps ∨ the copies disagree on event class ∨ the footprint is undetermined (non-finite spread or an unusable copy), with no exponent in the split test."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 1. The split rule — no exponent in the split test
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-15 — `Policy::Tolerance` governs refinement *(closes RQ-13)*"
  - file: decisions.md
    section: "R-91 — The temporal accumulators feed \"unresolved\" *(closes RQ-42)*"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-142 — The latch is evaluated on the GPU; only its verdict returns *(closes RQ-72)*"
  verify:
    method: unit test
    detail: "table test over footprints hitting each disjunct individually and none; a footprint whose spread fell back below eps after exceeding it stays unresolved"
  rulings:
  - R-15
  - R-91
  - R-99
  - R-142
  milestone: M6
- id: REQ-REF-023
  area: REF
  statement: "The stop rule must be alpha_area = log2(unresolved_area(coarse) / unresolved_area(children)) compared against alpha_lo, with shipped default alpha_lo = 0.005."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "2. The stop rule — `alpha_area` is a DIMENSION, not a tuned constant"
  - file: decisions.md
    section: "R-42 — The `alpha_area` defects: tell empty from full by `n_unresolved`; refuse the floor on a negative exponent *(RS-2 (b)+(c))*"
  verify:
    method: unit test
    detail: "on synthetic masks, a line reads 1, a sea reads 0; default config has alpha_lo = 0.005"
  rulings:
  - R-42
  milestone: M6
- id: REQ-REF-024
  area: REF
  statement: "The alpha_area exponent must be judged over two levels, from the grandparent's quadrant, not the one-level form."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 2.1 Two structural details, each of which cost a measurement
  verify:
    method: numerical gate
    detail: "on a line (shore) fixture across 15 splits, alpha_area lies in [1.00, 1.05] (the one-level form reads 0 for edge cells)"
  rulings: []
  milestone: M6
- id: REQ-REF-025
  area: REF
  statement: "An unresolved footprint must count as structure iff at least two of its eight neighbours share its class and land within ~11° on the shape sphere."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 2.1 Two structural details, each of which cost a measurement
  verify:
    method: unit test
    detail: "white-noise fixture: structured count ≈ chance (a few in ten thousand); coherent fixture: neighbours within 11° count"
  rulings: []
  milestone: M6
  note: "The angle: REQ-REF-043 (calibration)."
- id: REQ-REF-026
  area: REF
  statement: "Under a live playhead the tree must coarsen: a parent whose four children are all leaves that did not split this boundary must be merged back (children Decision::Merged, their footprints' latches going with them) when the parent is Keep or Floor, and the static result at the horizon must be bitwise the static tree."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 3. Merging — the split rule read backwards
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-143 — The live tree contains the static tree *(closes RQ-73)*"
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: golden image
    detail: "the moving pulse (fixtures/moving_pulse.toml, a synthetic field, not a chart slice; R-166): resident count rises and falls (≈37 → 85 → 37 → 69), merged children hold no latch, and the final tree contains the static tree at the horizon, equal to it on a fixture whose footprint spreads are monotone in time (R-143); no latch is dropped while its quad is resident"
  rulings:
  - R-99
  - R-143
  - R-166
  milestone: M6
- id: REQ-REF-027
  area: REF
  statement: "QuadTree::resident (what a live design holds) must be tracked separately from quads_computed."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 3. Merging — the split rule read backwards
  verify:
    method: unit test
    detail: "after merges, resident < quads_computed and both are reported"
  rulings: []
  milestone: M6
- id: REQ-REF-028
  area: REF
  statement: "A merged parent must remember its exponents so it is not re-split into the same children every boundary, and the memory must expire (via a separate expiry flag, never by deleting it) when the quad's structured weight leaves a factor of two of where the split was judged."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 3. Merging — the split rule read backwards
  verify:
    method: unit test
    detail: "expired memory is distinguishable from absent memory (not an assert_ne!); expiry does not revert to first-time flooring (421 vs 645 quads regression)"
  rulings: []
  milestone: M6
- id: REQ-REF-029
  area: REF
  statement: "Capped leaves (e.g. stopped by the camera floor) must stay re-decidable and be re-tested every boundary, so a parent that later resolves can merge them."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 3. Merging — the split rule read backwards
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: unit test
    detail: "the moving pulse (fixtures/moving_pulse.toml; R-166): the tree coarsens to the static 69 quads rather than 149"
  rulings:
  - R-166
  milestone: M6
- id: REQ-REF-030
  area: REF
  statement: "The refinement criterion and any error metric used to judge it must be scored in payload space (shape_vec and event class against eps), never in a render or colour space such as OKLab."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 4. THE METRIC MUST BE PAYLOAD-SPACE. THIS IS NOT OPTIONAL.
  - file: docs/read_first/principia_01_pitfalls.md
    section: 5. MEASURING IN THE WRONG SPACE — `error(B)` in OKLab
  verify:
    method: property test
    detail: "changing the palette/colouring leaves the refined tree and its error score bit-identical"
  rulings:
  - R-15
  milestone: M6
  note: "pitfall: \"5. MEASURING IN THE WRONG SPACE — `error(B)` in OKLab\" — 5. MEASURING IN THE WRONG SPACE — any criterion work scored in render space is void."
- id: REQ-REF-031
  area: REF
  statement: "A cheap sea_fraction(eps) estimator, computable before any descent, must be built, and it must be built after the tier controller."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "5.1 `sea_fraction` is the regime test, and it is computable before any descent"
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: decisions.md
    section: "R-48 — The `sea_fraction` estimator is built after the tier controller *(RS-8)*"
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: v0.5 means the NUMBERS are guesses; the SHAPE is not
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 4. Deriving the tier instead of choosing it
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  verify:
    method: unit test
    detail: "estimator's sea_fraction matches the full-cache footprint-spread CDF on the tolerance-study fixtures within a recorded tolerance"
  rulings:
  - R-48
  milestone: M6
  note: "The accuracy target: REQ-REF-044 (calibration)."
- id: REQ-REF-032
  area: REF
  statement: "tau must be a separate threshold, not computed as eps times a constant, and its value must be set by a calibrated grid measurement."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 5.2 The tolerance is inert exactly where the policy works
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: decisions.md
    section: "R-49 — The refinement policy's open measurements are taken at the first refine milestone *(RS-9)*"
  verify:
    method: benchmark
    detail: "calibrate tau per the tolerance grid (charts × horizons × eps) at the first refine milestone and record it; config has no tau = k·eps derivation"
  rulings:
  - R-49
  milestone: M6
- id: REQ-REF-033
  area: REF
  statement: "The decision enum must have exactly the variants Keep, Split, Floor, ScreenFloor, Undetermined, Collapsed, Merged and BalanceForced."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 6. Decision variants, and the second budget line
  verify:
    method: unit test
    detail: exhaustive match over the enum compiles against this list
  rulings: []
  milestone: M6
- id: REQ-REF-034
  area: REF
  statement: "Decision::Undetermined must be reported, treated as terminal and flagged, with no second refinement axis (finer eta) until a chart shows it non-zero."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 6. Decision variants, and the second budget line
  - file: decisions.md
    section: "R-47 — `Undetermined` is reported, terminal and flagged, until a chart shows it non-zero *(RS-7)*"
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  verify:
    method: unit test
    detail: "an undetermined footprint yields a terminal flagged quad and appears in the stop breakdown"
  rulings:
  - R-47
  milestone: M6
  note: "Doc §6 frames Undetermined as a second refinement axis; R-47 settles it as report-terminal-flag for now."
- id: REQ-REF-035
  area: REF
  statement: "The refinement latch must be per footprint and live with the resident quad, one-way while the quad is resident, and must go when the cache evicts or merges the quad, so it never pins memory and an evicted or merged region re-discovers its structure; the latch must be evaluated on the GPU in the resolve pass (R-135), its state kept in GPU-resident per-quad memory, with QuadReduction carrying only the verdict — the count of the quad's unresolved footprints, latched ones included — and staying the sole automatic return (R-142)."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Continuous refinement — temporal accumulators + spatial coherence"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Decisions — DECIDED (ratified in conversation; recorded here so they don't evaporate)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Footguns (all re-applications of disciplines already established)"
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 7 — The current-state cache (resume points, hard-capped)"
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  - file: decisions.md
    section: "R-91 — The temporal accumulators feed \"unresolved\" *(closes RQ-42)*"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-142 — The latch is evaluated on the GPU; only its verdict returns *(closes RQ-72)*"
  verify:
    method: unit test
    detail: "a latched footprint keeps its quad split while resident; evict or merge the quad: its latches are gone and no latch memory remains; revisiting the region re-discovers the split from fresh state; the latch is read and written only by GPU passes, and QuadReduction's only latch-derived member is the unresolved-footprint count, which includes latched footprints (R-142)"
  rulings:
  - R-15
  - R-91
  - R-99
  - R-142
  milestone: M6
  note: "R-91: the latched running maximum feeds Policy::Tolerance's \"unresolved\" under the same eps; there is no separate latch threshold. R-99 replaces the old 'persists across visits' (scheduler Part 8, caching Part 7 and the temporal note now say so)."
- id: REQ-REF-036
  area: REF
  statement: "The split decision must be made by Policy::Tolerance under the one knob eps, and the temporal refinement accumulators must be fixed-size O(1) fields, never stored history — one float per footprint for the latch, held with the resident quad, plus the running mean and first-divergence diagnostics — with no divergence trend and no separate θ_s / θ_max / θ_trend thresholds."
  source:
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Continuous refinement — temporal accumulators + spatial coherence"
  - file: decisions.md
    section: "R-15 — `Policy::Tolerance` governs refinement *(closes RQ-13)*"
  - file: decisions.md
    section: "R-91 — The temporal accumulators feed \"unresolved\" *(closes RQ-42)*"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  verify:
    method: review checklist
    detail: "the scheduler calls the refinement policy for splits; the accumulators' size is fixed in t; no trend accumulator or θ threshold exists in code or config"
  rulings:
  - R-15
  - R-91
  - R-99
  milestone: M6
- id: REQ-REF-037
  area: REF
  statement: "The free-group word must be stored per copy, and a symbolic-spread reduction over the E+1 words must produce an S_word channel that is shown and measured as a diagnostic and never read by the split."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Symbolic spread — the free-group word as a third resolve-stage reduction"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  verify:
    method: unit test
    detail: "a footprint whose copies share an outcome but differ in word yields nonzero S_word; the split decision is identical with S_word forced to any value"
  rulings:
  - R-99
  milestone: M6
  note: "The reduction metric: REQ-REF-042 (definition)."
- id: REQ-REF-038
  area: REF
  statement: "The refinement mechanism must be re-measured from scratch on uncontaminated fields; conclusions measured on the wedge-contaminated field (frac_hot_between saturation, α distributions, tree statistics, error(B) curves, 'nothing beats breadth-first') must not be carried forward."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.8 Sequencing — what is next, and why in this order
  verify:
    method: review checklist
    detail: "Every refinement default in the build cites a measurement taken after the wedge fix; only the DP ceiling machinery, Rank::Uniform baseline, labels-are-monotone and the blocked-vs-pooled ρ correction are carried over."
  rulings:
  - R-49
  milestone: M6
  retired: "R-74: the vertical slice settled the research phases (canonical_spec §11 governs; philosophy §7.8 is superseded), so the refinement mechanism is not re-taken as milestone work"
- id: REQ-REF-039
  area: REF
  statement: "Anisotropic splitting must be costed by counting splits whose four children immediately keep."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: "8.1 An 8D BVH over the manifold, instead of a per-slice quadtree"
  verify:
    method: numerical gate
    detail: "At the refine milestone, report the count/fraction of isotropic splits whose four children all immediately keep, on the standard charts."
  rulings:
  - R-49
  milestone: M6
  note: §8.1 says this salvage is 'live, not parked'.
- id: REQ-REF-040
  area: REF
  statement: "alpha_area must distinguish an empty mask from a full one by n_unresolved, refuse the floor on a negative exponent, and keep alpha_lo = 0.005."
  source:
  - file: decisions.md
    section: "R-42 — The `alpha_area` defects: tell empty from full by `n_unresolved`; refuse the floor on a negative exponent *(RS-2 (b)+(c))*"
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  - file: docs/read_first/principia_01_pitfalls.md
    section: 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN
  - file: decisions.md
    section: "R-73 — Apply the whole ruling-follow-up checklist now *(closes RQ-56)*"
  - file: docs/design/principia_dd_refinement_policy.md
    section: 2.2 TWO KNOWN DEFECTS IN `alpha_area` — both ruled (R-42)
  - file: decisions.md
    section: "R-107 — Apply the RQ-67 follow-ups; GUI_DESIGN_NOTES may be conformed *(closes RQ-67)*"
  verify:
    method: unit test
    detail: "Empty-mask and full-mask fixtures give different results (not both 0.0000); a negative-exponent fixture is refused (no d > 2); alpha_lo = 0.005."
  rulings:
  - R-42
  - R-59
  - R-73
  - R-107
  milestone: M6
  note: "pitfall: \"9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN\". Policy §2.2 records R-42's fixes (its heading now reads 'both ruled', R-107), which land with the refinement task; the ledger's alpha row is corrected (R-73)."
- id: REQ-REF-041
  area: REF
  kind: calibration
  statement: "The symbolic-spread split threshold on S_word must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Symbolic spread — the free-group word as a third resolve-stage reduction"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows S_word's distribution over a boundary-crossing survey and where the threshold fires relative to outcome impurity"
  rulings:
  - R-71
  milestone: M6
  retired: "R-99: S_word is a diagnostic, not a split input, so it has no split threshold."
- id: REQ-REF-042
  area: REF
  kind: definition
  statement: "The symbolic-spread metric over a footprint's E+1 words (shared-prefix length, edit distance or distinct-word count) must be defined in principia_sampling_msaa_note.md's symbolic-spread section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Symbolic spread — the free-group word as a third resolve-stage reduction"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  verify:
    method: review checklist
    detail: "the sampling note names one metric and its range; the reviewer checks it depends only on whole raw reduced words"
  rulings:
  - R-72
  - R-99
  milestone: M6
  note: S_word is a diagnostic, not a split input (R-99).
- id: REQ-REF-043
  area: REF
  kind: calibration
  statement: "The neighbour-agreement angle (given as ~11° on the shape sphere) for counting an unresolved footprint as structure must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 2.1 Two structural details, each of which cost a measurement
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "decisions.md records the exact angle with its evidence: structured counts on the white-noise fixture (≈ chance) and on coherent fixtures at the chosen angle"
  rulings:
  - R-71
  milestone: M6
- id: REQ-REF-044
  area: REF
  kind: calibration
  statement: "The sea_fraction estimator's accuracy target must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "5.1 `sea_fraction` is the regime test, and it is computable before any descent"
  - file: decisions.md
    section: "R-48 — The `sea_fraction` estimator is built after the tier controller *(RS-8)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "decisions.md records the target with its evidence: estimator sea_fraction against the full-cache footprint-spread CDF on the tolerance-study fixtures, and the tier-derivation error that error costs"
  rulings:
  - R-48
  - R-71
  milestone: M6
- id: REQ-REF-045
  area: REF
  kind: definition
  statement: "The storage layout of the per-footprint latch (`running_max_divergence`, f32, held with the resident quad in GPU-resident per-quad memory, R-142) must be defined in dd_generation_root's temporal-accumulators section by the task that builds it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Temporal accumulators (scheduler Part 8)
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-142 — The latch is evaluated on the GPU; only its verdict returns *(closes RQ-72)*"
  verify:
    method: review checklist
    detail: "the section gives the latch's layout per footprint with the resident quad, and shows it is dropped on evict and merge; physics reviewer approved"
  rulings:
  - R-72
  - R-99
  - R-113
  - R-142
  milestone: M5
- id: REQ-REF-046
  area: REF
  kind: definition
  statement: "Where the diagnostics `running_mean_divergence` and `first_divergence_t` are held (they are no longer split inputs or QuadReduction members, R-99) must be defined in dd_generation_root's temporal-accumulators section by the task that needs them and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Temporal accumulators (scheduler Part 8)
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states where both diagnostics live and how the GPU→CPU crossing carries them, if at all; physics reviewer approved"
  rulings:
  - R-72
  - R-99
  milestone: M6
- id: REQ-REF-047
  area: REF
  kind: definition
  statement: "The meaning of each Decision variant — which variant records AT_F32_FLOOR and which the integration floor, and what produces BalanceForced — must be defined in refinement_policy §6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 6. Decision variants, and the second budget line
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§6 gives, for each of the eight variants, the stop that produces it and whether it is terminal; the lifecycle state-machine test follows it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M6
- id: REQ-REF-048
  area: REF
  kind: definition
  statement: "The estimator fallback's third scale — which refinement level is acquired beside alpha_area's two, when it is acquired, and how the exponent is then fitted over three scales — must be defined in dd_generation_root's scaling-exponent section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  - file: decisions.md
    section: R-87 — `failed_fraction` is retired *(closes RQ-38)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section names the third level, the trigger (error_ratio departing from 1.0) and the three-point fit; REQ-REF-018's unit test follows it; physics reviewer approved"
  rulings:
  - R-72
  - R-87
  milestone: M6
- id: REQ-REF-049
  area: REF
  kind: definition
  statement: "The cheap sea_fraction(eps) estimator's method (what it samples before any descent and how it estimates the footprint-spread CDF from it) must be defined in refinement_policy §5.1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "5.1 `sea_fraction` is the regime test, and it is computable before any descent"
  - file: decisions.md
    section: "R-48 — The `sea_fraction` estimator is built after the tier controller *(RS-8)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§5.1 states the sampling and the estimate, computable before descent without a full cache; REQ-REF-031's comparison against the full-cache CDF uses it; physics reviewer approved"
  rulings:
  - R-48
  - R-72
  milestone: M6
- id: REQ-REF-050
  area: REF
  kind: calibration
  statement: "The value of `tau`, the stop rule's second threshold (not `eps` times a constant), must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 5.2 The tolerance is inert exactly where the policy works
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: decisions.md
    section: "R-49 — The refinement policy's open measurements are taken at the first refine milestone *(RS-9)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states tau with the tolerance-grid evidence (charts × horizons × eps) that REQ-REF-032's benchmark produces; recorded in decisions.md"
  rulings:
  - R-49
  - R-71
  milestone: M6
- id: REQ-REF-051
  area: REF
  kind: calibration
  statement: "The latch's cost — the extra resident quads it holds over the latch-free tree — must be measured on the named slices (dd_refinement_policy §5's near-field, deep interior, config_stability and tilt_plambda) and calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 3. Merging — the split rule read backwards
  - file: decisions.md
    section: "R-143 — The live tree contains the static tree *(closes RQ-73)*"
  - file: decisions.md
    section: R-158 — The six readings are accepted *(closes RQ-128)*
  verify:
    method: numerical gate
    detail: "per named slice, resident quads with and without the latch over a live playhead to the horizon, the difference and its share of the resident count; recorded in decisions.md"
  rulings:
  - R-71
  - R-143
  - R-158
  milestone: M6
- id: REQ-RENDER-001
  area: RENDER
  statement: "Generated WGSL must use the u32 overload of extractBits, use no f64, read f16 pairs via unpack2x16float (no shader-f16 feature), group r, p and shadow in vec2 members, and bind the word buffer separately with per-copy indexing."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: review checklist
    detail: "grep generated WGSL: all extractBits args are u32; no f64; no enable f16"
  rulings:
  - R-86
  milestone: M0
- id: REQ-RENDER-002
  area: RENDER
  statement: "No per-pair quantity (enc_01/02/12, encounter_count) and no dominant_pair view or colour mode may ship in v1 until symbolic_dynamics_contract §1–4's three open specifications are written, per-pair tallies must never be computed as a symbol histogram, and word storage and S_word must proceed meanwhile."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 5. Derived — computed at read, NOT stored
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: "Status: OPEN — specification required before per-pair quantities are trusted"
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: 3. Third-pair attribution algorithm
  - file: docs/contracts/principia_symbolic_dynamics_contract.md
    section: Downstream consumers waiting on this contract
  - file: docs/contracts/principia_render_contract.md
    section: Unpack layer (generated, one accessor per named field)
  - file: decisions.md
    section: "R-38 — Per-pair views are out of v1 until `dominant_pair` is specified *(PL-3)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  verify:
    method: review checklist
    detail: no per-pair view in the v1 catalogue or colour-mode list
  rulings:
  - R-38
  milestone: M0
  note: "debug_tooling_plan §B lists enc_XY/dominant_pair derived views; R-38 holds them out of v1"
- id: REQ-RENDER-003
  area: RENDER
  statement: "The fragment (colour) pipeline must be hand-written WGSL assembled from keyed snippets and hot-reloaded at runtime, so runtime-authored custom colour occupants work."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_core_design.md
    section: "4. Integrate and colour are separate passes — and now separate *mechanisms*"
  verify:
    method: unit test
    detail: "a colour snippet supplied at runtime compiles, swaps in without rebuilding the wasm binary, and leaves sim buffers byte-identical"
  rulings: []
  milestone: M1
- id: REQ-RENDER-004
  area: RENDER
  statement: "The sampling hierarchy must be QUAD → N×N SAMPLES (N = SAMPLES_PER_QUAD_AXIS, each a full SimState) → one TILE per sample → PIXELS, with no interpolation between samples."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  verify:
    method: unit test
    detail: "each sample rasterises to exactly its tile; pixel colours inside a tile come from one sample"
  rulings: []
  milestone: M1
- id: REQ-RENDER-005
  area: RENDER
  statement: "The fragment pipeline must be assembled at runtime from keyed WGSL snippets into the template [prelude][node functions][generated shade()], hashed, compiled async, cached, with last-valid-on-failure."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 2 — Two assembly mechanisms (the substrate split; the old \"one mechanism\" claim retires)"
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: unit test
    detail: "the same stain produces the same hash and hits the cache; a failed compile keeps the previous pipeline"
  rulings:
  - R-64
  milestone: M1
- id: REQ-RENDER-006
  area: RENDER
  statement: "Fragment-side lowering must bake the stain graph (node occupants + wiring) as the fragment key, generate one tiny baked source per debug field view on demand (never a mega-switch), pass node uniforms and view-only display state as uniforms, and use fixed precompiled compositor shaders (backdrop, separable blur ×2, composite)."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Fragment side
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: review checklist
    detail: "no debug shader switches over field ids at runtime; compositor pipelines created at startup"
  rulings:
  - R-64
  milestone: M1
  note: "The canonical graph form the key hashes: REQ-RENDER-075 (definition)."
- id: REQ-RENDER-007
  area: RENDER
  statement: "The logical RenderSample view must be realised as accessor functions over the physical structs, and no compute pass may materialise an unpacked copy of the payload."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/design/principia_dd_generation_root.md
    section: 2. Consolidated contract
  verify:
    method: review checklist
    detail: "no compute pipeline writes an unpacked RenderSample buffer; fragment code reads through generated accessors"
  rulings: []
  milestone: M1
- id: REQ-RENDER-008
  area: RENDER
  statement: "RenderContext must provide {sample: SimState, ic: ICDescriptor, quad: RenderQuad, uv, screen_uv, time} to the fragment stage."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  verify:
    method: unit test
    detail: "generated WGSL RenderContext declares these members; a debug view reading each compiles"
  rulings: []
  milestone: M1
- id: REQ-RENDER-009
  area: RENDER
  statement: "The fragment pipeline must run the stain graph on a fixed backbone — sources feed colour and brightness, which meet only at the combiner, then the post chain, then OUT, the graph acyclic — with colour → vec3 linear RGB, brightness → f32 nominal [0,1], combine(rgb, b) → vec3 and each post step vec3 → vec3."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 2 — Two assembly mechanisms (the substrate split; the old \"one mechanism\" claim retires)"
  - file: docs/design/principia_colour_composition.md
    section: 4. Pipeline shape
  - file: docs/design/principia_dd_colouring.md
    section: 1. What it is
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: review checklist
    detail: "generated shade() follows the backbone order; no configuration can reorder the backbone or feed post back into colour"
  rulings:
  - R-64
  milestone: M1
- id: REQ-RENDER-010
  area: RENDER
  statement: "Custom occupants must be the only extension mechanism: built-in, debug and custom occupants must share one compile path that regenerates the full fragment source from the shared prelude, one function per node and the generated shade(), with schema-driven uniforms."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 2 — Two assembly mechanisms (the substrate split; the old \"one mechanism\" claim retires)"
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: review checklist
    detail: "built-in occupants are loaded through the same assembler entry point as a user custom; no second compile path exists"
  rulings:
  - R-64
  milestone: M1
- id: REQ-RENDER-011
  area: RENDER
  statement: "A fragment occupant that fails to compile must keep the last valid source in its node while leaving the other nodes untouched (per-node failure isolation, async compile, last-valid fallback)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 2 — Two assembly mechanisms (the substrate split; the old \"one mechanism\" claim retires)"
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  - file: docs/design/principia_colour_composition.md
    section: 5. Codegen & eject
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: unit test
    detail: "inject a syntax error into one node's source; the pipeline keeps rendering with that node's previous source and the other nodes' current sources; an error is surfaced"
  rulings:
  - R-64
  milestone: M1
- id: REQ-RENDER-012
  area: RENDER
  statement: "The read-side SimState type must be fixed across all tiers and variants and accessed by plain member access (e.g. sample.ftle, sample.ensemble_spread, sample.d_min), never through getter functions, for built-in and custom occupants alike."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  verify:
    method: unit test
    detail: "a custom occupant reading sample.ftle, sample.ensemble_spread and sample.word compiles and runs at every tier variant"
  rulings: []
  milestone: M1
- id: REQ-RENDER-013
  area: RENDER
  statement: "A tier that bakes a feature out must make that field read NaN at unpack (ftle, ensemble_spread when E=0) or the empty/sentinel word (word buffer unbound) rather than removing the field, at zero stored-byte cost, with storage never holding NaN."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: unit test
    detail: "at a no-FTLE variant sample.ftle bitcasts to the canonical quiet-NaN pattern; at E=0 ensemble_spread does likewise; unbound word reads the sentinel word"
  rulings:
  - R-79
  milestone: M1
- id: REQ-RENDER-014
  area: RENDER
  statement: "The assembler must bake a compile-time const bool has_<feature> (has_ftle, has_word) for each tier-gated feature, dead-code-eliminated; has_ensemble must be read by the fragment side as a uniform, like the compute side's copy_index, so toggling E never re-bakes, with E = 0 still reading ensemble_spread as NaN (R-145)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  - file: decisions.md
    section: "R-145 — The fragment side reads `has_ensemble` as a uniform *(closes RQ-75)*"
  verify:
    method: unit test
    detail: "generated prelude contains the has_ftle and has_word consts with values matching the variant's tier bits and reads has_ensemble from a uniform; toggling E between 0 and 3 leaves the fragment variant unchanged and E = 0 reads ensemble_spread as NaN (R-145)"
  rulings:
  - R-145
  milestone: M1
- id: REQ-RENDER-015
  area: RENDER
  statement: "No correctness logic in any built-in occupant may rely on NaN propagation or isnan(); per-sample validity must be tested with the descriptor/validity predicates (sd_is_failed, ftle_valid, diffusion slope-valid), tier presence with has_<feature>, and any runtime absence test with an exact bitcast comparison against the canonical quiet-NaN bits."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  - file: decisions.md
    section: "R-114 — The debug NaN guard is the bitcast test *(closes RQ-82)*"
  - file: decisions.md
    section: "R-145 — The fragment side reads `has_ensemble` as a uniform *(closes RQ-75)*"
  verify:
    method: review checklist
    detail: "grep generated and built-in WGSL for isnan/isNan and for NaN self-comparison (x != x): zero hits; FTLE views guard with has_ftle && ftle_valid; the debug NaN guard is the bitcast test (R-114)"
  rulings:
  - R-79
  - R-114
  - R-145
  milestone: M1
- id: REQ-RENDER-016
  area: RENDER
  statement: "The public slot contract must stay linear RGB plus scalar even where built-ins work internally in OKLab/OKLCH."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  verify:
    method: review checklist
    detail: "colour slot signature returns linear RGB vec3; brightness returns f32"
  rulings: []
  milestone: M1
- id: REQ-RENDER-017
  area: RENDER
  statement: A RUNNING sample must be coloured like any other class.
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  verify:
    method: unit test
    detail: "a synthetic RUNNING sample renders with its palette colour, not discarded or blank"
  rulings: []
  milestone: M1
- id: REQ-RENDER-018
  area: RENDER
  statement: A Benettin shadow must never itself be coloured as a sample.
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: "3.5 The live-state block (replaces the checkpoint array — lockstep, ratified)"
  verify:
    method: review checklist
    detail: no render path reads r_sh/p_sh as a sample
  rulings: []
  milestone: M1
- id: REQ-RENDER-019
  area: RENDER
  statement: "Time fractions must be derived as `t_*_step / horizon_steps`, returning 0 when `horizon_steps` is 0."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`times` (u32)"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "6. Accessors (illustrative of generated output; source is the Rust layout definition — emitted as Rust for the kernel/host and WGSL for the fragment side)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  verify:
    method: unit test
    detail: tm_t_end_fraction(w, 0) == 0; (65535, 65535) → 1.0 exactly
  rulings: []
  milestone: M1
- id: REQ-RENDER-020
  area: RENDER
  statement: "The assembled WGSL must be three layers — a fixed shared prelude, the per-node functions, and shade() — with the prelude (colour-space maps, ramps, the reserved colour, value mapping) callable by any node including custom ones and emitted from the one Rust layout definition, not hand-maintained."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  verify:
    method: unit test
    detail: "the prelude is generated by the layout build step; a custom node calling ramp_viridis compiles"
  rulings: []
  milestone: M1
- id: REQ-RENDER-021
  area: RENDER
  statement: "range_norm(x, lo, hi, auto, meas) must return clamp((x − l)/(h − l), 0, 1) with (l, h) = select((lo, hi), meas, auto), where auto = false uses the manifest's declared fixed [lo, hi] and auto = true uses the per-draw measured min / max supplied in meas from the QuadReduction machinery."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  verify:
    method: unit test
    detail: "CPU reference vs shader for x in and out of range with auto on and off; out-of-range clamps under fixed"
  rulings:
  - R-53
  milestone: M1
- id: REQ-RENDER-022
  area: RENDER
  statement: "Each generated numeric debug field's colour() must be the NaN guard — the bitcast test against the canonical quiet-NaN pattern, never `raw != raw` — returning debug_invalid(frag_xy) (R-136), followed by ramp(range_norm(raw, lo, hi, RANGE_AUTO, u_range)), with RANGE_AUTO editable identically as the node parameter and in the code."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-114 — The debug NaN guard is the bitcast test *(closes RQ-82)*"
  - file: decisions.md
    section: "R-136 — `debug_invalid(frag_xy)` draws the hatch; sentinels show their value *(closes RQ-114)*"
  verify:
    method: unit test
    detail: "generated source matches the two-line template with the bitcast guard and contains no self-comparison; the guard's branch returns debug_invalid(frag_xy) (R-136); toggling RANGE_AUTO as the node parameter updates the code and vice versa"
  rulings:
  - R-79
  - R-113
  - R-114
  - R-136
  milestone: M1
  note: "R-114: render_gui_spec §10.1's `raw != raw` is dropped. R-113 (RQ-94): the node-inspector leg of RANGE_AUTO is in REQ-GUI-136's verify (M8)."
- id: REQ-RENDER-023
  area: RENDER
  statement: "Debug fields must be raw: apart from the NaN guard there must be no validity masking, a failed-state sentinel being shown as its literal value."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: unit test
    detail: "a sample with state = failed and field sentinel 0.0 renders the ramp colour of 0.0, not the invalid colour"
  rulings:
  - R-79
  milestone: M1
  note: "R-79 makes debug fields the stated exception to §13's validity-first rule: they show literal stored values, and NaN still goes to the invalid colour."
- id: REQ-RENDER-024
  area: RENDER
  statement: "Tier 1 structural overlays (quad and tile boundaries) must be ordinary post nodes drawing from ctx.quad.uv / ctx.tile.uv with an fwidth-based edge_line, giving a constant pixel width at any quad size, depth and zoom, antialiased, serialising with the graph with editable width, opacity, colour and level."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  verify:
    method: golden image
    detail: "boundary lines at depths 3 and 20 have the same pixel width in golden images; the overlay persists across save / load"
  rulings: []
  milestone: M1
- id: REQ-RENDER-025
  area: RENDER
  statement: "A ROUNDTRIP fragment preset must render per pixel the z → D → E → D → physical residual, recomputed in the fragment and log-scaled, dark everywhere except clamp boundaries, feasibility edges and the mirror tie, each tagged expected."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: Part 8 — Round-trip contracts and the new debug view
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: decisions.md
    section: "R-41 — `DEBUG_MODE` uses the baked variants, not flag bits *(RS-1 (b))*"
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "A. Kernel debug dispatch modes (skip integration; reuse payload slots as scratch)"
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  verify:
    method: golden image
    detail: "ROUNDTRIP preset render of the latent and (L_z, E) charts: residual below ε_phys except at tagged-expected regions; no kernel variant is involved"
  rulings:
  - R-41
  - R-75
  milestone: M2
  note: "The kernel keeps only the bring-up mode (R-75); R-41's baked-variant rule now applies to that mode alone. ε_phys: REQ-ENC-024 (calibration)."
- id: REQ-RENDER-026
  area: RENDER
  statement: "The shape-sphere chart must default to an equirectangular projection and offer an equal-area alternative (Mollweide or Hammer–Aitoff) for quantitative area comparisons."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3.3 The chart map
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: golden image
    detail: "one golden per projection (equirectangular default, the equal-area alternative) of the shape-sphere chart"
  rulings:
  - R-113
  milestone: M2
  note: "R-113 (RQ-95): the projection selector is a separate M8 GUI requirement (the Manifold view's Chart section)."
- id: REQ-RENDER-027
  area: RENDER
  statement: "A DECODE fragment preset (fragment-side decode of ctx.chart.z) must exist that certifies the decode rung before any physics, and every chart of the lowering appendix built by M2 (latent affine slice, shape sphere, (L_z, E), (L_z, K), ternary mass, Euclid ν plane, θ × K strip) must decode through it without special-casing."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: decisions.md
    section: "R-41 — `DEBUG_MODE` uses the baked variants, not flag bits *(RS-1 (b))*"
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: golden image
    detail: "render the DECODE preset for each M2 chart and compare with a CPU-decoded reference image"
  rulings:
  - R-41
  - R-75
  - R-113
  milestone: M2
  note: "UV / DECODE / ROUNDTRIP are fragment presets, not kernel variants (R-75). R-113 (RQ-95): the Burrau int lattice and the Anosova physical-frame chart are a separate M4 requirement."
- id: REQ-RENDER-028
  area: RENDER
  statement: "The diffusion sentinel must use the streaming slope defined by the payload ledger."
  source:
  - file: decisions.md
    section: "R-17 — The diffusion sentinel uses the streaming slope *(closes RQ-15)*"
  verify:
    method: unit test
    detail: "Sentinel reads the ledger's streaming-slope field; fixture with known slope trips the sentinel at the ledger-defined value."
  rulings:
  - R-17
  milestone: M3
- id: REQ-RENDER-029
  area: RENDER
  statement: "Checkerboard motion acceleration must skew half the pixels by exactly one dt only while the playhead advances, must self-erase at rest, pause and endpoints, must be opt-out, and must be forced off in export."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  verify:
    method: unit test
    detail: "paused/endpoint frames have zero skew; export frames never checkerboard; toggle off disables it"
  rulings: []
  milestone: M4
- id: REQ-RENDER-030
  area: RENDER
  statement: "A sim-key change must re-integrate (the march re-boots) and a render-key change must only recolour: switching a render mode must never recompute sim data, during playback included."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 0. The ladder — the organising abstraction
  - file: docs/design/principia_systems_architecture.md
    section: The two keys as ladder geometry
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_core_design.md
    section: "4. Integrate and colour are separate passes — and now separate *mechanisms*"
  - file: docs/design/principia_temporal_architecture_note.md
    section: Render swapping is unaffected — the struct is the boundary
  verify:
    method: unit test
    detail: "swap colour occupant mid-march: no compute dispatch is issued for the swap and sim buffers are byte-identical"
  rulings: []
  milestone: M4
- id: REQ-RENDER-031
  area: RENDER
  statement: "Each render pixel must integrate (E+1) full uniform samples plus, from Medium up, each sample's Benettin FTLE shadow, as tier-gated co-computations on the forward pass."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/design/principia_core_design.md
    section: "4. Integrate and colour are separate passes — and now separate *mechanisms*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "trajectory count per pixel = (E+1) with FTLE off and 2(E+1) with FTLE on, over several E"
  rulings:
  - R-113
  milestone: M4
  note: "R-113 (RQ-98): the tier names (below Medium / from Medium up) are checked by REQ-PERF-014 (M5)."
- id: REQ-RENDER-032
  area: RENDER
  statement: "Checkerboard must march the samples of half the render pixels per frame (a per-render-pixel checkerboard mask whose parity flips each frame), with the other half holding its own value from t − dt."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 1. What this is (and what it is NOT)
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "2. The mechanism: reconstruct-from-previous, resolved through SSAA"
  verify:
    method: unit test
    detail: "over two frames every pixel advances exactly once; per-frame trajectory count halves"
  rulings: []
  milestone: M4
- id: REQ-RENDER-033
  area: RENDER
  statement: "A render pixel's whole sample bundle (nominal sample, E ensemble copies and every Benettin shadow) must share one checkerboard phase; the mask must be per pixel, never per trajectory."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 1. What this is (and what it is NOT)
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  verify:
    method: unit test
    detail: "within any pixel all bundle members carry the same t after every frame"
  rulings: []
  milestone: M4
- id: REQ-RENDER-034
  area: RENDER
  statement: "A checkerboard-stale pixel must be reconstructed only from its own previous value (temporal reuse), never spatially interpolated from neighbours."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "2. The mechanism: reconstruct-from-previous, resolved through SSAA"
  verify:
    method: review checklist
    detail: "the reconstruction shader reads only the pixel's own previous colour/state"
  rulings: []
  milestone: M4
- id: REQ-RENDER-035
  area: RENDER
  statement: "Checkerboard must run at every tier, including E = 0, with SSAA a quality multiplier rather than a gate."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "2. The mechanism: reconstruct-from-previous, resolved through SSAA"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "E = 0 with checkerboard on: the mask is applied"
  rulings:
  - R-113
  milestone: M4
  note: "R-113 (RQ-98): that Potato is the E = 0 tier is checked by REQ-PERF-014 (M5)."
- id: REQ-RENDER-036
  area: RENDER
  statement: "When the playhead stops (rest, pause, t_end / all-terminated), the stale half must be marched one dt and promoted at the next barrier so the frame is full; frames at t = 0 and every quiescent moment must be complete, and no frame may be served checkerboarded except mid-advance."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "5. Self-erasing: catch-up at rest, endpoints, and pause"
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "6. The single-playhead invariant, relaxed (the one architectural concession)"
  verify:
    method: unit test
    detail: "pause mid-playback: the next presented frame has every pixel at t_now; t = 0 and t_end frames have no stale pixels"
  rulings: []
  milestone: M4
- id: REQ-RENDER-037
  area: RENDER
  statement: "On an identity change the previous content must stay on screen as a blurred backdrop — the live stale layer rendered from cached frozen states at the current render config (re-coloured, not animated) — until new-identity quads replace it, falling back to a blurred snapshot only when no payloads exist."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  verify:
    method: golden image
    detail: "tilt gesture fixture: frames show blurred old content under fresh quads; palette change recolours the backdrop"
  rulings: []
  milestone: M5
- id: REQ-RENDER-038
  area: RENDER
  statement: "Blur must be a separable compositor pass on the stale layer's texture above the stain pipeline, and staleness must be marked by blur only (no desaturation or tint)."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: golden image
    detail: backdrop chroma equals the unblurred render's chroma
  rulings:
  - R-64
  milestone: M5
- id: REQ-RENDER-039
  area: RENDER
  statement: "The backdrop reference must update only on an at-rest refined non-preview baseline, with exactly two identities pinned (current + backdrop) swapped atomically."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  verify:
    method: property test
    detail: a long drag never promotes a coarse cover to backdrop
  rulings: []
  milestone: M5
- id: REQ-RENDER-040
  area: RENDER
  statement: "Pipelines must be created with the async creation APIs and the finite variant set precompiled at startup, keeping the last valid pipeline while a new one compiles."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  verify:
    method: review checklist
    detail: no synchronous pipeline creation on the gesture path
  rulings: []
  milestone: M5
- id: REQ-RENDER-041
  area: RENDER
  statement: "The last-coherent-frame snapshot must be a GPU-side texture copy, never a readback and re-upload."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  verify:
    method: review checklist
    detail: snapshot path uses copyTextureToTexture
  rulings: []
  milestone: M5
- id: REQ-RENDER-042
  area: RENDER
  statement: "render_scale (internal render resolution ÷ display, 0.25–2.0) must be a render-side, non-sim-key knob that invalidates nothing."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: The two keys as ladder geometry
  verify:
    method: unit test
    detail: "changing render_scale across 0.25–2.0 leaves the sim key, cache and sim buffers unchanged"
  rulings: []
  milestone: M5
- id: REQ-RENDER-043
  area: RENDER
  statement: "SSAA must use the footprint's E+1 samples (copy 0 at the centre, copies 1..E at centred Halton (2,3) points 1..E) as both the SSAA pool and the spread pool, with spread derived at resolve and never stored, and Benettin shadows never in either pool."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Stability metrics are just running accumulators (they get simpler)"
  - file: docs/design/principia_temporal_architecture_note.md
    section: Still open (settle at implementation, or next edit pass)
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  verify:
    method: unit test
    detail: "copy offsets equal centred Halton (2,3) points 1..E with copy 0 at the centre; no stored spread field; shadows excluded from resolve"
  rulings:
  - R-80
  milestone: M5
- id: REQ-RENDER-044
  area: RENDER
  statement: "Blur must be the single signal for 'not current' (spatially stale, temporally behind the playhead, or still arriving/being refined), and sharp must mean live, in time and true."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_temporal_architecture_note.md
    section: Staleness semantics strengthen (a free win)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  verify:
    method: golden image
    detail: "scene with stale, catching-up and live regions: only live regions render sharp"
  rulings: []
  milestone: M5
- id: REQ-RENDER-045
  area: RENDER
  statement: "The display must never blank, never lie and never freeze: baseline-first is a hard tier and a blurred live backdrop (moving fallback) covers what is not ready, so the compositor always has something legal to draw."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "Refinement must be a live-to-live handoff (or it looks broken)"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: "during scripted pan/zoom/refine every screen pixel is covered by a layer every frame and the main thread never blocks"
  rulings:
  - R-113
  milestone: M5
  note: "R-113 (RQ-99 M5-5): 'the frame loop runs in the wasm-engine worker' is REQ-SYS-039/049's (M8)."
- id: REQ-RENDER-046
  area: RENDER
  statement: "Checkerboard must apply to the live-set march only; catch-up marches must never be checkerboarded."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 3. The three-state control
  verify:
    method: unit test
    detail: "catching-up quads advance every sample every step with checkerboard on"
  rulings: []
  milestone: M5
- id: REQ-RENDER-047
  area: RENDER
  statement: "Checkerboard must apply at render resolution before the render→display upscale and compose independently with render_scale."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 4. Composition with render-scale (they stack; upscale helps)
  verify:
    method: unit test
    detail: "with render_scale 0.5 the mask is over render pixels; both levers on at once work"
  rulings: []
  milestone: M5
- id: REQ-RENDER-048
  area: RENDER
  statement: "Each sample must be coloured independently through the full pipeline and a render-side resolve pass must average the sample colours into the pixel colour; no data value may ever be averaged before colouring."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The core move: colour per sample, average last"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_temporal_architecture_note.md
    section: Still open (settle at implementation, or next edit pass)
  verify:
    method: golden image
    detail: "dd_colouring unit test 8: a synthetic quad of 75% escape / 25% bounded samples renders the 75/25 blend of the two class colours, never a colour from averaged class indices"
  rulings: []
  milestone: M5
- id: REQ-RENDER-049
  area: RENDER
  statement: "The SSAA resolve must be display-only and terminal: the averaged pixel colour must never be read back as data, and quad outcome statistics must come from the data-side reduction of the copies' classified outcomes."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Ensemble copies ARE the SSAA samples
  - file: docs/notes/principia_sampling_msaa_note.md
    section: The two firewalls (unchanged, restated for this model)
  verify:
    method: review checklist
    detail: "no buffer written by the resolve pass is bound as input to any compute or reduction pass"
  rulings: []
  milestone: M5
- id: REQ-RENDER-050
  area: RENDER
  statement: "There must be no sample↔pixel interpolation: one sample rasterises to one tile, sharpness must come only from subdividing quads, and the only sample→pixel combining must be the SSAA ensemble resolve (plus the transient blurred-ancestor loading state)."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: docs/design/principia_memory_tiers.md
    section: "2. Two resolutions — display and internal-render (sample density is not a separate knob)"
  - file: docs/design/principia_memory_tiers.md
    section: 1. Taxonomy (locked vocabulary)
  - file: decisions.md
    section: R-88 — What stops in-view refinement *(closes RQ-39)*
  verify:
    method: golden image
    detail: "a quad above the screen floor renders as flat per-sample blocks (no interpolation); no TILE_PIXEL_RES / spp knob exists"
  rulings:
  - R-88
  milestone: M5
  note: "R-88: below the screen floor the criterion may supersample by further subdivision (capped by MAX_REL_DEPTH); that sharpness still comes from subdividing quads."
- id: REQ-RENDER-051
  area: RENDER
  statement: "Ensemble copies must feed two separate per-footprint reductions at the resolve stage: colours resolve terminally on the render side (never read as data), and outcomes reduce to the footprint spread, derived at resolve, aggregated into QuadReduction (ensemble_spread) and never stored per sample in SimState."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 9 — Ensemble / SSAA sampling (dispatch rules)
  verify:
    method: review checklist
    detail: "SimState has no spread field; the data-side spread path reads classified outcomes, not colours; QuadReduction carries ensemble_spread"
  rulings: []
  milestone: M5
- id: REQ-RENDER-052
  area: RENDER
  statement: "Internal render resolution must be render_px = display_px × render_scale², with render_scale a tier knob in {0.25, 0.5, 0.75, 1.0} and a Custom slider over 0.25–2.0, where values above 1.0 are Custom-only and used by no tier; there must be no separate samples-per-pixel knob."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "2. Two resolutions — display and internal-render (sample density is not a separate knob)"
  - file: docs/design/principia_memory_tiers.md
    section: What `render_scale` gets you (and its character)
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  verify:
    method: unit test
    detail: "tier presets only produce the four values; Custom accepts 0.25–2.0; render target size equals display × render_scale"
  rulings: []
  milestone: M5
- id: REQ-RENDER-053
  area: RENDER
  statement: "The same E+1 samples of a footprint must feed both the SSAA colour resolve (averaging E+1 colours) and the data-side spread metric, stored as spread_event (f16) in QuadReduction with any agreement value derived on the fly."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Ensemble copies ARE the SSAA samples
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: decisions.md
    section: "R-18 — The agreement value is `spread_event` *(closes RQ-16)*"
  - file: decisions.md
    section: R-80 — Samples per footprint *(closes RQ-31)*
  verify:
    method: unit test
    detail: "resolve and spread reductions iterate the same sample indices; QuadReduction holds spread_event f16 and no agreement field"
  rulings:
  - R-18
  - R-80
  milestone: M5
- id: REQ-RENDER-054
  area: RENDER
  statement: "FTLE must anti-alias as a point quantity computed per copy, while ensemble spread must be one value per nominal sample shared by its copies for display (no sub-footprint AA)."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The point-quantity vs footprint-quantity distinction (intrinsic, not an optimisation)"
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  verify:
    method: golden image
    detail: "FTLE colouring shows blended edges; spread colouring is constant across a footprint's copies"
  rulings: []
  milestone: M5
- id: REQ-RENDER-055
  area: RENDER
  statement: "Tier 3 tile debug shaders must be fed by a small CPU-packed per-visible-quad buffer read in the postprocess stage, share the compilation pipeline and prelude with the sim shaders, and cost nothing when off."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  verify:
    method: benchmark
    detail: "with every Tier 3 overlay off, no buffer upload or extra pass occurs (frame time equals the no-overlay baseline); on, the buffer is uploaded only while active"
  rulings: []
  milestone: M5
- id: REQ-RENDER-056
  area: RENDER
  statement: "Overlays must never move the per-sample SimState array across the GPU→CPU membrane; only per-visible-quad metadata may cross, and only while the overlay is on."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  verify:
    method: review checklist
    detail: no overlay code path reads back SimState buffers
  rulings: []
  milestone: M5
- id: REQ-RENDER-057
  area: RENDER
  statement: "A custom per-node WGSL occupant must use schema-driven uniforms, compile asynchronously, fall back to the last valid pipeline on error with per-node failure isolation, and show a compile-status indicator and an error surface."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: unit test
    detail: "invalid WGSL in one node: the previous pipeline keeps rendering, the error appears in Problems, other nodes are unaffected"
  rulings: []
  milestone: M7
- id: REQ-RENDER-058
  area: RENDER
  statement: "Colour occupants that are pure f(n̂) must be bakeable into an equirect texture keyed by the colour-node source and its uniforms, rebaked debounced (~120 ms), with the GUI preview canvas being the uploaded texture itself; the physics overlay is not one of them — it is a per-fragment occupant."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  - file: decisions.md
    section: R-121 — The physics overlay isn't baked *(closes RQ-89)*
  verify:
    method: review checklist
    detail: "bake key = hash(colour source, colour uniforms); preview widget samples the same GPU texture the render uses; the physics overlay is not on the bake path (R-121)"
  rulings:
  - R-64
  - R-121
  milestone: M7
  note: "The bake is an implementation strategy only; the occupant remains colour(ctx) publicly. R-121: render_contract Parts 3 and 4 are conformed (no physics blobs in the bake)."
- id: REQ-RENDER-059
  area: RENDER
  statement: "Occupants that read dynamical fields (event class, diffusion, etc.) must skip the bake tier and evaluate per fragment."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  verify:
    method: review checklist
    detail: "the bake path is only enabled for occupants whose inputs are n̂ and uniforms"
  rulings: []
  milestone: M7
- id: REQ-RENDER-060
  area: RENDER
  statement: "The frame tier must be keyed by a render key hashing the stain graph (nodes, their sources, wires, per-node params), uniform values and the overlay set."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  verify:
    method: unit test
    detail: "changing any of these changes the render key and re-composites; changing none reuses the frame"
  rulings:
  - R-64
  milestone: M7
  note: "The canonical graph form: REQ-RENDER-075 (definition)."
- id: REQ-RENDER-061
  area: RENDER
  statement: "The majority-class plus entropy-desaturation rule (C' = C·(1 − H/ln k)) must not be implemented; uncertainty marking, if offered, must be an optional independent post occupant reading the exposed spread/entropy field."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/design/principia_dd_colouring.md
    section: 6. Deferred / flagged
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "The core move: colour per sample, average last"
  verify:
    method: review checklist
    detail: "no class-colour code path references entropy; the fuzziness overlay is a removable post node"
  rulings: []
  milestone: M7
- id: REQ-RENDER-062
  area: RENDER
  statement: "sph_uv must take the config-space shape normal; view orbit must be display-only and never change which UV a physics point maps to; pattern auto-rotate must be a UV offset inside the colour function."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_colouring.md
    section: 3.3 Sphere sampling conventions
  verify:
    method: property test
    detail: "rotating the view orbit leaves the UV of each physics landmark unchanged; auto-rotate changes colour, not the sampled point"
  rulings: []
  milestone: M7
- id: REQ-RENDER-063
  area: RENDER
  statement: "The display chain must run, after the stain, in the order style → render→display scale → gamut clamp → colour-vision simulation → screen, with all stages before the display scale running at render resolution and the scale step doing bilinear upscale below native and box downsample above."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_colouring.md
    section: 3.8 Palettes and CVD
  - file: docs/design/principia_colour_composition.md
    section: 4.3 Display stage (terminal, outside the pipeline)
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  verify:
    method: unit test
    detail: "with render_scale ≠ 1 and a CVD mode set, verify the CVD matrix is applied to the post-scale, post-clamp buffer; scale step is a no-op at native"
  rulings:
  - R-67
  milestone: M7
- id: REQ-RENDER-064
  area: RENDER
  statement: "Gradient-magnitude-on-the-map must be a post occupant computing a screen-space finite difference over the colour buffer, distinct from the baked |∇c| on the sphere texture."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  verify:
    method: review checklist
    detail: "a post node exists for map gradient magnitude; the sphere gradient-magnitude preset is a FieldRamp over the colour node"
  rulings: []
  milestone: M7
- id: REQ-RENDER-065
  area: RENDER
  statement: "The sphere widget must project with s_x = (p_x−c_x)/R, s_y = −(p_y−c_y)/R, s_z = √max(0,1−s_x²−s_y²), R = W/2 − 4, and the equirect mapping must be (θ, φ) ∈ [0,2π]×[0,π] (R-14): θ the azimuth in the (u, v) plane on the horizontal axis, φ the polar angle from +w on the vertical axis, L⁺ (w = +1) at the top; `sph_uv` input is always the config-space normal."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.3 Sphere sampling conventions
  - file: docs/design/principia_dd_colouring.md
    section: 6. Deferred / flagged
  verify:
    method: unit test
    detail: "widget projection round-trips a pixel grid; equirect u is linear in θ and v linear in φ, with w = +1 on the top row and w = −1 on the bottom row; sph_uv is called with the config-space normal"
  rulings:
  - R-14
  - R-73
  milestone: M7
- id: REQ-RENDER-066
  area: RENDER
  statement: "Per-layer CVD must equal post-composite CVD for opaque quads (occupants emit layer content; blur and final CVD are compositor business)."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: property test
    detail: seam 7 linearity check on random opaque layers
  rulings: []
  milestone: M7
- id: REQ-RENDER-067
  area: RENDER
  statement: "Baked and directly-evaluated colour for every pure-f(n̂) occupant must agree within texture quantisation over a sphere lattice."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  verify:
    method: golden image
    detail: "dd_colouring unit test 11: texture-sampled vs direct evaluation over a sphere lattice, max difference ≤ one texel quantisation step"
  rulings: []
  milestone: M7
  note: "The tolerance is one texel quantisation step; the other colour tolerances: REQ-COL-049, REQ-COL-050, REQ-COL-051 (calibration)."
- id: REQ-RENDER-068
  area: RENDER
  statement: "The colour stage must colour the payload at the current playhead with occupants as data, never structure, under L-ownership; the stain is a typed node graph (R-64), of which the fixed post(combine(colour, brightness)) four-slot pipeline is one instance."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 0. The ladder — the organising abstraction
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: The pixel's life = one ladder traversal
  verify:
    method: review checklist
    detail: "colour occupants cannot alter the pipeline topology outside the stain graph; L is owned by the bound brightness metric"
  rulings:
  - R-64
  milestone: M7
- id: REQ-RENDER-069
  area: RENDER
  statement: "The display chain must be stain → style → display scale → gamut clamp → colour-vision simulation → screen, with the compositor (blurred backdrop, fresh cover, trace/overlays) above the colour stage and blur a compositor pass."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: The pixel's life = one ladder traversal
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  verify:
    method: golden image
    detail: "CVD simulation is applied to the final in-gamut, display-scaled colours; pass order asserted"
  rulings:
  - R-67
  milestone: M7
- id: REQ-RENDER-070
  area: RENDER
  statement: "Render config (slot uniforms, playhead t) must never touch sim buffers, and the bake texture must be chart- and IC-independent."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: decisions.md
    section: R-121 — The physics overlay isn't baked *(closes RQ-89)*
  verify:
    method: unit test
    detail: "render-config updates issue no writes to sim buffers; the baked texture is identical across charts and ICs; the physics overlay is evaluated per fragment and never enters the bake (R-121)"
  rulings:
  - R-121
  milestone: M7
- id: REQ-RENDER-071
  area: RENDER
  statement: "The stain pipeline must be source(s) → optional colour and optional brightness (None = identity) → a required combiner (Replace-L / Multiply) → a possibly-empty ordered chain of vec3 → vec3 post nodes → OUT, and codegen must walk the graph to produce the fragment shader."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 2. The pipeline
  - file: docs/gui/principia_render_gui_spec.md
    section: 4. The backbone — fixed vs free
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: unit test
    detail: "graphs with each of colour / brightness absent and with 0, 1, 3 posts compile to a fragment shader whose shade() follows the wiring"
  rulings:
  - R-64
  milestone: M7
- id: REQ-RENDER-072
  area: RENDER
  statement: "The stain graph must be evaluated on every change and always be renderable, a genuinely invalid graph showing a defined fallback (flat grey / error tint) and never crashing."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 13. Invariants
  verify:
    method: property test
    detail: "fuzz random graph edit sequences: every state renders; forced-invalid graphs render the fallback"
  rulings: []
  milestone: M7
- id: REQ-RENDER-073
  area: RENDER
  statement: "The hover trace must be an overlay layer above the stain pipeline, and no trace may be drawn where there is no current-identity payload under the cursor (stale/blurred region)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 7 — The hover trace (per-IC trajectory overlay)
  verify:
    method: GUI screenshot
    detail: "hover over a stale backdrop region: no trace; over a current region: trace drawn above the composite"
  rulings: []
  milestone: M8
- id: REQ-RENDER-074
  area: RENDER
  statement: "Each overlay must be implemented in the tier Part II §12.1's sorting rule assigns it: a post node if computable from the address alone (Tier 1), a sim shader if already GPU-resident (Tier 2), otherwise a tile debug shader fed by a scheduler-verdict buffer (Tier 3)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  verify:
    method: review checklist
    detail: "a table of every Overlays-menu and Display-window overlay with its tier and the §12.1 question that placed it; leaf/internal is not offered"
  rulings: []
  milestone: M8
- id: REQ-RENDER-075
  area: RENDER
  kind: definition
  statement: "The stain graph's canonical form (the canonical node order, wiring and post-chain encoding the fragment key and the render key hash) must be defined in lowering_contract Part 5 and render_contract Part 3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the canonical form; two wirings of the same graph hash equal and different graphs hash differently"
  rulings:
  - R-64
  - R-72
  milestone: M1
- id: REQ-RENDER-076
  area: RENDER
  kind: definition
  statement: "The units of checkerboard's per-frame playhead-advance ceiling (the ~0.05 figure; the value itself is measured, §8) must be defined in checkerboard_contract §7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 7. Measured imperceptibility (the evidence for the default)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the ceiling's units (sim time per frame, dt, or other) so the §8 measurement records a value in them"
  rulings:
  - R-72
  milestone: M5
- id: REQ-RENDER-077
  area: RENDER
  kind: definition
  statement: "The exact values of the tier-absent sentinels — the canonical quiet-NaN bit pattern that a derived field reads and that the bitcast absence test compares against, and the word an unbound word buffer returns (empty, length 0, or the length_raw 127 sentinel) — must be defined in lowering_contract Part 3a by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 3a — The uniform read-side interface (tier features degrade by NaN, not by struct shape)"
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Part 3a gives the quiet-NaN bits and the unbound-word value; REQ-RENDER-013's unit test asserts exactly those; physics reviewer approved"
  rulings:
  - R-72
  - R-79
  milestone: M1
- id: REQ-RENDER-078
  area: RENDER
  kind: definition
  statement: "Spread-extrapolation's `rate` — which value is extrapolated (a payload field or the resolved colour), which integration by-product supplies its local rate of change, and where the rate is 'cheaply available' — must be defined in checkerboard_contract §2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "2. The mechanism: reconstruct-from-previous, resolved through SSAA"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§2 names the extrapolated value, the rate's source and the gate; REQ-VAL-058's measured error reduction uses it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M4
- id: REQ-RENDER-079
  area: RENDER
  kind: calibration
  statement: "The stale backdrop's separable blur radius (kernel width, in render pixels) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the radius with evidence: captures of a tilt drag showing the backdrop reads as stale beside the fresh coarse cover, and the two blur passes' cost; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M5
- id: REQ-RENDER-080
  area: RENDER
  kind: calibration
  statement: "The equirect bake texture's resolution and texel format (which set the 'one texel quantisation step' of the bake-equivalence test) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the texture's width × height and texel format with the measured bake-vs-direct colour difference over a sphere lattice and the bake time; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M7
- id: REQ-RENDER-081
  area: RENDER
  kind: calibration
  statement: "The invalid-graph fallback (flat grey or error tint) and its sRGB value must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 13. Invariants
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  verify:
    method: review checklist
    detail: "the proposal names the fallback and its sRGB value, shows it is distinct from the both-None flat mid-grey and from the invalid-pixel pattern (REQ-COL-055, R-132); recorded in decisions.md"
  rulings:
  - R-71
  - R-132
  milestone: M7
- id: REQ-RENDER-082
  area: RENDER
  statement: "The Burrau int lattice and the Anosova physical-frame chart must decode through the DECODE fragment preset without special-casing."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Appendix — worked enumeration of the current chart set
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: golden image
    detail: "render the DECODE preset for each of the two charts and compare with a CPU-decoded reference image"
  rulings:
  - R-113
  milestone: M4
  note: Split from REQ-RENDER-027 (R-113, RQ-95).
- id: REQ-COL-001
  area: COL
  statement: "Every ScalarField must return (value, valid) and every Ramp and Compaction must carry an explicit invalid-pixel colour/value, defaulting to the invalid pattern REQ-COL-055 sets (a hatch colliding with no palette entry, R-132) and overridable per node, to which every colouring must map NaN or a sentinel — except debug fields, which must show literal stored values while NaN still maps to the invalid pattern."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.2 Family B — field-ramp  →  `vec3` or `f32`
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  - file: docs/gui/principia_render_gui_spec.md
    section: 13. Invariants
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  verify:
    method: golden image
    detail: "a field with invalid pixels (NaN and sentinel) renders them in the invalid pattern; overriding the node's invalid colour changes only those pixels; a debug field view shows a failed-state 0.0 as the literal 0.0 and a NaN as the invalid pattern"
  rulings:
  - R-16
  - R-79
  - R-132
  milestone: M1
  note: "R-132: the default is REQ-COL-055's hatched pattern, replacing the corpus's 'fixed magenta'."
- id: REQ-COL-002
  area: COL
  statement: "The outcome-state field's canonical default palette must be the nine-class table of colour_composition §1.4 (collision pair 2/1/0 → #DE2D2D/#2EBC4E/#3462E0, escape body 0/1/2 → #F0DE32/#E034C6/#30C8DC, bounded #141418, collision @ t=0 #F29620, degenerate #ECECF0), read from state plus the detail union with 'degenerate' = decode_failed and 'collision @ t=0' = a collision with t_end_step == 0, with running shown in neutral grey and sim_failed in the invalid colour, and must be user-editable."
  source:
  - file: docs/design/principia_colour_composition.md
    section: "1.4 Categorical colour-assignment — the outcome-state default palette"
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  - file: decisions.md
    section: "R-68 — Artboard values are illustrative; corpus values win *(closes RQ-24)*"
  - file: docs/gui/principia_render_gui_spec.md
    section: "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  - file: decisions.md
    section: R-77 — Replace-L, and the state palette *(closes RQ-28)*
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: golden image
    detail: "synthetic payload with one sample per class (a decode_failed sample, a collision with t_end_step == 0, a collision with t_end_step > 0 per pair, …) renders exactly these sRGB values; a running sample renders the neutral grey and a sim_failed sample the invalid colour; editing a swatch changes only that class"
  rulings:
  - R-22
  - R-68
  - R-77
  - R-96
  milestone: M1
  note: "Corpus hex codes win over artboard values (R-68). The neutral grey's value: REQ-COL-053 (calibration)."
- id: REQ-COL-003
  area: COL
  statement: "ctx must expose the screen, chart, quad, tile/sample, payload and validity lanes listed in colour_composition §3, with a validity lane paired with every field."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  verify:
    method: unit test
    detail: "generated ctx WGSL declares every listed lane; each payload field has a validity accessor"
  rulings: []
  milestone: M1
- id: REQ-COL-004
  area: COL
  statement: "The detail union's legend and palette segment must switch on state (escape → body id; collision → pair id)."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  verify:
    method: unit test
    detail: "dd_colouring unit test 8: the detail legend switches per state (three-colours-bug regression)"
  rulings:
  - R-22
  milestone: M1
- id: REQ-COL-005
  area: COL
  statement: "Colour occupants must read payload fields only through the generated accessors and ledger metadata, and no colour equation may reference chart, integrator or scheduler state."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: review checklist
    detail: "seam 5/6: grep colour WGSL for non-accessor payload reads and for chart/integrator/scheduler identifiers: none"
  rulings: []
  milestone: M1
- id: REQ-COL-006
  area: COL
  statement: "The chart decode and encode must be available to the fragment as WGSL generated from the one Rust source (R-116), so fragment presets can recompute decode(z) and encode(decode(z)) from ctx.chart.z, including agreement presets that check the translation by comparing fragment decode against the kernel payload."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: decisions.md
    section: "R-116 — The fragment decode and encode are generated from the one source *(closes RQ-84)*"
  - file: decisions.md
    section: "R-133 — The seven checkpoint-B interpretations are accepted *(closes RQ-111)*"
  verify:
    method: numerical gate
    detail: "agreement preset |E(fragment-decode) − E₀| is within REQ-DEC-043's calibrated f32 decode factor on a healthy survey, with E₀ the decode stage's K₀ + V₀ (R-86), not SimState.E_0 (R-133); a deliberate dispatch scramble shows spatial disagreement"
  rulings:
  - R-75
  - R-116
  - R-133
  milestone: M2
  note: "R-133: 'f32 noise' is read as REQ-DEC-043's calibrated f32 decode factor until REQ-VAL-064 sets Tier N (M4)."
- id: REQ-COL-007
  area: COL
  statement: "A category must never be interpolated: mixed categorical pixels must be resolved by per-sample colour then SSAA resolve, never by averaging state indices or by majority + desaturation."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  - file: docs/contracts/principia_gui_state_contract.md
    section: 8. Amendment to the colouring drill-down
  verify:
    method: golden image
    detail: "a pixel straddling states 1 and 3 renders the average of their colours, never state 2's colour"
  rulings: []
  milestone: M5
- id: REQ-COL-008
  area: COL
  statement: "A quantity displayed for quantitative reading must carry its Jacobian (measure honesty), and a bound brightness metric must own L via the combiner."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  - file: docs/contracts/principia_gui_state_contract.md
    section: 8. Amendment to the colouring drill-down
  verify:
    method: review checklist
    detail: "quantitative presets document their measure; the combiner gives L to a bound brightness"
  rulings: []
  milestone: M7
- id: REQ-COL-009
  area: COL
  statement: "When a brightness occupant is bound, L must belong to brightness and the colour occupant must contribute hue and chroma only."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  verify:
    method: unit test
    detail: "Replace-L output's OKLab L equals the brightness-derived L regardless of the colour occupant"
  rulings: []
  milestone: M7
- id: REQ-COL-010
  area: COL
  statement: "Everything in the colour subsystem except the Appendix A bring-up mode (the kernel's one debug mode) must be render-key — including the UV, DECODE and ROUNDTRIP views, which are fragment presets — so editing it must never invalidate the survey cache or re-integrate."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 0. Scope & membrane position
  - file: docs/design/principia_colour_composition.md
    section: Appendix A — kernel-side bring-up mode
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  verify:
    method: property test
    detail: "covered by the render-freedom test: edits to every colour node, and selecting the UV / DECODE / ROUNDTRIP presets, leave the sim key unchanged and issue no dispatch"
  rulings:
  - R-75
  milestone: M7
- id: REQ-COL-011
  area: COL
  statement: "A colour occupant must be an expression tree over the two primitive families (site-blend, field-ramp) and the combinators, where each node outputs vec3 or f32 and only the root is constrained by the slot's signature."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1. Primitive algebra
  verify:
    method: unit test
    detail: "a tree with an f32 node feeding a vec3 root type-checks; an f32 root in the colour slot fails type-checking"
  rulings: []
  milestone: M7
- id: REQ-COL-012
  area: COL
  statement: "SiteBlend must support kernels vmf(κ) with w_i = exp(κ(d_i − d_max)), topk(k, ks) as a softmax over the k largest d_i with sharpness ks, and nearest as a discrete argmax code path (never the κ→∞ limit of exp)."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.1 Family A — site-blend  →  `vec3`
  verify:
    method: unit test
    detail: "vmf weights shift-invariant under d_max subtraction; topk(1,·) equals nearest; nearest never evaluates exp"
  rulings: []
  milestone: M7
- id: REQ-COL-013
  area: COL
  statement: "BlendSpace must be an explicit SiteBlend parameter with oklab (blend a,b at the site colours' L,C) and rgb options."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.1 Family A — site-blend  →  `vec3`
  verify:
    method: unit test
    detail: "same sites/weights give different outputs for oklab vs rgb; oklab midpoint chroma is lower than endpoints"
  rulings: []
  milestone: M7
- id: REQ-COL-014
  area: COL
  statement: "A site's colour must always be a swatch (any OKLab colour), with the Full-OKLab and Okabe–Ito hue tables provided as preset swatch-sets and colours obtainable from palette generators (golden-angle, OI-cycle, gradient A→B) or by sampling a LUT at i/N."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.1 Family A — site-blend  →  `vec3`
  verify:
    method: unit test
    detail: "the FullOKLAB and OI swatch-sets reproduce dd_colouring §3.2's hue angles"
  rulings: []
  milestone: M7
- id: REQ-COL-015
  area: COL
  statement: "FieldRamp must map a ScalarField (payload fields, geometry-of-n̂ fields, ctx lanes and the derived operators gradient_magnitude via three evaluations at ±ε and topk_margin = d_1 − d_2) through a Ramp (lut, lerp, diverging, bands) or a Compaction (lin, log, symlog, cyclic, flag)."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.2 Family B — field-ramp  →  `vec3` or `f32`
  verify:
    method: unit test
    detail: "each source and ramp/compaction kind is constructible and compiles; gradient_magnitude of a constant map is 0"
  rulings: []
  milestone: M7
- id: REQ-COL-016
  area: COL
  statement: "Default ramps must follow field role: signed fields diverging through neutral, positive fields sequential, angles cyclic, and magnitude/diagnostic fields greyscale, with polarity white = high for FTLE, diffusion and ensemble spread and white = low/early for t_end."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.2 Family B — field-ramp  →  `vec3` or `f32`
  - file: docs/design/principia_colour_composition.md
    section: 4.1 Backbone & `Option` occupants
  verify:
    method: unit test
    detail: "the default ramp registry returns the stated ramp and polarity per field role"
  rulings: []
  milestone: M7
- id: REQ-COL-017
  area: COL
  statement: "The combinators mix_const, mix_field, bandmask and site_overlay must exist as vec3(+ctx) → vec3 operations, and the physics overlay must be site_overlay over a SiteBlend with physics sites, vmf(κ) kernel, per-site colours and per-blob strength s."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.3 Combinators  →  `vec3`
  verify:
    method: unit test
    detail: "each combinator compiles; physics overlay preset expands to site_overlay(base, SiteBlend{physics(m), vmf, per-site}, s)"
  rulings: []
  milestone: M7
- id: REQ-COL-018
  area: COL
  statement: "Categorical fields other than the outcome state must map through a palette swatch-set generated by golden-angle for many-class fields (word/basin) and an Okabe–Ito cycle for small class counts, while the outcome state must take colour_composition §1.4's nine-class canonical palette."
  source:
  - file: docs/design/principia_colour_composition.md
    section: "1.4 Categorical colour-assignment — the outcome-state default palette"
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: decisions.md
    section: R-77 — Replace-L, and the state palette *(closes RQ-28)*
  verify:
    method: unit test
    detail: "palette generator for n ≤ 8 returns OI colours; for larger n golden-angle hues θ_i = 2π·frac(i·φ_g); the state field resolves to the §1.4 palette, not to a generated index palette"
  rulings:
  - R-77
  milestone: M7
- id: REQ-COL-019
  area: COL
  statement: "Every categorical mode must admit a categorical filter (show class ∈ {…}, mute the rest), and there must be no separate escaper field or escaper render mode."
  source:
  - file: docs/design/principia_colour_composition.md
    section: "1.4 Categorical colour-assignment — the outcome-state default palette"
  verify:
    method: unit test
    detail: "filter the state map to escape classes; non-escape pixels are muted"
  rulings: []
  milestone: M7
- id: REQ-COL-020
  area: COL
  statement: "The static site generators axes6, corner8, ico12, fib(N) and ring(N, tilt, rot) must be computed once and bound as uniform arrays."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 2. Site-set kinds
  verify:
    method: unit test
    detail: "generator outputs are unit vectors matching §7.1's formulas; they are bound as uniforms"
  rulings: []
  milestone: M7
- id: REQ-COL-021
  area: COL
  statement: "Physics site generators (BC, Euler, Lagrange) must be computed from the decoded IC masses per pixel on the mass-weighted shape sphere, with the Euler landmarks the Euler central configurations (the collinear relative equilibria: roots of Euler's quintic in the mass ratios) mapped through the shape map, transcribed with citation, and hoisted to uniforms only when no basis axis or active tilt touches a mass dimension — an optimisation, never a bake tier."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 2. Site-set kinds
  - file: docs/design/principia_dd_colouring.md
    section: "3.4 Physics overlay (blob blend) and the house encoding (stability × hue)"
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: "R-50 — The shape-sphere collision landmarks are mass-weighted *(CO-1 (a))*"
  - file: decisions.md
    section: "R-126 — The Euler landmarks are the Euler central configurations *(closes RQ-106)*"
  - file: decisions.md
    section: R-121 — The physics overlay isn't baked *(closes RQ-89)*
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  verify:
    method: unit test
    detail: "equal masses: b̂01 = (−1,0,0), b̂12 = (½,√3/2,0), b̂20 = (½,−√3/2,0), ê = −b̂, l̂± = (0,0,±1); unequal masses: collision landmarks per the mass-weighted map and Euler landmarks at the mapped roots of Euler's quintic (against the cited source's values); hoisting toggles with mass-dimension involvement and never produces a baked texture"
  rulings:
  - R-14
  - R-50
  - R-121
  - R-124
  - R-126
  milestone: M7
  note: "R-50 (applied, R-124) settles audit decision B18: landmarks are mass-weighted. R-126: Euler landmarks are the Euler central configurations (physics-reviewed, confirmed at the M7 gate). R-121: the hoist is an allowed optimisation, not a bake tier."
- id: REQ-COL-022
  area: COL
  statement: "colour and brightness must be Option<Occupant> on a fixed backbone: removing a node must set None and render it ghosted, never structurally delete it."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.1 Backbone & `Option` occupants
  - file: decisions.md
    section: R-52 — Undo lives in the state contract *(GU-1 (a))*
  verify:
    method: GUI screenshot
    detail: "remove the brightness node: it shows ghosted; restore restores; undo covers the SetField"
  rulings:
  - R-52
  milestone: M7
- id: REQ-COL-023
  area: COL
  statement: "combine must treat None as identity per colour_composition §4.1's truth table (C+None → C; None+B → greyscale OKLab(L=B,0,0) or white·B; None+None → flat mid-grey OKLab(0.6,0,0)), where Replace-L's L = B is the range form at its defaults L_min = 0, L_max = 1."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.1 Backbone & `Option` occupants
  - file: docs/gui/principia_render_gui_spec.md
    section: 13. Invariants
  - file: docs/gui/principia_render_gui_spec.md
    section: 2. The pipeline
  - file: decisions.md
    section: R-77 — Replace-L, and the state palette *(closes RQ-28)*
  verify:
    method: unit test
    detail: "each of the four combinations under Replace-L and Multiply produces the tabled result"
  rulings:
  - R-77
  milestone: M7
- id: REQ-COL-024
  area: COL
  statement: "Any field must be able to occupy either the colour or the brightness role, constrained only by output signature (vec3 vs f32)."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.1 Backbone & `Option` occupants
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  - file: docs/contracts/principia_gui_state_contract.md
    section: 8. Amendment to the colouring drill-down
  verify:
    method: unit test
    detail: "bind FTLE as brightness and as colour (via a ramp); both compile and render"
  rulings: []
  milestone: M7
- id: REQ-COL-025
  area: COL
  statement: "The post chain must be an ordered list of at most 8 post nodes, each vec3(+ctx) → vec3, composed in declaration order, supporting insert and reorder."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.2 Post chain
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: unit test
    detail: "a 9th post node is rejected; reordering two nodes changes output accordingly"
  rulings:
  - R-64
  milestone: M7
- id: REQ-COL-026
  area: COL
  statement: "The display stage (style, display scale, gamut clamp, CVD) must be settings outside every occupant and preset, never seen by the codegen, and CVD simulation must apply uniformly to everything displayed (main render, sphere preview, equirect unwrap, node thumbnails)."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.3 Display stage (terminal, outside the pipeline)
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  - file: docs/gui/principia_render_gui_spec.md
    section: 2. The pipeline
  - file: docs/gui/principia_render_gui_spec.md
    section: 12. Display stage — global, outside the pipeline
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  verify:
    method: GUI screenshot
    detail: "enable deutan: every surface including thumbnails is simulated; no preset serialises CVD"
  rulings:
  - R-67
  milestone: M7
- id: REQ-COL-027
  area: COL
  statement: "Style must apply to the figure only, and scientific checks must run with plain style."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.3 Display stage (terminal, outside the pipeline)
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  verify:
    method: review checklist
    detail: golden-image and validation harnesses force style = plain
  rulings:
  - R-67
  milestone: M7
- id: REQ-COL-028
  area: COL
  statement: "The debug views must be presets over the one colouring system (field views as FieldRamp presets, structural views as ctx.quad presets, quad boundary lines as a post-chain bandmask on distance-to-quad-edge), forming a section of the preset library."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  verify:
    method: review checklist
    detail: "no separate debug render path exists; quad boundaries can be overlaid on a normal preset"
  rulings: []
  milestone: M7
- id: REQ-COL-029
  area: COL
  statement: "Every currently-specified map in colour_composition §7.1 and every debug view must be recreated as a composition preset."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 7. Preset table & golden-image obligation
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  verify:
    method: review checklist
    detail: "every §7.1 entry and debug view maps to a preset in the library"
  rulings:
  - R-16
  milestone: M7
- id: REQ-COL-030
  area: COL
  statement: "Each §7.1 map must implement the definition given in its row (vMF, LUT spheres incl. analytic Cubehelix, Turbo, direction cosines, Octant, Voronoi 6, Hemispheres, Icosahedral, Soft Voronoi, Fibonacci and dot lattices, checkerboard, latitude/longitude stripes, Truchet, grid overlay ε=0.04, iso-hue contours, gradient magnitude, 4-octave value noise, checker+VMF, real spherical harmonics ℓ≤3, Turing standing waves, custom N-pole, basin blend, physics overlay κ=11 BC / 9 Euler-Lagrange), with LUT data from the published matplotlib tables (viridis, cividis, plasma, magma, inferno, twilight; Cubehelix analytic, R-151) Google's published Turbo table (Mikhailov 2019, Apache-2.0; R-139) for Turbo, and Moreland's table for cool-warm and the Principia palette's stops from the colour explorer, and Stability × Hue must not be among them."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: docs/design/principia_dd_colouring.md
    section: 3.2 The vMF engine
  - file: docs/design/principia_dd_colouring.md
    section: 3.8 Palettes and CVD
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  - file: decisions.md
    section: R-76 — Stability × Hue is deleted *(closes RQ-27)*
  - file: decisions.md
    section: "R-122 — The reference HTML files are the colour oracle *(closes RQ-90 and RQ-101)*"
  - file: decisions.md
    section: "R-139 — Turbo is Google's table; the §7.1 labels name the oracle files *(closes RQ-117)*"
  - file: decisions.md
    section: "R-151 — Cubehelix's reference is the analytic form *(closes RQ-121, amends R-122)*"
  verify:
    method: golden image
    detail: "each map vs its render in the reference HTML files (principia_colour_explorer.html, principia_colour_presets.html; R-122); formula-level unit tests for the closed forms; LUT samples match the published tables, Turbo's Google's (R-139); the preset library has no Stability × Hue entry"
  rulings:
  - R-14
  - R-16
  - R-50
  - R-76
  - R-122
  - R-139
  - R-151
  milestone: M7
- id: REQ-COL-031
  area: COL
  statement: "Map parameters must use the adopted ranges L ∈ [0.35,0.90], C ∈ [0.05,0.22], κ ∈ [0.5,12], f ∈ [2,14], N ∈ [12,96], ks ∈ [1,20], s ∈ [0,1]."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 8. Amendments to other docs
  - file: docs/design/principia_colour_composition.md
    section: 1.1 Family A — site-blend  →  `vec3`
  verify:
    method: unit test
    detail: the parameter schema clamps each to its range
  rulings: []
  milestone: M7
- id: REQ-COL-032
  area: COL
  statement: "Colour compaction must take each field's scale from the ledger metadata (lin | log | cyclic | diverging | categorical | flag), so a ledger scale change re-styles the view with zero recompute."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_colouring.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: unit test
    detail: "seam 5: change a field's ledger scale; the view re-styles and the dispatch counter is unchanged"
  rulings: []
  milestone: M7
- id: REQ-COL-033
  area: COL
  statement: "The sRGB transfer, linear-sRGB ↔ OKLab (M1, M2, inverse coefficients, M1⁻¹) and OKLCH polar forms must be exactly those of dd_colouring §3.1, transcription-checked against Ottosson's reference implementation before entering the shared source."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.1 Colour spaces (exact transforms — transcription-check against Ottosson before entering the shared source)"
  - file: docs/design/principia_dd_colouring.md
    section: 6. Deferred / flagged
  - file: decisions.md
    section: "R-51 — The OKLab coefficients are checked against Ottosson *(CO-2)*"
  - file: open-questions.md
    section: Audit section C — transcription checks
  verify:
    method: unit test
    detail: "dd_colouring unit test 1: round trips across a gamut lattice within tolerance; white → (1,0,0); primaries match Ottosson's published values"
  rulings:
  - R-51
  - R-71
  milestone: M7
  note: "The round-trip tolerance: REQ-COL-049 (calibration)."
- id: REQ-COL-034
  area: COL
  statement: "The vMF engine must compute a(n̂), b(n̂) as the exp(κ n̂·p̂_i)-weighted mean of C·(cos θ_i, sin θ_i) over the six axis poles with the Full-OKLab (0°,180°,120°,300°,240°,60°) and Okabe–Ito (250°,70°,30°,210°,170°,350°) hue tables, using no atan2 in the blend."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.2 The vMF engine
  verify:
    method: unit test
    detail: "dd_colouring unit test 3: opposing-pole midpoints give a = b = 0; large κ gives nearest-pole colour; blend(Rn̂, R·poles) = blend(n̂, poles)"
  rulings: []
  milestone: M7
- id: REQ-COL-035
  area: COL
  statement: "The seamless LUT sphere must place N_e = 16 LUT samples as equatorial poles (cos 2πi/N_e, sin 2πi/N_e, 0) with the LUT endpoints at the poles and blend with the vMF law in RGB."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.2 The vMF engine
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  verify:
    method: unit test
    detail: "dd_colouring unit test 4: an equator sweep reproduces the 1-D LUT within blend tolerance; Twilight closes exactly at the wrap"
  rulings:
  - R-71
  milestone: M7
  note: "The blend tolerance: REQ-COL-050 (calibration)."
- id: REQ-COL-036
  area: COL
  statement: "The physics overlay must blend sequentially over the sites in order (BC, then Euler, then Lagrange), c ← mix(c, c_j, min(1, w_j)), with w_j = s·4·max(0, exp(κ_j(n̂·p̂_j − 1)) + 0.005), κ = 11 for BC and 9 for Euler/Lagrange, as the colour explorer does."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.4 Physics overlay (blob blend) and the house encoding (stability × hue)"
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: decisions.md
    section: "R-122 — The reference HTML files are the colour oracle *(closes RQ-90 and RQ-101)*"
  verify:
    method: unit test
    detail: "dd_colouring unit test 5: blob maxima exactly at b̂/ê/l̂; s = 0 is the identity; the overlay matches principia_colour_explorer.html's physics op on fixture inputs (R-122)"
  rulings:
  - R-50
  - R-122
  milestone: M7
  note: "R-122: the explorer's blob weight and its sequential clamped mix win over dd_colouring §3.4's additive form (conformed)."
- id: REQ-COL-037
  area: COL
  statement: "Stability × Hue must set L = 0.25 + 0.55·½(1 − max_j n̂·b̂_j), giving L = 0.25 at a BC point and 0.80 antipodal."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.4 Physics overlay (blob blend) and the house encoding (stability × hue)"
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: docs/design/principia_colour_composition.md
    section: 7. Preset table & golden-image obligation
  verify:
    method: unit test
    detail: "dd_colouring unit test 5: L endpoints 0.25 / 0.80 exact"
  rulings:
  - R-14
  milestone: M7
  retired: "R-76: Stability × Hue is deleted"
- id: REQ-COL-038
  area: COL
  statement: "Replace-L must convert base RGB to OKLab, set L ← L_min + (L_max − L_min)·b with defaults L_min = 0 and L_max = 1 (so the default is L = b) leaving (a, b) bit-stable, and convert back; Multiply must be rgb·b in linear space."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.5 Combiners
  - file: docs/design/principia_colour_composition.md
    section: 4.1 Backbone & `Option` occupants
  - file: decisions.md
    section: R-77 — Replace-L, and the state palette *(closes RQ-28)*
  verify:
    method: unit test
    detail: "dd_colouring unit test 6: Replace-L leaves (a,b) bit-stable; at default L_min/L_max the output L equals b exactly; a non-default range maps b = 0/1 to L_min/L_max; Multiply preserves channel ratios"
  rulings:
  - R-77
  milestone: M7
- id: REQ-COL-039
  area: COL
  statement: "Compaction must implement lin, log (x ≤ 0 → 0 with sentinel styling), cyclic (frac(x/period)), diverging symlog b = ½ + ½·sign(x)·ln(1+|x|/x0)/ln(1+x_max/x0) with the ε floor as x0, and flag."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.6 Compaction (payload scalar → b ∈ [0,1]; forms per ledger `scale`)"
  - file: docs/design/principia_dd_colouring.md
    section: 6. Deferred / flagged
  - file: docs/design/principia_colour_composition.md
    section: 1.2 Family B — field-ramp  →  `vec3` or `f32`
  verify:
    method: unit test
    detail: "dd_colouring unit test 7: each form monotone on its domain; symlog b(x)+b(−x) = 1 and b(0) = ½ exactly; log styles the −1.0 sentinel instead of ramping it"
  rulings: []
  milestone: M7
- id: REQ-COL-040
  area: COL
  statement: "Golden-angle categorical palettes must use θ_i = 2π·frac(i·φ_g), φ_g = (√5−1)/2, and consecutive indices must exceed a minimum OKLab hue separation up to the Fibonacci-lattice counts."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: dd_colouring unit test 9 for n up to 96
  rulings:
  - R-71
  milestone: M7
  note: "The minimum hue separation: REQ-COL-051 (calibration)."
- id: REQ-COL-041
  area: COL
  statement: "Cubehelix must be generated analytically with φ = 2π(s/3 − λt), a = h·t(1−t)/2, s = 0.5, λ = 1.5, h = 1 and the stated R, G, B coefficient forms, this analytic form being the reference, with matplotlib's cubehelix function called with the same parameters a cross-check only (R-151)."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.8 Palettes and CVD
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: decisions.md
    section: "R-151 — Cubehelix's reference is the analytic form *(closes RQ-121, amends R-122)*"
  verify:
    method: unit test
    detail: "sampled Cubehelix matches the formula; lightness monotone increasing; matplotlib's cubehelix(s=0.5, λ=1.5, h=1) agrees as a cross-check (R-151)"
  rulings:
  - R-151
  milestone: M7
- id: REQ-COL-042
  area: COL
  statement: "CVD simulation must be real Viénot simulation for protan and deutan and real Brettel simulation for tritan, through LMS space from linear sRGB, with matrices and golden values from a published reference implementation named with its version in dd_colouring §3.8, and achromatopsia must multiply linear-sRGB triplets by dd_colouring §3.8's M_achrom."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.8 Palettes and CVD
  - file: docs/design/principia_colour_composition.md
    section: 4.3 Display stage (terminal, outside the pipeline)
  - file: decisions.md
    section: "R-78 — Real Viénot and Brettel colour-vision simulation *(closes RQ-29)*"
  verify:
    method: unit test
    detail: "dd_colouring unit test 10: protan, deutan and tritan match the named reference implementation's golden values; achrom yields R = G = B exactly; applying any simulation pre-linearisation produces a detectable difference"
  rulings:
  - R-67
  - R-78
  milestone: M7
- id: REQ-COL-043
  area: COL
  statement: "The display chain must run in the fixed order SimState → stain → style → display scale → gamut clamp → colour-vision simulation → screen, so that the colour-vision simulation sees the final in-gamut colours."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  - file: docs/gui/principia_render_gui_spec.md
    section: 12. Display stage — global, outside the pipeline
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  - file: decisions.md
    section: "R-107 — Apply the RQ-67 follow-ups; GUI_DESIGN_NOTES may be conformed *(closes RQ-67)*"
  - file: decisions.md
    section: "R-111 — `SimResult` → `SimState`, `M` → `n_renorm`; the vocabulary lint covers the docs *(closes RQ-80)*"
  verify:
    method: unit test
    detail: "with an out-of-gamut stain output and CVD on, the CVD stage's input is in gamut and equals gamut_clamp(display_scale(style(stain)))"
  rulings:
  - R-67
  - R-107
  - R-111
  milestone: M7
  note: "GUI_DESIGN_NOTES now lists display scale and gamut clamp in this order (conformed to R-67, R-107). R-111: the chain starts at SimState (render_gui_spec and GUI_DESIGN_NOTES conformed)."
- id: REQ-COL-044
  area: COL
  statement: "The style stage must be optional and apply to the figure only, offering in v1 plain, watercolour & pencil (the poster's painted treatment) and the seven print presets (the paper.design halftone presets with the poster's patches: per-plate slip, cell ceiling), as the poster's implementation (workbench/principia_poster_both_sides.html and its press module) defines them."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: decisions.md
    section: R-130 ✱ — The styles are the poster's *(closes RQ-107)*
  verify:
    method: GUI screenshot
    detail: "screenshots of each v1 preset (plain, watercolour & pencil, the seven print presets) against 04_windows.png's Display window; chrome and legend are unstyled"
  rulings:
  - R-130
  milestone: M7
  note: "R-130: the styles are the poster's; render_gui_spec's 'print · Poster78, more' is read as the seven print presets."
- id: REQ-COL-045
  area: COL
  statement: "Colour-vision simulation must offer off, deuteranopia, protanopia, tritanopia and achromatopsia."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  - file: decisions.md
    section: "R-78 — Real Viénot and Brettel colour-vision simulation *(closes RQ-29)*"
  - file: decisions.md
    section: "R-123 — Achromatopsia is a fifth Display mode *(closes RQ-91)*"
  verify:
    method: unit test
    detail: "each mode transforms a test colour set by its Viénot (protan, deutan) or Brettel (tritan) simulation, matching the reference golden values, and achromatopsia by M_achrom (R = G = B); off is identity"
  rulings:
  - R-78
  - R-123
  milestone: M7
- id: REQ-COL-046
  area: COL
  statement: "The golden-image tests must be pinned to colour_composition §7.1's complete map lists, grouped under the two oracle files principia_colour_explorer.html and principia_colour_presets.html (R-139), with Stability × Hue deleted from them, against the two reference HTML files as the oracle, and invalid samples must render the invalid pattern REQ-COL-055 sets."
  source:
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: decisions.md
    section: R-76 — Stability × Hue is deleted *(closes RQ-27)*
  - file: decisions.md
    section: "R-122 — The reference HTML files are the colour oracle *(closes RQ-90 and RQ-101)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  - file: decisions.md
    section: "R-139 — Turbo is Google's table; the §7.1 labels name the oracle files *(closes RQ-117)*"
  verify:
    method: golden image
    detail: "One golden image per §7.1 map against the reference HTML render (R-122); none for Stability × Hue; invalid samples render the REQ-COL-055 pattern (R-132)."
  rulings:
  - R-16
  - R-71
  - R-76
  - R-122
  - R-132
  - R-139
  milestone: M7
  note: "The agreement tolerance: REQ-COL-052 (calibration). R-122: the oracle is docs/gui/reference/principia_colour_explorer.html and principia_colour_presets.html. R-132: the invalid colour collides with no palette entry (a hatched pattern), replacing 'magenta'."
- id: REQ-COL-047
  area: COL
  statement: "The equirect bake must be debounced (120 ms) via `copyExternalImageToTexture` and never land synchronously inside the frame callback."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  verify:
    method: unit test
    detail: bake upload scheduled outside the rAF callback
  rulings: []
  milestone: M8
- id: REQ-COL-048
  area: COL
  statement: "Colour output must be verified at M8 by the Playwright browser suite (Playwright's Chromium and WebKit, the R-110 pair; R-149) diffing the fragment output against the same golden baselines the native wgpu offscreen renderer has set since M1, within tolerance, outside the parity suite, run nightly, on GUI and colour PRs and at each gate (R-134), with no re-baselining without a gate decision."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: docs/contracts/principia_parity_contract.md
    section: 7. What this contract does *not* cover
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-134 — When the Playwright suite and the aggregate survey run *(closes RQ-112)*"
  - file: decisions.md
    section: "R-149 — The colour suite runs on Chromium and WebKit *(closes RQ-119)*"
  verify:
    method: golden image
    detail: "the nightly, GUI/colour-PR and gate job (R-134) renders the fragment goldens in the browser build and diffs them against the native-wgpu baselines within tolerance; no baseline file changes without a recorded gate decision (R-110)"
  rulings:
  - R-71
  - R-110
  - R-134
  - R-149
  milestone: M8
  note: "The diff tolerance: REQ-COL-052 (calibration). R-110: goldens render with native wgpu offscreen from M1; the browser suite reuses those baselines."
- id: REQ-COL-049
  area: COL
  kind: calibration
  statement: "The OKLab round-trip tolerance of dd_colouring unit test 1 (linear-sRGB ↔ OKLab across a gamut lattice) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.1 Colour spaces (exact transforms — transcription-check against Ottosson before entering the shared source)"
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows the measured f32 round-trip error over the gamut lattice and the margin the tolerance leaves above it"
  rulings:
  - R-71
  milestone: M7
- id: REQ-COL-050
  area: COL
  kind: calibration
  statement: "The LUT-sphere blend tolerance of dd_colouring unit test 4 (an equator sweep against the 1-D LUT) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 3.2 The vMF engine
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows the measured maximum deviation of the equator sweep from each shipped LUT at N_e = 16 and the margin the tolerance leaves"
  rulings:
  - R-71
  milestone: M7
- id: REQ-COL-051
  area: COL
  kind: calibration
  statement: "The minimum OKLab hue separation between consecutive golden-angle palette indices (dd_colouring unit test 9) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: "3.7 Categorical colour, and how mixed pixels resolve (colour-per-sample → SSAA)"
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal lists the achieved consecutive-index OKLab hue separations for n up to the Fibonacci-lattice counts (96) and the minimum chosen below them"
  rulings:
  - R-71
  milestone: M7
- id: REQ-COL-052
  area: COL
  kind: calibration
  statement: "The golden-image agreement tolerance by which a recreated preset matches its render in the reference HTML files (colour_composition §7's oracle, R-122), also used by the M8 Playwright fragment golden diffs (R-134), must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 7. Preset table & golden-image obligation
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-122 — The reference HTML files are the colour oracle *(closes RQ-90 and RQ-101)*"
  - file: decisions.md
    section: "R-134 — When the Playwright suite and the aggregate survey run *(closes RQ-112)*"
  verify:
    method: numerical gate
    detail: "the proposal shows per-map pixel differences between the composition engine and the reference HTML renders over the §7.1 list, and the tolerance with its margin"
  rulings:
  - R-71
  - R-122
  - R-134
  milestone: M7
- id: REQ-COL-053
  area: COL
  kind: calibration
  statement: "The sRGB value of the neutral grey the outcome palette shows for the running state must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M1 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_colour_composition.md
    section: "1.4 Categorical colour-assignment — the outcome-state default palette"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: review checklist
    detail: "the proposal shows the grey's OKLab lightness and its separation from bounded #141418, degenerate #ECECF0 and the other seven classes"
  rulings:
  - R-71
  - R-96
  milestone: M1
- id: REQ-COL-054
  area: COL
  kind: definition
  statement: "The standing composition-spec gaps render_gui_spec §16 lists (colour-source-as-axis, overlays-as-post-chain-over-configured-base, physics-as-overlay-op, gradient-unifies-the-ramp, per-footprint vs quad-aggregate spread, the debug taxonomy, the addressing channel convention, boundaries-as-overlay) must be defined in principia_colour_composition.md by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 16. Open / next
  - file: docs/design/principia_colour_composition.md
    section: 8. Amendments to other docs
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "colour_composition states each resolution the GUI spec assumes, and §16's item is closed"
  rulings:
  - R-72
  milestone: M7
- id: REQ-COL-055
  area: COL
  kind: calibration
  statement: "The default invalid-pixel rendering (colour_composition §3's invalid colour, the prelude's debug_invalid(frag_xy: vec2<f32>) -> vec3<f32>, which draws the hatch from the pixel position, R-136) must collide with no palette entry, as a hatched pattern rather than a flat colour, and its pattern and colours must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M1 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  - file: docs/design/principia_colour_composition.md
    section: "1.4 Categorical colour-assignment — the outcome-state default palette"
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  - file: decisions.md
    section: "R-136 — `debug_invalid(frag_xy)` draws the hatch; sentinels show their value *(closes RQ-114)*"
  verify:
    method: review checklist
    detail: "the proposal gives the hatch pattern and its colours and shows it collides with no palette entry (the outcome palette incl. body-1 escape #E034C6, the dbg_* palettes, the LUTs); recorded in decisions.md"
  rulings:
  - R-16
  - R-71
  - R-132
  - R-136
  milestone: M1
  note: "R-132: a hatched pattern, not a flat colour (the corpus's 'fixed magenta' collides with #E034C6)."
- id: REQ-COL-056
  area: COL
  kind: definition
  statement: "The tile/sample lane's within-tile coordinate `ctx.tile.uv` (its range and orientation, matching `ctx.quad.uv`) must be defined in colour_composition §3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§3's tile/sample lane lists `uv` with its range and orientation; the generated ctx declares it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M1
- id: REQ-COL-057
  area: COL
  kind: definition
  statement: "What `ctx.chart.z` (\"the full 8-D latent at this pixel, chart triple applied\") holds on a chart whose Φ does not produce a latent z (the shape sphere, the invariant charts, the Burrau family, the Anosova physical-frame chart), and so which chart maps the fragment side evaluates in WGSL, must be defined in colour_composition §3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/contracts/principia_lowering_contract.md
    section: Appendix — worked enumeration of the current chart set
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section states, for every row of lowering's appendix, what ctx.chart.z carries and how the fragment side obtains it (and its validity lane where it has none); physics reviewer approved"
  rulings:
  - R-72
  milestone: M2
- id: REQ-COL-058
  area: COL
  kind: definition
  statement: "How the categorical filter's muted classes (`show class ∈ {…}, mute the rest`) are drawn — the transform applied to a muted class's colour must be defined in colour_composition §1.4 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_colour_composition.md
    section: "1.4 Categorical colour-assignment — the outcome-state default palette"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the mute transform as a function of the class colour, keeps muted pixels distinguishable from the invalid colour and from the shown classes, and applies to every categorical mode; physics reviewer approved"
  rulings:
  - R-72
  milestone: M7
- id: REQ-COL-059
  area: COL
  kind: definition
  statement: "The display stage's gamut-clamp method (per-channel clip in linear sRGB, or a chroma-reducing clamp at constant OKLab lightness and hue, or another) must be defined in colour_composition §4.3 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 4.3 Display stage (terminal, outside the pipeline)
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the clamp as a function from linear RGB to in-gamut linear RGB, identity on in-gamut input; physics reviewer approved"
  rulings:
  - R-67
  - R-72
  milestone: M7
- id: REQ-GUI-001
  area: GUI
  statement: "Mouse/pointer picking (lock, hover, click-inspect) must flip canvas coordinates the same way before computing the picked quad or z."
  source:
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "Code paths that must honour the single convention (each is a separate path)"
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "The catch: a UV-passthrough debug view (debug-first)"
  verify:
    method: unit test
    detail: "picking cross-check: click each known corner, assert the reported UV/IC coordinate is that corner"
  rulings: []
  milestone: M1
- id: REQ-GUI-002
  area: GUI
  statement: "Navigation must implement pan as Δz₀ ∈ span(q₁, q₂), slice as Δz₀ ⊥ span(q₁, q₂) (z₀ ← z₀ + t·d), zoom as a common log-stepped scale of q₁, q₂, and tilt as a basis rotation."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The operation table
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Slice (centre edit, orthogonal component)
  verify:
    method: unit test
    detail: "after a slice step every pixel's IC changes by the identical hidden-coordinate step; pan keeps the plane; zoom keeps z₀"
  rulings: []
  milestone: M2
- id: REQ-GUI-003
  area: GUI
  statement: "Tilt must compute q'(τ) = normalise(cos τ·q + sin τ·d_target) for τ ∈ [−90°, +90°] with sign significant, offering the six directions orthogonal to the plane plus the named compound directions, and curve-axis tilt must rotate the tangent at the centre, q_tilt(τ) = normalise(cos τ·γ'(ν₀) + sin τ·d_target)."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Tilt (basis edit)
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Directions are axis kinds — the unifying rule
  verify:
    method: unit test
    detail: "τ = 0 leaves q unchanged; τ = ±90° gives ±d_target; +30° and −30° give different planes"
  rulings: []
  milestone: M2
- id: REQ-GUI-004
  area: GUI
  statement: "Setting the lock on an affine chart must compute z_locked = z₀ + (2s−1)q₁ + (2t−1)q₂ on the CPU with no GPU readback; on a nonlinear chart it must replay Φ and D on the CPU (preferred) or read the GPU buffer back, re-centring via the inverse-encode path."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The lock (projective microscope)
  - file: docs/design/principia_dd_encode.md
    section: 2. Consolidated contract
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 03 Chart builder
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 08 Lock — the reticle and the pin
  - file: decisions.md
    section: R-83 — The slice scale lives in q *(closes RQ-34)*
  verify:
    method: unit test
    detail: "lock a pixel on an affine and a nonlinear chart; centre pixel's IC equals the clicked pixel's IC"
  rulings:
  - R-83
  milestone: M2
- id: REQ-GUI-005
  area: GUI
  statement: "Tilt, zoom and chart-mode switch must preserve the locked anchor exactly (z(½,½) = z₀ regardless of basis), while locked slicing and slider moves must be tracked as an explicit excursion δ from a stored anchor, ghost-marked, with snap-back returning δ → 0 exactly."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The lock (projective microscope)
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 08 Lock — the reticle and the pin
  verify:
    method: property test
    detail: "random tilt/zoom/chart switches leave the centre IC bitwise constant; snap-back restores the stored anchor bitwise"
  rulings: []
  milestone: M2
- id: REQ-GUI-006
  area: GUI
  statement: "In locked mode sliders must be re-based to the anchor (showing z_anchor + δ live), not frozen, and unlock must preserve the current (z₀, q₁, q₂) as the new free-mode state."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The lock (projective microscope)
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 08 Lock — the reticle and the pin
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  verify:
    method: unit test
    detail: "move a slider while locked → excursion along that control; unlock → free state equals last locked state"
  rulings: []
  milestone: M2
- id: REQ-GUI-007
  area: GUI
  statement: "The lock flag, the anchor z_locked and the excursion δ must be SimConfig fields, and recentring on lock must be a SetField on z₀."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: unit test
    detail: "the lock action emits SetFields on SimConfig lock fields and z₀; none are ViewUI"
  rulings:
  - R-69
  milestone: M2
- id: REQ-GUI-008
  area: GUI
  statement: "The IC inspector must be the shared kernel at f64 on CPU for a single IC (not a separate viewer)."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: The IC inspector — the prebake standard at a single point
  - file: decisions.md
    section: "R-65 — One Inspector window; the standalone IC Inspector is absorbed *(closes RQ-21)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "Inspector integrates a clicked IC via the shared kernel instantiated at f64 on CPU"
  rulings:
  - R-65
  - R-113
  milestone: M3
  note: "R-113 (RQ-98): 'hosted in the one Inspector window' is REQ-GUI-112's (M8)."
- id: REQ-GUI-009
  area: GUI
  statement: "Checkerboard must be a user setting with three states — On-permanent (default; checkerboard whenever the playhead advances), On-motion-only (only during camera motion; static playback renders every frame in full) and Off (never)."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 3. The three-state control
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  verify:
    method: unit test
    detail: "default is permanent; motion-only renders static playback full; off never masks"
  rulings: []
  milestone: M5
- id: REQ-GUI-010
  area: GUI
  statement: "The snapshot must carry, GUI-sized, whether DECODE_SWITCHOVER has fired on visible quads and whether AT_F32_FLOOR has been hit."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: unit test
    detail: "snapshot fields for both events exist and flip when the scheduler reports the event on a visible quad"
  rulings:
  - R-54
  milestone: M6
- id: REQ-GUI-011
  area: GUI
  statement: "The quality selector and custom fields must edit SimConfig.quality (in the Run window), the arbiter must be engine-side writing SimConfig.quality during settled periods, and ViewUI must carry the arbiter debug overlay's visibility (throughput, current rung, recent decisions)."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  verify:
    method: unit test
    detail: "arbiter writes arrive as SimConfig.quality changes in the snapshot; the overlay toggle is a ViewUI field"
  rulings: []
  milestone: M6
- id: REQ-GUI-012
  area: GUI
  statement: "The deep-zoom precision warning must be event-driven: raised when DECODE_SWITCHOVER fires on visible quads and when AT_F32_FLOOR is hit, not by fixed depth thresholds."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 4 — Terminal vs refinable, tied to the two floors
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  - file: decisions.md
    section: R-54 — The precision warning is event-driven *(GU-3 (b))*
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: unit test
    detail: "zoom a visible region through the switchover and to the linear floor; the warning event is raised exactly at each event and not at any fixed depth when the events do not fire (e.g. early switchover at shallow depth with tiny q)"
  rulings:
  - R-54
  milestone: M6
- id: REQ-GUI-013
  area: GUI
  statement: "A lock-to-native toggle, default on, must pin render_scale = 1.0 and grey out the render-scale slider, and auto mode must respect it by adapting only via E and the refinement floor (then FTLE and word), never render_scale."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "\"Lock to native\" toggle"
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: docs/design/principia_memory_tiers.md
    section: 6. Auto-mode tier selection
  - file: docs/design/principia_quality_device_note.md
    section: "5. Timescale cascade (each layer absorbs what's too fast for the one above)"
  verify:
    method: unit test
    detail: "with lock on, the controller under load never changes render_scale; slider is disabled"
  rulings: []
  milestone: M6
  note: "Order when locked is E → refinement floor → FTLE → word (memory-tiers §5)."
- id: REQ-GUI-014
  area: GUI
  statement: "Custom mode must expose render_scale (0.25–2.0), N, MAX_REL_DEPTH, E, FTLE, motion gating and the lock-to-native toggle directly, with the arbiter off, in the Run window under 'quality: Custom'."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  - file: decisions.md
    section: "R-129 ✱ — Where the surfaces with no artboard live *(closes RQ-105)*"
  verify:
    method: GUI screenshot
    detail: "presence only (R-129; no layout comparison until the M8 dev GUI): the Run window's 'quality: Custom' section shows each control"
  rulings:
  - R-129
  milestone: M6
- id: REQ-GUI-015
  area: GUI
  statement: "Quality must be a preset selector (Auto / named tier / Custom) populating one QualitySettings struct: Auto computes it by device characterisation and is re-runnable (re-selecting Auto or re-detect re-measures), a named tier looks it up and skips the probe, Custom lets the user set it."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  - file: docs/contracts/principia_gui_state_contract.md
    section: "7. What a replacement GUI must honour (the teardown contract)"
  verify:
    method: unit test
    detail: "all three modes write the same struct type; re-selecting Auto runs a fresh probe"
  rulings: []
  milestone: M6
- id: REQ-GUI-016
  area: GUI
  statement: "Stain node ids, wires and per-node params must serialise with RenderState, with picking a node's occupant from the registry being one field edit."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: unit test
    detail: "round-trip a graph through RenderState; choosing an occupant emits one SetField"
  rulings:
  - R-64
  milestone: M7
- id: REQ-GUI-017
  area: GUI
  statement: "Binding brightness (or any occupant) to a field the current tier does not populate must not trigger a recompute: the UI must grey out unavailable sources, reading anyway must read NaN at unpack and show the invalid colour, and the only path to recompute must be an explicit user quality-tier change."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 3 — Cache tiers and the recompute rule
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  verify:
    method: unit test
    detail: "at a no-FTLE tier bind brightness to ftle; dispatch counter stays static; source is greyed; the unpacked value is NaN and the pixel shows the invalid colour"
  rulings:
  - R-79
  milestone: M7
- id: REQ-GUI-018
  area: GUI
  statement: "Pairing a monotone-L LUT colour (Viridis, Cividis, Magma, …) with the Replace-L combiner must raise a UI conflict flag."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 4 — Semantic rules
  - file: docs/design/principia_dd_colouring.md
    section: 2. Consolidated contract
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "dd_colouring unit test 6 wiring test: select a monotone-L LUT + Replace-L; the conflict flag is set; with Multiply it is not"
  rulings: []
  milestone: M7
- id: REQ-GUI-019
  area: GUI
  statement: "Every pipeline stage must expose its generated WGSL for reading."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 5. Codegen & eject
  verify:
    method: GUI screenshot
    detail: Node WGSL / Pipeline WGSL views show the generated code
  rulings: []
  milestone: M7
- id: REQ-GUI-020
  area: GUI
  statement: "Every colour map in colour_composition §7.1's principia_colour_explorer.html group (R-139) must offer the global Invert (v ↦ 255 − v), Blend (linear mix of any two modes) and Auto-rotate controls."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: decisions.md
    section: "R-139 — Turbo is Google's table; the §7.1 labels name the oracle files *(closes RQ-117)*"
  verify:
    method: GUI screenshot
    detail: controls present on each map of that group (R-139)
  rulings:
  - R-139
  milestone: M7
- id: REQ-GUI-021
  area: GUI
  statement: "The stain graph model must be nodes {id, kind, inputs, outputs, params} of kinds source, colour, brightness, combiner, post and OUT, with typed directional ports carrying field (subtyped scalar / vector / categorical), vec3 or f32, and each kind's inputs and output as §3.1's table gives."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 3. The node graph — model
  - file: docs/gui/principia_render_gui_spec.md
    section: 3.1 Node kinds
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: unit test
    detail: "construct each kind; its port list and types equal §3.1's row"
  rulings:
  - R-64
  milestone: M7
- id: REQ-GUI-022
  area: GUI
  statement: "OUT and combiner must be fixed singletons that cannot be added, deleted or duplicated, and a delete on either must be refused."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 4. The backbone — fixed vs free
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: unit test
    detail: "delete / duplicate / add on OUT and combiner are refused; the graph always has exactly one of each"
  rulings:
  - R-64
  milestone: M7
- id: REQ-GUI-023
  area: GUI
  statement: "The free interior must allow any number of source, colour, brightness and post nodes, wired subject only to type compatibility and acyclicity, and a wire that would close a cycle must be refused."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 4. The backbone — fixed vs free
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  verify:
    method: property test
    detail: "random wire sequences: the graph stays acyclic and every accepted wire is type-compatible"
  rulings:
  - R-64
  milestone: M7
- id: REQ-GUI-024
  area: GUI
  statement: "A source node must have one param (which ctx field) that sets its out-port subtype, one out-port must fan out to any number of consumers, and multi-source mappings must expose multiple input ports rather than multiple wires per port."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 5. Sources & multi-source
  - file: docs/gui/principia_render_gui_spec.md
    section: 3.1 Node kinds
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  verify:
    method: unit test
    detail: "picking a vector field sets subtype vector; one source wired to a colour and a brightness node renders both; a margin node has two field ins"
  rulings: []
  milestone: M7
- id: REQ-GUI-025
  area: GUI
  statement: "A wire must attach only when the out-port type matches the in-port type (field → field, vec3 → vec3, f32 → f32), and a mismatched wire must be rejected and snap back."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 6. Port typing & wire rules
  verify:
    method: unit test
    detail: "vec3 → f32 and field → vec3 drops are refused; matching drops attach"
  rulings: []
  milestone: M7
- id: REQ-GUI-026
  area: GUI
  statement: "An in-port must take exactly one wire, a new wire dropped on an occupied in-port replacing the old (last write wins), while out-ports take unbounded wires."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 6. Port typing & wire rules
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  verify:
    method: unit test
    detail: "drop a second wire on an in-port: the first is removed; an out-port holds five wires"
  rulings: []
  milestone: M7
- id: REQ-GUI-027
  area: GUI
  statement: "Deleting a mid-graph node must leave its dangling in-ports rendering as None and evaluating via occupant identity, so the pipeline stays renderable."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  - file: docs/gui/principia_render_gui_spec.md
    section: 13. Invariants
  verify:
    method: unit test
    detail: "delete the colour node of a two-slot graph: the output equals greyscale-of-brightness"
  rulings: []
  milestone: M7
- id: REQ-GUI-028
  area: GUI
  statement: "A preset must be an entire serialised graph that replaces the current graph wholesale on selection, and the catalogue's default composition must be that preset payload — one data source, not two."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 11. Presets = whole graphs
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: docs/contracts/principia_gui_state_contract.md
    section: 5. The stain editor — a free, typed node graph (R-64)
  - file: docs/contracts/principia_render_contract.md
    section: Part 2 — Fixed pipeline, swappable slots
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: unit test
    detail: "load a preset: the graph equals the preset data; catalogue metadata and preset graph come from the same file"
  rulings:
  - R-64
  milestone: M7
- id: REQ-GUI-029
  area: GUI
  statement: "Debug presets must load locked (read-only) with the first edit forking a custom copy, production presets must load editable, and the preset → graph relation must be one-way."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 11. Presets = whole graphs
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  verify:
    method: unit test
    detail: "edit a loaded debug preset: a custom copy is created and the preset is unchanged"
  rulings: []
  milestone: M7
- id: REQ-GUI-030
  area: GUI
  statement: "Bivariate presets must load a graph with both combiner inputs occupied (one source → colour, one source → brightness) and no special bivariate UI."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 11. Presets = whole graphs
  verify:
    method: unit test
    detail: "load n̂ × FTLE: combiner colour and brightness inputs are both wired"
  rulings: []
  milestone: M7
- id: REQ-GUI-031
  area: GUI
  statement: The stain editor must be a free, typed node graph.
  source:
  - file: decisions.md
    section: "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
  verify:
    method: GUI screenshot
    detail: "Against the stain-editor artboard; unit test that arbitrary typed wiring is accepted and type-mismatched wiring rejected."
  rulings:
  - R-64
  milestone: M7
- id: REQ-GUI-032
  area: GUI
  statement: "The egui dev debug menu must be F3-toggled inside the same wasm binary and walled off by the crate graph, with the engine crate exposing only its typed state surface as pub."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  verify:
    method: unit test
    detail: "F3 toggles the menu; a compile-fail test shows the dev-GUI crate cannot name engine internals beyond the pub state surface"
  rulings: []
  milestone: M8
- id: REQ-GUI-033
  area: GUI
  statement: "The dev GUI and the TS production GUI must be interchangeable bindings over one contract: each writes only through set_field and reads only state snapshots, with no in-process shortcut for the dev GUI."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  verify:
    method: review checklist
    detail: "every dev-GUI write is a set_field call and every read a snapshot field; no direct engine call"
  rulings: []
  milestone: M8
- id: REQ-GUI-034
  area: GUI
  statement: "Derived, invariant and coupled slice/tilt directions must be tangent vectors evaluated at the chart centre with the axis's residual convention, and the UI must label whether a line is a raw-control line or a physical one-quantity direction."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Directions are axis kinds — the unifying rule
  verify:
    method: GUI screenshot
    detail: "slider/direction labels distinguish 'one mass logit' from 'only m₀'"
  rulings: []
  milestone: M8
- id: REQ-GUI-035
  area: GUI
  statement: "Every SimConfig and RenderState edit — including navigation (z₀ and basis edits) and lock / unlock — must be undoable, ViewUI-only state (open panels, focus, selection, the kept-orbit list, playback transport) must not enter the undo history, and the GUI clock's per-frame playhead writes (a SetField marked \"no history\") must never enter it, a manual scrub being one coalesced entry."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  - file: decisions.md
    section: R-69 — What is undoable *(closes the step-6 open question)*
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
  verify:
    method: unit test
    detail: "pan, lock, unlock and a stain edit each add one history entry and undo restores the prior state; opening a window, moving focus, selecting, keeping an orbit, play / pause / speed / loop and ten seconds of playback add none; a scrub adds exactly one"
  rulings:
  - R-52
  - R-69
  - R-96
  - R-101
  milestone: M8
- id: REQ-GUI-036
  area: GUI
  statement: "The state must be split into SimConfig (sim key: chart id and params, z₀, q₁, q₂, slice values, lock flag, z_locked, δ, link ids — the chart's per-block link functions — integrator occupant, T / dt / thresholds / eps, r_coll, quality settings), RenderState (render key: the stain graph, overlay set, palette / compaction params, playhead t) and ViewUI (backdrop ref, debug category visibility, keyboard focus scope, selection, kept orbits, inspector t_cursor, open windows, linked views for side by side (v2), playback transport play / pause / speed / loop)."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
  - file: decisions.md
    section: "R-106 — The link ids are the chart's link functions *(closes RQ-66)*"
  verify:
    method: unit test
    detail: "schema test: each listed field lives in the named struct; playback transport and the linked views are ViewUI fields and are absent from SimConfig; SimConfig's link ids are the chart registry's link-function ids"
  rulings:
  - R-69
  - R-96
  - R-101
  - R-106
  milestone: M8
  note: "The GUI's clock reads the transport and advances RenderState's playhead each frame through a SetField marked \"no history\" (R-101)."
- id: REQ-GUI-037
  area: GUI
  statement: "The engine must never read ViewUI, so a replacement GUI can define its own ViewUI entirely."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: docs/contracts/principia_gui_state_contract.md
    section: "7. What a replacement GUI must honour (the teardown contract)"
  - file: docs/design/principia_trajectory_viewing.md
    section: 6. Placement
  - file: decisions.md
    section: "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
  verify:
    method: review checklist
    detail: no engine-crate code references ViewUI fields
  rulings:
  - R-101
  milestone: M8
  note: "Transport is read by the GUI's clock, not the engine: the clock writes RenderState's playhead through a SetField marked \"no history\" (R-101)."
- id: REQ-GUI-038
  area: GUI
  statement: "The GUI must expose every SimConfig and RenderState field and add no state of its own to them."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: review checklist
    detail: "a field-coverage test lists every schema field and the control that edits it"
  rulings: []
  milestone: M8
- id: REQ-GUI-039
  area: GUI
  statement: "SimConfig + RenderState must serialise as the provenance object, so that two GUIs can edit one schema side by side."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: unit test
    detail: "serialise, deserialise, compare; a second client's edit is visible in the first's snapshot"
  rulings: []
  milestone: M8
- id: REQ-GUI-040
  area: GUI
  statement: "Legibility guidance (e.g. hues over brightness levels for classes) must be surfaced as advice and never enforced."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  verify:
    method: review checklist
    detail: "a categorical → brightness wiring is accepted with an advisory note only"
  rulings: []
  milestone: M8
- id: REQ-GUI-041
  area: GUI
  statement: "Blend temperature must be presented as one blend-sharpness dial with a hard detent at the top edge that snaps to the nearest path, and support (all-site vMF vs top-k Voronoi) must be a discrete choice."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 1.1 Family A — site-blend  →  `vec3`
  verify:
    method: GUI screenshot
    detail: "dial at top detent selects nearest; support toggle switches vMF/Voronoi family"
  rulings: []
  milestone: M8
- id: REQ-GUI-042
  area: GUI
  statement: "A shape-sphere/equirect preview must render at one mass point (default the slice-centre masses z0, the inspected pixel's masses when a pixel is inspected) and must state which mass point it is showing."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 2. Site-set kinds
  verify:
    method: GUI screenshot
    detail: "preview label shows the mass point; inspecting a pixel changes it"
  rulings: []
  milestone: M8
- id: REQ-GUI-043
  area: GUI
  statement: "Eject must support per-node (with dependency tracking so upstream edits do not orphan downstream inputs) and whole-slot ejection into the custom-WGSL occupant, be one-way, grey out the ejected node's parameter widgets, use live compile with last-valid fallback, and offer 'revert to generated'."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 5. Codegen & eject
  verify:
    method: unit test
    detail: "eject one node; peers stay generated and live; revert restores generated code; parameter widgets disabled while ejected"
  rulings: []
  milestone: M8
- id: REQ-GUI-044
  area: GUI
  statement: "A user-visible target utilisation ('use up to N cores', 'cap at 30 fps') must be offered in the Run window, defaulting to leaving headroom with full utilisation opt-in, and telemetry must record which was in force."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: A deliberate ceiling, user-visible
  - file: decisions.md
    section: "R-129 ✱ — Where the surfaces with no artboard live *(closes RQ-105)*"
  verify:
    method: GUI screenshot
    detail: "presence (R-129): the utilisation control is in the Run window; default leaves headroom; the record names the ceiling"
  rulings:
  - R-129
  milestone: M8
- id: REQ-GUI-045
  area: GUI
  statement: "Quality warnings must fire only on an attempted setting change, with three non-blocking severities: Green (apply silently), Yellow (runs poorly: fps estimate plus a 'use recommended instead' escape) and Red (exceeds memory: needed vs estimated GB, still not blocked)."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "7. The \"are you sure?\" safety system (three severities, none blocking)"
  verify:
    method: GUI screenshot
    detail: "attempt a Yellow and a Red change; dialogs show the stated content and allow proceeding; no warning appears on current state"
  rulings: []
  milestone: M8
- id: REQ-GUI-046
  area: GUI
  statement: "Each warning dialog must offer a per-setting 'don't warn me again for this specific choice' suppression that does not silence different risky choices."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "7. The \"are you sure?\" safety system (three severities, none blocking)"
  verify:
    method: unit test
    detail: "suppress for E = 15; a later render_scale 2.0 change still warns"
  rulings: []
  milestone: M8
- id: REQ-GUI-047
  area: GUI
  statement: "When a named tier or Auto is selected, the Custom fields must display the resulting values read-only, and touching any field must switch to Custom with those values as the starting point."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: The auto → custom "show your work" flow
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  verify:
    method: GUI screenshot
    detail: "select High: fields show 16² samples, depth 6, E = 3, FTLE on greyed; edit E → mode shows Custom with the other values kept"
  rulings: []
  milestone: M8
- id: REQ-GUI-048
  area: GUI
  statement: "The hover trace, sonification and click inspector must be independently toggleable, none a prerequisite for another."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: 1. There is ONE mechanism, and it now has THREE consumers
  verify:
    method: unit test
    detail: each of the 2³ toggle combinations works
  rulings: []
  milestone: M8
  retired: "R-109: only pointer_channels §4 is normative; §1's independent toggles have no normative source."
- id: REQ-GUI-049
  area: GUI
  statement: "Sonification must scale to the audible range by mapping 1/t_c (the crossing time) to a fixed reference pitch, so frequency ratios are preserved across ICs."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "4. Audio-specific constraints, which are NOT the frame budget"
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: unit test
    detail: "two ICs with the same dynamical frequency ratio produce the same pitch interval"
  rulings:
  - R-71
  - R-96
  - R-109
  milestone: M8
  note: "The reference pitch: REQ-GUI-143 (calibration). Normative through render_gui_spec's listen, which cites pointer_channels §4, its one normative part (R-109)."
- id: REQ-GUI-050
  area: GUI
  statement: "Sonification must cross-fade between hovered ICs and never retrigger per pixel."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "4. Audio-specific constraints, which are NOT the frame budget"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: unit test
    detail: "the audio buffer across an IC change has no discontinuity (no click)"
  rulings:
  - R-109
  milestone: M8
- id: REQ-GUI-051
  area: GUI
  statement: "Hover and click must map the pointer through the single Y-flip (v = 1 − event.y / H) exactly as the render does, decode the IC on the CPU, and integrate it with one f64 computeIC returning the full state r(t), p(t)."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  verify:
    method: unit test
    detail: "pointer at the top edge decodes v ≈ 1 (top of the manifold); the trajectory holds r and p at every step"
  rulings: []
  milestone: M8
- id: REQ-GUI-052
  area: GUI
  statement: "The hover trace must be a transient overlay on the survey, and the click inspector a separate multi-panel surface that persists until closed."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 2. Two features = two projections of the one trajectory
  verify:
    method: unit test
    detail: "moving the pointer off clears the trace; the inspector stays open after the pointer leaves"
  rulings: []
  milestone: M8
- id: REQ-GUI-053
  area: GUI
  statement: "The hover trace must project the full CPU state onto the chart's actual basis vectors (trace_x = project(state, q₁), trace_y = project(state, q₂) via the chart's forward map), never onto an axis's dominant named coordinate, in any chart, tilt and slice."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: "3. Hover trace — projection onto the current (tilted, sliced) plane"
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: property test
    detail: "a conserved axis (E, L_z) gives a dot; tilting it toward a live direction grows a line then a curve; changing the slice shifts the trace without changing its shape"
  rulings: []
  milestone: M8
- id: REQ-GUI-054
  area: GUI
  statement: "The shape-sphere panel must draw n(t) as a polyline on S² with the landmarks (the three binary collisions on the equator, the Lagrange poles), its 3D and unwrapped views must share the config-space frame so they always agree, and the 3D orbit control must rotate the view, not the data."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  verify:
    method: unit test
    detail: "points picked in the 3D view and the unwrap map to the same n; orbiting leaves the stored n(t) unchanged"
  rulings:
  - R-14
  - R-50
  milestone: M8
- id: REQ-GUI-055
  area: GUI
  statement: "The inspector's scalar readout must show t_end_step, state, escaper, d_min and the drifts at the cursor."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  verify:
    method: GUI screenshot
    detail: screenshot against 05_inspectors.png's readouts
  rulings: []
  milestone: M8
- id: REQ-GUI-056
  area: GUI
  statement: "The hover overlay, the inspector panels and t_cursor must be ViewUI surfaces that reach the engine only through computeIC(chart, uv, simKey), adding no sim machinery."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 6. Placement
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: review checklist
    detail: "trajectory-viewing code calls only computeIC; its state lives in ViewUI"
  rulings: []
  milestone: M8
- id: REQ-GUI-057
  area: GUI
  statement: "The Inspector's canonical pane must show momentum (the chart's Jacobi q), not velocity."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  verify:
    method: GUI screenshot
    detail: "screenshot: canonical arrows are labelled/drawn as momenta"
  rulings: []
  milestone: M8
- id: REQ-GUI-058
  area: GUI
  statement: "The Jacobi frame (ρ, λ) and the interior vertex angles must always be drawn, and the CoM drawn and labelled, on both the editing and canonical panes, with no toggles."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Physical canvas — interaction
  verify:
    method: GUI screenshot
    detail: screenshot of both panes against 05_inspectors.png
  rulings: []
  milestone: M8
  note: "The scratchpad's overlay toggles are superseded by the as-built status."
- id: REQ-GUI-059
  area: GUI
  statement: "Both Inspector body panes must auto-fit, and the canonical pane must carry a √I = 1 stamp."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  verify:
    method: GUI screenshot
    detail: "screenshot with a large and a small configuration: both fit; the stamp is present"
  rulings: []
  milestone: M8
- id: REQ-GUI-060
  area: GUI
  statement: "The Inspector's shape-sphere unwrap must be the mass-weighted equirectangular map with θ azimuthal on the horizontal axis (0–360°) and φ polar on the vertical (0–180°, L⁺ at the top), collinear shapes on the equator, binary-collision landmarks computed from the actual masses, and the lower half shaded mirror-folded."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  verify:
    method: unit test
    detail: "landmarks for masses (1,1,1) and (1,2,3) match the mass-weighted formula; L⁺ maps to the top edge; collinear ICs map to φ = 90°"
  rulings:
  - R-14
  - R-50
  milestone: M8
- id: REQ-GUI-061
  area: GUI
  statement: "The Inspector must keep the CoM frame always on and shown: a config with net momentum is allowed, v_COM is drawn as its own vector, each body's velocity is drawn decomposed into the removed boost plus the CoM-frame residual, and the encode consumes CoM-frame velocities so total P feeding the chart is 0."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Whole-system handles — the invariance audit
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Canonical window (separate panel)
  verify:
    method: unit test
    detail: "build an IC with P ≠ 0: v_COM is displayed, the encoded P is 0 to round-off, z equals that of the boost-removed IC"
  rulings: []
  milestone: M8
- id: REQ-GUI-062
  area: GUI
  statement: "Right-clicking a body must open a properties popover with mass, position, velocity, momentum, distance to the CoM and to each other body, and the body's contribution to P, L and E, every field editable and kept in sync with dragging."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Physical canvas — interaction
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: unit test
    detail: "edit mass numerically: the popover and canvas agree; drag the body: the popover position updates"
  rulings:
  - R-96
  milestone: M8
- id: REQ-GUI-063
  area: GUI
  statement: "A body's disc radius must be proportional to the cube root of its mass."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Physical canvas — interaction
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: unit test
    detail: "radii for masses 1 and 8 are in ratio 1 : 2"
  rulings:
  - R-96
  milestone: M8
- id: REQ-GUI-064
  area: GUI
  statement: "The canonical pane must be recomputed on every edit as the class representative: CoM at the origin, ρ̃ along +x, R̃ = 1, CoM frame."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Canonical window (separate panel)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: The quotient set (the thing being audited)
  verify:
    method: unit test
    detail: "after random edits the canonical representative has CoM = 0, P = 0, ρ̃_y = 0 with ρ̃_x > 0, and R̃ = 1"
  rulings: []
  milestone: M8
- id: REQ-GUI-065
  area: GUI
  statement: "The Inspector must draw the round-trip residual ghost: decode(z) pushed back through g⁻¹ into the dragged frame and overlaid on the physical config, and report the round-trip error."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Canonical window (separate panel)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  verify:
    method: unit test
    detail: "for random ICs the ghost coincides with the dragged config to the round-trip bound; a deliberately broken decode shows a visible offset"
  rulings: []
  milestone: M8
- id: REQ-GUI-066
  area: GUI
  statement: "The Inspector's measurement panel must show pairwise separations r₀₁, r₀₂, r₁₂, distance from the CoM per body, triangle angles, hyperradius R, shape angles (α, β), |pᵢ| and the Jacobi momenta q0–q3, raw P and the removed v_COM, L, E, moment of inertia I, the live z and a shape-sphere marker."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: "Measurement readouts (gauge-invariant → hold under all four handles)"
  verify:
    method: GUI screenshot
    detail: screenshot of the readout panel against 05_inspectors.png
  rulings:
  - R-22
  milestone: M8
- id: REQ-GUI-067
  area: GUI
  statement: "The Inspector must show a live conditioning number for canonicalisation that flags the two ill-conditioning sites, ρ → 0 (rotation pin undefined) and R → 0 (scale undefined)."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Degeneracy routing
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  verify:
    method: unit test
    detail: "the readout rises without bound as ρ → 0 and as R → 0 and is finite elsewhere; pole configs stay finite and SAT-flagged"
  rulings:
  - R-21
  milestone: M8
  note: "R-21 names the conditioning readout as one of the pole fences."
- id: REQ-GUI-068
  area: GUI
  statement: "Clicking a point on the slice (IC Inspector… / Open full viewer…) must load that pixel's IC into the Inspector for perturbation."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Bridges to the main view
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  verify:
    method: unit test
    detail: "click a pixel then open the Inspector: pane 1's IC decodes to that pixel's z"
  rulings: []
  milestone: M8
- id: REQ-GUI-069
  area: GUI
  statement: "The dev GUI must provide every screen in the spec's artboard table — Explore, Stain, Chart builder, the Profiler / Export & share / Display / Run windows, Inspector, Legend, Lock, Import picture / saved views / record, Measure, Research and Console — with Explore and Stain as the only two modes and everything else a window or a tool."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Principia — Dev GUI (egui / F3)
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Dev GUI (egui) — design notes for the artboards
  verify:
    method: GUI screenshot
    detail: "one screenshot per screen compared for layout with 01_main.png … 12_console.png (values are illustrative, R-68); the mode switch offers exactly Explore / Stain"
  rulings:
  - R-68
  milestone: M8
- id: REQ-GUI-070
  area: GUI
  statement: "The dev GUI must be an egui debug layer toggled with F3 over the wgpu render, with egui-wgpu sharing the engine's wgpu context and painting onto the same surface."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  verify:
    method: GUI screenshot
    detail: "screenshots with F3 on and off against 01_main.png: the figure is identical underneath; review that egui-wgpu is constructed from the engine's device/queue, not a second context"
  rulings: []
  milestone: M8
- id: REQ-GUI-071
  area: GUI
  statement: "Undo and redo must be a single history of typed setField edits kept by the state contract and shared by every GUI, in which a drag coalesces into one entry; a GUI must send undo / redo as requests and keep no history of its own."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: docs/contracts/principia_gui_state_contract.md
    section: "7. What a replacement GUI must honour (the teardown contract)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: decisions.md
    section: R-52 — Undo lives in the state contract *(GU-1 (a))*
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: unit test
    detail: "apply edits from two GUI clients, undo from one: the contract reverts the last edit regardless of which client made it; a slider drag, a scrubber drag and a body drag each add exactly one entry; the gui crate holds no undo stack"
  rulings:
  - R-52
  - R-96
  milestone: M8
- id: REQ-GUI-072
  area: GUI
  statement: "No control, window or log line may be named after a camera: the Research artboard's \"linked camera\" and the console's \"SetField CameraZoom\" must not appear."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  verify:
    method: review checklist
    detail: "grep GUI strings, window titles, SetField variant names and log sources for 'camera' (case-insensitive): no hits"
  rulings:
  - R-68
  milestone: M8
  note: The artboards are overridden here (§G13).
- id: REQ-GUI-073
  area: GUI
  statement: "Every preview of the slice (including the Chart builder's domain preview and quick render and the Stain mode preview) must keep the viewport's aspect ratio — square for the slice — and never stretch or crop it."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 03 Chart builder
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 02 Stain — the plain node-graph editor
  verify:
    method: GUI screenshot
    detail: "screenshots of 03_chartbuilder and 02_stain layouts: both chart-builder previews and the stain preview are square for a square viewport and match the viewport aspect otherwise"
  rulings: []
  milestone: M8
- id: REQ-GUI-074
  area: GUI
  statement: "The figure must never be covered: no toasts or labels may be drawn over the plot, warnings must go to the footer console, and only the figure's own marks (the hover label and the lock reticle) may appear on it."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  verify:
    method: GUI screenshot
    detail: "trigger a warning and an error with the figure visible: screenshot against 01_main.png shows nothing new over the plot; the footer count increments"
  rulings: []
  milestone: M8
- id: REQ-GUI-075
  area: GUI
  statement: "The dev GUI must use egui's dark default theme with the Ubuntu / Ubuntu Mono fonts and no styling beyond egui's own."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png: dark theme, Ubuntu for text, Ubuntu Mono for numbers/code"
  rulings: []
  milestone: M8
- id: REQ-GUI-076
  area: GUI
  statement: "The dev GUI must not include Chazy subtitles or the object-based stain desk, and its Stain mode must be the plain node-graph editor."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 02 Stain — the plain node-graph editor
  verify:
    method: review checklist
    detail: "no Chazy subtitle or stain-desk code path exists; Stain mode opens the Part II graph editor"
  rulings: []
  milestone: M8
- id: REQ-GUI-077
  area: GUI
  statement: "Each control must know whether its field is sim key (re-integrates) or render key (live) and warn per field from the caching blast-radius table, not from the struct the field lives in."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: docs/contracts/principia_caching_contract.md
    section: Part 2 — What invalidates what (the dependency graph)
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  - file: decisions.md
    section: R-92 — What the sim key holds of navigation *(closes RQ-43)*
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: unit test
    detail: "for every exposed field the re-integrate warning equals the caching Part 2 blast-radius entry: render_scale, lock_to_native, MAX_REL_DEPTH, E and checkerboard_mode show no warning although they sit in SimConfig.quality; in-plane pan and zoom and lock show none; slicing out of the plane, tilt and rotate do; playback transport shows none"
  rulings:
  - R-89
  - R-92
  - R-96
  milestone: M8
- id: REQ-GUI-078
  area: GUI
  statement: "The Explore top bar must hold the File / View / Windows / Help menus, the Explore / Stain mode switch, Overlays ▾ with a count of overlays on, Run…, Profiler…, Export…, the keyboard breadcrumb, and a status line with t, fps, frame ms, quad count, 'budget-bound' while the frame budget binds, the undo / redo depth and 'F3 hide'."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png's top bar; force the budget to bind and check 'budget-bound' appears; make two edits and check the undo depth reads 2"
  rulings:
  - R-52
  milestone: M8
- id: REQ-GUI-079
  area: GUI
  statement: "Overlays ▾ must hold the grouped toggles — Quadtree (tile bounds, depth colouring, priority heatmap, refine_flagged, split / merge activity), Integration (substep density, substep-cap hits, NaN / invalid pixels, energy drift), Chart (forbidden region from the chart's domain function, grid, Burrau lattice), Stain (class edges, t_end contours) — each with an Alt+digit shortcut, plus 'all off' and 'save as default'."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  verify:
    method: GUI screenshot
    detail: "screenshot of the open menu against 01_main.png; each Alt+digit toggles its overlay; 'all off' clears the overlay set"
  rulings:
  - R-26
  - R-72
  milestone: M8
  note: "Where 'save as default' is stored: REQ-GUI-153 (definition)."
- id: REQ-GUI-080
  area: GUI
  statement: "There must be no global display bar: its controls must live in the Display window and the Overlays menu."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  - file: docs/gui/principia_render_gui_spec.md
    section: 1. Layout — four surfaces
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: decisions.md
    section: R-67 — The display chain *(closes RQ-23)*
  verify:
    method: GUI screenshot
    detail: "screenshots of Explore (01_main.png) and Stain (02_stain.png): no display bar; style / scale / gamut / CVD are reachable only from the Display window"
  rulings:
  - R-67
  milestone: M8
- id: REQ-GUI-081
  area: GUI
  statement: "The left panel must be one 'Manifold view' group holding Chart, Navigate, the depth readout (2^-k and quad level) with its precision warning, Lock, Centre z₀ (eight sliders z_α, z_β, z_q0…z_q3, z_μ1, z_μ2) and Slice & tilt (slice step, τ₁, τ₂, rotation γ)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png's left panel: one group, sub-sections in that order, eight named z₀ sliders"
  rulings: []
  milestone: M8
- id: REQ-GUI-082
  area: GUI
  statement: "The Chart sub-group must show the chart preset named by its axes (e.g. 'z_α × z_β', never a nickname), the basis vectors q₁ and q₂ each with an edit button, a Chart builder… button and the chart's kind (affine or nonlinear)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png; every shipped preset's display name is built from its axis names"
  rulings: []
  milestone: M8
- id: REQ-GUI-083
  area: GUI
  statement: "The Navigate sub-group must show the centre (u, v), the zoom as log₂, and all eight z₀ values, each editable by drag and by typing."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: unit test
    detail: "drag and typed entry on each of the eight z₀ fields both emit the same SetField and round-trip through the snapshot"
  rulings: []
  milestone: M8
- id: REQ-GUI-084
  area: GUI
  statement: "The figure's axis labels must carry the short axis name and the range at each end."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png; after a pan the end values update"
  rulings: []
  milestone: M8
- id: REQ-GUI-085
  area: GUI
  statement: "The hover path must be z(t) through the manifold projected onto this chart's q₁, q₂ — not real space — drawn solid near the slice and faint where it leaves the plane."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png: the path is drawn from the cursor's IC with opacity falling with distance from the slice"
  rulings: []
  milestone: M8
- id: REQ-GUI-086
  area: GUI
  statement: "The hover label must name a fate only once it is decided (a terminal state — escape, collision — or bounded at the horizon) and read 'undecided · still interacting at t of t_max' before that."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: unit test
    detail: "label strings for a running sample, an escape, a collision and a bounded-at-horizon sample"
  rulings: []
  milestone: M8
- id: REQ-GUI-087
  area: GUI
  statement: "All body and pair labels in the GUI (hover label, legend, Trajectory panel, Inspector readouts) must be 0-based, with pair k naming the side opposite body k, overriding the artboards' 1-based labels."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  - file: docs/notes/ic_inspector_scratchpad.md
    section: "Measurement readouts (gauge-invariant → hold under all four handles)"
  verify:
    method: unit test
    detail: "labels for body 0 escape read 'body 0'; pair 0 is (1, 2), pair 1 (2, 0), pair 2 (0, 1); no '1–2' style collision label"
  rulings:
  - R-22
  milestone: M8
- id: REQ-GUI-088
  area: GUI
  statement: "The right-hand Trajectory panel must show, for the IC under the cursor or a kept orbit (tabs 'under cursor', #1, #2, +), a one-line summary (fate, t_end, FTLE, E₀), real space (CoM frame) beside the shape sphere, the F₂ word, substeps, minimum separation and |ΔE/E|, a playhead, listen, Open full viewer… and IC Inspector… (both opening the Inspector window), and the kept orbits below with fate and time, each removable."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png's right panel; both buttons open the Inspector window"
  rulings:
  - R-65
  milestone: M8
- id: REQ-GUI-089
  area: GUI
  statement: "The shape sphere (in the Trajectory panel and the Inspector's pane 2) must turn slowly with visible x, y, z axes, switch to an unwrapped equirectangular view in the same pane, and stop turning when the 'turn' checkbox is cleared."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  verify:
    method: GUI screenshot
    detail: "screenshots against 01_main.png and 05_inspectors.png in sphere and unwrapped modes; with 'turn' cleared two frames are identical"
  rulings: []
  milestone: M8
- id: REQ-GUI-090
  area: GUI
  statement: "listen (in the Trajectory panel and the Inspector) must sonify the corpus's mapping, θ(t), φ(t) → spectrum → audible range, from the same computeIC integration as the trace, and be able to follow the cursor; the artboard's 'separations → pitch' selector must not be implemented."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "4. Audio-specific constraints, which are NOT the frame budget"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: unit test
    detail: "the audio path's input is the θ(t), φ(t) series of the hovered IC's computeIC trajectory; no separations-based mapping exists"
  rulings:
  - R-68
  - R-96
  - R-109
  milestone: M8
  note: "The artboard's selector is illustrative (R-68). Only pointer_channels §4 is normative, through render_gui_spec's listen (R-109). Its open choices: REQ-GUI-144 (definition); the reference pitch: REQ-GUI-143 (calibration)."
- id: REQ-GUI-091
  area: GUI
  statement: "The compass (nav cube) must sit bottom left under the view controls, show the slice plane inside the chart, switch mode by itself (slicing when a slice slider is touched, tilting when a tilt is touched), carry a gold pin at the pivot when locked with the plane turning about it, tilt when the plane is dragged and orbit when the cube is dragged, and read out the tilt and rotation angles."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  verify:
    method: GUI screenshot
    detail: "screenshots against 01_main.png after touching a slice slider and after touching a tilt, and against 08_lock.png when locked"
  rulings: []
  milestone: M8
- id: REQ-GUI-092
  area: GUI
  statement: "The time controls must offer play, step, a scrubber and speed, and scrubbing back must set the display time and re-integrate progressively, showing it is refining (e.g. 'refining · 72%'), never replaying stored frames."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  - file: decisions.md
    section: R-66 — The time scrubber stays *(closes RQ-22)*
  verify:
    method: unit test
    detail: "scrub from t=40 to t=10: the playhead SetField triggers re-integration from 0 with a progress indicator; no frame store is read"
  rulings:
  - R-66
  milestone: M8
  note: "The export contract's 'no scrub' applies to exported animations only (R-66)."
- id: REQ-GUI-093
  area: GUI
  statement: "The footer must show the warning and error counts, the latest message, memory (GPU, heap) and '? keys', and clicking it must open the console."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png's footer; a click opens the 12_console.png layout"
  rulings: []
  milestone: M8
- id: REQ-GUI-094
  area: GUI
  statement: "Run settings (horizon, integrator, escape settings, quality, budget, recompute / cancel) must not be on the Explore page and must live in the Run window."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  - file: docs/gui/principia_render_gui_spec.md
    section: Run — from the top bar
  - file: decisions.md
    section: "R-107 — Apply the RQ-67 follow-ups; GUI_DESIGN_NOTES may be conformed *(closes RQ-67)*"
  verify:
    method: GUI screenshot
    detail: "screenshot against 01_main.png shows none of these fields; all are in the Run window (04_windows.png)"
  rulings:
  - R-68
  - R-107
  milestone: M8
  note: "GUI_DESIGN_NOTES no longer lists an integrator 'tolerance' (conformed to R-68, R-107)."
- id: REQ-GUI-095
  area: GUI
  statement: "The GUI must be a tree of keyboard scopes with Tab order 1 top bar · 2 Manifold view · 3 Figure · 4 Trajectory · 5 Compass · 6 Time · 7 Legend, with Manifold view's sub-scopes (Chart, Navigate, Centre z₀, Slice & tilt) reached with Enter."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 07 Keyboard — design note, not a screen
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  verify:
    method: unit test
    detail: "Tab / Shift+Tab cycles the seven scopes in order; Enter on Manifold view enters Chart"
  rulings: []
  milestone: M8
- id: REQ-GUI-096
  area: GUI
  statement: "The global keys must follow §G3's table: Tab / Shift+Tab next / previous scope, Enter into the scope, Esc out one level, arrows between siblings or adjust a value, Shift ×10 and Alt ×0.1 steps, held keys delay-then-repeat (DAS / ARR), Ctrl+Z undo from the contract's history, and ? shows shortcuts over everything."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 07 Keyboard — design note, not a screen
  verify:
    method: unit test
    detail: "synthetic key events for each row; a held arrow repeats only after the delay; Ctrl+Z calls the contract's undo"
  rulings:
  - R-52
  - R-71
  milestone: M8
  note: "The delay and repeat rate: REQ-GUI-146 (calibration)."
- id: REQ-GUI-097
  area: GUI
  statement: "The in-scope keys must be: Figure — arrows pan, + / − zoom, Space keeps the orbit, L listens, K locks; Trajectory — Enter reaches the playhead, listen and kept orbits; Compass — arrows tilt, Shift+arrows orbit; Time — Space plays, ← → step; Legend read-only."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
  verify:
    method: unit test
    detail: "synthetic key events per scope produce the stated SetField / ViewUI change; keys in Legend change nothing"
  rulings: []
  milestone: M8
- id: REQ-GUI-098
  area: GUI
  statement: "Keyboard focus must be shown only as a focus ring on the current scope and a top-bar breadcrumb (e.g. 'Manifold view › Navigate › zoom'), with nothing else changing on screen."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 07 Keyboard — design note, not a screen
  verify:
    method: GUI screenshot
    detail: "screenshots before and after moving focus differ only in the ring and the breadcrumb (07_keyboard.png)"
  rulings: []
  milestone: M8
- id: REQ-GUI-099
  area: GUI
  statement: "Locking (K, or right-click → lock here) must recentre the view on that point and mark it with a gold reticle at the centre and a gold pin on the compass, and the lock badge must read '● locked at z_locked' and offer unlock and open in Inspector."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G4. Lock — the reticle and the pin (`08_lock.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 08 Lock — the reticle and the pin
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  verify:
    method: GUI screenshot
    detail: screenshot against 08_lock.png after K over a point
  rulings: []
  milestone: M8
- id: REQ-GUI-100
  area: GUI
  statement: "Present mode must hide all chrome and Esc must return from it."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Export & share
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  verify:
    method: GUI screenshot
    detail: "screenshot in present mode shows only the figure; Esc restores 01_main.png's layout"
  rulings: []
  milestone: M8
- id: REQ-GUI-101
  area: GUI
  statement: "The Display window must offer the overlays grid, class edges, t_end contours and cursor crosshair."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Display — the last stages
  verify:
    method: GUI screenshot
    detail: screenshot against 04_windows.png's Display window
  rulings: []
  milestone: M8
- id: REQ-GUI-102
  area: GUI
  statement: "Every Run-window field must be a SimConfig field exposed under its contract name, covering integrator_contract Part 3's parameters — T_horizon (∈ [50, 200]), dt_macro (derived, shown read-only with its rule max(1e-3, T/65535), not editable, R-138), N_max (default 64), r_sub / gamma_sub, r_coll, r_close, eps_E / eps_L — and the integrator occupant (stepper × regularisation, Heggie with KDK leapfrog by default, Aarseth–Zare available), with no 'tolerance' field."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Run — from the top bar
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 01 Explore — the everyday view
  - file: decisions.md
    section: "R-68 — Artboard values are illustrative; corpus values win *(closes RQ-24)*"
  - file: decisions.md
    section: "R-107 — Apply the RQ-67 follow-ups; GUI_DESIGN_NOTES may be conformed *(closes RQ-67)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  - file: decisions.md
    section: "R-138 — `dt_macro` is derived, shown read-only *(closes RQ-116)*"
  verify:
    method: unit test
    detail: "the Run window's field list equals the contract parameter names; defaults N_max = 64 and occupant = Heggie×KDK; T_horizon is clamped to [50, 200]; no field named tolerance; dt_macro is read-only, shows its rule and tracks T (R-138)"
  rulings:
  - R-68
  - R-107
  - R-132
  - R-138
  milestone: M8
  note: "Overrides the artboard's integrator 'tolerance' field (R-68); GUI_DESIGN_NOTES is conformed (R-107). R-132: dt_macro = max(10⁻³, T/65535) and T_horizon defaults to 50 (REQ-INT-081)."
- id: REQ-GUI-103
  area: GUI
  statement: "The Run window's escape settings must be tau and the escape window (0.4 time units, provisional), with no persistence count."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Run — from the top bar
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  verify:
    method: GUI screenshot
    detail: "screenshot against 04_windows.png's Run window shows tau and window, no 'persistence' field"
  rulings:
  - R-29
  milestone: M8
  note: Overrides the artboard's 'persistence 8' (R-29, change 11).
- id: REQ-GUI-104
  area: GUI
  statement: "The Run window's refinement section must offer the quality preset selector (Auto, named tiers, Custom), the frame budget (ms), max depth (MAX_REL_DEPTH) and ensemble E, plus Recompute and Cancel with progress."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Run — from the top bar
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  verify:
    method: GUI screenshot
    detail: "screenshot against 04_windows.png; Cancel during a recompute stops it and the progress resets"
  rulings: []
  milestone: M8
- id: REQ-GUI-105
  area: GUI
  statement: "The legend must be generated by walking back from OUT, sampling each colour and brightness node over its declared input domain and pushing the samples through the real combiner."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 06 Legend — generated by evaluating the stain
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  - file: docs/contracts/principia_gui_state_contract.md
    section: "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
  verify:
    method: unit test
    detail: "for a test stain, every legend swatch colour equals the stain's shader output at the sampled input"
  rulings:
  - R-53
  milestone: M8
- id: REQ-GUI-106
  area: GUI
  statement: "The legend must give each dimension its own key: categorical swatches with their shares of the view, a separate bar for brightness (or any second channel), ticks placed through the curve for number ramps (log-spaced for a log curve), a sphere key for direction fields, a colour square for coordinate maps, and line samples for post operations."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 06 Legend — generated by evaluating the stain
  verify:
    method: GUI screenshot
    detail: "screenshots of a bivariate stain and a log-ramp stain against 06_legend.png"
  rulings: []
  milestone: M8
- id: REQ-GUI-107
  area: GUI
  statement: "The legend must be called 'Legend', never 'Fate', and no GUI label may use 'fate' for a stain dimension (e.g. 'Time of fate', 'fate edges')."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 06 Legend — generated by evaluating the stain
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  verify:
    method: review checklist
    detail: "grep GUI strings for 'Fate' as a legend/overlay/node name: no hits"
  rulings: []
  milestone: M8
- id: REQ-GUI-108
  area: GUI
  statement: "Chart presets must be saved pairs of axes that the user can Save, Duplicate and Delete."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 03 Chart builder
  verify:
    method: unit test
    detail: "save, duplicate, edit and delete a preset; the list reflects each"
  rulings: []
  milestone: M8
- id: REQ-GUI-109
  area: GUI
  statement: "The chart builder's Domain preview must show the chart's admissible region in its own coordinates, the forbidden region hatched, the current view as a rectangle, the boundary's formula and 'forbidden in view: N%', computed from the chart's validate(u, v)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 03 Chart builder
  verify:
    method: unit test
    detail: "N% equals the fraction of a uniform grid over the view that validate rejects; screenshot against 03_chartbuilder.png"
  rulings:
  - R-26
  milestone: M8
- id: REQ-GUI-110
  area: GUI
  statement: "The chart builder must show a quick render (e.g. 64 × 64 at a short horizon) at the view's aspect."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  verify:
    method: GUI screenshot
    detail: screenshot against 03_chartbuilder.png
  rulings: []
  milestone: M8
- id: REQ-GUI-111
  area: GUI
  statement: "The chart builder's footer must check q₁ · q₂ = 0, list the hidden directions and the residual, and offer Revert / Apply."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  verify:
    method: unit test
    detail: "non-orthogonal q₁, q₂ shows the check failing; Revert restores the prior chart; Apply emits the chart SetFields"
  rulings: []
  milestone: M8
- id: REQ-GUI-112
  area: GUI
  statement: "The IC Inspector and the trajectory viewer must be one Inspector window that absorbs the standalone IC Inspector, with trajectory_viewing §4's panels hosted there and in Explore's Trajectory panel as §4's placement table says."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  - file: docs/design/principia_trajectory_viewing.md
    section: 6. Placement
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Build notes
  verify:
    method: review checklist
    detail: "each row of trajectory_viewing §4's placement table has its panel in the named host; no separate IC Inspector window exists"
  rulings:
  - R-65
  milestone: M8
- id: REQ-GUI-113
  area: GUI
  statement: "Inspector pane 1 must show the IC with draggable bodies 0, 1, 2 and velocity arrows (velocity-only editing), with ghost markers at the playhead."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Physical canvas — interaction
  verify:
    method: GUI screenshot
    detail: "screenshot against 05_inspectors.png; dragging an arrow edits velocity"
  rulings: []
  milestone: M8
- id: REQ-GUI-114
  area: GUI
  statement: "Inspector pane 2 must show the canonical representative as bodies (with momentum arrows) or the shape sphere, and 'ghost the gauge transform' must animate g stepping the dragged config into the canonical one in its four named stages (translate → boost → rotate → scale)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Canonical window (separate panel)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Whole-system handles — the invariance audit
  verify:
    method: GUI screenshot
    detail: "screenshots of both views and of the ghost animation's four stages against 05_inspectors.png"
  rulings: []
  milestone: M8
- id: REQ-GUI-115
  area: GUI
  statement: "Inspector pane 3 must show the trajectory in real space (CoM frame) as CPU-f64 visualisation, with no comparison claimed."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  verify:
    method: GUI screenshot
    detail: screenshot against 05_inspectors.png
  rulings: []
  milestone: M8
- id: REQ-GUI-116
  area: GUI
  statement: "One timeline must scrub all three Inspector panes through a shared ViewUI t_cursor independent of the survey's global playhead, and hovering a sphere point must jump real space to that configuration."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  verify:
    method: unit test
    detail: "setting t_cursor updates all panes; the global playhead is unchanged; t_cursor is a ViewUI field"
  rulings: []
  milestone: M8
- id: REQ-GUI-117
  area: GUI
  statement: "Editing the IC must re-integrate the orbit live through computeIC on the CPU in f64, the Inspector reaching the engine only through computeIC."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: docs/design/principia_trajectory_viewing.md
    section: 6. Placement
  verify:
    method: unit test
    detail: "a body drag triggers one computeIC call and the trajectory panes update; no other engine entry point is called"
  rulings: []
  milestone: M8
- id: REQ-GUI-118
  area: GUI
  statement: "The Inspector must offer Reset, Randomise, Burrau-ish, Rotate 15°, Scale × 1.25 and Boost → as buttons, and the canonical pane must not move under the gauge buttons."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Whole-system handles — the invariance audit
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Canonical window (separate panel)
  verify:
    method: property test
    detail: "random ICs: Rotate, Scale and Boost leave the canonical pane's representative and z unchanged (see the gauge gate)"
  rulings: []
  milestone: M8
- id: REQ-GUI-119
  area: GUI
  statement: "The Inspector's readouts must show latent z, α and β, the masses, E · L_z, the outcome, the F₂ word, the round-trip error and the gauge error, with plots of energy error and pair separations."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/design/principia_trajectory_viewing.md
    section: 4. Click inspector — the three-panel absolute surface
  verify:
    method: GUI screenshot
    detail: screenshot against 05_inspectors.png's readout row
  rulings: []
  milestone: M8
- id: REQ-GUI-120
  area: GUI
  statement: "The Inspector's Create & locate tab must build an IC, show where it lands in z and its distance from the slice, and offer lock the view on it, centre a slice through it, mark it in the figure, and keep it."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 05 Inspector — one IC, its trajectory, one timeline
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Bridges to the main view
  verify:
    method: unit test
    detail: "build an IC: 'mark' draws a crosshair at z's projection on the current slice; 'lock' sets the lock fields to that z; 'centre' sets z₀; 'keep' adds a kept orbit"
  rulings: []
  milestone: M8
- id: REQ-GUI-121
  area: GUI
  statement: "The Measure tool must let the user drag a square region on the plot itself, draw the sampled pairs on it with disagreeing pairs marked, leave on Esc, and offer Export CSV and Keep region."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 10 Measure — a tool on the figure
  verify:
    method: GUI screenshot
    detail: "screenshot against 10_measure.png; the CSV has one row per sampled pair"
  rulings:
  - R-71
  - R-72
  milestone: M8
  note: "Its defaults and fixture: REQ-GUI-148 and REQ-GUI-149 (calibration); REQ-GUI-147 (definition)."
- id: REQ-GUI-122
  area: GUI
  statement: "Research (v2) must seed periodic orbits from spiral cores where the winding number diverges, Newton-refine each seed, report residual and period per seed, and compare against the Šuvakov–Dmitrašinović catalogue."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 11 Research — first pass (v2)
  - file: decisions.md
    section: "R-133 — The seven checkpoint-B interpretations are accepted *(closes RQ-111)*"
  verify:
    method: numerical gate
    detail: "on a region containing a catalogue orbit, a seed refines to it and matches its catalogue period"
  rulings:
  - R-71
  - R-133
  milestone: M8
  note: "First pass, v2. The residual and match tolerance: REQ-GUI-150 (calibration). R-133: 'winding number' is the payload's winding — the unwrapped phase θ̃ and orbit_count = ⌊|θ̃|/2π⌋."
- id: REQ-GUI-123
  area: GUI
  statement: "Research (v2) must offer continuation along a parameter (e.g. a mass ratio) with a step, measuring periodic-orbit stability by the monodromy matrix's Floquet multipliers and marking a fold where a multiplier crosses +1 and a period-doubling where one crosses −1."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 11 Research — first pass (v2)
  - file: decisions.md
    section: "R-127 — Periodic-orbit stability is Floquet; the Poincaré sections are three *(closes RQ-109)*"
  verify:
    method: GUI screenshot
    detail: "screenshot against 11_research.png; on a continuation fixture the fold and period-doubling markers sit where a Floquet multiplier crosses +1 / −1"
  rulings:
  - R-127
  milestone: M8
  note: "v2. R-127: transcribed with citations, physics-reviewed, confirmed at the M8 gate."
- id: REQ-GUI-124
  area: GUI
  statement: "Research (v2) must offer a Poincaré return map for a kept orbit on a chosen section: syzygy crossings (w = 0), a chosen shape-sphere great circle, or a Jacobi-coordinate hyperplane."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 11 Research — first pass (v2)
  - file: decisions.md
    section: "R-127 — Periodic-orbit stability is Floquet; the Poincaré sections are three *(closes RQ-109)*"
  verify:
    method: GUI screenshot
    detail: "screenshot against 11_research.png; the section picker offers exactly the three sections"
  rulings:
  - R-127
  milestone: M8
  note: "v2. R-127: transcribed with citations, physics-reviewed, confirmed at the M8 gate."
- id: REQ-GUI-125
  area: GUI
  statement: "Research (v2) must offer a side-by-side view with a linked cursor and linked navigation, and a difference view."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 11 Research — first pass (v2)
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-106 — The link ids are the chart's link functions *(closes RQ-66)*"
  verify:
    method: GUI screenshot
    detail: "screenshot against 11_research.png; a pan in one side pans the other"
  rulings:
  - R-72
  - R-96
  - R-106
  milestone: M8
  note: "v2; 'linked camera' is renamed (§G13). The linked views are a separate ViewUI item, specified with the v2 research tools — not the chart's link ids in SimConfig (R-106): REQ-GUI-154 (definition)."
- id: REQ-GUI-126
  area: GUI
  statement: "The console must be the footer opened, with severity, time, source and message columns, filters (all, warnings, errors, info, text), copy and clear, and must open automatically on an error."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G12. Console (`12_console.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 12 Console
  verify:
    method: GUI screenshot
    detail: screenshot against 12_console.png; raising an error opens it
  rulings: []
  milestone: M8
- id: REQ-GUI-127
  area: GUI
  statement: "The stain editor must be one GUI over one object — the composition graph plus display settings — in which the library picks, the canvas wires, the node inspector edits one stage and Advanced drops to its code, with no separate render-mode, colour-map or debug GUI."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 0. The core claim
  - file: docs/gui/principia_render_gui_spec.md
    section: Part II — The stain editor
  verify:
    method: review checklist
    detail: "every stain surface edits the same RenderState graph; no parallel colour or debug editor exists"
  rulings:
  - R-64
  milestone: M8
- id: REQ-GUI-128
  area: GUI
  statement: "Stain mode must lay out the library drawer (collapsible) left, the graph canvas centre with its Graph | Code toggle (Graph · Pipeline WGSL · Node WGSL), the preview and node inspector right, and the assembled code and a Problems pane (compile status, notes, auto-recompile) under the canvas."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 1. Layout — four surfaces
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 02 Stain — the plain node-graph editor
  verify:
    method: GUI screenshot
    detail: screenshot against 02_stain.png
  rulings: []
  milestone: M8
- id: REQ-GUI-129
  area: GUI
  statement: "Stain mode must show a live preview of the current OUT, toggleable between the shape sphere and an illustrative slice."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 1. Layout — four surfaces
  verify:
    method: GUI screenshot
    detail: "screenshot in both preview modes against 02_stain.png; an edit updates the preview"
  rulings:
  - R-72
  milestone: M8
  note: "Which preview is default: REQ-GUI-152 (definition)."
- id: REQ-GUI-130
  area: GUI
  statement: "Field subtype must not gate a wire: a categorical field dropped on a colour node in gradient mode must be accepted, and the node's inspector must morph to the palette editor, carrying over the params it can and flagging those it cannot."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 6. Port typing & wire rules
  verify:
    method: unit test
    detail: "rewire a gradient colour node from scalar to categorical: the editor is the palette editor and unmappable params are flagged"
  rulings: []
  milestone: M8
- id: REQ-GUI-131
  area: GUI
  statement: "The canvas must support click-select (opening the node's inspector), click-empty deselect (inspector shows graph / global), marquee multi-select, dragging nodes with wires following and multi-selected nodes moving together, space-drag or middle-drag pan and scroll zoom."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  verify:
    method: unit test
    detail: "scripted pointer events for each gesture produce the stated selection / position change"
  rulings: []
  milestone: M8
- id: REQ-GUI-132
  area: GUI
  statement: "While a wire is dragged compatible in-ports must highlight and incompatible ones dim, and releasing on empty canvas must open a node-create menu filtered to nodes that accept the dragged type."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  verify:
    method: unit test
    detail: "drag from a vec3 out-port to empty canvas: the menu lists only post nodes and OUT-compatible inputs; f32 in-ports are dimmed"
  rulings: []
  milestone: M8
- id: REQ-GUI-133
  area: GUI
  statement: "A wire must be removable by dragging it off near its head or by selecting it and pressing Delete."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  verify:
    method: unit test
    detail: "both gestures remove the wire and the in-port reverts to None"
  rulings: []
  milestone: M8
- id: REQ-GUI-134
  area: GUI
  statement: "Right-clicking the canvas must open a categorised node palette (sources grouped by ctx group; colour / brightness / post), and the library must not be a source of single nodes."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  verify:
    method: unit test
    detail: "the palette's source group lists ctx fields by group; the library only loads whole graphs"
  rulings:
  - R-72
  milestone: M8
  note: "Palette contents: REQ-GUI-151 (definition)."
- id: REQ-GUI-135
  area: GUI
  statement: "Each node box must carry a title bar (kind + source / field name), a state glyph (gradient chip, mini shape sphere, palette dots or op icon), typed ports colour-coded by type (ins left, outs right), a '{ }' badge when its code is hand-edited, and an accent outline when selected."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 8. Node visuals — glyph-forward
  verify:
    method: GUI screenshot
    detail: "screenshot against 02_stain.png with one hand-edited and one selected node"
  rulings: []
  milestone: M8
- id: REQ-GUI-136
  area: GUI
  statement: "The node inspector must choose its editor from the node kind and, for colour / brightness, the wired source subtype, per §9's table (vector → shape-sphere / SiteBlend editor, scalar → gradient editor, categorical → palette editor, brightness → gradient / compaction editor, combiner → Replace-L / Multiply, post → its op's controls, source → field dropdown), morphing automatically with no manual toggle."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 9. Node inspector — morphing editors
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "for each row, select such a node and assert the editor type; rewiring the source morphs the editor; a numeric debug field's RANGE_AUTO toggled in the node inspector updates the graph node and the code, and vice versa (R-113)"
  rulings:
  - R-113
  milestone: M8
- id: REQ-GUI-137
  area: GUI
  statement: "Every node inspector must end with Advanced ▸ edit shader, which reveals that node's generated WGSL editable in place within its function boundary, marks the node with the '{ }' badge and greys its graphical controls, and offers revert-to-generated to restore them."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 9. Node inspector — morphing editors
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  verify:
    method: unit test
    detail: "edit a node's WGSL: badge on, controls disabled; revert: generated code and controls restored"
  rulings: []
  milestone: M8
- id: REQ-GUI-138
  area: GUI
  statement: "The Graph | Code toggle must switch the representation only: Code view shows the assembled fragment WGSL (node functions, shared library calls and shade()), Graph view must be offered iff the pipeline still has graph structure — lossless under boundary-preserving per-node edits, withdrawn after a whole-pipeline edit that dissolves node boundaries — with no one-way dialog or revert prompt."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 10. `Graph | Code` — a view toggle, not an eject
  - file: docs/gui/principia_render_gui_spec.md
    section: 15. Settled decisions (record)
  verify:
    method: unit test
    detail: "per-node edit: toggle round-trips; whole-pipeline edit that removes a function boundary: Graph is disabled; no modal appears"
  rulings: []
  milestone: M8
- id: REQ-GUI-139
  area: GUI
  statement: "The library must be the catalogue: production groups above the debug groups (addressing, quad-structure, decoded-IC, validity, diagnostic, profiling), each row showing its mapping glyph, with a filter box over the entries."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 11. Presets = whole graphs
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 02 Stain — the plain node-graph editor
  verify:
    method: GUI screenshot
    detail: "screenshot against 02_stain.png's library; typing in the filter narrows rows"
  rulings: []
  milestone: M8
- id: REQ-GUI-140
  area: GUI
  statement: "The library must have a 'Your stains' group with New, Rename, Import and Export."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 11. Presets = whole graphs
  - file: docs/gui/principia_render_gui_spec.md
    section: G14. Settled by the notes (record)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 02 Stain — the plain node-graph editor
  verify:
    method: unit test
    detail: create, rename, export and re-import a stain; it round-trips
  rulings: []
  milestone: M8
- id: REQ-GUI-141
  area: GUI
  statement: "Clicking a quad must show its metadata as text (inspector drill-down)."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  verify:
    method: unit test
    detail: "click a quad: depth, generation, refine decision and residency are listed"
  rulings: []
  milestone: M8
- id: REQ-GUI-142
  area: GUI
  statement: "Corpus values must win over artboard values (palette hex codes, substep cap default 64, sound mapping), and the Run window must expose the contract-defined parameters under their contract names."
  source:
  - file: decisions.md
    section: "R-68 — Artboard values are illustrative; corpus values win *(closes RQ-24)*"
  verify:
    method: GUI screenshot
    detail: "Run window screenshot lists contract parameter names; substep cap default reads 64; palette colours equal corpus hex codes."
  rulings:
  - R-68
  milestone: M8
- id: REQ-GUI-143
  area: GUI
  kind: calibration
  statement: "The sonification reference pitch to which 1/t_c is mapped must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "4. Audio-specific constraints, which are NOT the frame budget"
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: review checklist
    detail: "the proposal shows the resulting pitch range for the figure-eight and a set of chaotic ICs stays within the audible range"
  rulings:
  - R-71
  - R-96
  - R-109
  milestone: M8
- id: REQ-GUI-144
  area: GUI
  kind: definition
  statement: "The sonification mapping's open choices — θ(t) alone or stereo θ(t)/φ(t), and the whole trajectory's spectrum or a windowed spectrum tracking the playhead — must be defined in pointer_channels §4 by the task that builds listen and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "4. Audio-specific constraints, which are NOT the frame budget"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: review checklist
    detail: "pointer_channels §4 states both choices; the reviewer checks they keep the trace and the sound one measurement (§2)"
  rulings:
  - R-72
  - R-96
  - R-109
  milestone: M8
- id: REQ-GUI-145
  area: GUI
  kind: definition
  statement: "The frame in which the Inspector's measurement readouts are shown (canonical R̃ = 1 or physical), so that they do not move under scale-all must be defined in ic_inspector_scratchpad's measurement-readouts section and render_gui_spec §G8 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: "Measurement readouts (gauge-invariant → hold under all four handles)"
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Whole-system handles — the invariance audit
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the docs name the frame per readout; the reviewer checks every listed readout is invariant under all four handles in that frame"
  rulings:
  - R-72
  milestone: M8
- id: REQ-GUI-146
  area: GUI
  kind: calibration
  statement: "The held-key repeat delay and repeat rate (the DAS / ARR model) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal gives the delay and rate with the reference they follow (e.g. platform defaults)"
  rulings:
  - R-71
  milestone: M8
- id: REQ-GUI-147
  area: GUI
  kind: definition
  statement: "The Measure tool's validation fixture of known dimension and its default pair classifier must be defined in render_gui_spec §G10 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§G10 names the fixture, its known dimension and the default classifier (e.g. outcome class)"
  rulings:
  - R-72
  milestone: M8
- id: REQ-GUI-148
  area: GUI
  kind: calibration
  statement: "The Measure tool's defaults — the ε range and the samples (pairs) per ε must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal shows the log-log fit's stability over the chosen ε range and sample count on the known-dimension fixture"
  rulings:
  - R-71
  milestone: M8
- id: REQ-GUI-149
  area: GUI
  kind: calibration
  statement: "The tolerance on the Measure tool's fitted α ± error against the known-dimension fixture must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows the fitted α on the fixture at the default settings and the tolerance with its margin"
  rulings:
  - R-71
  milestone: M8
- id: REQ-GUI-150
  area: GUI
  kind: calibration
  statement: "The Research tool's Newton convergence residual and its catalogue-match tolerance against the Šuvakov–Dmitrašinović catalogue must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.4 Šuvakov–Dmitrašinović (13 families, 2013) and Broucke–Hénon–Hadjidemetriou"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows residuals and period errors for seeds that refine to catalogue orbits and for seeds that do not"
  rulings:
  - R-71
  milestone: M8
- id: REQ-GUI-151
  area: GUI
  kind: definition
  statement: "The stain editor's right-click node palette contents (the source fields from the ctx contract and the post ops it lists) must be defined in render_gui_spec Part II §7 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 7. Canvas interactions
  - file: docs/gui/principia_render_gui_spec.md
    section: 16. Open / next
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: review checklist
    detail: "§7 lists the palette's groups and entries; the reviewer checks every ctx field and post op is reachable"
  rulings:
  - R-72
  - R-113
  milestone: M7
- id: REQ-GUI-152
  area: GUI
  kind: definition
  statement: "Which preview (the shape sphere or an illustrative slice) the Stain mode preview shows by default must be defined in render_gui_spec Part II §1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 1. Layout — four surfaces
  - file: docs/gui/principia_render_gui_spec.md
    section: 16. Open / next
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: review checklist
    detail: §1 names the default and §16's open item is closed
  rulings:
  - R-72
  - R-113
  milestone: M7
- id: REQ-GUI-153
  area: GUI
  kind: definition
  statement: "Where the Overlays menu's 'save as default' set and the saved views (pxpack snapshots) are stored must be defined in render_gui_spec §G2 and §G9 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§G2 and §G9 name each store; the reviewer checks neither is engine state the engine reads"
  rulings:
  - R-72
  milestone: M8
- id: REQ-GUI-154
  area: GUI
  kind: definition
  statement: "The linked views for side by side — a ViewUI item, distinct from the chart's link ids in SimConfig — must be defined (what a linked view holds and how the views share cursor and navigation) in render_gui_spec §G11 and gui_state_contract §2 by the task that builds the v2 research tools and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-106 — The link ids are the chart's link functions *(closes RQ-66)*"
  verify:
    method: review checklist
    detail: "§G11 and gui_state §2 define the linked views as ViewUI state, none of it on the sim key; the chart's link ids are unchanged"
  rulings:
  - R-72
  - R-106
  milestone: M8
  note: "R-106 withdraws R-96's last bullet: the link ids in SimConfig and on the sim key are the chart's link functions, already specified."
- id: REQ-GUI-155
  area: GUI
  kind: definition
  statement: "The quantity the scrubber's re-integration progress ('refining · 72%') reports must be defined in render_gui_spec §G2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: decisions.md
    section: R-66 — The time scrubber stays *(closes RQ-22)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section defines the percentage from engine state the snapshot carries, monotone during a re-integration and 100% exactly when the in-view figure has reached the playhead; physics reviewer approved"
  rulings:
  - R-66
  - R-72
  milestone: M8
- id: REQ-GUI-156
  area: GUI
  kind: definition
  statement: "The hover path's opacity law — the distance of z(t) from the slice plane (the component orthogonal to q₁, q₂, in the latent metric) and the fall-off from solid to faint must be defined in render_gui_spec §G2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives opacity as a function of the out-of-plane distance, with full opacity on the plane, and names the metric; physics reviewer approved"
  rulings:
  - R-72
  milestone: M8
- id: REQ-GUI-157
  area: GUI
  kind: calibration
  statement: "The turning shape sphere's rotation rate (Trajectory panel and Inspector pane 2) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the rate in degrees per second (frame-rate independent) with the reason it reads as 'slowly'; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M8
- id: REQ-GUI-158
  area: GUI
  kind: calibration
  statement: "The keyboard base step of each adjustable value and in-scope arrow action (pan, tilt, orbit, time step), to which Shift ×10 and Alt ×0.1 apply must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal gives a table of base steps per field kind and scope (absolute or relative to the field's range or the view), with the reasoning; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M8
- id: REQ-GUI-159
  area: GUI
  kind: definition
  statement: "The side-by-side difference view — what it differences (per-pixel payload field, class disagreement, or colour) and how the difference is coloured must be defined in render_gui_spec §G11 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G11. Research — first pass (v2) (`11_research.png`)
  - file: decisions.md
    section: "R-106 — The link ids are the chart's link functions *(closes RQ-66)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the difference as a function of the two views' samples at the linked cursor grid and its colouring; physics reviewer approved"
  rulings:
  - R-72
  - R-106
  milestone: M8
- id: REQ-GUI-160
  area: GUI
  kind: definition
  statement: "Where the user-side stores are kept — the chart builder's saved presets, the stain library's 'Your stains' and the per-choice warning suppressions (native and browser), and that none is engine state must be defined in render_gui_spec §G7 and §11 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G7. Chart builder (`03_chartbuilder.png`)
  - file: docs/gui/principia_render_gui_spec.md
    section: 11. Presets = whole graphs
  - file: docs/design/principia_memory_tiers.md
    section: "7. The \"are you sure?\" safety system (three severities, none blocking)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§G7 and §11 name each store and its location on native and in the browser, consistent with REQ-GUI-153's stores; the reviewer checks none is engine state the engine reads; physics reviewer approved"
  rulings:
  - R-72
  milestone: M8
- id: REQ-GUI-161
  area: GUI
  statement: "The Manifold view's Chart section must offer, when the chart is the shape sphere, the projection selector (equirectangular / the equal-area alternative) and the hemisphere toggle (one labelled hemisphere, or both with the redundancy flag)."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 3.3 The chart map
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: GUI screenshot
    detail: "with the shape-sphere chart, the Chart section shows both controls and each changes the figure; with any other chart neither is shown"
  rulings:
  - R-113
  milestone: M8
  note: "R-113 (RQ-95 M2-G5) places the controls; no artboard shows them."
- id: REQ-TOOL-001
  area: TOOL
  statement: "Each session must record once: device (GPU/CPU model, core counts, VRAM or unified memory size in its own field), backend and driver version, precision support and f64 rate, build (commit hash, profile, feature flags) and display (resolution, refresh, DPI scale)."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 2. What to record — and the rule that makes it useful
  verify:
    method: unit test
    detail: "the session header contains every field; unified memory recorded separately from VRAM"
  rulings:
  - R-56
  milestone: M0
- id: REQ-TOOL-002
  area: TOOL
  statement: "The telemetry/profiler artefact must be one self-describing, human-readable plain-text JSON file (profiler schema v1): a session header block then the frame records, carrying the build hash and full config, with telemetry §2's frame record and its five stages at the top level and nested scopes, GPU passes, allocations and events beneath."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: "5. The artefact: one file, plain text, readable by the sender"
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 5.5 Profiling is FIRST-CLASS, not a debug mode
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "the file parses as JSON against schema v1; the header holds the build hash and the config as the M0 contract skeleton (TASK-M0-16) serialises it; prin profile reads it"
  rulings:
  - R-56
  - R-113
  milestone: M0
  note: "R-113 (RQ-93 M0-6): the dev GUI profiler reading the same file, with REQ-GUI-039's provenance object as the header config, is verified by REQ-TOOL-098 (M8)."
- id: REQ-TOOL-003
  area: TOOL
  statement: "All bit-field accessors must use the u32 extractBits overload, never the sign-extending i32 one."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-186 — GitHub-hosted runners first; no self-hosted runner *(amends R-110, R-169, R-174)*"
  verify:
    method: unit test
    detail: "fields with the top bit set round-trip without sign extension on the GPU: GitHub-hosted macos-15 (Metal) and lavapipe, every commit (R-110, R-186)"
  rulings:
  - R-110
  - R-186
  milestone: M0
- id: REQ-TOOL-004
  area: TOOL
  statement: "The debug tooling must be built first in Phase 0 in the order 0a generation root + codegen self-test, 0b synthetic payload harness with field views, 0c the kernel bring-up mode followed by the fragment-side UV, DECODE and ROUNDTRIP presets as their dependencies land, then structural views as soon as RenderQuad is defined (the UV-passthrough coordinate view earliest of all) and cross-checks as their seams arrive."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: Build order within Phase 0 (the only forced staggering)
  - file: docs/design/principia_debug_tooling_plan.md
    section: Principle
  - file: docs/design/principia_core_design.md
    section: Through-line
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  verify:
    method: review checklist
    detail: "milestone plan and history show the debug surfaces green before the component they observe"
  rulings:
  - R-75
  milestone: M0
- id: REQ-TOOL-005
  area: TOOL
  statement: "The profiler schema v1 must be JSON with telemetry §2's frame record and its five stages (integrate / reduce / colour / upload / present) at the top level and nested scopes, GPU passes, allocations and events beneath them, the finer categories (quadtree, stain + style, IC decode, readback, egui) being scopes nested under the five stages."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: docs/gui/principia_render_gui_spec.md
    section: G13. Where the artboards are overridden
  verify:
    method: unit test
    detail: "validate an exported trace against the v1 JSON schema; the top level has exactly the five stages; every other scope has one of them as ancestor"
  rulings:
  - R-56
  milestone: M0
  note: The artboard donut's categories are overridden (§G13).
- id: REQ-TOOL-006
  area: TOOL
  statement: "`prin profile --scenario NAME --frames N --json PATH` must run a registered, deterministic scenario headless and write the v1 JSON."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "run a registered synthetic scenario that emits frame records for N frames twice: same frame count and the same scope / event sequence; an unregistered NAME is refused"
  rulings:
  - R-113
  milestone: M0
  note: "R-113 (RQ-93 M0-4): the deep_zoom_03 scenario and its 600-frame run are a separate M5 requirement."
- id: REQ-TOOL-007
  area: TOOL
  statement: "`prin profile diff BASE NEW --threshold P%` must exit non-zero when NEW regresses on BASE by more than P%."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  verify:
    method: unit test
    detail: "diff of a trace against a copy with one scope's p95 raised 6% exits non-zero at 5% and zero at 10%"
  rulings: []
  milestone: M0
- id: REQ-TOOL-008
  area: TOOL
  statement: "Profiler schema v1 must be JSON and a superset of telemetry §2: the frame record and its five stages at the top level, with nested scopes, GPU passes, allocations and events beneath."
  source:
  - file: decisions.md
    section: "R-56 — Profiler schema v1 is a superset of telemetry §2, in JSON *(GU-5, amended)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "Schema validation: a profiler dump parses as telemetry §2's frame record (five stages) and contains the nested sections."
  rulings:
  - R-56
  - R-113
  milestone: M0
  note: "R-113 (RQ-93 M0-4): R-56's 'the measurement struct lands with the first frame loop' is a separate M1 requirement."
- id: REQ-TOOL-009
  area: TOOL
  statement: "The debug presentation layer must provide dbg_cat (Okabe–Ito cycle ≤ 8 classes, golden-angle beyond), dbg_lin (viridis ramp), dbg_log (log-compressed), dbg_flag (green/red), dbg_hash_u32 (hashed colour of a raw word) and dbg_sentinel(x, frag_xy) (absence-NaN via exact bitcast → debug_invalid(frag_xy), the hatch; a stored sentinel such as −1.0 shown as its literal value on the ramp; suspect styling hook; R-136)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: "Presentation layer (hand-written, small, reused by every debug view)"
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  - file: decisions.md
    section: "R-136 — `debug_invalid(frag_xy)` draws the hatch; sentinels show their value *(closes RQ-114)*"
  verify:
    method: unit test
    detail: "each helper returns the stated colour for fixture inputs; dbg_sentinel(-1.0, p) equals the ramp's colour for −1.0; NaN bits → debug_invalid(p), the hatch (R-136)"
  rulings:
  - R-16
  - R-136
  milestone: M1
- id: REQ-TOOL-010
  area: TOOL
  statement: "The debug views must include live shape views (mode 0: 0.5·(n+1) direction cosines of current n; mode 1: Twilight cyclic of unwrapped θ̃; mode 2: |n|−1 error view expected flat zero), accumulator views (running S/t, Welford slope C_ty/C_tt, drift running-max vs final), a word inspector (reduced length styled invalid when truncated, symbol-at-k slider, truncated flag) and ICDescriptor views."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Live-state & array inspection
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-122 — The reference HTML files are the colour oracle *(closes RQ-90 and RQ-101)*"
  - file: decisions.md
    section: "R-153 — The debug and live-march views have golden images of their own *(closes RQ-123)*"
  - file: decisions.md
    section: R-158 — The six readings are accepted *(closes RQ-128)*
  verify:
    method: golden image
    detail: "each view selectable in the debug picker renders on a synthetic payload and matches its own golden image, recorded at the M1 gate (R-153)"
  rulings:
  - R-113
  - R-122
  - R-153
  - R-158
  milestone: M1
  note: "R-113 (RQ-94): the real-march |n|−1 check and the live effort heatmap are a separate M3 requirement. LUT data (Twilight, Viridis): the published matplotlib tables (R-122)."
- id: REQ-TOOL-011
  area: TOOL
  statement: "The field-view catalogue must cover every group in render contract Part 6's field-view table that a synthetic payload can show: the UV fragment preset, all 12 ICDescriptor fields, the SimState scalars, the 10 sample_descriptor bits (including last_symbol), times and derived fields, live shape/accumulator views, word views including the whole-word hash, the optional Fourier block, all 9 quad fields plus payload/status flags, structural overlays and the uniform echo (quality_tier, n_renorm, thresholds)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  - file: decisions.md
    section: "R-111 — `SimResult` → `SimState`, `M` → `n_renorm`; the vocabulary lint covers the docs *(closes RQ-80)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: review checklist
    detail: "tick each table row against the generated catalogue on a synthetic payload; the DECODE row is REQ-RENDER-027's (M2), the ensemble rows and the live effort heatmap are the M5 and M3 requirements R-113 split off; the uniform echo reads n_renorm, not M (R-111)"
  rulings:
  - R-62
  - R-75
  - R-86
  - R-111
  - R-113
  milestone: M1
  note: "R-113 (RQ-94): DECODE row → REQ-RENDER-027 (M2); tier-gated ensemble views → a separate M5 requirement; the live current-substep effort heatmap → the M3 requirement split from REQ-TOOL-010. R-111: the uniform echo's `M` is `n_renorm`."
- id: REQ-TOOL-012
  area: TOOL
  statement: "Field views must keep the NaN absence sentinel and the −1 sentinel distinct — NaN hatched by debug_invalid(frag_xy), −1 shown as its literal value on the ramp (R-136) — so a NaN pixel reads as 'unavailable', not as data, and must otherwise show literal stored values (a failed-state 0.0 reads as 0.0, cross-checked against state), with NaN always mapped to the invalid colour."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: docs/design/principia_colour_composition.md
    section: 3. The `ctx` contract
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  - file: decisions.md
    section: R-79 — NaN and sentinels *(closes RQ-30)*
  - file: docs/gui/principia_render_gui_spec.md
    section: 10.1 The shared prelude library
  - file: decisions.md
    section: "R-136 — `debug_invalid(frag_xy)` draws the hatch; sentinels show their value *(closes RQ-114)*"
  verify:
    method: golden image
    detail: "synthetic payload with NaN-absent ftle and diffusion = −1 renders ftle hatched and diffusion = −1 as its literal ramp value (R-136); a forced-failure sample's d_min renders as its literal 0.0"
  rulings:
  - R-79
  - R-136
  milestone: M1
- id: REQ-TOOL-013
  area: TOOL
  statement: "The kernel must keep exactly one debug mode — colour_composition Appendix A's bring-up mode — as a baked pre-built kernel variant (not a dispatch flag bit) that skips integration and writes its known pattern into the existing payload slots (no new buffers), and UV, DECODE and ROUNDTRIP must not be kernel variants."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  - file: decisions.md
    section: "R-41 — `DEBUG_MODE` uses the baked variants, not flag bits *(RS-1 (b))*"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "A. Kernel debug dispatch modes (skip integration; reuse payload slots as scratch)"
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: docs/design/principia_colour_composition.md
    section: Appendix A — kernel-side bring-up mode
  verify:
    method: unit test
    detail: "the kernel's debug variants are exactly the bring-up variant; dispatch-flag bits 6–7 stay reserved (R-41); the bring-up output decodes via the normal unpack path"
  rulings:
  - R-41
  - R-75
  milestone: M1
- id: REQ-TOOL-014
  area: TOOL
  statement: "The rendering substrate and debug catalogue must be testable with synthetic CPU-filled payloads before any physics exists, and the build order must be contract + SDK → debug catalogue → flat compute → quads → adaptive."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  verify:
    method: review checklist
    detail: "a test uploads a CPU-filled SimState/ICDescriptor/RenderQuad set and renders each debug view; plan order follows the stated sequence"
  rulings: []
  milestone: M1
- id: REQ-TOOL-015
  area: TOOL
  statement: "A single kernel-side bring-up mode must exist that writes a known pattern instead of physics; it must be sim-key and monomorphised on the Rust side."
  source:
  - file: docs/design/principia_colour_composition.md
    section: Appendix A — kernel-side bring-up mode
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  verify:
    method: unit test
    detail: "enabling the bring-up variant writes the known pattern into the payload; it changes the sim key"
  rulings:
  - R-75
  milestone: M1
- id: REQ-TOOL-016
  area: TOOL
  statement: "The sub-field debug views must show live, sane values for every ledger field."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 4. Seams (obligations → integration tests)
  verify:
    method: golden image
    detail: "visual bit-layout test renders each sub-field view on a fixture quad"
  rulings: []
  milestone: M1
- id: REQ-TOOL-017
  area: TOOL
  statement: "One generated accessor core (WGSL for the fragment, Rust for kernel/host) must be called by both the debug shader and the test assertion for every field."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: Principle
  verify:
    method: unit test
    detail: "shader and test reference the same generated accessor symbols"
  rulings: []
  milestone: M1
- id: REQ-TOOL-018
  area: TOOL
  statement: "Every payload surface must be validated against CPU-filled synthetic SimState/ICDescriptor buffers (a set state, a known escaper, a ramp in energy_drift, a bitwise-adversarial descriptor), asserting both that the accessor returns the value and that the shader renders it, before the decoder exists."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: Principle
  - file: docs/design/principia_debug_tooling_plan.md
    section: Build order within Phase 0 (the only forced staggering)
  verify:
    method: golden image
    detail: "synthetic buffers render the expected golden images and accessor tests pass"
  rulings: []
  milestone: M1
- id: REQ-TOOL-019
  area: TOOL
  statement: "The UV preset (fragment-side, from the quad-local uv in ctx) must reconstruct quad-local (u, v) equal to c ± h·(2t−1) for sampled quads with no banding (adjacent-sample deltas smooth, not step-quantised)."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "A. Kernel debug dispatch modes (skip integration; reuse payload slots as scratch)"
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: docs/design/principia_colour_composition.md
    section: 6. Debug views as presets
  verify:
    method: unit test
    detail: "sampled quads' reconstructed (u, v) match the formula; adjacent deltas vary smoothly"
  rulings:
  - R-75
  milestone: M1
- id: REQ-TOOL-020
  area: TOOL
  statement: "Every ledger field must have exactly one generated debug view and one test, generated from the ledger so the catalogue is exhaustive by construction."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "C. Payload field views — `times` (u32), f16-packed scalars & `free_group_word` (separate buffer)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: D. Payload field views — `SimState` scalars (ledger §3.4)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "E. Payload field views — `ICDescriptor` (64 B) & the live-state block"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: unit test
    detail: the catalogue's field list equals the ledger's field list
  rulings: []
  milestone: M1
- id: REQ-TOOL-021
  area: TOOL
  statement: "The state field (bits 0–2) must round-trip values 0–5 with the six-colour dbg_cat categorical palette for the six states (escape, bounded, collision, running = 3, sim_failed, decode_failed), each rendered distinctly."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: decisions.md
    section: "R-115 — The raw `state` view keeps six colours *(closes RQ-83)*"
  verify:
    method: unit test
    detail: "sd_state round-trips 0–5; golden image shows six distinct colours"
  rulings:
  - R-53
  - R-96
  - R-115
  milestone: M1
  note: "R-53: the node inherits the manifest's per-field domain. R-115: the raw state view keeps a six-colour dbg_cat palette; R-77's nine-class palette (colour_composition §1.4, read per R-96) governs the outcome palette (state ⊕ detail) only."
- id: REQ-TOOL-022
  area: TOOL
  statement: "The detail field (bits 3–4) must decode per state (escape → body, collision → pair, sim/decode_failed → failure category, undefined while running) with a legend keyed by state, and a regression test must prove detail is read, not dropped."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: H. Codegen self-test (the tooling that tests the tooling)
  - file: docs/design/principia_dd_generation_root.md
    section: 5. Tests (properties any generator must satisfy)
  verify:
    method: unit test
    detail: "three-colours-bug regression: escapes of different bodies render different colours"
  rulings:
  - R-22
  - R-53
  milestone: M1
- id: REQ-TOOL-023
  area: TOOL
  statement: "f16-packed scalars (d_min, dE_max, dLz_max) must round-trip through unpack2x16float within f16 eps, and sentinels (including diffusion's −1) must be bit-exact and shown as their literal values on the ramp, never scaled (R-136)."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: D. Payload field views — `SimState` scalars (ledger §3.4)
  - file: docs/design/principia_debug_tooling_plan.md
    section: H. Codegen self-test (the tooling that tests the tooling)
  - file: decisions.md
    section: "R-136 — `debug_invalid(frag_xy)` draws the hatch; sentinels show their value *(closes RQ-114)*"
  verify:
    method: unit test
    detail: "pack/unpack within f16 eps; sentinel values render as their literal values on the ramp (R-136)"
  rulings:
  - R-17
  - R-136
  milestone: M1
- id: REQ-TOOL-024
  area: TOOL
  statement: "Derived views (orbit_count / retrograde from θ̃, the reduced crossing count, ftle = S_final/(step_count·dt) with the partial renorm finalised, current drift H(r,p) − E_0) must each be computed in the fragment and match a reference computed the same way."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  verify:
    method: unit test
    detail: each derived view equals a CPU reference on synthetic states
  rulings: []
  milestone: M1
- id: REQ-TOOL-025
  area: TOOL
  statement: "The whole-word hash view must give distinct words distinct colours, and the symbol-at-k view must decode fgw_symbol(word, k) per slot on the cold path."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "C. Payload field views — `times` (u32), f16-packed scalars & `free_group_word` (separate buffer)"
  verify:
    method: unit test
    detail: "distinct synthetic words hash to distinct colours; symbol-at-k matches the appended sequence"
  rulings: []
  milestone: M1
- id: REQ-TOOL-026
  area: TOOL
  statement: "Structural views over RenderQuad metadata must include quad-depth, quad-state (loaded / pending / refinable / terminal / stale), coherence/impurity (= reduction output), ensemble spread (present iff contains-ensemble), suspect fraction, priority score, cache age / ancestor gap, leaf outlines, fallback tint and pending hatch."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  verify:
    method: golden image
    detail: "each structural view renders from a synthetic RenderQuad set; asserted values match the CPU structs"
  rulings: []
  milestone: M1
- id: REQ-TOOL-027
  area: TOOL
  statement: "The UV passthrough coordinate view must show raw post-flip (u, v) as u→red, v→green (plus quad-local δ mode) running bottom-left-origin, Y-up, and a picking cross-check must report v ≈ 1 for a top-left click and v ≈ 0 for bottom-left."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: Build order within Phase 0 (the only forced staggering)
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "The catch: a UV-passthrough debug view (debug-first)"
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  verify:
    method: golden image
    detail: "green increases upward in the golden image; picking returns the expected corners"
  rulings: []
  milestone: M1
- id: REQ-TOOL-028
  area: TOOL
  statement: "Observation taps (debug catalogue, hover trace, sonification, telemetry) must read payloads and uniforms only and never perturb results."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: property test
    detail: "payload bytes are identical with all debug views and telemetry on versus off"
  rulings: []
  milestone: M1
- id: REQ-TOOL-029
  area: TOOL
  statement: "The DECODE preset (fragment-side decode of ctx.chart.z) must output decoded (m, r, p) equal to the f64 decodeOnly() to Tier-N tolerance, satisfying Σm = 1, CoM = 0 and I = 1."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "A. Kernel debug dispatch modes (skip integration; reuse payload slots as scratch)"
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: decisions.md
    section: "R-85 — Native wgpu sets the Tier-N tolerances *(closes RQ-36)*"
  - file: decisions.md
    section: "R-133 — The seven checkpoint-B interpretations are accepted *(closes RQ-111)*"
  verify:
    method: unit test
    detail: "GPU decoded ICs vs f64 decodeOnly within REQ-DEC-043's calibrated f32 decode factor (R-133; Tier N, REQ-VAL-064, is set only at M4); identities hold"
  rulings:
  - R-75
  - R-85
  - R-133
  milestone: M2
  note: "R-133: before REQ-VAL-064 sets Tier N (M4), 'Tier-N tolerance' here is read as REQ-DEC-043's calibrated f32 decode factor."
- id: REQ-TOOL-030
  area: TOOL
  statement: "ICDescriptor views must equal the f64 decode values (masses, virial_ratio, rho_ratio, rho_angle, K_0, V_0, r_min_pair_0), certifying the decoder independent of integration."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "E. Payload field views — `ICDescriptor` (64 B) & the live-state block"
  verify:
    method: unit test
    detail: GPU descriptor fields vs f64 decode within tolerance
  rulings: []
  milestone: M2
- id: REQ-TOOL-031
  area: TOOL
  statement: "The occupant-comparison harness must refuse to run arms that differ in stepper or step control."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "The comparison discipline is part of the seam, NOT a convention"
  verify:
    method: unit test
    detail: "configure two arms with different steppers (e.g. GBS logH vs RK4 Heggie) -> harness returns an error, not a result"
  rulings: []
  milestone: M3
- id: REQ-TOOL-032
  area: TOOL
  statement: "Every arm of an occupant comparison must use the same fixed colour-ramp constants (no per-panel auto-ranging) and print its full provenance including the occupant and its profile."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "The comparison discipline is part of the seam, NOT a convention"
  verify:
    method: review checklist
    detail: "comparison output: ramp constants identical across panels; each arm's provenance block names stepper, regularisation and full profile"
  rulings: []
  milestone: M3
- id: REQ-TOOL-033
  area: TOOL
  statement: "The saturated flag (bit 5) must be sticky and set iff N_sub == N_max occurred at some macro-step."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "a trajectory that hits the cap once then relaxes keeps saturated = 1; with a non-power-of-two N_max the flag sets exactly when N_sub reaches N_max"
  rulings:
  - R-68
  - R-86
  milestone: M3
  note: "R-68: the substep cap default is 64"
- id: REQ-TOOL-034
  area: TOOL
  statement: "total_substeps must be an exact u32 matching Σ N_sub, with the log proxy ⌊log₂ Σ N_sub⌋ (0 for a total ≤ 1) derived at read via countLeadingZeros."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "C. Payload field views — `times` (u32), f16-packed scalars & `free_group_word` (separate buffer)"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "proxy equals ⌊log₂⌋ of the exact count at powers of two and their neighbours; 0 for totals 0 and 1"
  rulings:
  - R-86
  milestone: M3
- id: REQ-TOOL-035
  area: TOOL
  statement: "fgw_truncated must be the length sentinel length_raw == 127 in the word buffer's .w bits 25–31 (no flag bit), firing iff a non-cancelling push arrives at length 76 (the length cap, not numeric overflow); fgw_reduced_length must count net reduced crossings and be valid only when not truncated, and word-derived quantities must be styled invalid once truncated."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  - file: docs/design/principia_debug_tooling_plan.md
    section: "C. Payload field views — `times` (u32), f16-packed scalars & `free_group_word` (separate buffer)"
  - file: decisions.md
    section: "R-70 — Where two docs conflict, the later consolidated doc wins"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "push a 77th symbol: length reads 127, including for a word whose numeric W is small; reduction pops decrement the length"
  rulings:
  - R-70
  - R-86
  milestone: M3
- id: REQ-TOOL-036
  area: TOOL
  statement: "SimState scalar views must use the ledger scale per field (signed drift fields on diverging scales, not sequential-log) and assert: t_end_step is the completed-step count latched at termination; d_min > 0 and is the trajectory minimum; delta_E_max_abs ≥ |energy_drift| and delta_Lz_max_abs ≥ |Lz_drift|; E_0 = K₀ + V₀; ftle ≈ 0 on Kepler-embedded ICs and large on Burrau; energy_drift oscillates for symplectic steppers."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: D. Payload field views — `SimState` scalars (ledger §3.4)
  verify:
    method: unit test
    detail: each assertion on synthetic and golden-IC payloads
  rulings: []
  milestone: M3
- id: REQ-TOOL-037
  area: TOOL
  statement: "Live shape views (u_mode 0 dir-cosines, 1 running θ̃, 2 |n| − 1) must show ‖n‖ = 1 (flat zero error), θ̃ continuous with no 2π jumps, and terminal-latched samples frozen; accumulator views must show live FTLE S/t, the Welford diffusion slope and drift max-vs-final."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "E. Payload field views — `ICDescriptor` (64 B) & the live-state block"
  verify:
    method: golden image
    detail: "golden playback frames: mode-2 view is flat zero; θ̃ view has no seams"
  rulings:
  - R-71
  milestone: M3
  note: "Tolerance on ‖n‖ = 1: REQ-VAL-122 (calibration)."
- id: REQ-TOOL-038
  area: TOOL
  statement: "A diagnostic that re-runs trajectories must reuse the original discretisation (same n_sync and t_max grid), never rescale it per window."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  verify:
    method: unit test
    detail: "The diagnostic harness asserts its n_sync, t_max and step parameters equal those of the run under test and refuses to run otherwise; regression: a persistence check with per-window rescaled n_sync is rejected."
  rulings: []
  milestone: M3
  note: "pitfall: \"3. Standing rules earned in this sequence\" — 3. — A DIAGNOSTIC MUST REUSE THE ORIGINAL DISCRETISATION."
- id: REQ-TOOL-039
  area: TOOL
  statement: "dt_max must not fold from 0.0: a pixel with no usable step must report 'no usable step' distinguishably from a step of zero."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN
  verify:
    method: unit test
    detail: "Fixture with all steps unusable: assert dt_max is the explicit none/sentinel, not 0.0; fixture with the 2.209e128 overflow case is reported."
  rulings: []
  milestone: M3
  note: "pitfall: \"9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN\" — 9. — same family: dt_max folding from 0.0."
- id: REQ-TOOL-040
  area: TOOL
  statement: "The pixel inspector (click any pixel → trajectories in shape and real space) must ship on day one, running the shared kernel on the CPU at f64 or higher with adaptive stepping, the on-demand single-IC f32 GPU trace, and an overlay/diff of both that exposes divergence time and separation growth without reconciling them."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "7. Precision & validation model *(authoritative: `parity_contract`, `validation_ground_truth_note`, `core_design`)*"
  - file: docs/design/principia_core_design.md
    section: "Principle: divergence is the observable"
  - file: docs/design/principia_core_design.md
    section: "4. Integrate and colour are separate passes — and now separate *mechanisms*"
  verify:
    method: unit test
    detail: "on a Burrau IC the inspector returns both traces and a divergence time; no path nudges one trace toward the other"
  rulings: []
  milestone: M4
- id: REQ-TOOL-041
  area: TOOL
  statement: "The inspector must offer a match-integrator mode (same occupant, coefficients and step size as the active GPU pipeline) and reserve adaptive RK45 for chasing near-collision singularities."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 6 — Three boundaries stated honestly (deferred work lands here)"
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: unit test
    detail: "match-integrator mode binds the active GPU occupant/params on the f64 CPU build; RK45 is not a wrapper occupant"
  rulings: []
  milestone: M4
- id: REQ-TOOL-042
  area: TOOL
  statement: "Debug views must be second-pass WGSL colourings of payload fields, and the CPU trajectory diff must be the same kernel at two precisions, not two integrators."
  source:
  - file: docs/design/principia_core_design.md
    section: "4. Integrate and colour are separate passes — and now separate *mechanisms*"
  verify:
    method: review checklist
    detail: "debug views are fragment occupants; the diff calls the shared kernel twice"
  rulings: []
  milestone: M4
- id: REQ-TOOL-043
  area: TOOL
  statement: "The instrument must declare its predictability horizon, t_max(IC) = ln(1/eps)/ftle(IC), available as a horizon map computed from the Benettin FTLE (never the ensemble spread), with the finite-time-lambda caveat carried by any such figure."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 4.2 The horizon is a field, not a constant
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 4.3 It bounds what the renderer may claim
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 5. Theoretical framing, and its use in the papers
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.1 The instrument must say where it stops knowing
  verify:
    method: unit test
    detail: "horizon map equals ln(1/eps)/ftle per sample; no code path feeds it ensemble spread"
  rulings: []
  milestone: M4
- id: REQ-TOOL-044
  area: TOOL
  statement: "The f32 closure field must not be used to rank orbits, and no near-periodic pixel may be reported as a periodic solution until confirmed by an f64 closure test."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 6.4 Two cautions
  verify:
    method: review checklist
    detail: "any orbit-candidate report routes through the f64 inspector closure test; UI does not rank by f32 closure"
  rulings: []
  milestone: M4
- id: REQ-TOOL-045
  area: TOOL
  statement: "A live view must render an animated heatmap of the current macro-step's substep count read off the marching state."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  verify:
    method: golden image
    detail: "a close-encounter IC shows a hot spot that moves over playback"
  rulings: []
  milestone: M4
- id: REQ-TOOL-046
  area: TOOL
  statement: "The global playhead must advance by fixed dt through the frame loop with every visible pixel's SimState marching with it and no stored temporal data, so exported animations have no scrub."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  verify:
    method: unit test
    detail: "no per-pixel history buffer exists; the playhead advances dt per step"
  rulings:
  - R-66
  milestone: M5
- id: REQ-TOOL-047
  area: TOOL
  statement: "Pause must freeze the playhead but not the compute: navigation still works, and revealed quads catch up to the frozen t in the background and promote."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  verify:
    method: unit test
    detail: "pause, pan: new quads catch up to the frozen t and promote; the playhead stays fixed"
  rulings: []
  milestone: M5
- id: REQ-TOOL-048
  area: TOOL
  statement: "Restart must set the playhead to 0 and discard and re-decode the live-set states."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  verify:
    method: unit test
    detail: "after restart all visible SimStates equal fresh decodes at t = 0"
  rulings: []
  milestone: M5
- id: REQ-TOOL-049
  area: TOOL
  statement: "Loop must restart automatically at t_end, the horizon T or the moment every visible sample has latched terminal."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 1 — Playback is the temporal mechanism
  verify:
    method: unit test
    detail: "a region that fully latches before T restarts at the latch time"
  rulings: []
  milestone: M5
- id: REQ-TOOL-050
  area: TOOL
  statement: "Every frame record must carry frame_ms, quads_computed, quads_reused, samples, substeps_total, playhead_dt, camera_delta, tree_depth_max (and leaf count) and stage_ms broken down into integrate / reduce / colour / upload / present."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 2. What to record — and the rule that makes it useful
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 5.5 Profiling is FIRST-CLASS, not a debug mode
  verify:
    method: unit test
    detail: a recorded frame deserialises with every field present
  rulings:
  - R-56
  milestone: M5
- id: REQ-TOOL-051
  area: TOOL
  statement: "Frame-time reports must give p50, p95, p99 and max (never only a mean), separate moving from still frames by camera_delta > 0, and headline frac_frames_over_41.7ms during motion."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 3. Percentiles, not means — and the specific thresholds
  verify:
    method: unit test
    detail: "synthetic frame log: percentiles and the motion-only headline match hand-computed values"
  rulings: []
  milestone: M5
- id: REQ-TOOL-052
  area: TOOL
  statement: "The telemetry file size must be bounded by downsampling on write (every frame during motion, every Nth while idle) or rolling up idle stretches into summaries."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: "5. The artefact: one file, plain text, readable by the sender"
  verify:
    method: unit test
    detail: "a simulated long 60 fps session with idle stretches produces a file below a bound and keeps every motion frame"
  rulings:
  - R-71
  milestone: M5
  note: "Which scheme, and N: REQ-TOOL-117 (calibration)."
- id: REQ-TOOL-053
  area: TOOL
  statement: "stage_ms and the frame record must be measured in every build and never compiled out, with only reporting toggleable, and the overhead small enough to leave on."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 5.5 Profiling is FIRST-CLASS, not a debug mode
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: benchmark
    detail: "release build emits stage timings; measured instrumentation overhead is negligible against 16.7 ms"
  rulings:
  - R-56
  milestone: M5
- id: REQ-TOOL-054
  area: TOOL
  statement: "The batch renderer (prin) and the interactive path must share the frame record: a batch render emits the same structure with camera_delta = 0 and no present stage, parsed by one parser and one percentile code."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 5.5 Profiling is FIRST-CLASS, not a debug mode
  verify:
    method: unit test
    detail: a prin batch output parses with the interactive parser
  rulings:
  - R-56
  milestone: M5
- id: REQ-TOOL-055
  area: TOOL
  statement: "Per chart, the tooling must report leaf count, alpha distribution and tree shape, and must compare leaf counts only within a chart, never across charts."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.3 What to report per chart
  verify:
    method: review checklist
    detail: "per-chart report contains the three quantities; cross-chart comparison views use the alpha distribution, not leaf counts"
  rulings: []
  milestone: M6
- id: REQ-TOOL-056
  area: TOOL
  statement: "Stop-reason breakdowns must be reported weighted by the subtree each stop forecloses, and a leaf count must never be quoted without its stop breakdown."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 6. Decision variants, and the second budget line
  - file: docs/read_first/principia_01_pitfalls.md
    section: "7. A STOP-REASON BREAKDOWN NEEDS WEIGHTING BY WHAT IT FORECLOSES"
  verify:
    method: unit test
    detail: "near-field fixture: the 4 level-2 floor leaves report ≈ the whole frame's foreclosed weight"
  rulings: []
  milestone: M6
  note: "pitfall: \"7. A STOP-REASON BREAKDOWN NEEDS WEIGHTING BY WHAT IT FORECLOSES\" — 7. A STOP-REASON BREAKDOWN NEEDS WEIGHTING BY WHAT IT FORECLOSES."
- id: REQ-TOOL-057
  area: TOOL
  statement: "The binding axis (converged / budget-bound / cap-bound) must be reported per frame, and a cap-bound image must say it is not what was asked for."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: ALWAYS REPORT WHICH AXIS BOUND
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: v0.5 means the NUMBERS are guesses; the SHAPE is not
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 4. Deriving the tier instead of choosing it
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Collect everything relevant, in one file
  - file: docs/design/principia_memory_tiers.md
    section: "Principia — quality tiers & memory model (auto-mode reference)"
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  verify:
    method: unit test
    detail: force each binding case; the frame record and UI report it
  rulings:
  - R-47
  milestone: M6
- id: REQ-TOOL-058
  area: TOOL
  statement: "A debug overlay for the arbiter, in the Profiler window's Arbiter tab (at M6 a minimal Profiler window shell holding that tab, the rest filled in at M8; R-152), must show estimated throughput, current rung, headroom, and recent decisions with their reasons."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "10. Two sanctities: the user, and observability"
  - file: decisions.md
    section: "R-129 ✱ — Where the surfaces with no artboard live *(closes RQ-105)*"
  - file: decisions.md
    section: "R-152 — A minimal Profiler window holds the Arbiter tab at M6 *(closes RQ-122)*"
  verify:
    method: GUI screenshot
    detail: "presence only (R-129; no layout comparison until the M8 dev GUI): the Profiler tab shows the four items"
  rulings:
  - R-129
  - R-152
  milestone: M6
- id: REQ-TOOL-059
  area: TOOL
  statement: "Share-export PNGs must embed the provenance payload in the low bit of every RGB channel as whole records in a 2-D square tile grid, with the alpha low bit carrying a second systematic copy."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 2. Layout
  - file: docs/design/principia_dd_image_embedding.md
    section: 3. Tiles, not raster order
  verify:
    method: unit test
    detail: "embed then read with alpha stripped: RGB alone recovers the payload"
  rulings: []
  milestone: M7
- id: REQ-TOOL-060
  area: TOOL
  statement: "Each embedded record must be header(16 B: magic 4, version 1, flags 1, payload_len 4, n_records 2, crc32(header) 4) ‖ payload ‖ crc32(payload), trusted or discarded whole."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 2. Layout
  verify:
    method: unit test
    detail: "corrupt one byte in a record: that record is discarded, others decode"
  rulings:
  - R-71
  - R-72
  milestone: M7
  note: "Magic, version byte, flag bits, byte and bit order, payload serialisation and tEXt keyword: REQ-TOOL-109 (calibration); REQ-TOOL-110 (definition)."
- id: REQ-TOOL-061
  area: TOOL
  statement: "The reader must collect every intact record and majority-vote per byte."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 2. Layout
  verify:
    method: unit test
    detail: "with a minority of records carrying consistent corrupted-but-CRC-passing bytes the majority wins"
  rulings: []
  milestone: M7
- id: REQ-TOOL-062
  area: TOOL
  statement: Tile side must be ceil(sqrt(ceil(record_bits / 3))).
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 3. Tiles, not raster order
  verify:
    method: unit test
    detail: tile side for the measured 382 B record
  rulings: []
  milestone: M7
- id: REQ-TOOL-063
  area: TOOL
  statement: "The reader must search tile offset (dx,dy), the 8 dihedral transforms and nearest-neighbour decimation factors, using the CRC as the oracle, and must report what it found (transform, decimation, tiles recovered)."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 4. The three searches on read
  verify:
    method: unit test
    detail: "recover after 37 px crop, rot270, ×2 upscale; report {transform, decimate, tiles}"
  rulings: []
  milestone: M7
- id: REQ-TOOL-064
  area: TOOL
  statement: "The tiled variant must be the default, with a hybrid (per-bit k-fold redundancy plus tiling) variant available for arbitrary rotation."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 5. Two variants, one choice
  verify:
    method: unit test
    detail: "hybrid variant recovers after 7°, 34°, 45°, 7.3°, 34.7° rotation"
  rulings:
  - R-71
  milestone: M7
  note: "Default k: REQ-TOOL-111 (calibration)."
- id: REQ-TOOL-065
  area: TOOL
  statement: "The embedded payload must carry the build hash, the slice (chart id + params, z₀ over the 8-D latent, q₁ and q₂ with zoom as their common scale, slice values, lock flag, z_locked, δ), the sim settings (T_horizon, dt_macro, N_max, r_sub, gamma_sub, r_coll, r_close, eps_E, eps_L, tau and the escape window, and the integrator occupant as stepper × regularisation), the ensemble (E, N, with no jitter field), the full colour spec including brightness field, polarity and ramp window, and the tier settings used, every field named as the contracts name it."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 6. What travels
  - file: decisions.md
    section: "R-81 — The embedded record uses the contract names *(closes RQ-32)*"
  verify:
    method: unit test
    detail: "embed → extract round-trips every listed field; field names match gui_state_contract §2 and integrator_contract Part 3; the record has no jitter_frac and no 10-component z0"
  rulings:
  - R-80
  - R-81
  - R-83
  milestone: M7
- id: REQ-TOOL-066
  area: TOOL
  statement: "Recreating a slice from an embedded payload whose build hash indicates a different decoder must be refused rather than done silently."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 6. What travels
  verify:
    method: unit test
    detail: payload with a mismatched build hash yields a refusal
  rulings:
  - R-72
  milestone: M7
  note: "Which hash component detects a decoder change: REQ-TOOL-112 (definition)."
- id: REQ-TOOL-067
  area: TOOL
  statement: "Colour modes must be embedded as enum IDs plus parameters, with WGSL source embedded only for genuinely ejected nodes, and any fallback to a different colouring on recreate must be visible to the user."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 6. What travels
  verify:
    method: unit test
    detail: "default preset payload contains no source; ejected node payload contains its source; unknown enum id triggers a visible fallback notice"
  rulings: []
  milestone: M7
- id: REQ-TOOL-068
  area: TOOL
  statement: "The embedding must survive PNG save/load (incl. optimize and compress_level 9), alpha stripping, crops (50%, arbitrary 37 px offset), rotations by 90/180/270, flips, transpose, nearest upscale ×2/×3 (also combined with rot90) and overwrite of the top 50%, with max pixel delta 1."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 7. Measured capacity and behaviour
  verify:
    method: property test
    detail: the §7 'Survives' list as a test suite at 512²
  rulings: []
  milestone: M7
- id: REQ-TOOL-069
  area: TOOL
  statement: "From 128² up, an embedded image must carry the config plus the full shader source."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 7. Measured capacity and behaviour
  verify:
    method: numerical gate
    detail: capacity at 128² ≥ 1 tile of config + full shader
  rulings: []
  milestone: M7
- id: REQ-TOOL-070
  area: TOOL
  statement: "Pixel embedding must be off by default for figure export and on by default for share export, with a tEXt chunk carrying the same payload alongside."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 9. Obligations
  verify:
    method: unit test
    detail: "figure export pixels equal renderer output bit-for-bit; share export has LSB payload and a tEXt chunk"
  rulings: []
  milestone: M7
- id: REQ-TOOL-071
  area: TOOL
  statement: "The reader must report exactly one of three distinguishable outcomes: no embedded state, state present but corrupt, or recovered with how."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 9. Obligations
  - file: docs/design/principia_dd_image_embedding.md
    section: 4. The three searches on read
  verify:
    method: unit test
    detail: "plain PNG → none; corrupted all records → corrupt; valid → recovered + transform report"
  rulings: []
  milestone: M7
- id: REQ-TOOL-072
  area: TOOL
  statement: "In-pixel LSB embedding must carry the full slice, sim, colour mode and ramp window (plus a custom shader's source from 128² up) with a max pixel delta of 1/255, must survive crop, rotation, flip, PNG re-encode and alpha stripping, and must be off by default for figure export."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: unit test
    detail: "embed, apply each transform, recover the config; max |Δ| per channel ≤ 1; figure export default has embedding off"
  rulings: []
  milestone: M7
- id: REQ-TOOL-073
  area: TOOL
  statement: "Checkerboard must be forced off in export and screenshots regardless of the setting."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 3. The three-state control
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "5. Self-erasing: catch-up at rest, endpoints, and pause"
  verify:
    method: golden image
    detail: "export with checkerboard permanent matches the export with it off, bit-for-bit"
  rulings: []
  milestone: M8
- id: REQ-TOOL-074
  area: TOOL
  statement: "The export planner must decompose a mixed animation per keyframe segment into fixed-view time playback, view animation (a blocking 0 → t_frame march per frame) or render-only animation (recolour, no integration), price each, and report the estimate before the job runs."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 2 — The two playback types and their costs
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 3 — The keyframe system (retained)
  verify:
    method: unit test
    detail: "a three-segment spec is classified (a) / (b) / (c) and an estimate is shown before start; the (c) segment triggers no integration"
  rulings: []
  milestone: M8
- id: REQ-TOOL-075
  area: TOOL
  statement: "Keyframe interpolation must ease z₀ componentwise, log-lerp zoom, interpolate the basis along the Grassmannian geodesic re-orthonormalised per frame (never vector lerp), ease slice values, lerp render uniforms per their scale metadata, and treat chart switches as cuts or dissolves, never parameter interpolation."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 3 — The keyframe system (retained)
  verify:
    method: property test
    detail: "random keyframe pairs: every interpolated basis is orthonormal; zoom midpoint equals the geometric mean; a chart change yields no interpolated chart"
  rulings: []
  milestone: M8
- id: REQ-TOOL-076
  area: TOOL
  statement: "A keyframe animation must serialise as {keyframes: [(t_wall, ViewState, RenderState, playhead policy)], easing, fps, duration}, provenance-complete, URL-encodable and re-renderable."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 3 — The keyframe system (retained)
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 6 — Sharing: the spec is the object, the video is its shadow"
  verify:
    method: unit test
    detail: "serialise, URL-encode, decode, re-render: the frames match the original"
  rulings: []
  milestone: M8
- id: REQ-TOOL-077
  area: TOOL
  statement: "Export must run the same frame loop with a hard barrier over all visible quads per captured frame: a frame is captured only when every visible quad is at the frame's t and required refinement, with no moving fallback, blur or progressive fill in exported pixels."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 4 — Export: the frame loop in blocking mode"
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  verify:
    method: golden image
    detail: "an exported frame equals a fully refined still render at the same t"
  rulings: []
  milestone: M8
- id: REQ-TOOL-078
  area: TOOL
  statement: "The export job must be non-blocking to the UI — a background job with progress %, ETA, a live thumbnail of the last frame and cancel — and must stream frames to the encoder, never accumulating raw frames in memory."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 4 — Export: the frame loop in blocking mode"
  verify:
    method: benchmark
    detail: "exporting 600 frames keeps peak memory flat (independent of frame count); the UI stays responsive; cancel stops the job"
  rulings: []
  milestone: M8
- id: REQ-TOOL-079
  area: TOOL
  statement: "The export quality tier may exceed the interactive tier with no motion gating, and the difference must be priced into the estimate."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 4 — Export: the frame loop in blocking mode"
  verify:
    method: unit test
    detail: "a higher export tier raises the estimate; E is full during export"
  rulings: []
  milestone: M8
- id: REQ-TOOL-080
  area: TOOL
  statement: "Checkerboard must be forced off in export regardless of the user setting, and export must take none of the motion-time levers (E reduction, coarse refinement floor)."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 4 — Export: the frame loop in blocking mode"
  verify:
    method: unit test
    detail: "checkerboard on in the UI: every exported frame computes all visible pixels at one t"
  rulings: []
  milestone: M8
- id: REQ-TOOL-081
  area: TOOL
  statement: "The exporter must render offscreen at the target resolution, independent of the window, with resolution, fps and duration as job parameters."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 4 — Export: the frame loop in blocking mode"
  verify:
    method: unit test
    detail: "export 3840×2160 from a 1280×720 window: output frames are 3840×2160"
  rulings: []
  milestone: M8
- id: REQ-TOOL-082
  area: TOOL
  statement: "Export must ignore wall-clock pacing and speed settings, stepping dt exactly per the spec object."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 5 — Determinism & reproducibility
  verify:
    method: unit test
    detail: exports with speed 0.5× and 4× produce identical files
  rulings: []
  milestone: M8
- id: REQ-TOOL-083
  area: TOOL
  statement: "Shared and exported artefacts must embed or link the spec (ViewState + sim key + render state + transport / keyframe spec), and receiving it must re-render bit-comparable playback."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 6 — Sharing: the spec is the object, the video is its shadow"
  - file: docs/gui/principia_render_gui_spec.md
    section: Export & share
  verify:
    method: unit test
    detail: "an exported video's embedded spec re-renders identical frames"
  rulings: []
  milestone: M8
- id: REQ-TOOL-084
  area: TOOL
  statement: "The spotlight reel must be produced by the blocking exporter from a curated list of spec objects, each segment showing its spec's provenance and loading that spec live when clicked."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 7 — The spotlight reel (attract mode)
  verify:
    method: review checklist
    detail: "the reel build script invokes the exporter over the spec list; clicking a segment loads its spec"
  rulings: []
  milestone: M8
- id: REQ-TOOL-085
  area: TOOL
  statement: "Hovering an IC pixel must overlay that IC's trajectory computed by one CPU f64 computeIC integration of the decoded IC and projected onto the current chart's actual (q1, q2) basis vectors, with no GPU-payload trace path."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 7 — The hover trace (per-IC trajectory overlay)
  verify:
    method: unit test
    detail: "hover a pixel: the trace equals computeIC(decoded IC) projected onto the chart basis; no GPU buffer is read"
  rulings: []
  milestone: M8
- id: REQ-TOOL-086
  area: TOOL
  statement: A quantitative export must never read a preview quad.
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 5 — Preview vs refine is a second sim key
  verify:
    method: unit test
    detail: "run an export with only preview payloads cached; assert it dispatches full-T computations and reads no PREVIEW_MODE entry"
  rulings: []
  milestone: M8
- id: REQ-TOOL-087
  area: TOOL
  statement: "A fixed scripted suite (fixed slices, zoom ladder, pan path and playhead march, no user input) must run identically on every device, sweeping several quality settings, and must stay runnable on first launch after calibration."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 1.1 The fixed suite — comparability across devices
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Then invert
  verify:
    method: benchmark
    detail: "the suite replays deterministically and reports per-setting results across the sweep"
  rulings: []
  milestone: M8
- id: REQ-TOOL-088
  area: TOOL
  statement: "Passive telemetry must be a mode that logs normal interactive use, explicit and opt-in through a Profiler switch, with a visible indicator in the footer while active; no telemetry may be automatic."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 1.2 Passive telemetry — what interaction actually looks like
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 8. What this is not
  - file: decisions.md
    section: "R-129 ✱ — Where the surfaces with no artboard live *(closes RQ-105)*"
  verify:
    method: GUI screenshot
    detail: "presence (R-129): the Profiler switch exists; the footer indicator is visible while passive logging is on; logging is off by default"
  rulings:
  - R-129
  milestone: M8
- id: REQ-TOOL-089
  area: TOOL
  statement: "Telemetry must report, per device, which resource binds: samples, bandwidth, memory or dispatch overhead."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 4. Deriving the tier instead of choosing it
  verify:
    method: unit test
    detail: the report names one binding resource per run
  rulings: []
  milestone: M8
- id: REQ-TOOL-090
  area: TOOL
  statement: "Telemetry must log any exposed throttling signal or else infer thermal throttling from a monotone decline in achieved fps at constant settings and scene, and tiers must be derived from sustained, not burst, performance."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Thermal is the honest budget
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Collect everything relevant, in one file
  verify:
    method: benchmark
    detail: "a long constant-scene run flags a throttling inference when fps declines monotonically"
  rulings: []
  milestone: M8
- id: REQ-TOOL-091
  area: TOOL
  statement: "Every failure or degradation must be logged, shown and carried in telemetry so that a degraded session never looks like a healthy one."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.5 The rule underneath all of these
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6. Failing gracefully — and what "gracefully" means here
  verify:
    method: review checklist
    detail: "each fallback/failure path emits a telemetry event and a UI indicator"
  rulings: []
  milestone: M8
- id: REQ-TOOL-092
  area: TOOL
  statement: "The calibration record must collect, beyond the frame record and session block: device (incl. unified vs discrete, total and available memory), os (version, thermal state, power source, low-power mode), runtime limits, memory pressure per frame, load (incl. backgrounding), cores vs pool and ceiling, in-flight queue depth and overrun dispatches, thermal signal, binding axis per frame, and slice with sea_fraction."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Collect everything relevant, in one file
  verify:
    method: unit test
    detail: a campaign file contains every listed field
  rulings: []
  milestone: M8
- id: REQ-TOOL-093
  area: TOOL
  statement: "The sonification spectrum must reuse the frequency-diffusion FFT machinery and computeIC rather than adding a separate spectral path."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: 6. What this reuses rather than adds
  - file: docs/gui/principia_render_gui_spec.md
    section: G2. Explore — the everyday view (`01_main.png`)
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: review checklist
    detail: "the audio path calls the existing FFT used by frequency diffusion"
  rulings:
  - R-96
  milestone: M8
  note: "pointer_channels binds here because render_gui_spec §G2 cites it for sonification (R-96)."
  retired: "R-109: only pointer_channels §4 is normative; §6's FFT reuse has no normative source."
- id: REQ-TOOL-094
  area: TOOL
  statement: "The hover trace must re-integrate the IC on the CPU via computeIC in the inspector worker and read no payload, and hover trace and sonification must be two projections of one integration."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/design/principia_systems_architecture.md
    section: The return paths
  verify:
    method: unit test
    detail: "hover issues no GPU readback; trace and sound consume the same computeIC result"
  rulings: []
  milestone: M8
- id: REQ-TOOL-095
  area: TOOL
  statement: "Everything a result is conditional on (ViewState, sim key, link ids, animation spec) must serialise with it, be URL-encodable and re-renderable, the spec being the shareable object and a video its shadow."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: unit test
    detail: "serialise → URL → deserialise → re-render reproduces the payload bytes"
  rulings: []
  milestone: M8
- id: REQ-TOOL-096
  area: TOOL
  statement: "Keyframes and export must re-run the ladder per frame wherever the sim key moves."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  verify:
    method: unit test
    detail: "a keyframed sim-key change triggers re-integration for the affected frames"
  rulings: []
  milestone: M8
- id: REQ-TOOL-097
  area: TOOL
  statement: "The inspector's footnote in the tool and paper must use the honest form (agreement where regular; exponential f32 / f64 divergence where chaotic, neither being the true path), never 'more accurate, minor differences'."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 5. What is honestly comparable (click inspector)
  verify:
    method: review checklist
    detail: "the shipped footnote text matches trajectory_viewing §5's wording"
  rulings: []
  milestone: M8
- id: REQ-TOOL-098
  area: TOOL
  statement: "The Profiler window must have Timeline, Flame, GPU, Memory and Counters tabs with live / pause / Capture, and show the frame-time trace (frame ms, p50, p95, p99, worst frame), a where-the-frame-goes donut, stacked per-frame bars for the last 60 frames, a substeps-per-pixel histogram with the cap marked, a main-thread flame chart, GPU timestamps per pass, memory (heap, GPU, tile cache) over time, live allocations by type with their 60 s change, and Export trace (JSON), Open in Tracy and Headless render… buttons."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: GUI screenshot
    detail: "screenshot against 04_windows.png's profiler; the histogram's cap marker sits at N_max; the window reads the profiler file prin profile writes, and that file's header config is REQ-GUI-039's provenance object (R-113)"
  rulings:
  - R-68
  - R-113
  milestone: M8
- id: REQ-TOOL-099
  area: TOOL
  statement: "The profiler's leak detector must flag steady memory growth while idle."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  verify:
    method: unit test
    detail: "synthetic idle trace with steady heap growth is flagged; a flat idle trace and growth during activity are not"
  rulings:
  - R-71
  milestone: M8
  note: "What counts as idle, the growth threshold and the window: REQ-TOOL-114 (definition); REQ-TOOL-115 (calibration)."
- id: REQ-TOOL-100
  area: TOOL
  statement: "The profiler JSON must carry precomputed leak flags and hot-path summaries so an agent reads conclusions, not raw traces."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  verify:
    method: unit test
    detail: "an exported trace contains leak-flag and hot-path summary sections consistent with its raw scopes"
  rulings:
  - R-71
  - R-72
  milestone: M8
  note: "What a hot-path summary holds: REQ-TOOL-114 (definition); leak flags follow REQ-TOOL-115 (calibration)."
- id: REQ-TOOL-101
  area: TOOL
  statement: "`prin profile query \"…\" --live` must query a running app and answer in the same schema."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  verify:
    method: unit test
    detail: "'top 10 scopes by p95' against a running app returns ten v1-schema scope records"
  rulings: []
  milestone: M8
- id: REQ-TOOL-102
  area: TOOL
  statement: "Image export must offer a size in multiples of the view, a format, embedding the view (pxpack) and optionally the stain's WGSL, so that opening the picture recreates the view exactly."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Export & share
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  verify:
    method: unit test
    detail: "export a PNG with pxpack, import it: the restored SimConfig + RenderState equal the exported ones field for field"
  rulings: []
  milestone: M8
- id: REQ-TOOL-103
  area: TOOL
  statement: "The State export must offer copy snapshot (JSON), save snapshot, load and a principia://view?… share link, each of which recreates the view when loaded."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Export & share
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 04 Windows
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 6 — Sharing: the spec is the object, the video is its shadow"
  verify:
    method: unit test
    detail: "round-trip each of JSON copy, saved file and share link: the state is equal after load"
  rulings: []
  milestone: M8
- id: REQ-TOOL-104
  area: TOOL
  statement: "Import picture (drop a Principia PNG, or File › Import picture…) must read the pxpack, show what is stored (chart, centre, zoom, tilt, stain, time, when made) and what differs from now, and offer restore this view, only its stain, and open side by side."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 09 Import picture · saved views · record a sweep
  verify:
    method: unit test
    detail: "import a PNG exported with a different stain: the diff lists the stain; 'only its stain' changes RenderState's stain and nothing else; screenshot against 09_importrecord.png"
  rulings: []
  milestone: M8
- id: REQ-TOOL-105
  area: TOOL
  statement: "Saved views must be pxpack snapshots listed with a thumbnail and 'go', with a 'Save this view' action."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 09 Import picture · saved views · record a sweep
  verify:
    method: unit test
    detail: "save a view, change it, press go: the state equals the saved one"
  rulings:
  - R-72
  milestone: M8
  note: "Where saved views are stored: REQ-GUI-153 (definition)."
- id: REQ-TOOL-106
  area: TOOL
  statement: "Record a time sweep must take a time range, a frame count, the per-frame refinement quality, a size at the view's aspect, a format, overlays on or off, and embed pxpack in every frame; the formats are, natively, PNG frames, GIF and MP4 through a system ffmpeg when present, and in the browser PNG frames (zipped), GIF via a wasm encoder, GIF being one encoder in both builds — the Rust gif crate (MIT or Apache-2.0) with color_quant for palettes, compiled to wasm for the browser (R-155) — and MP4/WebM through WebCodecs where supported."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  - file: decisions.md
    section: R-131 — The video encoders *(closes RQ-108)*
  - file: decisions.md
    section: R-155 — One GIF encoder for both builds *(closes RQ-125)*
  verify:
    method: unit test
    detail: "record 5 frames as PNG frames: each carries a pxpack whose time is its own frame time; size aspect equals the view's; natively MP4 is offered only when a system ffmpeg is found; in the browser MP4/WebM is offered only where WebCodecs supports it (R-131)"
  rulings:
  - R-131
  - R-155
  milestone: M8
- id: REQ-TOOL-107
  area: TOOL
  statement: "Recording a sweep must integrate each frame to its own t through the export contract's blocking mode, a hard barrier per captured frame, not by scrubbing."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 09 Import picture · saved views · record a sweep
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 4 — Export: the frame loop in blocking mode"
  verify:
    method: golden image
    detail: "a recorded frame at t equals a fresh blocking render at t pixel for pixel"
  rulings: []
  milestone: M8
- id: REQ-TOOL-108
  area: TOOL
  statement: "The console must read the same telemetry stream as the profiler, with sources including the stain, the integrator, the quadtree, the contract (every SetField logged) and the app."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G12. Console (`12_console.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 12 Console
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: unit test
    detail: "each SetField produces one console entry with source 'contract'; console and profiler events share ids"
  rulings: []
  milestone: M8
- id: REQ-TOOL-109
  area: TOOL
  kind: calibration
  statement: "The embedding header's magic bytes and version byte (the version bumped for R-81's contract-name record layout) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 2. Layout
  - file: docs/design/principia_dd_image_embedding.md
    section: 6. What travels
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-81 — The embedded record uses the contract names *(closes RQ-32)*"
  verify:
    method: review checklist
    detail: "the proposal gives the four magic bytes (checked not to collide with PNG signatures or the prototype's) and a version byte above the prototype's"
  rulings:
  - R-71
  - R-81
  milestone: M7
- id: REQ-TOOL-110
  area: TOOL
  kind: definition
  statement: "The embedding format's flag bits, byte order, bit order within a tile, payload serialisation and tEXt keyword must be defined in dd_image_embedding §2 and §9 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 2. Layout
  - file: docs/design/principia_dd_image_embedding.md
    section: 3. Tiles, not raster order
  - file: docs/design/principia_dd_image_embedding.md
    section: 7. Measured capacity and behaviour
  - file: docs/design/principia_dd_image_embedding.md
    section: 9. Obligations
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "dd_image_embedding states each item; the reviewer checks the serialisation reproduces §7's measured payload size class and that reader and writer are generated from one description"
  rulings:
  - R-72
  milestone: M7
- id: REQ-TOOL-111
  area: TOOL
  kind: calibration
  statement: "The hybrid variant's default per-bit redundancy k must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 5. Two variants, one choice
  - file: docs/design/principia_dd_image_embedding.md
    section: 7. Measured capacity and behaviour
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal cites §5/§7's measurements (k = 9 → 5%, k = 25 → 15% uniform noise; ~8% bit error after nearest rotation) and the capacity cost of the chosen k"
  rulings:
  - R-71
  milestone: M7
- id: REQ-TOOL-112
  area: TOOL
  kind: definition
  statement: "Which build-hash component detects a decoder change, so that recreating across it is refused must be defined in dd_image_embedding §6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 6. What travels
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "dd_image_embedding §6 names the component and what changes it; the reviewer checks that a decode or chart-version change alters it and an unrelated build change does not"
  rulings:
  - R-72
  milestone: M7
- id: REQ-TOOL-113
  area: TOOL
  kind: calibration
  statement: "The list of supported resolutions for the embed → PNG → extract round-trip golden test must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M7 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 9. Obligations
  - file: docs/design/principia_dd_image_embedding.md
    section: 7. Measured capacity and behaviour
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal cites §7's capacity measurements (64² to 1024²) and shows every listed resolution holds at least one intact record of the current payload"
  rulings:
  - R-71
  milestone: M7
- id: REQ-TOOL-114
  area: TOOL
  kind: definition
  statement: "What 'idle' means for the profiler's leak detector, and what a precomputed hot-path summary holds must be defined in render_gui_spec §G5 Profiler by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the Profiler section defines idle and lists the hot-path summary's fields within profiler schema v1"
  rulings:
  - R-72
  milestone: M8
- id: REQ-TOOL-115
  area: TOOL
  kind: calibration
  statement: "The leak detector's growth threshold and observation window must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows idle memory traces with and without an injected leak and a threshold and window that flag the leak and not the clean trace"
  rulings:
  - R-71
  milestone: M8
- id: REQ-TOOL-116
  area: TOOL
  kind: definition
  statement: "The legal state-transition table of the quad-state view (loaded / pending / refinable / terminal / stale) must be defined in debug_tooling_plan §F by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "debug_tooling_plan §F (or the scheduler contract it cites) gives the transition table the view checks against, listing every legal transition; the quad-state view's test asserts only listed transitions occur; physics reviewer approved"
  rulings:
  - R-72
  milestone: M5
- id: REQ-TOOL-117
  area: TOOL
  kind: calibration
  statement: "The telemetry size bound — downsampling on write with its keep-every-Nth-idle-frame N, or roll-up of idle stretches — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: "5. The artefact: one file, plain text, readable by the sender"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "file sizes of a long 60 fps session with idle stretches under the chosen scheme and N, with every motion frame kept"
  rulings:
  - R-71
  milestone: M5
- id: REQ-TOOL-118
  area: TOOL
  statement: "The contract-named embedded record must carry a bumped embedding `version` in its header, distinct from the prototype record's version."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 6. What travels
  - file: decisions.md
    section: "R-81 — The embedded record uses the contract names *(closes RQ-32)*"
  verify:
    method: unit test
    detail: "the header version of a newly embedded record differs from the prototype layout's version"
  rulings:
  - R-81
  milestone: M7
  note: "The byte value is set with the rest of the header: REQ-TOOL-109 (calibration)."
- id: REQ-TOOL-119
  area: TOOL
  kind: definition
  statement: "The regression that `prin profile diff BASE NEW --threshold P%` tests — the statistic compared (frame-time percentile or other), the scopes it compares and how a scope present in only one file is treated — must be defined in render_gui_spec's Profiler section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 3. Percentiles, not means — and the specific thresholds
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section states the compared statistic, the scope set and the missing-scope rule, and REQ-TOOL-007's test follows it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M0
- id: REQ-TOOL-120
  area: TOOL
  kind: definition
  statement: "Profiler schema v1's concrete JSON form — the session header's keys, the frame record's keys (telemetry §2's fields and five stages) and the record shape and nesting of scopes, GPU passes, allocations and events — must be defined in dd_telemetry_and_tiers §5 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: "5. The artefact: one file, plain text, readable by the sender"
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 2. What to record — and the rule that makes it useful
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: decisions.md
    section: "R-56 — Profiler schema v1 is a superset of telemetry §2, in JSON *(GU-5, amended)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the v1 key names and nesting for the header, frame records, scopes, GPU passes, allocations and events; the JSON schema TASK-M0-17 ships matches it; physics reviewer approved"
  rulings:
  - R-56
  - R-72
  milestone: M0
- id: REQ-TOOL-121
  area: TOOL
  kind: definition
  statement: "How the session header obtains the f64 rate (a reported value, a measured probe, or recorded as unavailable) and what its display fields (resolution, refresh rate, DPI scale) hold in a headless session must be defined in dd_telemetry_and_tiers §2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 2. What to record — and the rule that makes it useful
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section states the f64-rate source and the headless display values, and REQ-TOOL-001's session-header test covers both; physics reviewer approved"
  rulings:
  - R-72
  milestone: M0
- id: REQ-TOOL-122
  area: TOOL
  kind: definition
  statement: "The debug presentation helpers' renderings — dbg_sentinel's hatch pattern for absence-NaN, dbg_cat's golden-angle lightness and chroma beyond the Okabe–Ito cycle, dbg_log's compression formula and its eps, dbg_hash_u32's hash, and dbg_flag's green and red — must be defined in render_contract's presentation-layer section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: "Presentation layer (hand-written, small, reused by every debug view)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives each helper's formula or pattern and colours; the prelude's helpers and their unit tests (REQ-TOOL-009) match it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M1
- id: REQ-TOOL-123
  area: TOOL
  kind: definition
  statement: "The bring-up mode's known pattern — the function of the sample address it writes and the existing payload slots it writes it into — must be defined in colour_composition Appendix A by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_colour_composition.md
    section: Appendix A — kernel-side bring-up mode
  - file: decisions.md
    section: R-75 — The kernel keeps one debug mode *(closes RQ-26)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Appendix A names the pattern and the slots; REQ-TOOL-015's test decodes exactly that pattern through the normal unpack path; physics reviewer approved"
  rulings:
  - R-72
  - R-75
  milestone: M1
- id: REQ-TOOL-124
  area: TOOL
  kind: definition
  statement: "The structural overlays' styling — the pending-hatch pattern and the fallback-ancestor tint (its colour and opacity) — must be defined in debug_tooling_plan §F by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§F gives the hatch pattern and the tint; REQ-TOOL-026's golden images use them; physics reviewer approved"
  rulings:
  - R-72
  milestone: M1
- id: REQ-TOOL-125
  area: TOOL
  kind: definition
  statement: "Where dt_max (the largest usable step, reported distinguishably from 'no usable step') lives — an occupant diagnostic outside the payload, or a payload field added as a ledger change — must be defined in integrator_contract Part 2c by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Rules the new kernel must hold by construction
  - file: docs/read_first/principia_01_pitfalls.md
    section: 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section names dt_max's home and its 'no usable step' sentinel (a ledger change goes through the generation root); REQ-TOOL-039's fixture reads it there; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-TOOL-126
  area: TOOL
  kind: definition
  statement: "Which eps the horizon map's t_max(IC) = ln(1/eps)/ftle(IC) uses — the f32 kernel's machine epsilon, the f64 kernel's, or the ensemble jitter of the measurement horizon — and whether the map shows one horizon or several must be defined in dd_predictability_horizon §4.2 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 4.2 The horizon is a field, not a constant
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 1. The result
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§4.2 names the eps (and the precision whose FTLE feeds the map); REQ-TOOL-043's unit test uses it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M4
- id: REQ-TOOL-127
  area: TOOL
  kind: definition
  statement: "The on-demand single-IC f32 GPU trace — how the survey kernel writes the dense n(t) series (a baked trace variant or a per-step readback), the buffer's layout and sampling cadence, the shared times at which it is compared with the CPU f64 curve, and the criterion that reads the divergence time off the two traces — must be defined in trajectory_viewing §5 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 5. What is honestly comparable (click inspector)
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  - file: docs/contracts/principia_canonical_spec.md
    section: "7. Precision & validation model *(authoritative: `parity_contract`, `validation_ground_truth_note`, `core_design`)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§5 gives the trace's dispatch form, buffer layout, sampling and the divergence-time criterion (any threshold in it is a value proposed under R-71 at the M4 gate); REQ-TOOL-040 and REQ-VAL-106 use it; physics reviewer approved"
  rulings:
  - R-72
  milestone: M4
- id: REQ-TOOL-128
  area: TOOL
  kind: definition
  statement: "How `prin profile query --live` reaches a running app — the transport, how the app is discovered, and what the browser build exposes must be defined in render_gui_spec's Profiler section by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: decisions.md
    section: "R-56 — Profiler schema v1 is a superset of telemetry §2, in JSON *(GU-5, amended)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section names the transport and discovery for the native app and the browser build (or states the browser is out of scope), answering in the v1 schema; physics reviewer approved"
  rulings:
  - R-56
  - R-72
  milestone: M8
- id: REQ-TOOL-129
  area: TOOL
  kind: definition
  statement: "The URL encoding of the spec object (ViewState, sim key, render state, keyframe spec) and the `principia://view?…` query format — field keys, serialisation, compression and versioning must be defined in export_animation_contract Part 6 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 3 — The keyframe system (retained)
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 6 — Sharing: the spec is the object, the video is its shadow"
  - file: docs/gui/principia_render_gui_spec.md
    section: Export & share
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the query grammar and the encoding with a version field and one worked example that round-trips; physics reviewer approved"
  rulings:
  - R-72
  milestone: M8
- id: REQ-TOOL-130
  area: TOOL
  kind: definition
  statement: "The `deep_zoom_03` profiler scenario (what it runs headless: chart, camera path, depth and frame count) must be defined in render_gui_spec § Profiler by the task that needs it and reviewed by the physics reviewer before merge, and `prin profile --scenario deep_zoom_03 --frames 600` must run it with a stable scope and event sequence."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: Profiler
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "the Profiler section defines deep_zoom_03; running it for 600 frames twice gives the same frame count and the same scope / event sequence; physics reviewer approved"
  rulings:
  - R-72
  - R-113
  milestone: M5
  note: "Split from REQ-TOOL-006 (R-113, RQ-93 M0-4): needs the quadtree and deep zoom."
- id: REQ-TOOL-131
  area: TOOL
  statement: "The profiler's measurement struct must land with the first frame loop (M1's fragment pipeline), each frame emitting a schema-v1 frame record."
  source:
  - file: decisions.md
    section: "R-56 — Profiler schema v1 is a superset of telemetry §2, in JSON *(GU-5, amended)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "rendering N frames of the synthetic payload through the fragment pipeline with profiling on writes N frame records that validate against schema v1"
  rulings:
  - R-56
  - R-113
  milestone: M1
  note: Split from REQ-TOOL-008 (R-113, RQ-93 M0-4).
- id: REQ-TOOL-132
  area: TOOL
  statement: "On a real march, the live shape view's |n|−1 error mode must read flat zero, and the live current-substep effort heatmap must render from the live march."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Live-state & array inspection
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-153 — The debug and live-march views have golden images of their own *(closes RQ-123)*"
  - file: decisions.md
    section: R-158 — The six readings are accepted *(closes RQ-128)*
  verify:
    method: golden image
    detail: "the |n|−1 view is flat zero on a real march; the effort heatmap shows the march's per-pixel substep counts; both match their own golden images, recorded at the M3 gate (R-153)"
  rulings:
  - R-113
  - R-153
  - R-158
  milestone: M3
  note: "Split from REQ-TOOL-010 and REQ-TOOL-011's live effort row (R-113, RQ-94)."
- id: REQ-TOOL-133
  area: TOOL
  statement: "The field-view catalogue must cover render contract Part 6's tier-gated ensemble views, derived at resolve from the footprint's E+1 samples."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Field views (one per field, every struct)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: review checklist
    detail: "tick the ensemble rows of the field-view table against the generated catalogue on a real E ≥ 1 resolve"
  rulings:
  - R-113
  milestone: M5
  note: Split from REQ-TOOL-011 (R-113, RQ-94).
- id: REQ-TOOL-134
  area: TOOL
  statement: "The GUI screenshot runner (`cargo xtask screenshot <suite>`) must exist before the first GUI screenshot requirement: it captures a GUI surface headless beside the artboard it names for layout comparison only (R-68), or asserts a presence-only case's named controls where no artboard exists (R-129)."
  source:
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-129 ✱ — Where the surfaces with no artboard live *(closes RQ-105)*"
  - file: decisions.md
    section: "R-183 — TASK-M0-06 is split *(closes S1; reverses R-156's size exemption)*"
  verify:
    method: unit test
    detail: "cargo xtask screenshot selftest: the layout case writes its capture; the presence-only case passes, and fails naming the control when one is removed"
  rulings:
  - R-68
  - R-113
  - R-129
  - R-183
  milestone: M0
- id: REQ-VAL-001
  area: VAL
  statement: "Error meters and verification claims must be checked against the formulation, not only the data (e.g. COM drift must not be used as an error meter for an integrator that integrates relative coordinates)."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.5 Verify against the formulation, not just the data
  - file: decisions.md
    section: R-180 — pr-check is item-level *(closes A5)*
  verify:
    method: review checklist
    detail: "Each error meter documents which quantity the occupant actually integrates; meters on non-dynamical quantities are rejected."
  rulings:
  - R-180
  milestone: M0
- id: REQ-VAL-002
  area: VAL
  statement: "Validation reports must include negative and messy results, scatter and the number of regions sampled, not only a clean summary."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.6 Report the negative, and the messy
  verify:
    method: review checklist
    detail: "Report template has sections for scatter, region count and negative results; conclusions from few regions are flagged."
  rulings: []
  milestone: M0
- id: REQ-VAL-003
  area: VAL
  statement: "An image artefact must be diagnosed by a controlled reproduction with exactly one variable changed, and every hypothesis (e.g. t_end quantisation, 8-bit output quantisation, colour map, chart geometry) must be tested against a measurement before it is accepted."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.2 Four wrong theories, in order
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.3a Do not reason about an image — reproduce it
  - file: docs/read_first/principia_01_pitfalls.md
    section: "1.3 What settled it — a controlled experiment, not an argument"
  verify:
    method: review checklist
    detail: "Every artefact investigation record shows (a) the one-variable-changed reproduction, (b) the measurement that killed or confirmed each hypothesis (e.g. RGB values across a line crossing vs the ~3-step 8-bit staircase; the structure re-rendered in event, greyscale and hue modes to rule out colouring)."
  rulings: []
  milestone: M0
  note: "pitfall: \"1.2 Four wrong theories, in order\"; pitfall: \"3. Standing rules earned in this sequence\"; pitfall: \"1.3 What settled it — a controlled experiment, not an argument\" — 1.2 Four wrong theories, in order; 3. Standing rules (RUN IT, DO NOT REASON ABOUT IT)."
- id: REQ-VAL-004
  area: VAL
  statement: "Any aggregate quantity quoted as a result (e.g. escape fraction) must be shown to converge under sampling refinement before it is quoted; a quantity whose largest relative step is at the finest sampling must not be reported as converged."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  - file: docs/read_first/principia_00_philosophy.md
    section: "4.5a A quantity that does not converge under refinement is measuring the sampling"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: R-171 — The convergence gate, defined *(closes A1, A2, T2)*
  verify:
    method: numerical gate
    detail: "R-171: order samples coarse → fine (strides 32, 4, 1, 0; stride 0 unstrided, the finest); r_k = |x_k − x_{k−1}| / |x_{k−1}|; pass iff r_k strictly decreasing and the finest r_k below the threshold (provisional 0.1 against REQ-VAL-135 until M3). The recorded sequence 0.2153 → 0.4423 → 0.5494 → 0.0947 (r = 1.054, 0.242, 0.828) must fail; a strictly shrinking fixture must pass."
  rulings:
  - R-71
  - R-171
  milestone: M0
  note: "pitfall: \"3. Standing rules earned in this sequence\" — 3. Standing rules — A QUANTITY THAT DOES NOT CONVERGE UNDER REFINEMENT IS MEASURING THE SAMPLING. The convergence threshold: REQ-VAL-135 (calibration)."
- id: REQ-VAL-005
  area: VAL
  statement: "A discriminator must not be computed from a quantity altered by the behaviour under test (e.g. d_min of runs stopped early to judge whether early stops were spurious)."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  - file: decisions.md
    section: R-180 — pr-check is item-level *(closes A5)*
  verify:
    method: review checklist
    detail: "Each discriminator in a validation record lists its dependency on termination; any that depends on the tested termination (d_min, t_end-truncated statistics) is rejected or recomputed on fully integrated runs."
  rulings:
  - R-180
  milestone: M0
  note: "pitfall: \"3. Standing rules earned in this sequence\" — 3. — A DISCRIMINATOR CAN BE POISONED BY ITS OWN SUBJECT."
- id: REQ-VAL-006
  area: VAL
  statement: "A threshold on a quantity spanning decades must be relative, set from the observed distribution or a measured gap, never an absolute cutoff chosen by eye."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.2 Every constant must be derived or absent
  verify:
    method: review checklist
    detail: "Every threshold constant in the ledger cites the distribution or gap it was set from; regression: an absolute cutoff lying inside the bound population's range (closure 2e-3) or at an extreme percentile (tau_display at the 0.4th percentile) fails review."
  rulings: []
  milestone: M0
  note: "pitfall: \"3. Standing rules earned in this sequence\" — 3. — A THRESHOLD ON A QUANTITY SPANNING DECADES MUST BE RELATIVE."
- id: REQ-VAL-007
  area: VAL
  statement: "Every test, check and measurement must be shown able to fire before its result is read: its reachable output set must include the failure it guards against, demonstrated by a discriminating control (sign-flipped variant, known-answer field, or comparison that must differ)."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.4 A test that cannot fail is not a test
  - file: docs/read_first/principia_01_pitfalls.md
    section: 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN
  - file: decisions.md
    section: "R-176 — Controls come before the tests that need them *(closes G3, S2)*"
  verify:
    method: review checklist
    detail: "Each test in the suite has a recorded negative control that makes it fail (mutation or contaminated input); arithmetically impossible tests (e.g. expecting thousands of distinct values from a fraction over N² = 64 footprints) and tests true by construction (n_hot < N² under a quantile rule) are rejected."
  rulings:
  - R-176
  milestone: M0
  note: "pitfall: \"3. Standing rules earned in this sequence\"; pitfall: \"9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN\" — 3. — CHECK THE MEASUREMENT CAN FIRE BEFORE READING IT; 9. general form: a check whose reachable output set does not include the failure."
- id: REQ-VAL-008
  area: VAL
  statement: "Any mechanism found by an investigation during the build must be recorded in principia_01_pitfalls.md, not only in the investigation record."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.5 THIS WAS ALREADY ON RECORD
  verify:
    method: review checklist
    detail: "Each closed investigation PR either adds/updates a pitfalls entry (observed / assumed / actual / measurement) or states why none applies."
  rulings: []
  milestone: M0
  note: "pitfall: \"4.5 THIS WAS ALREADY ON RECORD\" — 4.5 THIS WAS ALREADY ON RECORD — a finding found twice is a filing problem."
- id: REQ-VAL-009
  area: VAL
  statement: "When a fix removes one symptom, every co-located symptom must be re-measured separately; an unchanged column must be treated as evidence of a second defect."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 8. TWO ARTEFACTS ARE NOT ONE DEFECT
  verify:
    method: review checklist
    detail: "Ablation reports tabulate each symptom in its own column per arm; a fix is closed only against the symptoms whose columns it moved."
  rulings: []
  milestone: M0
  note: "pitfall: \"8. TWO ARTEFACTS ARE NOT ONE DEFECT\" — 8. — the unchanged 0.0026 → 0.0026 column is the finding."
- id: REQ-VAL-010
  area: VAL
  statement: "For every vector field the generated debug occupants must offer '‖·‖ as scalar' beside 'as direction-cosines', and a paired test must assert ‖n‖ = 1 to tolerance."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: unit test
    detail: "registry lists both views for n; ‖n‖ − 1 is within tolerance over a test render"
  rulings:
  - R-71
  milestone: M1
  note: "Tolerance: REQ-VAL-122 (calibration)."
- id: REQ-VAL-011
  area: VAL
  statement: "The t_dmin round-trip view must verify the 16-bit absolute macro-step t_dmin quantisation."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  verify:
    method: unit test
    detail: "pack/unpack t_dmin_step round-trips exactly for all u16 values"
  rulings: []
  milestone: M1
- id: REQ-VAL-012
  area: VAL
  statement: "A structure that survives every colour map must be treated as a physics/logic artefact, not a colouring one; the build must be able to render the same slice in event, greyscale and hue modes to perform that check."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.2 Four wrong theories, in order
  verify:
    method: golden image
    detail: "Render a fixture slice in event, greyscale and hue modes from the same payload; assert geometric structure (edge map of the payload-derived field) is identical across modes, proving colouring is a pure function applied after the physics."
  rulings: []
  milestone: M1
  note: "pitfall: \"1.2 Four wrong theories, in order\" — 1.2 (c) colour-map theory, refuted because colouring is independent of the physics."
- id: REQ-VAL-013
  area: VAL
  statement: "Every encode/round-trip tolerance must be asserted in physical space, never as a z-space residual near saturation."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 4 — Conditioning: assert in physical units, never in z"
  - file: docs/design/principia_dd_encode.md
    section: 3.5 Conditioning (numbers, and the tolerance rule)
  - file: docs/design/principia_dd_encode.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "encode test 8: a near-clamp input whose z-residual is ~10⁵× the physical residual passes the physical tolerance and would fail a naive z-tolerance"
  rulings: []
  milestone: M2
- id: REQ-VAL-014
  area: VAL
  statement: "For ground-truth ingestion encode must expose, beside z, the physical-space residual ‖D(z) − C(x)‖_phys and a conditioning number κ(z) at the point."
  source:
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 7 — Ground-truth ingestion (the validation programme's demand)"
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: New demands this places on the inverse-encode contract
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "Everything is representable in principle — so a failed round-trip is a BUG signal"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "ingest Lagrange, figure-eight and Burrau ICs; the encode result carries z, residual and κ"
  rulings:
  - R-72
  milestone: M2
  note: "κ(z): REQ-ENC-025 (definition)."
- id: REQ-VAL-015
  area: VAL
  statement: "The invariant-chart gradient view (E_0 or Lz_0 on an (L_z,E)/(L_z,K) chart) must render a perfect axis-aligned gradient."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "G. Cross-check views (certify a *seam*, not a field — integration tests with a display)"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "on an (L_z,E) chart, E_0 is constant along the L_z axis and monotone along E to within f32 rounding of the target"
  rulings:
  - R-71
  milestone: M2
  note: "The view's tolerance: REQ-VAL-112 (calibration)."
- id: REQ-VAL-016
  area: VAL
  statement: "Burrau at ν = 1/2 must reproduce (3,4,5) and the classical configuration to a stated tolerance."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: unit test
    detail: decode ν = 1/2 and compare to the (m,n) = (2,1) construction
  rulings:
  - R-71
  milestone: M2
  note: "Tolerance: REQ-VAL-118 (calibration)."
- id: REQ-VAL-017
  area: VAL
  statement: "The CPU-f64 and GPU-f32 (and browser WGSL) instantiations must agree on the golden IC and fuzzed interiors to f32-eps-scaled tolerance on continuous values, and every decoder branch tag (DEGENERATE, feasibility) must be bit-identical across backends on identical inputs, with branch inputs formed as comparisons against constants / single-rounded values, never a runtime transcendental at the decision."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  - file: docs/design/principia_dd_decoder.md
    section: 4. Seams (its side of each — the integration-test list)
  - file: decisions.md
    section: "R-34 — FMA: explicit fma at every branch input, enumerated *(IE-6)*"
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: property test
    detail: "decoder test 12: fuzz z on all backends; continuous outputs within f32-eps-scaled tolerance; branch tags bitwise equal; feasibility K* ≥ L_z²/2 formed with explicit fma per the enumerated branch inputs"
  rulings:
  - R-34
  - R-71
  - R-84
  milestone: M2
  note: "R-34 extends explicit fma to every multi-op branch input; R-84: branch decisions are identical on identical inputs. The eps scale factor: REQ-DEC-043 (calibration)."
- id: REQ-VAL-018
  area: VAL
  statement: "z = 0 must decode to the canonical golden IC — masses (⅓,⅓,⅓), α = π/4, β = π/2, rest (q = 0), I = 1 — with every derived value asserted in f64 and f32."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "golden anchor of decoder §5; also encode's golden pair (encode §5)"
  rulings: []
  milestone: M2
- id: REQ-VAL-019
  area: VAL
  statement: "Anosova region-D and Burrau rest starts must encode → decode with their invariants matching the papers' stated values after the recorded rescale."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  - file: docs/design/principia_dd_encode.md
    section: 1. What it is
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "import the literature ICs; compare E, L_z after multiplying back by the recorded rescale"
  rulings:
  - R-71
  milestone: M2
  note: "The papers' reference values and the tolerance: REQ-VAL-120 (calibration)."
- id: REQ-VAL-020
  area: VAL
  statement: "A chart-landmark test must assert the decoder places the Lagrange equilateral configuration at `z = 0` and the Euler collinear configurations at `beta → 0` and `beta → pi`."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.3 Lagrange and Euler central configurations — the only analytic ones"
  - file: docs/design/principia_dd_validation_orbits.md
    section: 5. Immediate action
  verify:
    method: unit test
    detail: "decode z = 0 -> equilateral; decode near beta edges -> collinear (approached as a limit)"
  rulings: []
  milestone: M2
- id: REQ-VAL-021
  area: VAL
  statement: "Translate-all, boost-all, rotate-all and scale-all must each leave z fixed, to the bound verified in the reference tool (1.6×10⁻¹⁴)."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Whole-system handles — the invariance audit
  - file: docs/notes/ic_inspector_scratchpad.md
    section: The quotient set (the thing being audited)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "2×10⁴ random ICs, each handle applied: max ‖Δz‖ ≤ 1.6×10⁻¹⁴ (the reference tool's re-verified bound)"
  rulings:
  - R-71
  milestone: M2
  note: "The bound is the standalone tool's measured value; the Rust Inspector's gate: REQ-VAL-121 (calibration)."
- id: REQ-VAL-022
  area: VAL
  statement: "Decode → canonicalise → z must round-trip with max ‖z − z′‖ ≤ 1.2×10⁻¹³ over 2×10⁴ random z with |z| < 4, with the canonical CoM at the origin, equilateral configurations at a shape-sphere pole and collinear ones on the equator exactly."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "2×10⁴ random z, |z| < 4: max round-trip error ≤ 1.2×10⁻¹³; canonical CoM ≤ 1.7×10⁻¹⁶ from origin; equilateral → pole and collinear → w = 0 exactly"
  rulings:
  - R-10
  - R-21
  - R-71
  milestone: M2
  note: "Uses μ_max = 5, q_max = 2, ε = 1e-6, α_min = 0 (R-10, R-21). Bounds are the reference tool's measured values; the Rust Inspector's gate: REQ-VAL-121 (calibration)."
- id: REQ-VAL-023
  area: VAL
  statement: "The shape-sphere dot must land on a binary-collision landmark exactly when two bodies coincide, at any mass ratio."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  verify:
    method: property test
    detail: "random masses, bodies i and j brought together: the dot's distance to BC landmark (opposite k) → 0 and to the others stays > 0"
  rulings:
  - R-22
  - R-50
  milestone: M2
- id: REQ-VAL-024
  area: VAL
  statement: "A link-variation diagnostic must, for each registry link, invert the canonicalised IC and report its residual and conditioning, and any per-validation link choice must be recorded in provenance; validation reachability must never change the default link."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "Link variation as a conditioning diagnostic (not a default change)"
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 7 — Ground-truth ingestion (the validation programme's demand)"
  verify:
    method: review checklist
    detail: "diagnostic reports residual_L and κ_L per link; provenance records the link used; default link unchanged by validation code"
  rulings: []
  milestone: M2
- id: REQ-VAL-025
  area: VAL
  statement: "The independent convergence reference must be a separate CPU arbitrary-precision integrator with convergence gating (raise precision and tighten tolerance until the result stops changing), not the shared kernel, used for the integration-floor probe and Burrau ground truth; double-double is a fast screen only and RK45 stays the inspector's reference."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "7. Precision & validation model *(authoritative: `parity_contract`, `validation_ground_truth_note`, `core_design`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: decisions.md
    section: "R-33 — The independent convergence reference is Brutus-style *(IE-5, amended)*"
  - file: docs/contracts/principia_parity_contract.md
    section: 1. The principle
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "The correctness factoring: validate on CPU, inherit on GPU"
  - file: docs/read_first/principia_00_philosophy.md
    section: Why using the same kernel is load-bearing
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: numerical gate
    detail: "reference output unchanged between successive precision/tolerance steps before it is accepted"
  rulings:
  - R-33
  - R-72
  milestone: M3
  note: "R-33 is now folded into canonical_spec §7, parity §1 and the ground-truth note. The protocol, when it runs and how it plugs into the harness: REQ-VAL-131 (definition)."
- id: REQ-VAL-026
  area: VAL
  statement: "Every drill-down test must carry a parity tier, with the integrator suite assigned as: L for substep integer, detector labels and priority; N for order scaling, one-step and drift; S for the Burrau smoke and divergence structure."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: 8. Test index (drill-down suite → tier)
  verify:
    method: review checklist
    detail: "each test in the suite is tagged L/B/N/S matching the §8 index"
  rulings: []
  milestone: M3
- id: REQ-VAL-027
  area: VAL
  statement: "The energy-agreement view must show log|E_0 − (K_0 + V_0)|, comparing SimState's E_0 with the ICDescriptor's derived energy (E₀ is K₀ + V₀, derived, not stored)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: docs/design/principia_dd_decoder.md
    section: "3.6 ICDescriptor derived quantities (decode-time, pre-integration)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/design/principia_debug_tooling_plan.md
    section: "G. Cross-check views (certify a *seam*, not a field — integration tests with a display)"
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "on a synthetic and a real survey, |E_0 − (K_0+V_0)| is within the calibrated E₀ agreement tolerance everywhere; the ICDescriptor stores no E₀ field"
  rulings:
  - R-71
  - R-86
  milestone: M3
  note: "Tolerance: REQ-VAL-117 (calibration)."
- id: REQ-VAL-028
  area: VAL
  statement: "The BodyPlane chart (today's slice) must be kept and reproduce the recorded reference, the Python reference's output: bit-exact where the operation order is identical (parity_contract), otherwise within a calibrated tolerance (R-71), with the Python cross-check green (R-166)."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: docs/contracts/principia_parity_contract.md
    section: "3. The load-bearing discipline: never accumulate before comparing"
  - file: decisions.md
    section: R-166 — The fixtures
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: golden image
    detail: "BodyPlane render/dump against the fixture M3 records from the Python reference (docs/reference/prin-rs/tools/xcheck, reference/*.py): bit-exact where the operation order is identical, otherwise within the calibrated tolerance; the Python cross-check passes"
  rulings:
  - R-71
  - R-72
  - R-166
  milestone: M3
  note: "BodyPlane's map and the reference artefact it must match: REQ-VAL-119 (definition). The tolerance where the operation order differs: REQ-VAL-146 (calibration)."
- id: REQ-VAL-029
  area: VAL
  statement: "The integrator unit suite must use the golden anchors `z = 0` (equal-mass, α = π/4, β = π/2, rest) and the Burrau IC encoded from the (3,4,5) construction."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "both anchors decode deterministically and are exercised by the suite"
  rulings: []
  milestone: M3
- id: REQ-VAL-030
  area: VAL
  statement: "Energy error over one Kepler-embedded orbit (`m₂ → ε`) must scale as `dt²` for KDK, `dt⁴` for Yoshida-4 and RK4, and `dt⁶` for Yoshida-6 across a dt ladder."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  verify:
    method: numerical gate
    detail: "dd test 1: fitted log-log slope of energy error vs dt equals the occupant's order within tolerance"
  rulings: []
  milestone: M3
- id: REQ-VAL-031
  area: VAL
  statement: "On a long bounded orbit, KDK/Yoshida energy error must oscillate about `E_0` without secular trend while RK4 and Euler drift secularly, and Euler must light up `SUSPECT_ENERGY`."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 2 — Occupants and the capability profile
  verify:
    method: numerical gate
    detail: "dd test 2: no secular trend (fitted slope ≈ 0) for symplectic occupants; secular slope for RK4/Euler; Euler's energy-drift view shows SUSPECT_ENERGY (the pass condition)"
  rulings: []
  milestone: M3
- id: REQ-VAL-032
  area: VAL
  statement: "For each symplectic occupant, `STEP(dt)`, momentum negation, `STEP(dt)`, negation must return the state to precision (without wrapper projection or substepping)."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "dd test 3: residual at rounding level for KDK/Yoshida-4/Yoshida-6"
  rulings: []
  milestone: M3
- id: REQ-VAL-033
  area: VAL
  statement: A close-encounter IC must show `max|ΔE| ≫ |ΔE_final|`.
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  verify:
    method: unit test
    detail: "dd test 7: spike that recovered is visible in the stored max vs final"
  rulings: []
  milestone: M3
- id: REQ-VAL-034
  area: VAL
  statement: "The classical Burrau rest start must resolve to binary plus escaper within the default horizon, asserted as outcome class, escaper identity and a coarse `t_end` window qualitatively matching Szebehely & Peters (1967), never pointwise trajectories."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  - file: docs/design/principia_dd_validation_orbits.md
    section: 1.5 Burrau itself
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Chaos forbids pointwise comparison (exact vs structural)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Four tiers of ground truth (increasing in what they prove)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: numerical gate
    detail: "dd test 12: outcome class and escaper (lightest body) match; t_end within a stated window; compare close-encounter sequence and interaction topology structurally"
  rulings:
  - R-71
  - R-72
  milestone: M3
  note: "The coarse t_end window: REQ-VAL-132 (calibration); the feature set: REQ-VAL-127 (definition); the r_coll pin: REQ-VAL-128 (calibration)."
- id: REQ-VAL-035
  area: VAL
  statement: "Any result quoted past the predictability horizon must have higher precision or a shadowing argument before it is treated as a measurement, and escape statistics at t = 240 must stay labelled indicative until tested there."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: "5.1 Retrospective: which existing measurements are inside the horizon"
  - file: docs/design/principia_dd_predictability_horizon.md
    section: "7.6 Escape statistics are shadowing-robust to `t=80`, but `t=240` is untested"
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 7.2 `lambda` is 0.6–0.8, not 1 — and `t` is not e-foldings
  verify:
    method: review checklist
    detail: "each quoted long-t figure cites its precision or shadowing evidence"
  rulings: []
  milestone: M3
  note: "§5.1 quotes t ≈ 36 (f64) / 16 (f32); §7.2 revises to ≈ 52 / 23 at lambda = 0.7"
- id: REQ-VAL-036
  area: VAL
  statement: "The change-10 cross-checks must be re-run with the patched NumPy reference and the divergence-vs-horizon table regenerated or retired before any further integrator measurement."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 7. Corrections from external replication (report 4)
  - file: decisions.md
    section: "R-35 — The change-10 cross-checks are re-run and the NumPy reference patched *(IE-7)*"
  - file: docs/design/principia_dd_validation_orbits.md
    section: 0.1 The mechanism, and the fix — measured, not proposed
  - file: docs/design/principia_dd_validation_orbits.md
    section: 5. Immediate action
  - file: decisions.md
    section: R-119 — `t_max(f32)` is the GPU measurement *(closes RQ-87)*
  - file: open-questions.md
    section: Pending-changes register
  verify:
    method: numerical gate
    detail: "re-run outputs recorded; table regenerated or marked retired; the f64 horizon figure and the measurement method REQ-VAL-071 applies to the GPU kernel are recorded (R-119)"
  rulings:
  - R-35
  - R-119
  milestone: M3
- id: REQ-VAL-037
  area: VAL
  statement: "The word truncation rate must be measured to decide whether 76 symbols suffice."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "record the fraction of truncated words over the reference charts at production horizon"
  rulings:
  - R-71
  milestone: M3
  note: "The acceptance threshold: REQ-PAY-083 (calibration)."
- id: REQ-VAL-038
  area: VAL
  statement: "The displacement-vs-absolute f32 shadow choice must be tested against known-Lyapunov periodic orbits, switching to displacement only if FTLE accuracy meaningfully improves."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "FTLE error vs the known exponent for both shadow forms on the periodic-orbit set"
  rulings:
  - R-71
  milestone: M3
  note: "'Meaningfully': REQ-PAY-085 (calibration)."
- id: REQ-VAL-039
  area: VAL
  statement: "The `sim_failed`/`decode_failed` detail codes must be confirmed against the integrator contract's failure enumeration once finalised."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "cross-check payload §2's failure enum against the integrator contract's list"
  rulings:
  - R-72
  milestone: M3
  note: "The DEGENERATE reasons and their map onto the decode_failed detail codes: REQ-DEC-038 (definition)."
- id: REQ-VAL-040
  area: VAL
  statement: "The escape criterion's precision and recall must be re-validated with R-29's `E_rel` against check 2's independent ground truth (pitfalls §2.4), with the legacy t = 30 set kept as a comparison, and the `tau` gap re-measured."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 3 — Parameter ownership (who owns what)
  - file: docs/design/principia_dd_integrator.md
    section: 3.6 Detectors (per `STEP`, on the projected state)
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: R-166 — The fixtures
  - file: open-questions.md
    section: Pending-changes register
  verify:
    method: numerical gate
    detail: "re-run the escape validation against check 2's ground truth and against the legacy t = 30 set, which the validation harness regenerates on the config slices (config_stability, config_basin; fixtures/slices.toml) at t = 30 under the legacy classifier (docs/reference/prin-rs/src/outcome.rs) and checks in as a fixture (R-166); record precision, recall (previously 100% / 96.3% on the legacy set) and the tau gap (previously 383×) for both"
  rulings:
  - R-29
  - R-95
  - R-166
  milestone: M3
  note: "pitfall: \"2.2 The criterion\" — doc numbers predate R-29 and are measured on the legacy t = 30 ground truth; R-95 moves the re-validation to check 2's independent ground truth."
- id: REQ-VAL-041
  area: VAL
  statement: "The figure-eight closure test must integrate one period (`T = 6.32591398`, `E = −1.2871419918`, `L_z = 0`, `Σp = 0`) and report `|dr|` across an `eta`/step ladder together with the fitted convergence order, which must match the occupant's stated order."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: 5. Immediate action
  - file: docs/design/principia_dd_validation_orbits.md
    section: 1.2 Figure-eight — the canonical closure test
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "closure |dr| per eta and fitted order; fail if the order is below the occupant's (the check that exposed §0's first-order error)"
  rulings:
  - R-71
  milestone: M3
  note: "The |dr| pass threshold and the order tolerance: REQ-VAL-133 (calibration)."
- id: REQ-VAL-042
  area: VAL
  statement: "Two or three free-fall (brake) periodic orbits from Hristov et al. 2023 or the Three-body Gallery must be added as closure tests asserting a return to rest in the same configuration, with the convergence order reported."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: 5. Immediate action
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.1 Free-fall periodic orbits — the best fit for this project"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: numerical gate
    detail: "each orbit returns to rest in the same configuration after one period; order reported"
  rulings:
  - R-71
  - R-72
  milestone: M3
  note: "Which orbits: REQ-VAL-126 (definition); the closure threshold: REQ-VAL-133 (calibration)."
- id: REQ-VAL-043
  area: VAL
  statement: "A word-calculus test must assert the free-group word encoder reproduces the published braid class of Šuvakov–Dmitrašinović orbits."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.4 Šuvakov–Dmitrašinović (13 families, 2013) and Broucke–Hénon–Hadjidemetriou"
  - file: decisions.md
    section: "R-125 — The branch-cut convention is M3's, transcribed from the literature *(closes RQ-96)*"
  verify:
    method: unit test
    detail: "for each gallery orbit used, computed word (under REQ-PAY-070's transcribed convention) equals the published class"
  rulings:
  - R-125
  milestone: M3
- id: REQ-VAL-044
  area: VAL
  statement: "The reversibility test on free-fall brake orbits must be implemented on the reversible occupant, logH's TTL time mode (R-162)."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.1 Free-fall periodic orbits — the best fit for this project"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: R-162 — The reversible occupant is logH's TTL time mode
  - file: docs/contracts/principia_integrator_contract.md
    section: "Part 2a — Widening the slot: `owns_time_mapping`, and the `advance` signature"
  verify:
    method: numerical gate
    detail: "runs on logH-TTL; asserts time symmetry via the reversibility measure `xi`"
  rulings:
  - R-72
  - R-162
  milestone: M3
  note: "xi: REQ-VAL-134 (definition)."
- id: REQ-VAL-045
  area: VAL
  statement: "The two-body radial collision gate must be kept as the regularisation test, since collisionless periodic orbits do not exercise regularisation."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 4. What the suite cannot do
  verify:
    method: numerical gate
    detail: "radial collision case passes through d_min ≈ 1e-11 with bounded energy drift (the AZ validation reported 6.2e-15 at d_min = 1.35e-11)"
  rulings: []
  milestone: M3
- id: REQ-VAL-046
  area: VAL
  statement: "A ground-truth validation suite, separate from parity, must compare the f64 CPU instantiation against external truth in four tiers: conservation, analytic solutions (Lagrange, Euler), known periodic orbits (figure-eight, Broucke–Hadjidemetriou–Hénon, catalogue orbits), and literature benchmarks (Burrau, Lehto et al.), comparing chaotic cases (Burrau) at structural features only, never pointwise."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "Self-consistency ≠ correctness (why this is a separate suite)"
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Four tiers of ground truth (increasing in what they prove)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "The correctness factoring: validate on CPU, inherit on GPU"
  - file: docs/contracts/principia_parity_contract.md
    section: 7. What this contract does *not* cover
  - file: docs/contracts/principia_canonical_spec.md
    section: "7. Precision & validation model *(authoritative: `parity_contract`, `validation_ground_truth_note`, `core_design`)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "suite exists with at least one case per tier, run on the CPU-f64 build; GPU correctness is claimed only via parity"
  rulings:
  - R-72
  milestone: M3
  note: "Which Lehto et al. numbers are benchmarked: REQ-VAL-126 (definition)."
- id: REQ-VAL-047
  area: VAL
  statement: "Non-chaotic ground-truth cases (analytic and periodic) must be checked precisely: the Lagrange triangle stays rigid (equilateral at every sampled `t`, not just at `T`) and the figure-eight closes and traces its known shape-sphere curve."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Four tiers of ground truth (increasing in what they prove)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Chaos forbids pointwise comparison (exact vs structural)
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.3 Lagrange and Euler central configurations — the only analytic ones"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "Lagrange circular: equilateral residual at every sampled t within a tight f64 tolerance; figure-eight shape-sphere curve matches the published one"
  rulings:
  - R-71
  milestone: M3
  note: "Tolerances: REQ-VAL-130 (calibration)."
- id: REQ-VAL-048
  area: VAL
  statement: "Known-IC validation must use Path A for interior ICs — canonicalise into the gauge (CoM, orientation, I = 1, body order), invert the decode, check the round-trip residual, then run `z` through the general forward pipeline and check behaviour — and Path B for exactly-degenerate analytic configs, via a lower harness entry that takes physical `(m, r, p)` and bypasses decode."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Two entry paths (routed by interior-vs-degenerate)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "Everything is representable in principle — so a failed round-trip is a BUG signal"
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/contracts/principia_inverse_encode_contract.md
    section: "Part 7 — Ground-truth ingestion (the validation programme's demand)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "figure-eight/Burrau via Path A report CHECK 1 residual and CHECK 2 behaviour; Euler collinear via Path B entry point"
  rulings:
  - R-72
  milestone: M3
  note: "The Path B API is a validation-only entry parallel to computeIC; its signature: REQ-VAL-129 (definition)."
- id: REQ-VAL-049
  area: VAL
  statement: "Benchmark comparisons must record and fix `r_coll` to match the regularisation the reference computation assumed."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "Burrau and other benchmark runs carry the pinned r_coll in provenance"
  rulings:
  - R-71
  milestone: M3
  note: "The r_coll value: REQ-VAL-128 (calibration)."
- id: REQ-VAL-050
  area: VAL
  statement: "Smooth, clean geometric boundaries in a chaotic field, structures that start and stop, and two implementations agreeing where tame but diverging elsewhere must be treated as signatures of a logic fault (a field sampled at inconsistent times), and the build must keep a cross-implementation comparison that can localise such faults."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.7 What to recognise next time
  - file: docs/read_first/principia_01_pitfalls.md
    section: 1.1 What was observed
  verify:
    method: review checklist
    detail: "Artefact triage checklist includes the three §1.7 signatures; for any flagged image, t_end (or the termination-time field) is rendered to test whether the boundaries are its level sets, and the independent occupant is run on the same ICs to compare tame vs wild regions."
  rulings: []
  milestone: M3
  note: "pitfall: \"1.7 What to recognise next time\"; pitfall: \"1.1 What was observed\" — 1.7 What to recognise next time; §1.1 records the user observation that the ribbons match where well behaved."
- id: REQ-VAL-051
  area: VAL
  statement: "The tau gap must be re-measured with the corrected E_rel, and tau must be set inside the measured gap between escapers' and bound trajectories' |Δn̂|, not tuned."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.2 The criterion
  - file: decisions.md
    section: "R-29 — The escape criterion's undefined parts *(IE-1, amended)*"
  - file: open-questions.md
    section: Pending-changes register
  verify:
    method: numerical gate
    detail: "Measure the |Δn̂| distributions of escapers and bound trajectories across t = 25–30; record the gap ratio (claimed 383x, 7.04e-05 vs 2.70e-02; prin-rs reports at best 6.8x) and assert the chosen tau lies within the gap and that outcomes are unchanged across the middle of the gap."
  rulings:
  - R-29
  milestone: M3
  note: "pitfall: \"2.2 The criterion\" — '383x, untuned' is marked 'to re-measure' until this is done."
- id: REQ-VAL-052
  area: VAL
  statement: "The re-registration regression (wedges from repeated chart re-registration) must not recur: the sensitivity of the drift field to doubling the sync-boundary re-registration count at fixed step size must be measured for the default occupant."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.1 The finding
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.2 What it caused
  - file: docs/read_first/principia_01_pitfalls.md
    section: "4. RE-REGISTRATION COUNT — the mechanism that had to be found twice"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "Double the sync-boundary re-registration count with eta adjusted so steps p50 stays flat within 6%; record the drift-field change in decades against the controls (LC branch 2.5e-6, hysteresis 7.5e-5, AZ re-registration x2 4.4e-1); render config_stability and assert no pale straight-edged wedges."
  rulings:
  - R-71
  milestone: M3
  note: "pitfall: \"4.1 The finding\"; pitfall: \"4.2 What it caused\"; pitfall: \"4. RE-REGISTRATION COUNT — the mechanism that had to be found twice\" — 4.1 The finding / 4.2 What it caused. The pass threshold: REQ-VAL-136 (calibration)."
- id: REQ-VAL-053
  area: VAL
  statement: "The logH-vs-Heggie comparison harness must run both arms under the same RK4 stepper and the same step control, and must refuse mismatched arms."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.4 The remedy, and its own falsification test
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.5 Regularisation-method comparison as a scientific result
  - file: decisions.md
    section: "R-74 — The research phases are settled by the vertical slice *(closes RQ-25)*"
  verify:
    method: unit test
    detail: "the harness asserts both arms share the stepper and step-control configuration; a run configured with a different stepper or step control on one arm is refused"
  rulings:
  - R-74
  milestone: M3
  note: "pitfall: \"4.4 The remedy, and its own falsification test\" — same RK4, same step control, or the comparison scores the integrator. The falsification check itself is the R-74 M3 validation requirement, REQ-VAL-115."
- id: REQ-VAL-054
  area: VAL
  statement: "Pitfalls §2.4's escape checks 2 (independent ground truth: separation growing without bound) and 3 (the `deep interior` chart) must be run in the validation harness, which marches past escape on its own state, with check 1 recorded as passed from prin-rs."
  source:
  - file: decisions.md
    section: "R-31 — Escape doesn't terminate until pitfalls §2.4's three checks pass *(IE-3)*"
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: R-95 — After escape fires *(closes RQ-48, in part)*
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  verify:
    method: numerical gate
    detail: "recorded results of check 2 and check 3 on the corrected detector"
  rulings:
  - R-31
  - R-71
  - R-95
  - R-103
  milestone: M3
  note: "pitfall: \"2.4 Should it terminate?\" — the current ground truth shares a term with the criterion; the config chart's base rates are too high to discriminate. R-103: the checks belong to the harness; production ends the loop at escape. Each check's pass threshold, horizon and fixture: REQ-VAL-123 and REQ-VAL-124 (calibration)."
- id: REQ-VAL-055
  area: VAL
  statement: "δ_dep must be set by a recorded measurement, and the placement of the per-sample 'departed' bit must be decided and written into the ledger."
  source:
  - file: decisions.md
    section: "R-37 — `t_min` is a departure threshold on the shape sphere *(PL-2 (b))*"
  - file: docs/design/principia_dd_validation_orbits.md
    section: 6. The closure field — from validation to discovery
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: open-questions.md
    section: Pending-changes register
  verify:
    method: numerical gate
    detail: "Measurement report of |n̂(t) − n̂(0)| distributions on periodic-orbit and generic fixtures justifying δ_dep; ledger row for the departed bit exists and schema version changes."
  rulings:
  - R-37
  milestone: M3
  note: "R-37: set by measurement; no protocol is given. The departed bit's placement is tracked in open-questions.md (R-37's entry)."
- id: REQ-VAL-056
  area: VAL
  statement: "Parity, branch determinism and the toolchain must be proven in a native cargo test before the browser build is developed on top."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  verify:
    method: review checklist
    detail: "the milestone order puts the native parity/determinism test green before browser work"
  rulings: []
  milestone: M4
- id: REQ-VAL-057
  area: VAL
  statement: "The parity suite must be permanent in CI and run on every commit as a native in-process #[test] that instantiates the CPU-f64 kernel, dispatches the GPU kernel via native wgpu, reads back SimState and diffs it in the same process, with branch decisions bit-exact on identical inputs, per step, labels on chaotic trajectories allowed to differ across precisions (reported, not failed), and continuous words compared within envelope, aimed at backend miscompilation and branch determinism."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/contracts/principia_canonical_spec.md
    section: "7. Precision & validation model *(authoritative: `parity_contract`, `validation_ground_truth_note`, `core_design`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_core_design.md
    section: "1. Physics defined once, compiled twice — now *structurally*, not by discipline"
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: docs/notes/principia_gpu_determinism_note.md
    section: "The parity consequence (what shared source does and does not buy)"
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  verify:
    method: property test
    detail: "cargo test runs the CPU-vs-GPU SimState diff; branch decisions exact on shared per-step inputs; continuous within the windowed tolerance; chaotic-trajectory label flips are reported, not asserted equal"
  rulings:
  - R-84
  milestone: M4
- id: REQ-VAL-058
  area: VAL
  statement: "The spread-extrapolation default (on vs opt-in) must be decided and recorded."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  - file: docs/contracts/principia_checkerboard_contract.md
    section: "2. The mechanism: reconstruct-from-previous, resolved through SSAA"
  verify:
    method: review checklist
    detail: "a recorded decision with the measured error reduction (≈ −69%) and its cost"
  rulings: []
  milestone: M4
- id: REQ-VAL-059
  area: VAL
  statement: "Parity Tier L must assert exact agreement, on identical inputs, per step, of: `N_sub`, the terminal label and its precedence resolution, the `state` enum, escaper identity, collision pair (`detail`), feasibility and degenerate branch + reason, mirror-tie choice, coincident-pair rejection, decode-mode selection and loop iteration structure."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: Tier L — exact, no tolerance (the bulk of the suite)
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: "stateless test over boundary states: every listed decision identical across CPU-f64, CPU-f32 and native GPU (Metal, and lavapipe as the second native backend, R-110); spike baseline 705 states, 0 forks"
  rulings:
  - R-30
  - R-32
  - R-84
  - R-110
  - R-113
  milestone: M4
  note: "R-113 (RQ-97): the browser-GPU leg is the M8 browser Tier-L requirement closed with REQ-VAL-116."
- id: REQ-VAL-060
  area: VAL
  statement: "Parity Tier B must assert bit-exact CPU↔GPU agreement, given the same branch decisions and integer inputs, of `t_end_step`, `t_dmin_step`, `total_substeps`, every packed descriptor field (`state`, `detail`, `saturated`, `dmin_pair`, `last_symbol`) and its bit offset, and the free-group word arithmetic including `fgw_length_raw`; the f16 display scalars must not be asserted at Tier B."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier B — integer-exact *given the same branch decisions* (integer & packed fields)"
  - file: docs/notes/principia_gpu_determinism_note.md
    section: "The parity consequence (what shared source does and does not buy)"
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: property test
    detail: "feed identical branch decisions/integer inputs to both sides; fields match bit-for-bit; d_min, dE_max, dLz_max checked at Tier N/S only"
  rulings:
  - R-84
  - R-86
  milestone: M4
  note: "R-86: descriptor bits 8–9 are last_symbol. R-84: along a chaotic trajectory the branch decisions, and so these fields, may differ across precisions."
- id: REQ-VAL-061
  area: VAL
  statement: "Parity Tier N must compare elementwise within tight tolerance: decode → `(m, r, p)`, `ICDescriptor` and `E₀`; one STEP from a shared state for every occupant; short pre-divergence trajectories against a growing bounded envelope; and the encode round-trip in physical units."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier N — tight numerical (checkable because nothing amplifies)"
  verify:
    method: numerical gate
    detail: "starting tolerances: IC/decode ~1e-5 relative; one-step state ~1e-5 scaled by force magnitude; E₀/L_z at t=0 ~1e-6 — to be replaced by measured values"
  rulings: []
  milestone: M4
- id: REQ-VAL-062
  area: VAL
  statement: "Parity on chaotic full trajectories must be structural only: never assert `‖state_cpu − state_gpu‖ < ε` at `t_end` on a chaotic pixel, assert the same outcome class only on non-chaotic fixtures, and assert `t_end` within a window, consistent divergence-onset time, and aggregate-survey agreement."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier S — structural only (asserting the pointwise gap would assert a falsehood)"
  - file: docs/contracts/principia_parity_contract.md
    section: "3. The load-bearing discipline: never accumulate before comparing"
  - file: docs/notes/principia_gpu_determinism_note.md
    section: "Why branches are special (continuous divergence is fine; branch divergence is not)"
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  verify:
    method: review checklist
    detail: "no pointwise end-state assertion exists for chaotic pixels in the suite; outcome-class equality is asserted only on fixtures marked non-chaotic"
  rulings:
  - R-84
  milestone: M4
- id: REQ-VAL-063
  area: VAL
  statement: "Per-step and per-decode parity comparisons must feed both pipelines the same input (including synthetic near-collision, high-substep and near-feasibility-edge states) and compare immediately, never after accumulation."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "3. The load-bearing discipline: never accumulate before comparing"
  verify:
    method: review checklist
    detail: "stepOnce harness takes shared states, not shared ICs; synthetic state generators exist for the three named regimes"
  rulings: []
  milestone: M4
- id: REQ-VAL-064
  area: VAL
  statement: "Tier-N tolerances must be set by a recorded measurement of the spread on the decode/step quantities with the kernel run through native in-process wgpu, covering it comfortably, rather than guessed or validated only against f64, and no Dawn CI backend may be required."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: 4. Tolerance — and the cross-backend reality
  - file: decisions.md
    section: "R-85 — Native wgpu sets the Tier-N tolerances *(closes RQ-36)*"
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  verify:
    method: numerical gate
    detail: "run the kernel through native in-process wgpu; record the spread per quantity; set the tolerance above it; CI has no Dawn job"
  rulings:
  - R-85
  milestone: M4
- id: REQ-VAL-065
  area: VAL
  statement: "An aggregate-survey agreement test must compute the same region on a uniform grid (not quadtree leaves) on both instantiations and assert agreement of outcome-class fractions, class-boundary location within a pixel or two, and (where computed) island prevalence and decay-time distribution shape, within sampling noise."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "5. The aggregate-survey agreement test (what certifies \"the picture is the same\")"
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-134 — When the Playwright suite and the aggregate survey run *(closes RQ-112)*"
  verify:
    method: numerical gate
    detail: "nightly and before-release headless run (R-134); fractions within grid sampling noise; boundary sets match within 1–2 pixels"
  rulings:
  - R-71
  - R-134
  milestone: M4
  note: "The statistic and threshold (Q3): REQ-VAL-125 (calibration)."
- id: REQ-VAL-066
  area: VAL
  statement: "The winding-consistency view must compare the derived orbit_count = ⌊|θ̃|/2π⌋ and retrograde = θ̃ < 0 against the running θ̃ accumulator."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  verify:
    method: unit test
    detail: "synthetic θ̃ values straddling multiples of 2π give the expected orbit_count/retrograde"
  rulings: []
  milestone: M4
- id: REQ-VAL-067
  area: VAL
  statement: "The Welford diffusion view must compare the fragment slope C_ty/C_tt against a reference computed the same way, and a full fit-vs-fit check must live in the CPU parity suite via computeIC."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: unit test
    detail: "fragment slope equals CPU-recomputed slope from the same accumulators bit-for-bit (or to f32 rounding)"
  rulings:
  - R-72
  milestone: M4
  note: "What the per-sample y is: REQ-PAY-073 (definition)."
- id: REQ-VAL-068
  area: VAL
  statement: "The word-bookkeeping view must compare fgw_reduced_length against the derived reduced-crossing count and fgw_truncated."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: decisions.md
    section: "R-86 — The payload doc governs the eight payload items *(closes RQ-37)*"
  verify:
    method: unit test
    detail: "synthetic words with known crossings: lengths agree when not truncated; truncated words flagged"
  rulings:
  - R-86
  milestone: M4
  note: Accessor names are payload §6's (R-86).
- id: REQ-VAL-069
  area: VAL
  statement: "The drift-shape view must compare final energy_drift against delta_E_max_abs (and the same pair for L_z)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  verify:
    method: golden image
    detail: "synthetic payload with a recovered transient spike shows max > final; secular loss shows max ≈ final"
  rulings: []
  milestone: M4
- id: REQ-VAL-070
  area: VAL
  statement: "The CPU↔GPU continuous-value cross-check must run only for t < t_max(f32), with t_max(f32) the value measured against the GPU kernel (REQ-VAL-071), and must be reported as not applicable beyond it."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 4.1 The two kernels have different horizons
  - file: docs/design/principia_dd_generation_root.md
    section: Refinement — the scaling exponent
  - file: decisions.md
    section: "R-93 — The f32 predictability horizon gates the cross-check only *(closes RQ-44)*"
  - file: decisions.md
    section: "R-35 — The change-10 cross-checks are re-run and the NumPy reference patched *(IE-7)*"
  - file: decisions.md
    section: R-105 — R-93's re-run is R-35's *(closes RQ-65)*
  - file: decisions.md
    section: R-119 — `t_max(f32)` is the GPU measurement *(closes RQ-87)*
  verify:
    method: unit test
    detail: "the gate reads REQ-VAL-071's recorded GPU measurement of t_max(f32); inspector/cross-check past it reports 'not applicable' rather than a divergence"
  rulings:
  - R-35
  - R-93
  - R-105
  - R-119
  milestone: M4
  note: "R-119: the gate reads the GPU measurement; R-35's change-10 re-run (REQ-VAL-036) supplies the f64 figure and the method (R-93 and predictability §4.1 conformed). R-93 also says t_max annotates refinement and does not gate it."
- id: REQ-VAL-071
  area: VAL
  statement: "The f32 predictability horizon must be measured against the GPU kernel directly rather than derived."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 6. Open
  - file: docs/design/principia_dd_predictability_horizon.md
    section: 7.2 `lambda` is 0.6–0.8, not 1 — and `t` is not e-foldings
  - file: decisions.md
    section: "R-93 — The f32 predictability horizon gates the cross-check only *(closes RQ-44)*"
  - file: decisions.md
    section: R-105 — R-93's re-run is R-35's *(closes RQ-65)*
  - file: decisions.md
    section: R-119 — `t_max(f32)` is the GPU measurement *(closes RQ-87)*
  verify:
    method: numerical gate
    detail: measured f32 horizon recorded from GPU-kernel runs
  rulings:
  - R-93
  - R-105
  - R-119
  milestone: M4
  note: "R-119: this GPU measurement is the t_max(f32) REQ-VAL-070's gate reads; R-35's change-10 re-run (REQ-VAL-036) supplies the f64 figure and the method."
- id: REQ-VAL-072
  area: VAL
  statement: "The packed descriptor, `t_end_step`, `t_dmin_step`, `closure_step`, `total_substeps`, the `state` enum values and the word's integer mixed-radix arithmetic must match bit-for-bit between CPU and GPU."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  verify:
    method: property test
    detail: "run the parity fixture set on CPU and GPU; assert bitwise equality of these fields (for trajectories away from the crossing ambiguity band)"
  rulings:
  - R-34
  milestone: M4
- id: REQ-VAL-073
  area: VAL
  statement: "Particle state, energy, `L_z` and trajectory-derived quantities must match between the GPU path and the f64 CPU inspector path within declared tolerances (not bit-for-bit)."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  verify:
    method: numerical gate
    detail: "compare GPU vs f64 CPU on the parity fixtures; assert |diff| within the parity contract's declared tolerances (tolerances are not stated in this file)"
  rulings: []
  milestone: M4
- id: REQ-VAL-074
  area: VAL
  statement: "The closure precision floor (about 1e-7 on the f32 GPU path, about 1e-16 on the f64 CPU path) must be reported as a saturation limit, not treated as a lost orbit."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: Why closure is here, at this width, unconditionally
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "closure readouts/exports carry the precision path and its floor; a 1e-9 reference orbit reading ~1e-7 on GPU is labelled saturated"
  rulings:
  - R-72
  milestone: M4
  note: "Where the floor is reported (readout, export, legend): REQ-PAY-074 (definition)."
- id: REQ-VAL-075
  area: VAL
  statement: "CPU↔GPU binary word parity must be tested on trajectories away from the numerical-ambiguity band, or under a fully specified quantised comparison."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  verify:
    method: property test
    detail: "parity fixture words are bit-identical CPU vs GPU outside the ambiguity band"
  rulings: []
  milestone: M4
- id: REQ-VAL-076
  area: VAL
  statement: "Rendering the closure field on the Config α×β chart's `z_q = 0` plane must show low closure at the locations of known free-fall periodic orbits."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 6.1 Why rendering beats searching
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: golden image
    detail: "render the z_q = 0 plane; known free-fall orbit locations light up"
  rulings:
  - R-71
  milestone: M4
  note: "The 'lights up' threshold: REQ-VAL-137 (calibration)."
- id: REQ-VAL-077
  area: VAL
  statement: "Cross-check views must confirm: derived orbit_count = ⌊|θ̃|/2π⌋ and retrograde = θ̃ < 0 agree with the running θ̃ with the terminal latch respected; the Welford slope C_ty/C_tt matches a reference computed identically; and energy_drift (final) vs delta_E_max_abs (max) distinguishes secular loss from a recovered spike."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "G. Cross-check views (certify a *seam*, not a field — integration tests with a display)"
  verify:
    method: unit test
    detail: each cross-check passes on synthetic and golden-IC payloads
  rulings: []
  milestone: M4
- id: REQ-VAL-078
  area: VAL
  statement: "Descriptor determinism must be audited at the branch inputs that feed it, not at the packing expression."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN
  verify:
    method: review checklist
    detail: "Parity audit enumerates the branch inputs feeding each descriptor field (per R-34's enumerated list) and fork-tests those, since packed and packed_ctl are the same shift-and-mask (0 forks vs 83)."
  rulings:
  - R-34
  milestone: M4
  note: "pitfall: \"9. A PARITY CHECK THAT MASKS THE BITS THE FORK LANDS IN\" — 9. — a descriptor is exactly as deterministic as the branches feeding it."
- id: REQ-VAL-079
  area: VAL
  statement: "A determinism result must state its domain: cross-backend outcome-label agreement must be tested on accumulated trajectories, not inferred from fixed-input single comparisons, and label differences caused by differing inputs must be reported as precision."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 10. GENERALISING A STATELESS RESULT TO A TRAJECTORY
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-186 — GitHub-hosted runners first; no self-hosted runner *(amends R-110, R-169, R-174)*"
  verify:
    method: numerical gate
    detail: "Parity run over full trajectories on two backends — Metal on GitHub-hosted macos-15 and lavapipe (R-110, R-186); report the label-flip rate and, for each flip, that the branch operation itself agreed on identical inputs (705 boundary states, 0 forks); the report header states the domain (fixed inputs vs trajectory)."
  rulings:
  - R-34
  - R-58
  - R-110
  - R-186
  milestone: M4
  note: "pitfall: \"10. GENERALISING A STATELESS RESULT TO A TRAJECTORY\" — 10. GENERALISING A STATELESS RESULT TO A TRAJECTORY."
- id: REQ-VAL-080
  area: VAL
  statement: "Totality must extend to reductions: a quad where nothing integrated must report Decision::Undetermined / footprint_undetermined (terminal and flagged), and no reduction may silently discard samples (e.g. filter(is_finite))."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  verify:
    method: unit test
    detail: "a quad of all-failed samples reports Undetermined, not a small spread; a grep finds no filtering in reductions"
  rulings:
  - R-47
  milestone: M5
- id: REQ-VAL-081
  area: VAL
  statement: "Checkerboard must be gated by a dt ceiling (disabled or its fraction reduced when the per-frame playhead advance exceeds it) such that mean SSAA-resolved reconstruction error stays ≲ 2 8-bit levels, with the ceiling (proxy ~0.05) measured on the M5 render (real integrator, the M1 debug/ramp colour views)."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 7. Measured imperceptibility (the evidence for the default)
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: "R-128 ✱ — The checkerboard's ceiling form is chosen at the M5 gate *(closes RQ-104)*"
  verify:
    method: numerical gate
    detail: "on the M5 render at E + 1 = 8, measure mean reconstruction error vs dt; ceiling set where it crosses ~2 levels; both forms above it — the hard gate and the ramp — are captured at the ceiling for the human's choice at the M5 gate (R-128)"
  rulings:
  - R-72
  - R-113
  - R-128
  milestone: M5
  note: "R-128: hard gate or ramp is chosen by the human at the M5 gate from the captures. The units of the ~0.05 ceiling: REQ-RENDER-076 (definition). R-113 (RQ-99 M5-3): the re-confirmation on the real VMF/OKLab mapping is a separate M7 requirement."
- id: REQ-VAL-082
  area: VAL
  statement: "The memory-pressure path must be tested with an artificially low cap, asserting that the system stays responsive, keeps drawing, reports cap-bound and never blanks."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: And test it deliberately
  verify:
    method: property test
    detail: "low-cap run: no blank frame, frame time within budget, cap-bound reported"
  rulings: []
  milestone: M5
- id: REQ-VAL-083
  area: VAL
  statement: "The impurity mask must compare each sample's class ⊕ detail with the quad's dominant_outcome at the joint grain (no majority_class field), and its spatial mean must equal outcome_impurity."
  source:
  - file: docs/design/principia_debug_tooling_plan.md
    section: "G. Cross-check views (certify a *seam*, not a field — integration tests with a display)"
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: decisions.md
    section: R-20 — No `majority_class` field *(closes RQ-18)*
  verify:
    method: unit test
    detail: mask mean equals the reduction's outcome_impurity
  rulings:
  - R-20
  milestone: M5
- id: REQ-VAL-084
  area: VAL
  statement: "Quantitative claims must either correct by the Jacobian of every arbitrary choice (link, warp, refinement density) or be barred, must be measured on uniform grids with |det J_D| rather than adaptive structure (whose density is never read as probability), and must survive link swaps and tilts to count as dynamics."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  verify:
    method: review checklist
    detail: "every quantitative figure's pipeline uses a uniform grid with Jacobian correction and is repeated under a link swap and a tilt"
  rulings: []
  milestone: M6
- id: REQ-VAL-085
  area: VAL
  statement: "Checkerboard, coarser-refinement-during-motion and E-reduction-under-motion must be confirmed to compose without over-degrading."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  verify:
    method: benchmark
    detail: "motion trace with all three active: reconstruction error and visual quality recorded against each alone"
  rulings: []
  milestone: M6
- id: REQ-VAL-086
  area: VAL
  statement: "Quadtree leaf density must never feed a quantitative claim: statistics such as basin fractions, island prevalence or fractal dimension must come from a flat grid at fixed resolution (uniform dispatch, no adaptive subdivision) or from a known measure with the link/warp Jacobian correction (|det J_D|), never from counting or density-weighting adaptive leaves."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: "Part 2 — Refinement density is not probability density (enforced here, because this is where it would break)"
  - file: docs/design/principia_deep_zoom.md
    section: 3. Three-layer quadtree
  verify:
    method: review checklist
    detail: "every quantitative statistic in the code base is traced to the uniform-grid measurement path or a |det J_D|-weighted estimator; no statistic reads quadtree leaf counts"
  rulings: []
  milestone: M6
- id: REQ-VAL-087
  area: VAL
  statement: "Whether S_word adds signal as a diagnostic must be measured once it is built, with S_word kept out of the split."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 8 — Continuous refinement & the live-to-live handoff
  - file: decisions.md
    section: "R-99 — The latch is per footprint and lives with the resident quad *(closes RQ-59)*"
  verify:
    method: numerical gate
    detail: "record where S_word fires relative to outcome impurity on the tolerance-study fixtures; the split's output is unchanged with S_word present or absent"
  rulings:
  - R-99
  milestone: M6
- id: REQ-VAL-088
  area: VAL
  statement: "The quality gate threshold and the integrator tolerance must be specified as a pair."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Open
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: a recorded measurement fixes (threshold, eta) together
  rulings:
  - R-71
  milestone: M6
  note: "The pair: REQ-PAY-082 (calibration)."
- id: REQ-VAL-089
  area: VAL
  statement: "alpha_E must not certify a frame (it measures integration error only and is a property of the aggregation), and error_ratio_max's max-aggregation must be treated as a correctness condition."
  source:
  - file: docs/design/principia_dd_predictability_horizon.md
    section: "7.1 `alpha_E` is structurally blind to the measurement horizon — my experiment could not work"
  - file: docs/design/principia_dd_predictability_horizon.md
    section: "7.4 The `t=40` failure is a rate, and mostly an aggregation artefact"
  verify:
    method: review checklist
    detail: "no acceptance gate uses alpha_E; frame certification uses error_ratio_max"
  rulings: []
  milestone: M6
- id: REQ-VAL-090
  area: VAL
  statement: "Zooming in must not make in-view texels grow or max depth go backwards (the 39 → 181 px, max_depth 12 → 8 regression), and the degeneration alpha_area closes must stay closed."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: "0.1 In view, the camera decides depth and the criterion decides ORDER"
  verify:
    method: benchmark
    detail: "scripted zoom over two octaves on config_stability and preset_shape_h1: converged in-view texel size stays flat (~1.5 px) and max_depth tracks camera target depth; quad count stays far below the alpha_lo = 0 degeneration (+49% / +222%)"
  rulings: []
  milestone: M6
- id: REQ-VAL-091
  area: VAL
  statement: "Refinement-saving benchmarks must report the exact-optimum ratio (dp/u) alongside tolerance-vs-uniform (tol/u), so a field with nothing to find is distinguished from a policy failing to find it."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 5. WHERE THE SAVING GENERALISES — and where it does not
  - file: docs/read_first/principia_01_pitfalls.md
    section: 6. READ `dp/u` BEFORE `tol/u`
  verify:
    method: review checklist
    detail: every refinement benchmark report carries dp/u next to tol/u
  rulings:
  - R-49
  milestone: M6
  note: "pitfall: \"6. READ `dp/u` BEFORE `tol/u`\" — 6. READ dp/u BEFORE tol/u."
- id: REQ-VAL-092
  area: VAL
  statement: "At the first refine milestone, the refinement policy's open measurements must be taken and recorded: depth beyond level 6, a calibrated grid / tau, and merging under real camera motion."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 7. Open
  - file: docs/design/principia_dd_refinement_policy.md
    section: 5. WHERE THE SAVING GENERALISES — and where it does not
  - file: decisions.md
    section: "R-49 — The refinement policy's open measurements are taken at the first refine milestone *(RS-9)*"
  verify:
    method: benchmark
    detail: "a recorded results entry covers each of the three measurements"
  rulings:
  - R-49
  milestone: M6
- id: REQ-VAL-093
  area: VAL
  statement: "The branch-cut crossing distribution must be measured to decide whether `S_word` is additive to outcome impurity."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "record crossing-count distribution and its correlation with outcome impurity on reference charts"
  rulings:
  - R-71
  milestone: M6
  note: "The decision threshold: REQ-PAY-084 (calibration)."
- id: REQ-VAL-094
  area: VAL
  statement: "The per-quad measure weight for quantitative claims must be |det J_D| taken from the same J_D the linear decode uses."
  source:
  - file: docs/design/principia_deep_zoom.md
    section: Couplings to the contract
  verify:
    method: unit test
    detail: measure weight equals |det J_D| for the quad; computed once
  rulings: []
  milestone: M6
- id: REQ-VAL-095
  area: VAL
  statement: "Whether symbolic spread is additive to outcome impurity must be measured once built and the result recorded."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Symbolic spread — the free-group word as a third resolve-stage reduction"
  verify:
    method: numerical gate
    detail: "record where S_word fires relative to outcome impurity and position spread on a reference survey"
  rulings: []
  milestone: M6
- id: REQ-VAL-096
  area: VAL
  statement: "Every recreated preset must pass a golden-image test against the corresponding render of the reference HTML files (docs/gui/reference/principia_colour_explorer.html and principia_colour_presets.html, the oracle by R-122); the golden suite is complete only when every §7.1 entry has a preset and a passing test."
  source:
  - file: docs/design/principia_colour_composition.md
    section: 7. Preset table & golden-image obligation
  - file: docs/design/principia_colour_composition.md
    section: 7.1 The complete map list (R-16)
  - file: decisions.md
    section: "R-16 — The colour PDF's map lists are ported *(closes RQ-14)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: R-76 — Stability × Hue is deleted *(closes RQ-27)*
  - file: decisions.md
    section: "R-122 — The reference HTML files are the colour oracle *(closes RQ-90 and RQ-101)*"
  verify:
    method: golden image
    detail: "per preset, render vs the reference HTML render within tolerance"
  rulings:
  - R-16
  - R-71
  - R-76
  - R-122
  milestone: M7
  note: "Golden tolerance: REQ-COL-052 (calibration). Stability × Hue is not in §7.1 (R-76)."
- id: REQ-VAL-097
  area: VAL
  statement: "Every map declared continuous (all vMF modes, the LUT sphere, latitude and longitude stripes for integer f, 3-D Cartesian noise) must have zero colour difference across dense sample pairs straddling the antimeridian and both poles, with the intentionally discontinuous maps excluded by name."
  source:
  - file: docs/design/principia_dd_colouring.md
    section: 5. Unit tests
  verify:
    method: property test
    detail: "dd_colouring unit test 2: exclusion list Octant, Voronoi 6, Hemispheres, Icosahedral, Fibonacci (hard), Checkerboard, Truchet"
  rulings: []
  milestone: M7
- id: REQ-VAL-098
  area: VAL
  statement: "Embed → PNG round-trip → extract must yield a bitwise-equal config at every supported resolution."
  source:
  - file: docs/design/principia_dd_image_embedding.md
    section: 9. Obligations
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: golden image
    detail: round trip at 64², 128², 256², 512², 1024²
  rulings:
  - R-71
  milestone: M7
  note: "The supported-resolution list: REQ-TOOL-113 (calibration)."
- id: REQ-VAL-099
  area: VAL
  statement: "The parity gate must be run on a non-Metal backend (Vulkan or D3D12) before any Paper-2 numbers are produced; it gates Paper 2, not the build."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 2. The determinism law (the other defining decision)
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: decisions.md
    section: "R-58 — The non-Metal parity run gates Paper 2, not the build *(TO-2)*"
  - file: docs/notes/principia_gpu_determinism_note.md
    section: "The parity consequence (what shared source does and does not buy)"
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  verify:
    method: numerical gate
    detail: "the parity suite passes on a real (hardware) Vulkan or D3D12 GPU — lavapipe, the per-commit second backend, does not satisfy this (R-110); recorded before Paper-2 survey results"
  rulings:
  - R-58
  - R-110
  milestone: M8
- id: REQ-VAL-100
  area: VAL
  statement: "The same spec object must re-render the same video bit-identically on the same backend and match across backends to the parity contract's f32 tolerances, with branch decisions identical on identical inputs, per step, and terminal labels and outcome classes identical except on chaotic trajectories, where they may differ across precisions."
  source:
  - file: docs/contracts/principia_export_animation_contract.md
    section: Part 5 — Determinism & reproducibility
  - file: docs/contracts/principia_export_animation_contract.md
    section: "Part 6 — Sharing: the spec is the object, the video is its shadow"
  - file: decisions.md
    section: R-84 — Branch decisions across precisions *(closes RQ-35)*
  verify:
    method: golden image
    detail: "render a spec twice on one backend: bit-identical frames; on a second backend: label / class images identical outside chaotic regions (flips there reported), values within parity tolerance"
  rulings:
  - R-84
  milestone: M8
- id: REQ-VAL-101
  area: VAL
  statement: "A calibration campaign must run each device across the whole ladder, including settings it will fail, long enough to see thermal behaviour, and invert the corpus to tier boundaries (for each device the highest eps and budget that held 16.7 ms, and where it fell back toward 41.7 ms)."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Push every device past where it is comfortable
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Then invert
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Thermal is the honest budget
  verify:
    method: benchmark
    detail: "campaign records per device across all settings; derived boundaries recorded"
  rulings: []
  milestone: M8
- id: REQ-VAL-102
  area: VAL
  statement: "The sonification spectrum of θ(t) must separate periodic from chaotic orbits: spectral entropy 0.076 for the figure-eight against 0.177 for the scratchpad's chaotic IC."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: numerical gate
    detail: "spectral entropy for the figure-eight ≈ 0.076 and for the chaotic reference ≈ 0.177; periodic < chaotic"
  rulings:
  - R-71
  - R-96
  - R-109
  milestone: M8
  note: "Measured values used as an anchor, stated in trajectory_viewing §1 (pointer_channels §2 is working notes, R-109). The chaotic IC and the tolerance: REQ-VAL-113 (definition); REQ-VAL-114 (calibration)."
- id: REQ-VAL-103
  area: VAL
  statement: "The minimum trajectory length t for a usable spectrum (about 20+ oscillation periods) must be set by a recorded measurement, not guessed."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "4. Audio-specific constraints, which are NOT the frame budget"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: numerical gate
    detail: "a recorded measurement of spectral line resolution vs t gives the minimum t used by listen"
  rulings:
  - R-109
  milestone: M8
- id: REQ-VAL-104
  area: VAL
  statement: "The two sonification predictions must be tested on inspector data: a line of ICs across a known fractal boundary sounds a note dissolving into noise and re-forming as a different note, and a 3:2 resonance gives a 3:2 frequency ratio (a fifth)."
  source:
  - file: docs/design/principia_scratchpad_pointer_channels.md
    section: "5. The prediction worth testing early, because it is the demo"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  verify:
    method: numerical gate
    detail: "spectral entropy along the line rises then falls across the boundary; the 3:2 resonant IC's top two peaks are in ratio 1.5"
  rulings:
  - R-96
  milestone: M8
  note: "Scratchpad: 'worth testing early'. pointer_channels is normative only where render_gui_spec, trajectory_viewing or a ruling cites it (R-96); render_gui_spec §G2 cites it for listen."
  retired: "R-109: only pointer_channels §4 is normative; §5's predictions have no normative source."
- id: REQ-VAL-105
  area: VAL
  statement: "Every row of the seam catalogue must have at least one integration test."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 8. What drills down from here
  verify:
    method: review checklist
    detail: "a test index maps each of seams 1–14 to at least one integration test"
  rulings: []
  milestone: M8
- id: REQ-VAL-106
  area: VAL
  statement: "The inspector must offer an optional shape-divergence overlay, off by default, that dispatches an on-demand one-pixel f32 GPU trace of the clicked IC through the survey kernel, writing a dense n(t) drawn over the CPU f64 curve at shared times."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 5. What is honestly comparable (click inspector)
  verify:
    method: unit test
    detail: "overlay off by default; enabling it dispatches one job; for a periodic IC the two curves coincide to f32 tolerance"
  rulings: []
  milestone: M8
- id: REQ-VAL-107
  area: VAL
  statement: "The inspector must report the CPU and GPU outcome class, escaper and t_end side by side, and disagreement in a regular region must be treated as a pipeline bug."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 5. What is honestly comparable (click inspector)
  verify:
    method: numerical gate
    detail: "on a regular-region fixture of ICs, CPU and GPU classification agree for every IC"
  rulings: []
  milestone: M8
- id: REQ-VAL-108
  area: VAL
  statement: "Every gauge-invariant Inspector readout must stay unchanged under the four whole-system handles."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: "Measurement readouts (gauge-invariant → hold under all four handles)"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: property test
    detail: "random ICs: translate-, boost- and rotate-all leave every readout unchanged to round-off; scale-all leaves angles, α, β and z unchanged"
  rulings:
  - R-72
  milestone: M8
  note: "The scratchpad lists the readouts as invariant; the frame scale-dependent readouts (R, |pᵢ|) are shown in under scale-all: REQ-GUI-145 (definition)."
- id: REQ-VAL-109
  area: VAL
  statement: "The Inspector window must call the engine's actual Rust decode and encode, and the standalone JS ic_inspector.html must be kept as an independent cross-implementation oracle whose shape-sphere n is cross-checked against the Rust on random ICs."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Build notes
  - file: decisions.md
    section: "R-14 — One shape-sphere convention, the IC Inspector's *(closes RQ-12, corrects R-12's premise)*"
  - file: decisions.md
    section: R-8 — The IC Inspector copies
  verify:
    method: property test
    detail: "random ICs: Rust n and the JS tool's n agree to the round-trip bound; the Inspector links no second decode implementation"
  rulings:
  - R-8
  - R-14
  - R-65
  milestone: M8
- id: REQ-VAL-110
  area: VAL
  statement: "The Measure tool must estimate by uncertainty exponent or box count with stated samples (pairs per ε), ε range and pair classifier (e.g. outcome class), and report the log-log fit α ± error and D = 2 − α for the slice."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 10 Measure — a tool on the figure
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "on a fixture region of known boundary dimension, D = 2 − α is reported with its fit error; the method, samples, ε range and classifier are shown"
  rulings:
  - R-71
  milestone: M8
  note: "The fixture, the tolerance on α ± error and the defaults: REQ-GUI-147 (definition); REQ-GUI-149 and REQ-GUI-148 (calibration)."
- id: REQ-VAL-111
  area: VAL
  statement: "The Measure tool must expose the resolution hazard as two actions: a threshold sweep and a matched N / 2N pair."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: 10 Measure — a tool on the figure
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: unit test
    detail: "each button produces its paired result; the N / 2N pair differs only in sample count"
  rulings:
  - R-39
  - R-71
  milestone: M8
  note: "The ε range, samples per ε and classifier defaults: REQ-GUI-148 (calibration); REQ-GUI-147 (definition)."
- id: REQ-VAL-112
  area: VAL
  kind: calibration
  statement: "The render_contract cross-check views' agreement tolerances — the invariant-chart gradient's allowed deviation from a perfect axis-aligned gradient and the Welford slope C_ty/C_tt against its reference must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal shows the measured f32 disagreement of each view on a healthy survey and a deliberately broken case each tolerance must flag"
  rulings:
  - R-71
  milestone: M4
- id: REQ-VAL-113
  area: VAL
  kind: definition
  statement: "The chaotic IC of the spectral-entropy anchor (0.177 against the figure-eight's 0.076) must be defined in trajectory_viewing §1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: review checklist
    detail: "trajectory_viewing §1 gives the chaotic IC (as z or physical state) and the horizon of the measurement"
  rulings:
  - R-72
  - R-96
  - R-109
  milestone: M8
  note: "Only pointer_channels §4 is normative (R-109), so the definition goes beside the anchor values in trajectory_viewing §1."
- id: REQ-VAL-114
  area: VAL
  kind: calibration
  statement: "The tolerance on the spectral-entropy anchor values (0.076 figure-eight, 0.177 chaotic) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"
  verify:
    method: numerical gate
    detail: "the proposal shows the entropy's spread under the spectrum's windowing and trajectory-length choices and a tolerance that still separates the two orbits"
  rulings:
  - R-71
  - R-109
  milestone: M8
- id: REQ-VAL-115
  area: VAL
  statement: "The logH falsification check of the re-registration mechanism must be run in M3 — logH (no coordinate transformation) against Heggie under the same RK4 and the same step control — and if logH does not match or beat Heggie the re-registration mechanism must be recorded as wrong."
  source:
  - file: decisions.md
    section: "R-74 — The research phases are settled by the vertical slice *(closes RQ-25)*"
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.8 Sequencing — what is next, and why in this order
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.4 The remedy, and its own falsification test
  verify:
    method: numerical gate
    detail: "run logH and Heggie on the same case matrix with the same RK4 and step control; record per-case err and whether logH matches or beats Heggie; the verdict (mechanism supported or recorded wrong) is written to pitfalls §4.4"
  rulings:
  - R-74
  milestone: M3
  note: "Philosophy §7.8 is superseded (R-74) but still states the test; canonical_spec §11 governs and names this as the one M3 validation requirement. The harness discipline (matched arms) is the logH-vs-Heggie harness requirement, REQ-VAL-053."
- id: REQ-VAL-116
  area: VAL
  statement: "Real browsers — Chrome stable and Safari (R-110) — must be checked against the Tier-N tolerances set on native in-process wgpu, with the browser build."
  source:
  - file: decisions.md
    section: "R-85 — Native wgpu sets the Tier-N tolerances *(closes RQ-36)*"
  - file: docs/contracts/principia_parity_contract.md
    section: 4. Tolerance — and the cross-backend reality
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  verify:
    method: numerical gate
    detail: "the browser build runs the Tier-N suite on Chrome stable and on Safari; every quantity falls within the native-wgpu tolerance; out-of-tolerance quantities are recorded"
  rulings:
  - R-85
  - R-110
  milestone: M8
- id: REQ-VAL-117
  area: VAL
  kind: calibration
  statement: "The E₀ agreement tolerance — the kernel's E_0 at t = 0 against the decode's derived K₀ + V₀ (the energy-agreement view and debug §G's ≤ tol) — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: "3.6 ICDescriptor derived quantities (decode-time, pre-integration)"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.4 `SimState` scalars — with presentation metadata
  - file: docs/design/principia_debug_tooling_plan.md
    section: "G. Cross-check views (certify a *seam*, not a field — integration tests with a display)"
  - file: docs/contracts/principia_render_contract.md
    section: Cross-check views (the seams)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures |E_0 − (K_0+V_0)| on a synthetic and a real survey at each precision and sets the tolerance above it; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-VAL-118
  area: VAL
  kind: calibration
  statement: "The tolerance to which Burrau at ν = 1/2 must reproduce (3,4,5) and the classical configuration must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal compares the ν = 1/2 decode with the (m,n) = (2,1) construction at f64 and states the tolerance; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-VAL-119
  area: VAL
  kind: definition
  statement: "The BodyPlane chart's map and the reference artefact it must reproduce bit-for-bit must be defined in chart_reference §5.1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_chart_reference.md
    section: 5.1 One trait, one dispatch
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  - file: decisions.md
    section: R-166 — The fixtures
  verify:
    method: review checklist
    detail: "chart_reference §5.1 gives BodyPlane's Φ and names the reference it matches: the Python reference's output (docs/reference/prin-rs/tools/xcheck, reference/*.py), recorded at M3 as a fixture (R-166); physics reviewer approved"
  rulings:
  - R-72
  - R-166
  milestone: M3
- id: REQ-VAL-120
  area: VAL
  kind: calibration
  statement: "The literature-import reference values (Anosova region-D and Burrau rest starts, as the papers state them) and the tolerance to which the imported invariants must match them must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_encode.md
    section: 4. Seams (obligations → integration tests)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal cites each paper's stated values, the recorded rescale and the measured import residuals; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-VAL-121
  area: VAL
  kind: calibration
  statement: "The Inspector's round-trip and invariance gates for the Rust Inspector (max ‖z − z′‖ and max ‖Δz‖ under the whole-system handles) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M2 gate and recorded in decisions.md."
  source:
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Status — as built (supersedes stale details below)
  - file: docs/notes/ic_inspector_scratchpad.md
    section: Whole-system handles — the invariance audit
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal re-measures the bounds on the Rust decode/encode (reference tool: 1.2×10⁻¹³ round trip, 1.6×10⁻¹⁴ handles) and states the gates; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M2
- id: REQ-VAL-122
  area: VAL
  kind: calibration
  statement: "The tolerance of the paired test asserting ‖n‖ = 1 for vector fields must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M1 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 4. The occupant model — typed by signature, free inside
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal measures ‖n‖ − 1 over a test render at f32 and states the tolerance; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M1
- id: REQ-VAL-123
  area: VAL
  kind: calibration
  statement: "Escape check 2's pass threshold, horizon and fixture (independent ground truth: separation growing without bound) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: "R-31 — Escape doesn't terminate until pitfalls §2.4's three checks pass *(IE-3)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  verify:
    method: numerical gate
    detail: "the proposal gives the separation-growth test, its horizon and the fixture set, with the check's result on the corrected detector; recorded in decisions.md"
  rulings:
  - R-31
  - R-71
  - R-103
  milestone: M3
  note: "R-103: check 2 runs in the validation harness, on its own state; the production loop ends at escape."
- id: REQ-VAL-124
  area: VAL
  kind: calibration
  statement: "Escape check 3's pass threshold, horizon and fixture (the deep-interior chart) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 2.4 Should it terminate?
  - file: decisions.md
    section: "R-31 — Escape doesn't terminate until pitfalls §2.4's three checks pass *(IE-3)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-103 — Escape ends the production loop; the §2.4 checks run in the harness *(closes RQ-63 and RQ-69)*"
  verify:
    method: numerical gate
    detail: "the proposal names the deep-interior chart region, the horizon and the pass threshold, with the check's result on the corrected detector; recorded in decisions.md"
  rulings:
  - R-31
  - R-71
  - R-103
  milestone: M3
  note: "R-103: check 3 runs in the validation harness, on its own state; the production loop ends at escape."
- id: REQ-VAL-125
  area: VAL
  kind: calibration
  statement: "The aggregate-survey agreement statistic and threshold (parity §5's Q3) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "5. The aggregate-survey agreement test (what certifies \"the picture is the same\")"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal names the statistic and sets the threshold from the measured sampling noise on uniform grids; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M4
- id: REQ-VAL-126
  area: VAL
  kind: definition
  statement: "The ground-truth fixture set — the analytic ICs to encode, the periodic orbits beyond the figure-eight, and which Lehto et al. numbers are benchmarked — must be defined in validation_ground_truth_note by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Four tiers of ground truth (increasing in what they prove)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the note lists each fixture with its source and the quantities asserted; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-VAL-127
  area: VAL
  kind: definition
  statement: "The Burrau structural feature set asserted against the literature (escaper identity, close-encounter sequence, approximate ejection time, as read from Szebehely and the recomputations) must be defined in validation_ground_truth_note by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/design/principia_dd_validation_orbits.md
    section: 1.5 Burrau itself
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the note lists each feature and how it is compared structurally; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-VAL-128
  area: VAL
  kind: calibration
  statement: "The r_coll pinned for benchmark comparisons, matching the regularisation the reference computation assumed, must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/contracts/principia_integrator_contract.md
    section: Part 7 — Detectors as the `SimState` producer
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal cites the reference computation's regularisation and derives r_coll from it; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-VAL-129
  area: VAL
  kind: definition
  statement: "The Path B harness entry (physical (m, r, p) → integrator + shape, bypassing decode, parallel to computeIC) must be defined in validation_ground_truth_note by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Two entry paths (routed by interior-vs-degenerate)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the note gives the entry's signature and what it bypasses; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-VAL-130
  area: VAL
  kind: calibration
  statement: "The ground-truth tolerances — the exact-case tolerances (tight, f64: Lagrange rigidity, figure-eight closure and shape curve) and the structural-case timing windows — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: Chaos forbids pointwise comparison (exact vs structural)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures each exact case at f64 and states its tolerance, and states each structural window; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
  note: "The figure-eight closure's |dr| threshold is the one REQ-VAL-133 calibrates (with the fitted-order tolerance per occupant); one recorded value serves both."
- id: REQ-VAL-131
  area: VAL
  kind: definition
  statement: "The independent convergence reference's protocol — how convergence gating proceeds, when it runs, and how it plugs into the harness — must be defined in validation_ground_truth_note by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/notes/principia_validation_ground_truth_note.md
    section: "The correctness factoring: validate on CPU, inherit on GPU"
  - file: decisions.md
    section: "R-33 — The independent convergence reference is Brutus-style *(IE-5, amended)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc states the precision/tolerance ladder and the stop rule, when the reference runs, and its harness interface; physics reviewer approved"
  rulings:
  - R-33
  - R-72
  milestone: M3
- id: REQ-VAL-132
  area: VAL
  kind: calibration
  statement: "The Burrau smoke test's coarse t_end window (dd_integrator test 12), given that the classical result sits past the default horizon, must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  - file: docs/design/principia_dd_validation_orbits.md
    section: 1.5 Burrau itself
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the window and horizon against Szebehely & Peters (1967) and validation_orbits §1.5; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-VAL-133
  area: VAL
  kind: calibration
  statement: "The figure-eight closure gate — the |dr| pass threshold after one period and the tolerance on the fitted order, including whether AZ+RK4's roughly third order is an expected failure — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 3. Proposed suite
  - file: docs/design/principia_dd_validation_orbits.md
    section: 0.1 The mechanism, and the fix — measured, not proposed
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal gives |dr| and fitted order per occupant across the eta/step ladder and states the threshold and tolerance; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-VAL-134
  area: VAL
  kind: definition
  statement: "The reversibility measure xi must be defined in dd_validation_orbits by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: "1.1 Free-fall periodic orbits — the best fit for this project"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the doc gives xi's formula and what a reversible occupant should read; physics reviewer approved"
  rulings:
  - R-72
  milestone: M3
- id: REQ-VAL-135
  area: VAL
  kind: calibration
  statement: "The convergence-under-refinement threshold an aggregate quantity must meet before it is quoted must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 3. Standing rules earned in this sequence
  - file: docs/read_first/principia_00_philosophy.md
    section: "4.5a A quantity that does not converge under refinement is measuring the sampling"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: R-171 — The convergence gate, defined *(closes A1, A2, T2)*
  verify:
    method: review checklist
    detail: "the proposal states the threshold on the finest r_k (R-171's definition), shown to fail the recorded 0.2153 → 0.4423 → 0.5494 → 0.0947 sequence and to pass a converging aggregate from the M3 march; replaces M0's provisional 0.1; recorded in decisions.md"
  rulings:
  - R-71
  - R-113
  - R-171
  milestone: M3
  note: "R-113 (RQ-93 M0-16): moved to M3; the convergence runner and gate stay in M0 with the provisional threshold 0.1 (R-171)."
- id: REQ-VAL-136
  area: VAL
  kind: calibration
  statement: "The default occupant's acceptance level for the re-registration control (the drift-field change, in decades, under doubled re-registration, against the controls 2.5e-6, 7.5e-5 and 4.4e-1) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/read_first/principia_01_pitfalls.md
    section: 4.1 The finding
  - file: docs/read_first/principia_01_pitfalls.md
    section: "4. RE-REGISTRATION COUNT — the mechanism that had to be found twice"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal measures the default occupant (Heggie) under the doubling protocol and states the acceptance level relative to the controls; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-VAL-137
  area: VAL
  kind: calibration
  statement: "The closure threshold at which known free-fall periodic orbits count as lighting up on the Config α×β chart's z_q = 0 plane must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_validation_orbits.md
    section: 6.1 Why rendering beats searching
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: numerical gate
    detail: "the proposal renders the plane, reads closure at the known orbit locations against the background and states the threshold; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M4
- id: REQ-VAL-138
  area: VAL
  kind: calibration
  statement: "The golden-image runner's diff metric and the default tolerance a golden-image test passes within (before REQ-COL-052 sets the preset-vs-reference tolerance) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M0 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: 6. The harness
  - file: docs/design/principia_colour_composition.md
    section: 7. Preset table & golden-image obligation
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  verify:
    method: review checklist
    detail: "the proposal states the metric (e.g. per-channel or per-pixel difference) and the default tolerance, shown to pass a re-render on the same backend (native wgpu offscreen, the golden renderer from M1, R-110) and to fail a one-variable change (REQ-VAL-003); recorded in decisions.md"
  rulings:
  - R-71
  - R-110
  milestone: M0
- id: REQ-VAL-139
  area: VAL
  kind: calibration
  statement: "dd_integrator tests 1 and 2's tolerances — on the fitted log-log slope of energy error against dt versus the occupant's order, and on the fitted secular slope that counts as 'no secular trend' — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 5. Unit tests
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal gives both tolerances with the measured slopes over the dt ladder per occupant, and shows RK4 and Euler fail the no-trend tolerance; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-VAL-140
  area: VAL
  kind: definition
  statement: "Tier N's growing-but-bounded envelope for short pre-divergence trajectories — its functional form in time and how the divergence-onset time that ends the comparison is determined — must be defined in parity_contract Tier N by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier N — tight numerical (checkable because nothing amplifies)"
  - file: docs/contracts/principia_parity_contract.md
    section: 4. Tolerance — and the cross-backend reality
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "Tier N states the envelope's form and the onset rule; its constants are set by REQ-VAL-064's native-wgpu measurement; physics reviewer approved"
  rulings:
  - R-72
  milestone: M4
- id: REQ-VAL-141
  area: VAL
  kind: calibration
  statement: "The numerical-ambiguity band outside which CPU↔GPU binary word parity is tested — how near a branch cut a crossing's signed distance (or τ) may fall before the trajectory is excluded — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal gives the band's width with the measured CPU/GPU crossing-distance spread as evidence, and the fraction of the parity fixture it excludes; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M4
- id: REQ-VAL-142
  area: VAL
  kind: calibration
  statement: "The bound that makes checkerboard, the motion refinement floor and E-reduction under motion compose \"without over-degrading\" (the allowed reconstruction error and visual-quality loss of all three together against each alone) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the bound with evidence: the motion trace's reconstruction error with all three levers and with each alone; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M6
- id: REQ-VAL-143
  area: VAL
  kind: definition
  statement: "The Measure tool's threshold sweep — which threshold it sweeps (the pair classifier's threshold or another), over what range, and what it reports must be defined in render_gui_spec §G10 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G10. Measure — a tool on the figure (`10_measure.png`)
  - file: docs/read_first/principia_00_philosophy.md
    section: "4.5a A quantity that does not converge under refinement is measuring the sampling"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section names the swept threshold and the reported result (α per threshold), and shows how it exposes the resolution hazard; physics reviewer approved"
  rulings:
  - R-72
  milestone: M8
- id: REQ-VAL-144
  area: VAL
  statement: "Parity Tier L's branch decisions (including N_sub) and the 100-macro-step `done`-flag dispatch must match the CPU branch words through the browser build's WGSL path (SPIR-V → WGSL → the browser's compiler)."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: Tier L — exact, no tolerance (the bulk of the suite)
  - file: docs/notes/principia_gpu_determinism_note.md
    section: The discipline (each rule = one measured failure)
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: property test
    detail: "in Chrome stable and Safari (R-110), the boundary-state set's Tier-L decisions and a 100-macro-step dispatch's branch words equal the CPU's (0 forks)"
  rulings:
  - R-110
  - R-113
  milestone: M8
  note: "Split from REQ-INT-028, REQ-VAL-059 and REQ-INT-059 (R-113, RQ-97); closed with REQ-VAL-116."
- id: REQ-VAL-145
  area: VAL
  statement: "Checkerboard's dt ceiling must be re-confirmed on the real VMF/OKLab colour mapping, and lowered if the mapping is more sensitive than the M5 measurement's."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: numerical gate
    detail: "on the real render with the VMF/OKLab mapping at E + 1 = 8, measure mean reconstruction error vs dt; where it crosses ~2 8-bit levels below the M5 ceiling, the ceiling is lowered to it"
  rulings:
  - R-113
  milestone: M7
  note: Split from REQ-VAL-081 (R-113, RQ-99 M5-3).
- id: REQ-VAL-146
  area: VAL
  kind: calibration
  statement: "The tolerance within which BodyPlane must reproduce the Python reference's output where the operation order differs must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: decisions.md
    section: R-166 — The fixtures
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: docs/design/principia_chart_reference.md
    section: 5.2 Tests that can fail
  verify:
    method: numerical gate
    detail: "the proposal lists the operations whose order differs from the Python reference, measures BodyPlane's difference against the recorded fixture and states the tolerance; recorded in decisions.md"
  rulings:
  - R-71
  - R-166
  milestone: M3
- id: REQ-PERF-001
  area: PERF
  statement: "The per-step energy evaluation must reuse the pairwise distances from the force computation."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: COM projection and invariant monitoring
  - file: docs/design/principia_dd_integrator.md
    section: 3.1 Force (G = 1)
  verify:
    method: review checklist
    detail: "energy potential term reads the pair distances already computed for the force; ‖·‖³ is shared with r_min"
  rulings: []
  milestone: M3
- id: REQ-PERF-002
  area: PERF
  statement: "The kernel's hot path (physics and driver layers) must perform no allocation."
  source:
  - file: docs/contracts/principia_integrator_contract.md
    section: Rules the new kernel must hold by construction
  verify:
    method: review checklist
    detail: "no Vec/Box/dyn/std allocation in step, deriv, Hamiltonians, physics/ or the driver loop"
  rulings: []
  milestone: M3
- id: REQ-PERF-003
  area: PERF
  statement: "The rayon pool must leave at least one core free (more on small-core devices) and be configurable."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: CPU
  verify:
    method: unit test
    detail: "pool size ≤ available cores − 1 by default; config overrides it"
  rulings: []
  milestone: M3
  note: "How many more cores small-core devices keep free: REQ-PERF-083 (calibration)."
- id: REQ-PERF-004
  area: PERF
  statement: "One hover trajectory must fit inside a 60 fps frame on a single CPU core at production horizons, so it can run every frame during motion."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-144 — pointer_channels §3 is normative too *(closes RQ-74, amends R-109)*"
  verify:
    method: benchmark
    detail: "computeIC for a typical IC at t = 50 on one core < 16.7 ms (measured 6.1 ms; 1.6 ms at t = 13)"
  rulings:
  - R-144
  milestone: M3
- id: REQ-PERF-005
  area: PERF
  statement: "Checkerboard must allocate no extra or half-size buffer and must keep every stale SimState resident."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 1. What this is (and what it is NOT)
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: GPU allocation is identical with checkerboard on and off
  rulings:
  - R-113
  milestone: M4
  note: "R-113 (RQ-98): 'the memory estimator must credit it with no memory saving' is REQ-PERF-018's (M5)."
- id: REQ-PERF-006
  area: PERF
  statement: "At runtime the compute side must never compile source; pipeline objects are created from pre-built SPIR-V, eagerly for the active (occupant, tier, links) of every chart at startup and lazily inside the re-integration cost for occupant/tier/link/threshold changes."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  verify:
    method: unit test
    detail: "chart switch creates no pipeline; occupant change creates its pipeline during the re-integration"
  rulings: []
  milestone: M4
- id: REQ-PERF-007
  area: PERF
  statement: "The build-time monomorphisation set must be bounded to the active/plausible variant set, not the full cross-product."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  verify:
    method: review checklist
    detail: "the build enumerates an explicit variant list and records its size and build time"
  rulings: []
  milestone: M4
  note: "The set: REQ-PERF-077 (definition)."
- id: REQ-PERF-008
  area: PERF
  statement: "The word buffer must be a separable allocation that can be dropped when symbolic features are off without touching `SimState`."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  verify:
    method: unit test
    detail: "disable symbolic features: word buffer freed, SimState allocation and contents unchanged"
  rulings: []
  milestone: M4
- id: REQ-PERF-009
  area: PERF
  statement: "The march's actual bottleneck (bandwidth vs arithmetic/occupancy/register pressure) and any register spilling must be confirmed by measurement."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 3. The word buffer — `free_group_word`
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_generation_root.md
    section: "3.3 `free_group_word` (uint4) — mixed-radix packing, **in a separate buffer**"
  verify:
    method: benchmark
    detail: "profile the march kernel at production dispatch granularity; record bound type and spill counts"
  rulings: []
  milestone: M4
- id: REQ-PERF-010
  area: PERF
  statement: "Long compute must be split into dispatch chunks small enough to return before a watchdog, carrying state between chunks, sharing one mechanism with CPU-side yielding."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.3 Device loss, timeouts, driver resets
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: GPU
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: CPU
  verify:
    method: unit test
    detail: "a long march runs as multiple bounded dispatches with identical results to one long dispatch"
  rulings: []
  milestone: M4
  note: "The chunk bound: REQ-PERF-081 (calibration)."
- id: REQ-PERF-011
  area: PERF
  statement: "Whether N = 16 (256 threads per quad, at WebGPU's invocation ceiling) is used on WebGPU versus N = 8 must be decided by a recorded measurement on hardware before WebGPU deployment, with workgroup limits measured rather than quoted, and every N the tiers can select must fit the one-workgroup-per-quad thread ceiling (256 invocations on WebGPU), fixing the tier table's N = 24 / 32 entries that need 576 / 1024 threads."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4. The six quality tiers
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: decisions.md
    section: "R-43 — `N = 16` vs 8 is measured; the thread-count inconsistencies are fixed now *(RS-3)*"
  - file: docs/design/principia_systems_architecture.md
    section: Still to check
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  verify:
    method: benchmark
    detail: "profile N = 8 vs N = 16 on a WebGPU target and record the result; a validation check asserts N² ≤ maxComputeInvocationsPerWorkgroup for every tier and Custom value"
  rulings:
  - R-43
  - R-132
  milestone: M4
  note: "R-43 is recorded (applied before WebGPU deployment); R-132 sets Ultra and Extreme to N = 16 (REQ-PERF-086). Measure, do not assume."
- id: REQ-PERF-012
  area: PERF
  statement: "The compute dispatch must be one workgroup per quad with one thread per texel (N² threads, 64 at N = 8), each ensemble copy being the same kernel dispatched again with copy_index a uniform (R-102), with workgroup shared memory holding only reduction accumulators, never live states; the across-copy reduction must be its own resolve pass, run after all E+1 copy dispatches for a quad complete, reading their SimState slices and writing the footprint resolve and the QuadReduction fields (R-135)."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: The shape
  - file: docs/design/principia_systems_architecture.md
    section: "5.5 THE DISPATCH SHAPE — one thread per texel, one dispatch per ensemble copy"
  - file: decisions.md
    section: "R-124 — Apply the R-25, R-50 and R-102 follow-ups now *(closes RQ-92)*"
  - file: decisions.md
    section: R-102 — The ensemble isn't a baked variant *(closes RQ-62)*
  - file: decisions.md
    section: "R-135 — The across-copy reduction is its own resolve pass *(closes RQ-113)*"
  - file: decisions.md
    section: "R-156 — The plan-pass readings are accepted *(closes RQ-126)*"
  verify:
    method: unit test
    detail: "shader reflection: workgroup size N²; shared-memory size independent of E; the kernel has no loop over ensemble copies; the resolve pass is dispatched after the E+1 copy dispatches and is the only writer of the footprint resolve and QuadReduction (R-135)"
  rulings:
  - R-102
  - R-124
  - R-135
  - R-156
  milestone: M4
  note: "R-124: systems_architecture §5.5's 'ensemble copies serial' is conformed to R-102 (its heading changes; the source is re-pointed)."
- id: REQ-PERF-013
  area: PERF
  statement: "There must be no texel-tiling knob (k = 1 texel per thread) unless profiling shows a specific problem."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: "Not doing: worker tiles"
  verify:
    method: review checklist
    detail: no k parameter exists in the dispatch configuration
  rulings: []
  milestone: M4
- id: REQ-PERF-014
  area: PERF
  statement: "There must be six quality tiers — Potato, Low, Medium, High, Ultra, Extreme — with FTLE (Benettin) computed from Medium up."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "5. Temporal & rendering model *(authoritative: `temporal_architecture_note`, `render_contract`, `scheduler_contract`, `checkerboard_contract`)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: unit test
    detail: "the tier enum has six variants; has_ftle is false for Potato/Low and true from Medium; per tier, trajectories per pixel = (E+1) below Medium and 2(E+1) from Medium up; Potato (E = 0) runs checkerboard (R-113)"
  rulings:
  - R-40
  - R-113
  milestone: M5
  note: "R-40 folds the eps / frame-budget / hard-cap axes into the ladder"
- id: REQ-PERF-015
  area: PERF
  statement: "Render-target memory must follow the hybrid model: k_d ≈ 3 × display_px × 4 (swap chain + final composite, display-sized) plus k_r ≈ 3 × render_px × 4 (backdrop + intermediates, scaling with render_scale²)."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  verify:
    method: unit test
    detail: "the allocator's render-target bytes match the formula at several display sizes and render_scale values"
  rulings: []
  milestone: M5
- id: REQ-PERF-016
  area: PERF
  statement: "Quality fields N, ftle and word must be sim key, and E (live, copies cached per copy_index), render_scale, lock_to_native, MAX_REL_DEPTH and checkerboard_mode must invalidate nothing."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 1 — Two-level keying: identity vs validity"
  - file: docs/contracts/principia_caching_contract.md
    section: Part 2 — What invalidates what (the dependency graph)
  - file: docs/design/principia_quality_device_note.md
    section: "Consequence: quality changes are (mostly) sim-key changes"
  verify:
    method: unit test
    detail: "changing render_scale, E or MAX_REL_DEPTH leaves the cache generation unchanged; changing N invalidates it; lowering E from 3 to 1 and raising it back never invalidates the nominal entries, drops copies 2..3 then respawns them (or reuses them from cache per copy_index), with no re-boot to t = 0"
  rulings:
  - R-89
  milestone: M5
- id: REQ-PERF-017
  area: PERF
  statement: "The application must stay responsive: it must show the coarse root quad immediately and keep improving, and must not wait for a frame to converge within the frame budget."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 0. What the mechanism is actually for
  verify:
    method: benchmark
    detail: "time from slice change to first presented frame is one root-quad dispatch; frames keep presenting while refinement is incomplete"
  rulings: []
  milestone: M5
- id: REQ-PERF-018
  area: PERF
  statement: "Payload memory must be accounted as `bytes × (E+1) × live_pixels` for `SimState` (the shadow is inside the struct) plus `16 B × (E+1) × live_pixels` for the word buffer when symbolic features are on, the estimator must reproduce payload §7's table, and it must credit checkerboard with no memory saving."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 7. Memory
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 1. What this is (and what it is NOT)
  verify:
    method: unit test
    detail: "estimator at 1080p E=3 FTLE-on returns 1.194 + 0.133 = 1.327 GB; 4K E=3 FTLE-on 5.308 GB; the estimator's figure is unchanged with checkerboard on and off"
  rulings:
  - R-40
  - R-113
  milestone: M5
- id: REQ-PERF-019
  area: PERF
  statement: "The full process memory budget (render targets, metadata, staging, transients, driver, browser/OS) must be measured, not inferred from the payload figure."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 7. Memory
  verify:
    method: benchmark
    detail: "record peak process memory for the tier configs on a 16 GB unified-memory machine"
  rulings: []
  milestone: M5
- id: REQ-PERF-020
  area: PERF
  statement: "The logical `SimState` and word buffers must be sharded per quad into chunk buffers that respect the baseline 128 MiB binding and 256 MiB buffer limits, with the scheduler tracking which quad lives in which chunk."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 7. Memory
  - file: docs/design/principia_dd_generation_root.md
    section: 3.3a The word buffer — a parallel cold buffer
  - file: docs/design/principia_memory_tiers.md
    section: 8. Caveats
  verify:
    method: unit test
    detail: "allocate a multi-GB payload: no GPUBuffer > 256 MiB, no binding > 128 MiB; quad→chunk map is consistent"
  rulings: []
  milestone: M5
- id: REQ-PERF-021
  area: PERF
  statement: "Memory pressure must be handled continuously as three states — comfortable (refine freely), pressured (stop growing the tree, keep serving the cache, report cap-bound; the image stops improving but does not degrade) and reclaiming (evict) — re-evaluated through the session rather than fixed at startup."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.2 Memory pressure — CONTINUOUS, not an error condition
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Three states, not two
  verify:
    method: unit test
    detail: "a scripted budget drop mid-session moves the state through pressured to reclaiming"
  rulings: []
  milestone: M5
- id: REQ-PERF-022
  area: PERF
  statement: "Eviction must take the lowest cost-weighted resistance first and must never drop the coarse ancestors or the pinned chain the fill draws during motion."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Eviction order, and the trap in it
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.2a Budgeting before allocating
  - file: docs/design/principia_debug_tooling_plan.md
    section: "F. Structural views — `RenderQuad` / quadtree (read quad metadata, not payload)"
  - file: decisions.md
    section: "R-120 — Eviction takes the lowest cost-weighted resistance first *(closes RQ-88)*"
  verify:
    method: property test
    detail: "under eviction, every visible region keeps an ancestor; the pinned chain survives; of two unpinned quads the one with lower cost-weighted resistance goes first, whatever their depths"
  rulings:
  - R-120
  milestone: M5
  note: "R-120: telemetry's 'deepest first' passages are conformed to scheduler Part 6 and caching Part 7."
- id: REQ-PERF-023
  area: PERF
  statement: "Before allocating, the requirement quads × N² × (E+1) × sizeof(SimState) must be checked against the adapter's reported limits, and if it does not fit the tier must be reduced and reported instead of attempting the allocation, with OOM treated as expected on unified memory."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.2a Budgeting before allocating
  verify:
    method: unit test
    detail: "an adapter with small limits causes a reported tier reduction, never a failed allocation"
  rulings: []
  milestone: M5
- id: REQ-PERF-024
  area: PERF
  statement: "The GPU in-flight queue depth must be kept shallow (no queueing frames ahead)."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: GPU
  verify:
    method: benchmark
    detail: "measured input-to-photon latency with in-flight depth recorded in telemetry"
  rulings: []
  milestone: M5
  note: "The depth: REQ-PERF-082 (calibration)."
- id: REQ-PERF-025
  area: PERF
  statement: "The memory estimate must be payload_mem = (E+1) × bytes × render_px plus render_targets = k_d × display_px × 4 + k_r × render_px × 4 (k_d ≈ 3, k_r ≈ 3) plus fixed, using the payload's current SimState widths (144 B FTLE-on / 96 B FTLE-off), and reproduce memory_tiers §4's recomputed table."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 3. Memory model — payload + render targets
  - file: docs/design/principia_memory_tiers.md
    section: "Total GB (payload + render targets) by tier × display resolution"
  - file: docs/design/principia_memory_tiers.md
    section: "Principia — quality tiers & memory model (auto-mode reference)"
  - file: decisions.md
    section: "R-40 — Memory tiers and the quality device key off `eps` *(PL-5)*"
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  - file: decisions.md
    section: "R-73 — Apply the whole ruling-follow-up checklist now *(closes RQ-56)*"
  - file: decisions.md
    section: "R-137 — The Ultra and Extreme rows are provisional *(closes RQ-115)*"
  verify:
    method: unit test
    detail: "estimator output for each tier × {1080p, 1440p, 4K} equals the formula at 144/96 B; the Ultra and Extreme rows and the totals that depend on them are provisional and are recomputed when REQ-PERF-086 lands (R-137)"
  rulings:
  - R-40
  - R-59
  - R-73
  - R-137
  milestone: M5
- id: REQ-PERF-026
  area: PERF
  statement: "Named tiers must populate render_scale, E, FTLE and word per the §4 table (Potato 0.25×/E 0/no FTLE/no word; Low 0.5×/0/no FTLE/word on; Medium 0.75×/1/FTLE; High 1.0×/3/FTLE; Ultra 1.0×/7/FTLE; Extreme 1.0×/15/FTLE), with N and depth indicative, and the Ultra and Extreme rows provisional until REQ-PERF-086's calibration lands (R-137)."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4. The six quality tiers
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  - file: decisions.md
    section: "R-137 — The Ultra and Extreme rows are provisional *(closes RQ-115)*"
  verify:
    method: unit test
    detail: "each named preset's QualitySettings matches its row; the Ultra and Extreme rows are marked provisional in the config until REQ-PERF-086 is recorded (R-137)"
  rulings:
  - R-43
  - R-132
  - R-137
  milestone: M5
  note: "The N column's 24 and 32 are subject to R-43; R-132 caps Ultra and Extreme at N = 16, their E and render scale calibrated by REQ-PERF-086."
- id: REQ-PERF-027
  area: PERF
  statement: "FTLE must be on by default from Medium up and off for Potato and Low, and Custom must be able to force it on at any render_scale."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4. The six quality tiers
  verify:
    method: unit test
    detail: "tier presets' ftle flag; Custom with render_scale 0.25 accepts ftle = on"
  rulings: []
  milestone: M5
- id: REQ-PERF-028
  area: PERF
  statement: "Each tier's eps, frame budget and hard cap must follow §4.1's table as labelled placeholders in the config, with eps tightening monotonically with tier, and the real values must be set by the calibration campaign."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  - file: decisions.md
    section: "R-4 — \"spec-keyed defaults\" means the markdown's tier tables *(closes RQ-5)*"
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: v0.5 means the NUMBERS are guesses; the SHAPE is not
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Then invert
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  - file: open-questions.md
    section: Audit section D — deliberately parked
  verify:
    method: benchmark
    detail: "config marks the six rows as placeholders; eps strictly decreases Potato → Extreme; the calibration campaign's recorded per-device results replace them"
  rulings:
  - R-4
  - R-40
  milestone: M5
  note: "INDEX: the numbers are guesses by design. The QualitySettings fields and whether eps varies per internal rung or per named tier: REQ-PERF-084 (definition)."
- id: REQ-PERF-029
  area: PERF
  statement: "The hard cap must be a fraction of available memory, never an absolute byte count."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  verify:
    method: unit test
    detail: "hard cap scales with the reported available memory for each tier"
  rulings: []
  milestone: M5
- id: REQ-PERF-030
  area: PERF
  statement: "A forced setting that fails to allocate must be caught and dropped to a working tier with a message, never a crash or black screen."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "7. The \"are you sure?\" safety system (three severities, none blocking)"
  verify:
    method: unit test
    detail: "inject allocation failure at Extreme; app falls back to a lower tier and shows the fallback message"
  rulings: []
  milestone: M5
- id: REQ-PERF-031
  area: PERF
  statement: "Memory budgeting must use the process estimate with margin (payload + render targets + bake texture, staging, transient export/resize allocations and overhead), not raw payload."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 8. Caveats
  - file: docs/design/principia_memory_tiers.md
    section: 6. Auto-mode tier selection
  verify:
    method: review checklist
    detail: the fit check sums every term and applies the margin
  rulings: []
  milestone: M5
- id: REQ-PERF-032
  area: PERF
  statement: "The interactive frame budget must be 16 ms, and export must ignore the frame budget (blocking)."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  verify:
    method: unit test
    detail: "default frame_budget = 16 ms; export path does not consult it"
  rulings: []
  milestone: M5
  note: "Listed under open sub-questions; §4.1's per-tier budgets differ by tier (placeholders)."
- id: REQ-PERF-033
  area: PERF
  statement: "E must be the tier's SSAA knob, free-valued 0–15 (High = 3, Extreme = 15), with E = 0 (Potato/Low) giving one sample per pixel and no AA."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Uniform samples: every sample is a full, normal SimState"
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Ensemble copies ARE the SSAA samples
  - file: docs/notes/principia_sampling_msaa_note.md
    section: Amendments this makes
  verify:
    method: unit test
    detail: tier table E values; E = 0 renders with no copies
  rulings: []
  milestone: M5
- id: REQ-PERF-034
  area: PERF
  statement: "Auto must run device characterisation (limits gate → throughput probe → adapter.info sanity check → thermal headroom), be re-runnable via re-detect, and run the live quality controller, which named and custom presets switch off."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  verify:
    method: unit test
    detail: "Auto starts the arbiter; selecting a named tier or Custom stops it; re-detect re-runs the probe"
  rulings: []
  milestone: M6
- id: REQ-PERF-035
  area: PERF
  statement: "SAMPLES_PER_QUAD_AXIS (N) and MAX_REL_DEPTH must both be tier-gated with a Custom override, and the quality tier must also gate the resident co-computations (ensemble copies and per-sample Benettin shadows)."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 6 — The settled policy
  - file: docs/design/principia_memory_tiers.md
    section: 4. The six quality tiers
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  verify:
    method: unit test
    detail: "each named tier populates N, MAX_REL_DEPTH, E and FTLE from its table row; Custom can override each"
  rulings: []
  milestone: M6
- id: REQ-PERF-036
  area: PERF
  statement: "E must be motion-gated by the quality controller via e_motion_gating: reduced (→0/1) during an active live march when the cost model says 2(E+1) trajectories per pixel is unaffordable, and full at rest and in export, with the gating threshold set by measurement on a working system."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 9 — Ensemble / SSAA sampling (dispatch rules)
  - file: docs/design/principia_quality_device_note.md
    section: What this subsystem resolves (two previously-open questions)
  - file: docs/design/principia_temporal_architecture_note.md
    section: Still open (settle at implementation, or next edit pass)
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "with a low-throughput model, E drops to ≤ 1 during a march and returns to full E at rest and in export"
  rulings:
  - R-89
  milestone: M6
  note: "R-89: E changes live — copies are cached per copy_index and the nominal's key excludes E."
- id: REQ-PERF-037
  area: PERF
  statement: "eps must be the user-facing quality knob, a tolerance in spread_shape's own units, and the tier tables must key off it rather than resolution."
  source:
  - file: docs/design/principia_dd_refinement_policy.md
    section: 1. The split rule — no exponent in the split test
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  - file: docs/design/principia_memory_tiers.md
    section: "Principia — quality tiers & memory model (auto-mode reference)"
  - file: decisions.md
    section: "R-4 — \"spec-keyed defaults\" means the markdown's tier tables *(closes RQ-5)*"
  - file: decisions.md
    section: "R-40 — Memory tiers and the quality device key off `eps` *(PL-5)*"
  verify:
    method: review checklist
    detail: "QualitySettings carries eps; the refinement policy reads eps from it; each tier row sets eps"
  rulings:
  - R-4
  - R-40
  milestone: M6
- id: REQ-PERF-038
  area: PERF
  statement: "The quality-controller thresholds/ladder and the device budget heuristics (unified vs discrete memory) must be set by measurement."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_dd_simstate_payload.md
    section: 7. Memory
  verify:
    method: benchmark
    detail: "device-characterisation runs record the thresholds and budget heuristics used"
  rulings:
  - R-4
  - R-40
  milestone: M6
  note: "R-40: the quality device keys off eps (telemetry §3.5's three-axis model, R-4)."
- id: REQ-PERF-039
  area: PERF
  statement: "The frame target must be 16.7 ms (60 fps) with 41.7 ms (24 fps) the hard floor: tier derivation solves for 16.7 ms first and falls back toward 41.7 ms only when nothing fits, recording that it did."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 3. Percentiles, not means — and the specific thresholds
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 4. Deriving the tier instead of choosing it
  verify:
    method: benchmark
    detail: "on a device that cannot hold 16.7 ms, the derived tier records the fallback"
  rulings: []
  milestone: M6
- id: REQ-PERF-040
  area: PERF
  statement: "The tier must be three independent axes — eps (convergence target), per-frame budget, and a mandatory hard cap on quads/bytes — folded into QualitySettings and the ladder."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 3.5 THE TIER IS THREE COUPLED AXES, NOT ONE — v0.5
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: The three axes
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: v0.5 means the NUMBERS are guesses; the SHAPE is not
  verify:
    method: unit test
    detail: "QualitySettings has the three fields; a pathological slice stops at the cap instead of OOM"
  rulings:
  - R-40
  milestone: M6
- id: REQ-PERF-041
  area: PERF
  statement: "The tier must be derived by inverting measured samples/s and bytes/s against the frame budget and sea_fraction(eps) — solving eps first, then the per-frame budget, then the hard cap — never from a table of device names."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 4. Deriving the tier instead of choosing it
  verify:
    method: unit test
    detail: "given synthetic throughput numbers the solver returns the expected three axes in order"
  rulings: []
  milestone: M6
- id: REQ-PERF-042
  area: PERF
  statement: "Tiers must ship with defaults keyed on crude specs (VRAM/unified memory, core counts, backend), with tier boundaries then found from data."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: "7. The calibration campaign — collect everything, decide nothing yet"
  verify:
    method: unit test
    detail: the default-tier selector uses only the listed specs
  rulings: []
  milestone: M6
- id: REQ-PERF-043
  area: PERF
  statement: "The tier table must carry a written rationale for which side of each measured boundary its defaults sit, and the output must be a settings set, not a benchmark score."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 8. What this is not
  verify:
    method: review checklist
    detail: the tier config documents the rationale per default
  rulings: []
  milestone: M6
- id: REQ-PERF-044
  area: PERF
  statement: "Quality must be three coupled axes — eps (convergence target), frame budget (per-frame cap) and a mandatory hard cap on quads/bytes — folded into QualitySettings and the ladder."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "Principia — quality tiers & memory model (auto-mode reference)"
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  - file: decisions.md
    section: "R-40 — Memory tiers and the quality device key off `eps` *(PL-5)*"
  - file: decisions.md
    section: "R-4 — \"spec-keyed defaults\" means the markdown's tier tables *(closes RQ-5)*"
  verify:
    method: unit test
    detail: "QualitySettings has eps, frame_budget and hard_cap fields; a pathological slice with an artificially low cap stops at the cap instead of allocating further"
  rulings:
  - R-4
  - R-40
  milestone: M6
  note: "The fields: REQ-PERF-084 (definition)."
- id: REQ-PERF-045
  area: PERF
  statement: "E must be free-valued (any integer, not only powers of two) on the controller's internal rungs, with only the named tiers snapping E to 1/3/7/15 (samples = E + 1)."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4. The six quality tiers
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "the ladder admits E = 2, 4, 5, 6; named tiers use 0/0/1/3/7/15"
  rulings:
  - R-89
  milestone: M6
- id: REQ-PERF-046
  area: PERF
  statement: "Under pressure the controller must pull levers in §5's order (render_scale, E, refinement floor, then FTLE, word), may prefer E and the refinement floor before render_scale for a research view, and memory must be the hard clamp while compute is soft."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "simulated pressure sequence shows the lever order; memory estimate never exceeds the ceiling while compute overrun only lowers rungs"
  rulings:
  - R-89
  milestone: M6
- id: REQ-PERF-047
  area: PERF
  statement: "FTLE and word must change only at natural invalidation moments (chart change, restart, re-detect), never mid-march, while E and the refinement floor may move live under motion without invalidating the nominal samples (copies cached per copy_index, the nominal's key excluding E)."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: docs/design/principia_quality_device_note.md
    section: 6. Change under cover (the perceptual magic)
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "a controller-driven FTLE/word change is deferred until the next invalidation event; E drops under motion leave nominal payloads intact"
  rulings:
  - R-89
  milestone: M6
- id: REQ-PERF-048
  area: PERF
  statement: "At boot, auto must probe memory limits and detect display resolution, pick the highest tier whose total (payload + render targets at the display resolution and the tier's render_scale) fits with margin, then run a warmed-up compute probe at the tier's 2(E+1) trajectory count and step down if compute-bound."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 6. Auto-mode tier selection
  - file: docs/design/principia_quality_device_note.md
    section: "\"Auto\" = device characterisation (three legs: limits · probe · info)"
  verify:
    method: unit test
    detail: "with mocked limits/probe: 2 GB integrated at 4K boots Low; a Medium memory fit that fails the compute probe is held at Low"
  rulings: []
  milestone: M6
- id: REQ-PERF-049
  area: PERF
  statement: "Live adjustment must pull render_scale / E / refinement floor down sub-rungs under motion and restore at rest, never exceeding the boot-selected memory ceiling."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 6. Auto-mode tier selection
  - file: docs/design/principia_quality_device_note.md
    section: "5. Timescale cascade (each layer absorbs what's too fast for the one above)"
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "scripted motion: levers drop and restore; memory estimate stays ≤ ceiling throughout"
  rulings:
  - R-89
  milestone: M6
- id: REQ-PERF-050
  area: PERF
  statement: "Device characterisation's limits leg must read adapter.limits, navigator.deviceMemory and adapter type as authoritative hard caps on cache size, live-set size, quad resolution, render-target size and buffers, which nothing else may exceed."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "\"Auto\" = device characterisation (three legs: limits · probe · info)"
  verify:
    method: unit test
    detail: "with mocked low limits, no solved knob or allocation exceeds them"
  rulings: []
  milestone: M6
- id: REQ-PERF-051
  area: PERF
  statement: "The throughput probe must warm up and discard its first dispatches, then measure cost-per-dt-per-sample on a spread of cheap and expensive quads at the real 2(E+1) trajectory count over a few hundred dt, using timestamp queries where available else batched wall-clock, and report a high percentile."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "\"Auto\" = device characterisation (three legs: limits · probe · info)"
  - file: docs/design/principia_quality_device_note.md
    section: The five ways the probe can lie (guard each)
  verify:
    method: unit test
    detail: "probe log shows discarded warm-up, ≥ 2 quad classes, real trajectory count, timing source, and a high-percentile result"
  rulings: []
  milestone: M6
- id: REQ-PERF-052
  area: PERF
  statement: "The info leg must use adapter.info only as a soft cross-check: if the probe is wildly inconsistent with the expected device class, distrust the probe and fall back to conservative defaults; it must never be a device lookup table."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "\"Auto\" = device characterisation (three legs: limits · probe · info)"
  verify:
    method: unit test
    detail: "fast probe + mobile-iGPU info → conservative defaults; no name-keyed table in the code"
  rulings: []
  milestone: M6
- id: REQ-PERF-053
  area: PERF
  statement: "The solve must bound N, E, live-set size and MAX_REL_DEPTH together from one base measurement (samples per frame = frame_budget / (cost_per_dt_per_sample × steps_per_frame)), clamp every knob to the hard limits and apply thermal headroom of ~60–70% of the cold measurement."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "\"Auto\" = device characterisation (three legs: limits · probe · info)"
  verify:
    method: unit test
    detail: "solve output for a mocked throughput matches the formula with headroom and clamping"
  rulings: []
  milestone: M6
- id: REQ-PERF-054
  area: PERF
  statement: "Hard limits must clamp every preset, including named tiers and Custom; Custom sliders must max out at the device ceiling with a tooltip explaining why."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: Hard limits clamp EVERY preset (including custom)
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  verify:
    method: unit test
    detail: "High on a weak mocked device clamps down; Custom slider max equals the limit and the tooltip is present"
  rulings: []
  milestone: M6
- id: REQ-PERF-055
  area: PERF
  statement: "The cache cap must be sized by the characterisation solve within the limits leg's hard ceiling."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: What this subsystem resolves (two previously-open questions)
  verify:
    method: unit test
    detail: cache_cap ≤ hard ceiling and derived from the solve
  rulings: []
  milestone: M6
- id: REQ-PERF-056
  area: PERF
  statement: "The auto controller must have exactly one policy that writes knobs; the steady-state and transition measurements must be sensors that only update a shared model."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 1. One arbiter, many sensors
  verify:
    method: review checklist
    detail: "only the arbiter module has write access to QualitySettings in auto mode"
  rulings: []
  milestone: M6
- id: REQ-PERF-057
  area: PERF
  statement: "The controller must maintain one slowly-varying throughput model (cost-per-dt-per-sample), initialised by the boot probe and refined by the sensors, with knob settings a pure function of the model and frame budget."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 2. Model-based, not reactive
  verify:
    method: unit test
    detail: "same model value → same rung; sensor updates change the model, never knobs directly"
  rulings: []
  milestone: M6
- id: REQ-PERF-058
  area: PERF
  statement: "The controller's output must be one integer rung on an offline-designed ladder of ~8–12 complete presets in total, the six named tiers pinned rungs on it, with monotonic cost, degrading E/edge quality and render_scale before structural sample density."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 3. The quality ladder
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  verify:
    method: review checklist
    detail: "the ladder is a data table of ~8–12 rungs containing the six named tiers; cost is monotone across rungs; the policy returns an integer"
  rulings:
  - R-89
  milestone: M6
  note: The ladder's rung contents are an offline design task.
- id: REQ-PERF-059
  area: PERF
  statement: "The motion rung must be resting_rung − offset, with the offset learned from the transition sensor as a monotone function of playhead depth."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 4. Resting rung + motion offset (one ladder, two setpoints)
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  verify:
    method: unit test
    detail: "motion rung ≤ resting rung always; offset non-decreasing in t"
  rulings: []
  milestone: M6
- id: REQ-PERF-060
  area: PERF
  statement: "Per-frame transients must never change settings; render_scale (or E and the refinement floor when locked) may slide under motion; resting-rung changes must happen only during sustained quiescence (no navigation, chart change, catch-up or in-flight quality change)."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "5. Timescale cascade (each layer absorbs what's too fast for the one above)"
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "a single slow frame changes nothing; a rung change is never issued during navigation or catch-up"
  rulings:
  - R-89
  milestone: M6
- id: REQ-PERF-061
  area: PERF
  statement: "Render-key knobs must adjust only under motion/blur cover and sim-key knobs (N, FTLE, word) only at natural invalidation moments (chart change, restart, loop wrap, re-detect); the controller must never cause a visible recompute and must never toggle checkerboard, only count its ~2× saving in the cost model."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 6. Change under cover (the perceptual magic)
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  - file: docs/design/principia_memory_tiers.md
    section: 5. Controller levers, ranked by impact
  - file: decisions.md
    section: R-89 — Depth and E are not on the sim key *(closes RQ-40)*
  verify:
    method: unit test
    detail: "a sim-key rung change waits for the next invalidation event; checkerboard_mode is never written by the arbiter"
  rulings:
  - R-89
  milestone: M6
- id: REQ-PERF-062
  area: PERF
  statement: "The controller must drop fast (one bad settled window → down a rung at the next covered moment), rise slow (sustained headroom → up one rung), hold within a dead zone, and remember failed rungs, requiring stronger evidence to retry them with caution decaying over tens of minutes."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 7. Hysteresis + failed-rung memory
  verify:
    method: unit test
    detail: "simulated load traces show no oscillation into a remembered failed rung before the decay"
  rulings: []
  milestone: M6
- id: REQ-PERF-063
  area: PERF
  statement: "The controller's steady-state signals must be sim-time debt (with 95th-percentile frame time secondary) and its transition signals time-to-resync and fallback-visible duration."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 8. Sense what the user feels, not what the GPU reports
  verify:
    method: unit test
    detail: "sensor outputs are these quantities; injected sim-time debt drops the rung"
  rulings: []
  milestone: M6
- id: REQ-PERF-064
  area: PERF
  statement: "The controller must persist {throughput estimate, resting rung, motion-offset curve, failed-rung memory, provenance signature} and restore it only on matching provenance (adapter.info + key limits + probe/kernel version), re-probing on mismatch or explicit re-detect, and must honour the user's stored preset intent verbatim."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: 9. Persist the model, not just the rung
  - file: docs/contracts/principia_gui_state_contract.md
    section: "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
  verify:
    method: unit test
    detail: "reload with matching provenance: no probe, same rung; changed kernel version: re-probe; stored Custom values restored unchanged"
  rulings: []
  milestone: M6
- id: REQ-PERF-065
  area: PERF
  statement: "Selecting a named tier or Custom must switch the arbiter off entirely."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "10. Two sanctities: the user, and observability"
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  verify:
    method: unit test
    detail: under load with High selected, no knob changes
  rulings: []
  milestone: M6
- id: REQ-PERF-066
  area: PERF
  statement: "The controller's dead-zone band, settled-window length, rise-evidence duration, failed-rung decay and motion-offset curve parameters must be set by recorded measurement on a working system."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/design/principia_quality_device_note.md
    section: 7. Hysteresis + failed-rung memory
  verify:
    method: benchmark
    detail: record each tunable's value and the measurement that set it
  rulings: []
  milestone: M6
- id: REQ-PERF-067
  area: PERF
  statement: "Memory tiers and the quality device must key off eps: the eps / frame-budget / hard-cap axes must be folded into QualitySettings and the ladder, reporting the binding axis, with payload widths recomputed from the current layout (not 136/88 B)."
  source:
  - file: decisions.md
    section: "R-40 — Memory tiers and the quality device key off `eps` *(PL-5)*"
  - file: decisions.md
    section: R-59 — Fix D1 to D6 as listed *(sheet §8)*
  - file: docs/read_first/principia_INDEX.md
    section: Known open items
  - file: decisions.md
    section: "R-73 — Apply the whole ruling-follow-up checklist now *(closes RQ-56)*"
  verify:
    method: unit test
    detail: "QualitySettings has the three axes; controller reports which binds; memory totals are computed from the generated payload width."
  rulings:
  - R-4
  - R-40
  - R-59
  - R-73
  milestone: M6
  note: "D6's width recompute is applied in the docs (144 / 96 B). The QualitySettings fields and whether eps varies per internal rung or per named tier: REQ-PERF-084 (definition)."
- id: REQ-PERF-068
  area: PERF
  statement: "Render presets and compositor shaders must be precompiled at startup, other slot combinations, debug views and customs compiled on selection asynchronously with last-valid fallback, within a startup budget of about 10 compute pipeline objects, 6 fragment and 3 compositor pipelines."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  verify:
    method: benchmark
    detail: "count pipeline objects created at startup and measure startup time"
  rulings: []
  milestone: M7
- id: REQ-PERF-069
  area: PERF
  statement: "The render-target buffer counts k_d and k_r must be confirmed by measurement once the render graph is built."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 8. Caveats
  verify:
    method: benchmark
    detail: "count display-res and render-res targets allocated by the built render graph and record k_d, k_r"
  rulings: []
  milestone: M7
- id: REQ-PERF-070
  area: PERF
  statement: "The at-rest catch-up of the stale half must be measured on weak devices and spread over 2–3 frames if the one-frame fill causes a visible hitch."
  source:
  - file: docs/contracts/principia_checkerboard_contract.md
    section: 8. Build-time settles (measure on the real system)
  verify:
    method: benchmark
    detail: "frame time of the fill-in frame on the weakest target device, recorded"
  rulings: []
  milestone: M8
- id: REQ-PERF-071
  area: PERF
  statement: "The snapshot must carry only GUI-sized state (view state, current tier, a few scalars, the reported events) and never engine-sized state such as the payload, the quad tree or the reductions."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  - file: docs/contracts/principia_gui_state_contract.md
    section: 2. The editable state is the entire coupling surface
  verify:
    method: unit test
    detail: "the snapshot type contains no payload / quadtree / reduction types, and its serialised size stays below a fixed small bound at any zoom depth"
  rulings: []
  milestone: M8
- id: REQ-PERF-072
  area: PERF
  statement: "The hover integration must run in a dedicated inspector worker separate from the render-loop worker, debounced and cancel-on-move with a latest-wins generation counter, never awaited on the main thread."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 7 — The hover trace (per-IC trajectory overlay)
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-144 — pointer_channels §3 is normative too *(closes RQ-74, amends R-109)*"
  verify:
    method: review checklist
    detail: "rapid hover moves issue at most one completed integration per settle; frame time unaffected by an in-flight trace"
  rulings:
  - R-144
  milestone: M8
- id: REQ-PERF-073
  area: PERF
  statement: "Potato must run acceptably on a phone (the v0.5 acceptance criterion)."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  verify:
    method: benchmark
    detail: "run Potato on a phone already owned; record frame rate and responsiveness"
  rulings: []
  milestone: M8
- id: REQ-PERF-074
  area: PERF
  statement: "The yellow/red fps thresholds must be set by measurement against feel at build time, not hard-coded now."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: "7. The \"are you sure?\" safety system (three severities, none blocking)"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: benchmark
    detail: record the measured thresholds with their method
  rulings:
  - R-113
  milestone: M8
  note: "R-113 (RQ-100): the memory-fit safety margin is a separate M5 calibration requirement (TASK-M5-10)."
- id: REQ-PERF-075
  area: PERF
  statement: "A hover trajectory must run under a step budget while the pointer moves, report what it got and mark itself incomplete when the budget runs out (a shorter trace, a coarser spectrum) rather than lowering quality, and have the bound lifted on dwell so trace and spectrum converge."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-144 — pointer_channels §3 is normative too *(closes RQ-74, amends R-109)*"
  verify:
    method: unit test
    detail: "a near-collision IC under motion returns a partial trajectory flagged incomplete; after dwell the full trajectory arrives"
  rulings:
  - R-144
  milestone: M8
  note: "The budget and the rest time before it lifts: REQ-PERF-078 (calibration)."
- id: REQ-PERF-076
  area: PERF
  statement: "The Tier 3 overlay buffer must fit the ~16 KB design budget for a typical viewport, and per-quad detail that does not fit must go to inspector drill-down instead."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  verify:
    method: unit test
    detail: buffer size for the typical-viewport fixture ≤ 16 KB
  rulings: []
  milestone: M8
  note: "The typical viewport: REQ-PERF-080 (calibration); REQ-PERF-079 (definition)."
- id: REQ-PERF-077
  area: PERF
  kind: definition
  statement: "The build-time monomorphisation set — the active/plausible (chart × occupant × tier × links) variant combinations generated ahead of time must be defined in lowering_contract Part 4 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "lowering Part 4 lists the set and the rule that bounds it; the reviewer checks it is not the full cross-product and covers every shipped chart, occupant, tier and link"
  rulings:
  - R-72
  milestone: M4
- id: REQ-PERF-078
  area: PERF
  kind: calibration
  statement: "The hover trajectory's step budget, and the pointer rest time after which the budget lifts must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
  - file: decisions.md
    section: "R-144 — pointer_channels §3 is normative too *(closes RQ-74, amends R-109)*"
  verify:
    method: benchmark
    detail: "the proposal shows the total_substeps distribution (p1 to p99) of hover ICs and the per-frame cost of the chosen budget against the 60 fps frame"
  rulings:
  - R-71
  - R-96
  - R-144
  milestone: M8
- id: REQ-PERF-079
  area: PERF
  kind: definition
  statement: "The Tier-3 tile-debug buffer's per-visible-quad record must be defined in render_gui_spec §12.1 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "§12.1 gives the record's fields and size; the reviewer checks it holds the scheduler verdicts the Tier-3 overlays read"
  rulings:
  - R-72
  milestone: M8
- id: REQ-PERF-080
  area: PERF
  kind: calibration
  statement: "The typical viewport's visible-quad count behind the Tier-3 buffer's ~16 KB budget must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: 12.1 Structural overlays and tile debug shaders
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: benchmark
    detail: "the proposal shows visible-quad counts measured over the deep-zoom scenarios and the resulting buffer size with the defined per-quad record"
  rulings:
  - R-71
  milestone: M8
- id: REQ-PERF-081
  area: PERF
  kind: calibration
  statement: "The dispatch chunk bound (the largest compute chunk small enough to return before a driver watchdog) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.3 Device loss, timeouts, driver resets
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: GPU
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the bound with its evidence: measured per-chunk durations against the target backends' watchdog limits"
  rulings:
  - R-71
  milestone: M4
- id: REQ-PERF-082
  area: PERF
  kind: calibration
  statement: "The GPU in-flight queue depth must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: GPU
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the depth with its evidence: measured input-to-photon latency and throughput at each candidate depth"
  rulings:
  - R-71
  milestone: M5
- id: REQ-PERF-083
  area: PERF
  kind: calibration
  statement: "The CPU core reserve (how many cores beyond the one always left free the rayon pool leaves free on small-core devices, and what counts as small-core) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: CPU
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "decisions.md records the reserve rule with its evidence: UI and OS responsiveness measured on the smallest-core target devices under a full pool"
  rulings:
  - R-71
  milestone: M3
- id: REQ-PERF-084
  area: PERF
  kind: definition
  statement: "QualitySettings' quality axes (the eps, frame-budget and hard-cap fields, and whether eps varies per internal rung or per named tier) must be defined in the quality/device note's QualitySettings struct by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "The reframe: quality is a preset selector populating one settings struct"
  - file: docs/design/principia_memory_tiers.md
    section: 4.1 The same six tiers on the three axes
  - file: decisions.md
    section: "R-40 — Memory tiers and the quality device key off `eps` *(PL-5)*"
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the struct lists the three fields with types and owners, and the doc says at which granularity eps varies"
  rulings:
  - R-40
  - R-72
  milestone: M6
- id: REQ-PERF-085
  area: PERF
  kind: calibration
  statement: "The IC set that stands for 'one trajectory' in the hover-trajectory benchmark (the typical IC, and the tail case given that total_substeps is bimodal) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M3 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal names the benchmark ICs with their total_substeps percentile, and the recorded 6.1 ms at t = 50 is reproduced on the median one; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M3
- id: REQ-PERF-086
  area: PERF
  kind: calibration
  statement: "Ultra and Extreme must cap N at 16 (replacing memory_tiers §4's 24 and 32, which exceed the one-workgroup-per-quad ceiling of 256 invocations) and scale through E and render scale, and the E and render-scale values by which they scale must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M4 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 4. The six quality tiers
  - file: decisions.md
    section: "R-43 — `N = 16` vs 8 is measured; the thread-count inconsistencies are fixed now *(RS-3)*"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-132 — The R-71/R-72 classification is accepted, with three changes *(closes RQ-110)*"
  verify:
    method: review checklist
    detail: "Ultra and Extreme have N = 16 (N² = 256); the proposal gives their E and render scale with evidence (REQ-PERF-011's N = 8 vs 16 measurement, the memory estimator); memory_tiers §4 conformed; recorded in decisions.md"
  rulings:
  - R-43
  - R-71
  - R-132
  milestone: M4
  note: "R-132: N = 16 is ruled; the E and render-scale values stay calibrated."
- id: REQ-PERF-087
  area: PERF
  kind: calibration
  statement: "The memory-pressure threshold at which the comfortable state becomes pressured (how near the hard cap counts as \"approaching it\") must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: Three states, not two
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.2 Memory pressure — CONTINUOUS, not an error condition
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the pressured-onset threshold as a fraction of the hard cap, with evidence from a scripted budget-drop run (no oscillation between states, no reclaim before growth stops); recorded in decisions.md"
  rulings:
  - R-71
  milestone: M5
- id: REQ-PERF-088
  area: PERF
  kind: calibration
  statement: "The device-characterisation probe's values — the probe quad set (which cheap and expensive regions, how many), its length in dt, the high percentile it reports, the thermal headroom within ~60–70% of the cold measurement, and the probe-vs-device-class inconsistency that counts as \"wildly inconsistent\" — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: "\"Auto\" = device characterisation (three legs: limits · probe · info)"
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states each value with evidence: probe repeatability and boot time on the devices at hand, the cold vs warm throughput ratio, and the inconsistency ratio against the fast-probe + mobile-iGPU fixture; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M6
- id: REQ-PERF-089
  area: PERF
  kind: calibration
  statement: "The arbiter's tunables — the dead-zone band, the settled-window length, the rise-evidence duration, the failed-rung memory decay and the motion-offset curve's parameters — must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  - file: docs/design/principia_quality_device_note.md
    section: 7. Hysteresis + failed-rung memory
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states each tunable with the recorded measurement REQ-PERF-066's benchmark produces (no oscillation, drop latency, rise latency on scripted load traces); recorded in decisions.md"
  rulings:
  - R-71
  milestone: M6
- id: REQ-PERF-090
  area: PERF
  kind: calibration
  statement: "The quality ladder (its rung count within ~8–12 and each rung's preset) and the device-characterisation budget heuristics (the unified vs discrete memory budget) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M6 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_simstate_payload.md
    section: 8. Build-time settles (measure / specify once running)
  - file: docs/design/principia_quality_device_note.md
    section: Open sub-questions (settle at implementation)
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the rung count, each rung's preset and the unified/discrete budget heuristic, with the device-characterisation runs REQ-PERF-038 records; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M6
- id: REQ-PERF-091
  area: PERF
  kind: calibration
  statement: "The fixed bound on the GUI snapshot's serialised size (the 'GUI-sized' snapshot of gui_state_contract §1) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  - file: decisions.md
    section: R-94 — The GUI snapshot is ~10 Hz *(closes RQ-45)*
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the byte bound with the measured snapshot size across zoom depths and tiers and its margin; recorded in decisions.md"
  rulings:
  - R-71
  milestone: M8
- id: REQ-PERF-092
  area: PERF
  kind: calibration
  statement: "The target-utilisation ceiling's default headroom (the default core count and frame-rate cap, or utilisation fraction, in force until the user opts into full utilisation) must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M8 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: A deliberate ceiling, user-visible
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  verify:
    method: review checklist
    detail: "the proposal states the default ceiling with measured frame time and throughput under it and at full utilisation on the reference devices, and its relation to the rayon core reserve (REQ-PERF-083); recorded in decisions.md"
  rulings:
  - R-71
  milestone: M8
- id: REQ-PERF-093
  area: PERF
  kind: calibration
  statement: "The memory-fit safety margin the process estimate is budgeted with must be calibrated: proposed with its evidence by the task that needs it, checked by a reviewer, confirmed by the human at the M5 gate and recorded in decisions.md."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 8. Caveats
  - file: docs/design/principia_memory_tiers.md
    section: 6. Auto-mode tier selection
  - file: docs/design/principia_memory_tiers.md
    section: "7. The \"are you sure?\" safety system (three severities, none blocking)"
  - file: decisions.md
    section: "R-71 — A missing value becomes a calibration requirement *(closes RQ-46 to RQ-55, values)*"
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  verify:
    method: benchmark
    detail: "the proposal records the margin with its method: estimator figures against measured process memory across tiers and resolutions; recorded in decisions.md"
  rulings:
  - R-71
  - R-113
  milestone: M5
  note: Split from REQ-PERF-074 (R-113, RQ-100).
- id: REQ-SYS-001
  area: SYS
  statement: "Every settled default constant must be traceable to a citation of where it was measured."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: Principia — canonical architecture & design (the yardstick)
  - file: docs/read_first/principia_INDEX.md
    section: The evidence base — where settled defaults were measured
  verify:
    method: review checklist
    detail: "each default in the config/ledger carries a citation (prin-rs FINDINGS/results path or corpus section) to its measurement; uncited defaults are flagged"
  rulings: []
  milestone: M0
  note: "the doc words this as 'should be traceable'; the stated consequence (folklore) makes it normative"
- id: REQ-SYS-002
  area: SYS
  statement: "Retired terms must not reappear: TileID / computeTile / samples_per_tile, the M checkpoint count, a TIMEOUT state, sd_is_untrusted, 'N ensemble shadows', the Math.fround N_sub rule, a TS layout constant generating WGSL, and SimResult."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 8. Locked vocabulary
  - file: docs/design/principia_temporal_architecture_note.md
    section: The rename
  - file: decisions.md
    section: "R-111 — `SimResult` → `SimState`, `M` → `n_renorm`; the vocabulary lint covers the docs *(closes RQ-80)*"
  - file: decisions.md
    section: R-140 — The four readings are accepted *(closes RQ-118)*
  verify:
    method: review checklist
    detail: "the vocabulary lint over code and docs (excluding archive and passages wrapped in <!-- retired-terms --> … <!-- /retired-terms -->, R-111) finds none of the retired identifiers, matched case-sensitively as identifiers (TIMEOUT, not the prose \"timeout\"; R-140)"
  rulings:
  - R-111
  - R-140
  milestone: M0
- id: REQ-SYS-003
  area: SYS
  statement: "Code must use the locked taxonomy: QUAD (quadtree node), SAMPLE (one of N×N per quad), TILE (a sample's screen footprint), PIXEL, with SAMPLES_PER_QUAD_AXIS and QuadReduction/QuadSummary, and no TILE_PIXEL_RES."
  source:
  - file: docs/design/principia_memory_tiers.md
    section: 1. Taxonomy (locked vocabulary)
  verify:
    method: review checklist
    detail: "grep: no SAMPLES_PER_TILE_AXIS, TileReduction, TileSummary or TILE_PIXEL_RES identifiers"
  rulings: []
  milestone: M0
- id: REQ-SYS-004
  area: SYS
  statement: "The build must respect the dependency DAG: the layout table and link registry are roots of all generated code, the decoder precedes the compute kernel, canonicalise precedes the integrator, and the payload precedes fragment assembly, the scheduler and the inspector/export consumers."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 7. Hierarchy and dependency (the build DAG, abstract)
  - file: decisions.md
    section: R-172 — There is no contract crate *(closes C1, C4)*
  - file: docs/design/principia_systems_architecture.md
    section: 7.1 Crate map
  - file: decisions.md
    section: R-170 — The crate map *(closes G2)*
  - file: decisions.md
    section: "R-185 — The crate map is confirmed; kernel → ledger is a build-dependency only *(closes TASK-M0-00)*"
  - file: decisions.md
    section: "R-187 — kernel and ledger may take validation as a dev-dependency; validation never depends on prin *(closes RQ-129)*"
  - file: decisions.md
    section: "R-188 — TASK-M0-01 is accepted over its size; the source scan also follows `include!`"
  - file: decisions.md
    section: "R-189 — kernel and ledger `src/` use neither `#[path]` nor `include!` *(amends R-188 item 2)*"
  verify:
    method: review checklist
    detail: "cargo xtask deps finds no workspace edge outside systems_architecture §7.1's allowed-edge table; kernel → ledger passes only as a build-dependency, and a normal dependency on that edge fails (R-185); every crate but gui may take validation as a dev-dependency only, never normal or build, gui → validation and validation → prin fail in every kind, and in kernel and ledger no .rs file under src/ uses validation (R-187); in kernel and ledger src/, #[path] (including under cfg_attr) and include! are forbidden, and cargo xtask deps fails on any occurrence, naming the file and line (R-189); a fixture with each forbidden edge fails naming it; module-level edges inside one crate are checked in review"
  rulings:
  - R-170
  - R-172
  - R-185
  - R-187
  - R-188
  - R-189
  milestone: M0
- id: REQ-SYS-005
  area: SYS
  statement: "Every constant must be derived or absent: admissible only if bounded by its own achievable maximum, fixed by a conservation law, or expressed in canonical units."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.2 Every constant must be derived or absent
  - file: docs/read_first/principia_00_philosophy.md
    section: 6. How to tell if a decision is drifting
  verify:
    method: review checklist
    detail: "Each ledger constant records which admissibility class it belongs to (or its measurement citation); constants 'chosen by eye' fail review."
  rulings: []
  milestone: M0
- id: REQ-SYS-006
  area: SYS
  statement: "Design decisions must answer the four drift questions: which path it is for, where each constant comes from, whether the measurement could have failed, and whether the instrument still says where it stops knowing."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 6. How to tell if a decision is drifting
  verify:
    method: review checklist
    detail: Design PR template contains the four questions with answers.
  rulings: []
  milestone: M0
- id: REQ-SYS-007
  area: SYS
  statement: "A parked §7 item must not be built or half-started before 1.0, other than decisions that cost nothing now and prevent a fork later (currently only §7.1's payload genericity)."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 7.7 The rule for this section
  - file: docs/read_first/principia_00_philosophy.md
    section: 7. Parked — deliberate post-1.0
  verify:
    method: review checklist
    detail: "Build plan contains no work items for extended precision, tiled prebake, dump tiers, headless datasets, the regularisation matrix or the GPU-port lever beyond the Real-generic payload."
  rulings: []
  milestone: M0
- id: REQ-SYS-008
  area: SYS
  statement: "Files marked archived (principia_ARCHIVE_* and the Archived table) must not be implemented from and must never be cited; their standing parts (the structure-criterion brief's §4–4.6 slippy map, the kernel-build brief's §5 gates) are superseded by the consolidated docs (deep_zoom §3, scheduler, parity), and any standing obligation missing from those docs must be ported into them."
  source:
  - file: docs/read_first/principia_INDEX.md
    section: Principia — document index and reading order
  - file: docs/read_first/principia_INDEX.md
    section: Archived — record only, do not implement
  - file: decisions.md
    section: "R-112 — The archived briefs' standing parts are superseded *(closes RQ-81)*"
  verify:
    method: review checklist
    detail: "requirement/source and task-reference audit shows nothing sourced from docs/archive or an ARCHIVE_ brief; TASK-M0-02's record lists each standing obligation of the two briefs with the consolidated-doc section that holds it (ported where missing); INDEX's rows conformed (R-112)"
  rulings:
  - R-112
  milestone: M0
- id: REQ-SYS-009
  area: SYS
  statement: "There must be exactly one internal orientation (bottom-left origin, Y-up) and exactly one flip, at the framebuffer↔UV boundary (v = 1 − frag_coord.y/H, commented as the convention flip), mirrored exactly once at image export; a mirrored image must be fixed at that seam, never by a compensating flip elsewhere."
  source:
  - file: docs/design/principia_coordinate_conventions_note.md
    section: Principia — coordinate conventions
  - file: docs/design/principia_coordinate_conventions_note.md
    section: The one-line rule
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "Code paths that must honour the single convention (each is a separate path)"
  verify:
    method: review checklist
    detail: "grep finds one framebuffer→UV flip and one export flip, both named; no other Y inversion in coordinate code"
  rulings: []
  milestone: M1
- id: REQ-SYS-010
  area: SYS
  statement: "The 8D latent space must be exactly 2 configuration ⊕ 4 momentum ⊕ 2 mass controls, with the masses inside it (mass ratio is directions 6–7, never a global or external parameter) and no latent coordinate spent on a gauge direction (translation, boost, rotation, scale)."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Part 1 — The physics: what the 8D actually is"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 2 — The decoder
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Design axioms (the six that must survive contact with a code agent)"
  - file: docs/design/principia_dd_decoder.md
    section: "2. Consolidated contract (every clause that binds this component)"
  - file: docs/design/principia_chart_reference.md
    section: 1. The 8D latent chart — the reference coordinate system
  - file: docs/contracts/principia_canonical_spec.md
    section: "3. The physics & manifold model *(authoritative: `chart_decoder_contract`, `dd_decoder`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  verify:
    method: review checklist
    detail: "no global mass/physics parameter exists outside z and the chart; rotation and scale are fixed by the canonical-frame decode (ρ̃ on +x, R̃ = 1), not by latent coordinates; the latent type has exactly 8 components"
  rulings: []
  milestone: M2
- id: REQ-SYS-011
  area: SYS
  statement: "The exported ViewState must record (z₀, q₁, q₂), the chart id and params, the axis warps and the link id per block, so any chart — tilted or not — serialises, saves and restores exactly and a figure's provenance pins its links."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Integrity: the link is part of the experiment"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The operation table
  verify:
    method: unit test
    detail: "export a tilted view with a non-default link, reload, assert bitwise-identical z per pixel and identical link ids"
  rulings: []
  milestone: M2
- id: REQ-SYS-012
  area: SYS
  statement: "The kernel must receive only (z₀, q₁, q₂, chart id + params), identical in free and locked mode and before/after any tilt, slice or excursion; the lock, δ memory, ghost marks and direction library must live CPU-side, with no locked flag or second kernel code path."
  source:
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "What the GPU knows about all of this: nothing"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: The lock (projective microscope)
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: "Design axioms (the six that must survive contact with a code agent)"
  - file: decisions.md
    section: R-69 — What is undoable *(closes the step-6 open question)*
  verify:
    method: review checklist
    detail: "kernel uniform struct has no lock field; locked and unlocked renders of the same (z₀,q₁,q₂) are bitwise identical"
  rulings:
  - R-69
  - R-73
  milestone: M2
- id: REQ-SYS-013
  area: SYS
  statement: "Chart validation (well-posedness, flags, feasibility, and each chart's declared domain function validate(u,v)) must run CPU-side before resolution so resolve only sees valid charts."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: decisions.md
    section: R-26 — Every chart declares its domain function *(CD-6)*
  verify:
    method: unit test
    detail: "resolve on an invalid chart is unreachable: validation rejects it first"
  rulings:
  - R-26
  milestone: M2
  note: "R-26 (recorded) adds validate() to the Chart trait and names the lowering appendix."
- id: REQ-SYS-014
  area: SYS
  statement: "UV / quad addressing must stay unsigned [0,1]² (no negative quad indices) and IC space must be signed and centred on z₀ via the placement z₀ + (2s−1)q₁ + (2t−1)q₂."
  source:
  - file: docs/design/principia_coordinate_conventions_note.md
    section: "The three coordinate spaces (they nest; each is right for its job)"
  - file: docs/contracts/principia_chart_decoder_contract.md
    section: Part 3 — Charts
  - file: decisions.md
    section: R-83 — The slice scale lives in q *(closes RQ-34)*
  verify:
    method: unit test
    detail: "UV (½,½) → z₀; UV (0,0) → z₀ − q₁ − q₂; addresses are non-negative at any z₀"
  rulings:
  - R-83
  milestone: M2
- id: REQ-SYS-015
  area: SYS
  statement: "The decoder must exist as one shared Rust source instantiated as the f32 GPU compute kernel (Rust to SPIR-V), the f64 CPU path (lock, inspector, deep-zoom x₀/J_D) and, inverted, inside encode, with the fragment WGSL decode and encode generated from that source (rust-gpu → SPIR-V → WGSL translation); there must be no separate transcriptions of the decode logic and no hand-written WGSL decode or encode."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: Drill-down — the Decoder (Matter rung)
  - file: docs/design/principia_dd_decoder.md
    section: 1. What it is
  - file: docs/design/principia_dd_encode.md
    section: Drill-down — Encode (the Question rung's upward door)
  - file: decisions.md
    section: "R-116 — The fragment decode and encode are generated from the one source *(closes RQ-84)*"
  verify:
    method: review checklist
    detail: "one decode module is referenced by the kernel crate, the CPU crate and encode; the fragment WGSL decode/encode is a build output of the translation; no second implementation of any §3 formula exists (grep for the mass/config/momentum formulae outside the shared source, including .wgsl)"
  rulings:
  - R-116
  milestone: M2
- id: REQ-SYS-016
  area: SYS
  statement: "Body indices must be 0-based throughout (inner pair (0,1), outer body 2, softmax reference body 0), and pair id k must name the side opposite body k (pair 0 = (1,2), pair 1 = (2,0), pair 2 = (0,1)); the 1-based Burrau literature convention must be translated at the boundary."
  source:
  - file: docs/design/principia_dd_decoder.md
    section: 6. Deferred / flagged
  - file: docs/design/principia_chart_reference.md
    section: Chart reference — the maths, for implementation
  - file: docs/design/principia_chart_reference.md
    section: 4.2 Positions and masses — Burrau convention
  - file: decisions.md
    section: "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"
  - file: docs/design/principia_dd_simstate_payload.md
    section: "`sample_descriptor` (low 16 bits of `packed_a`) — 10 used, rest reserved"
  - file: docs/design/principia_dd_generation_root.md
    section: 3.1 `sample_descriptor` (u32)
  - file: docs/design/principia_dd_generation_root.md
    section: 6. Deferred / flagged
  - file: docs/contracts/principia_canonical_spec.md
    section: 11. Still open / downstream (not yet fully in the corpus)
  - file: docs/design/principia_debug_tooling_plan.md
    section: "B. Payload field views — `sample_descriptor` (bit-packed u32)"
  verify:
    method: unit test
    detail: "collision of bodies 0 and 1 reports pair id 2; ICDescriptor fields are m0 m1 m2; Burrau (3,4,5) loads with 0-based masses (5,4,3)/12"
  rulings:
  - R-22
  - R-73
  milestone: M2
- id: REQ-SYS-017
  area: SYS
  statement: "The CPU-f64 instantiation of the kernel must ship as the Precision ring (hover dwell-tier, click inspector, offline uniform-survey certifier) and expose `computeIC(chart, uv, simKey)`, `computeQuad(chart, quadID, simKey)`, `stepOnce(state, dt, params)` and `decodeOnly(chart, uv, simKey)` with the signatures of parity §1."
  source:
  - file: docs/contracts/principia_parity_contract.md
    section: 1. The principle
  verify:
    method: unit test
    detail: "call each of the four entry points from a native test; computeIC returns a full-f64 SimState; stepOnce performs one STEP for any bound occupant; decodeOnly returns (m, r, p, ICDescriptor)"
  rulings: []
  milestone: M3
- id: REQ-SYS-018
  area: SYS
  statement: "`r_coll` must be nonzero by default and `epsilon` 0 by default and optional, both expressed in canonical units and fixed at t = 0 (never co-moving), and ε > 0 runs must be tagged and never pooled with ε = 0 runs."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: Open
  verify:
    method: unit test
    detail: "defaults; co-moving option absent; ε>0 results carry a tag in provenance/sim key"
  rulings: []
  milestone: M3
- id: REQ-SYS-019
  area: SYS
  statement: "Nothing may be discarded: a trajectory that cannot be integrated must be recorded as a measurement outcome (undetermined), and no drift-based filtering of pixels is permitted."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.3 Failure is a measurement, not missing data
  verify:
    method: unit test
    detail: "Force an integration failure on a fixture pixel; assert it is stored with a failure/undetermined outcome and counted, not dropped or masked; no code path filters samples by drift."
  rulings:
  - R-47
  milestone: M3
  note: "Discarding is chaos-selective (escape fraction understated by up to 0.19)."
- id: REQ-SYS-020
  area: SYS
  statement: "The physics (EoM, regularisation, event detection, decoder) must be one Rust source compiled by rust-gpu to f32 SPIR-V (then WGSL for browser WebGPU) for the GPU and by rustc to f64 or higher for the CPU, with no second implementation on either side."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/design/principia_core_design.md
    section: "1. Physics defined once, compiled twice — now *structurally*, not by discipline"
  verify:
    method: review checklist
    detail: "both the GPU shader module and the CPU reference build from the same kernel crate; a grep finds no hand-written WGSL or duplicated Rust physics"
  rulings: []
  milestone: M4
- id: REQ-SYS-021
  area: SYS
  statement: "A resolve(ViewState, SimKey, RenderConfig) function running engine-side in Rust must produce the compute key and uniforms, the fragment key and uniforms, and the dispatch plan (quad list, per-quad bindings, workgroups)."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 1 — What lowering is
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  verify:
    method: unit test
    detail: "resolve on a fixture config returns all five outputs; resolve is Rust, not TS"
  rulings:
  - R-72
  milestone: M4
  note: "The fragment key hashes the stain graph's canonical form: REQ-RENDER-075 (definition)."
- id: REQ-SYS-022
  area: SYS
  statement: "No runtime interpreter of axis kinds, links, occupants or slots may exist on the GPU; chart map, decode, canonicalise, wrapper and integrator occupant must lower into the compute pipeline and the slots plus compositor into the fragment pipeline, meeting only through SimState/ICDescriptor."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 1 — What lowering is
  - file: docs/contracts/principia_lowering_contract.md
    section: Appendix — worked enumeration of the current chart set
  verify:
    method: review checklist
    detail: "kernel source has no switch on axis-kind/link/occupant enums at runtime (except the workgroup-uniform decode-mode branch)"
  rulings: []
  milestone: M4
- id: REQ-SYS-023
  area: SYS
  statement: "The compute pipeline must be one monomorphised Rust kernel generic over float type and chart/occupant, compiled by rust-gpu to SPIR-V and from the same source to the CPU f64 reference, with no runtime-authored custom compute occupants."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: "Part 2 — Two assembly mechanisms (the substrate split; the old \"one mechanism\" claim retires)"
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  verify:
    method: review checklist
    detail: "CPU reference and GPU kernel build from the same crate; no WGSL compute source exists"
  rulings: []
  milestone: M4
- id: REQ-SYS-024
  area: SYS
  statement: "Compute-side lowering must bake chart type, link selection, integrator occupant and the FTLE shadow co-computation as monomorphised variants, and must pass wrapper config (T, dt, N_max, r_*, eps_*, n_renorm) as SimUniforms, chart params (z0, q1, q2, slice values, curve tangent, invariant targets) as uniforms and the ensemble's copy_index as a uniform."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: decisions.md
    section: R-102 — The ensemble isn't a baked variant *(closes RQ-62)*
  verify:
    method: review checklist
    detail: "a navigation edit changes only uniform buffers; a tier change of N or FTLE selects a different pre-built variant; a change of E selects none"
  rulings:
  - R-102
  milestone: M4
- id: REQ-SYS-025
  area: SYS
  statement: "The physics kernel must be one Rust source, generic over `Real` and monomorphised, compiled to both the CPU-f64 build and the GPU-f32 build, with no second implementation of any physics or wrapper logic."
  source:
  - file: docs/design/principia_dd_integrator.md
    section: 1. What it is
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/contracts/principia_parity_contract.md
    section: Tier L — exact, no tolerance (the bulk of the suite)
  - file: docs/contracts/principia_parity_contract.md
    section: 1. The principle
  verify:
    method: review checklist
    detail: "inspect the source tree: exactly one implementation of step/wrapper/detectors, instantiated at f32 and f64; no GPU-only or CPU-only copy of regularisation or detector logic (parity Tier L part 1: verified by inspection, never by agreement of results)"
  rulings: []
  milestone: M4
- id: REQ-SYS-026
  area: SYS
  statement: "Continuous CPU/GPU divergence (positions, momenta, energy, and the outcome it eventually decides) must be exposed as the honest divergence and never reconciled, suppressed or tightened toward agreement."
  source:
  - file: docs/notes/principia_gpu_determinism_note.md
    section: "Why branches are special (continuous divergence is fine; branch divergence is not)"
  - file: docs/design/principia_dd_integrator.md
    section: 2. Consolidated contract
  - file: docs/contracts/principia_parity_contract.md
    section: "Tier B — integer-exact *given the same branch decisions* (integer & packed fields)"
  verify:
    method: review checklist
    detail: "no code path copies or snaps GPU state to CPU state; Tier N/S envelopes are not tightened below measured spread"
  rulings: []
  milestone: M4
- id: REQ-SYS-027
  area: SYS
  statement: "The prebake path must use uniform tiles with refinement switched off, and no scheduler, camera or frame budget."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: The prebake — a measuring instrument
  - file: docs/read_first/principia_00_philosophy.md
    section: 3. The consequence people get wrong
  verify:
    method: review checklist
    detail: "Prebake entry point advances every tile to t and stitches; asserts Policy/quadtree refinement is disabled and no camera/frame budget is consulted."
  rulings: []
  milestone: M4
  note: "Extended-precision/tiled prebake variants are parked (§7.1, §7.2)."
- id: REQ-SYS-028
  area: SYS
  statement: "The atlas, the IC inspector and the prebake must run the same kernel and the same charts, differing only in precision and scope."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: Why using the same kernel is load-bearing
  - file: docs/read_first/principia_00_philosophy.md
    section: "Three rungs, one kernel, and the standard of truth rises with the cost"
  - file: docs/read_first/principia_00_philosophy.md
    section: 1. The one-sentence version
  verify:
    method: review checklist
    detail: "One shared kernel crate is used by all three paths (no second formulation); a test runs one IC through the atlas path (f32) and the inspector path (f64) and asserts agreement inside the f32 horizon."
  rulings: []
  milestone: M4
- id: REQ-SYS-029
  area: SYS
  statement: "The thread-count values must be consistent across systems_architecture §5.5, memory_tiers §4, the scheduler and lowering contracts and the quality device note."
  source:
  - file: decisions.md
    section: "R-43 — `N = 16` vs 8 is measured; the thread-count inconsistencies are fixed now *(RS-3)*"
  verify:
    method: review checklist
    detail: "One constant for quad thread count in the shared source; all docs/config reference it."
  rulings:
  - R-43
  milestone: M4
  note: "R-43 fixes that the values agree; it doesn't state them (the N = 16 vs 8 choice is set by measurement)."
- id: REQ-SYS-030
  area: SYS
  statement: "The backdrop must never be read as data: lock must compute `z` from the current chart on the CPU, exports must read payloads, the inspector must integrate fresh, and the hover trace must draw only from current-identity payloads."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 5 — The stale backdrop: blur means loading"
  verify:
    method: review checklist
    detail: "no path reads the screen/backdrop texture into data; hover over backdrop shows no trace"
  rulings: []
  milestone: M5
- id: REQ-SYS-031
  area: SYS
  statement: "The main thread must never wait on the GPU, a readback or scheduler work; input → view-state update → uniform write → draw must be the only synchronous path and O(1)."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6 — The responsiveness invariant: the main thread never waits"
  - file: docs/contracts/principia_caching_contract.md
    section: Part 8 — The composed guarantee
  verify:
    method: benchmark
    detail: "under heavy scheduler load, main-thread frame/input latency shows no hitch (record max frame time)"
  rulings: []
  milestone: M5
- id: REQ-SYS-032
  area: SYS
  statement: "After any navigation, a current-identity picture must appear within a dispatch or two, and the canvas must never be blank."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: Part 8 — The composed guarantee
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 4 — Baseline-first: an absolute tier, not a priority weight"
  - file: docs/contracts/principia_caching_contract.md
    section: Principia — caching & display continuity contract
  verify:
    method: benchmark
    detail: "navigation fixture: count dispatches until first current cover; assert no blank frame"
  rulings: []
  milestone: M5
- id: REQ-SYS-033
  area: SYS
  statement: "QuadReduction must be the sole automatic GPU-to-CPU crossing; SimState and ICDescriptor must not leave the GPU in normal operation, and the only other pulls must be user-initiated async operations (decoded export, on-demand single-IC GPU trace)."
  source:
  - file: docs/contracts/principia_render_contract.md
    section: Part 1 — The payload (render input)
  verify:
    method: review checklist
    detail: "audit every buffer map/readback: only QuadReduction is read back per frame; other readbacks are behind a user action and async"
  rulings: []
  milestone: M5
- id: REQ-SYS-034
  area: SYS
  statement: "The frame loop must never await GPU work and must run on a dedicated render-loop thread, off the input/GUI thread (natively, a dedicated render thread)."
  source:
  - file: docs/contracts/principia_scheduler_contract.md
    section: Part 7 — The frame loop (lockstep presentation)
  - file: decisions.md
    section: "R-113 — The placement fixes are accepted as written *(closes RQ-93 to RQ-100)*"
  - file: decisions.md
    section: R-140 — The four readings are accepted *(closes RQ-118)*
  verify:
    method: review checklist
    detail: "the loop has no blocking await on GPU completion; it is hosted on a dedicated render thread, not the input/GUI thread"
  rulings:
  - R-113
  - R-140
  milestone: M5
  note: "R-113 (RQ-99 M5-5): the wasm-engine worker is REQ-SYS-039/049's (M8)."
- id: REQ-SYS-035
  area: SYS
  statement: "A gesture must never reach the compiler: code differences are baked, moving values are uniforms, instant things precompile and re-integrating changes compile lazily within their own cost."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 1. The rings — cross-cutting services
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 4 — The precompile rule
  verify:
    method: review checklist
    detail: "no gesture handler triggers pipeline compilation; all instant-toggle variants are precompiled at load"
  rulings: []
  milestone: M5
- id: REQ-SYS-036
  area: SYS
  statement: "The GPU↔CPU membrane must have exactly five crossings: dispatch uniforms (CPU→GPU), render config (slot uniforms, playhead t), bake (equirect texture), the QuadReduction (~80 B/quad, the sole automatic GPU→CPU return, feeding only the Allocation ring), and sanctioned user-initiated pulls (single-IC f32 trace, columnar export decode) that are tiny, async and latest-wins."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/design/principia_systems_architecture.md
    section: The return paths
  - file: docs/design/principia_systems_architecture.md
    section: 2. Component inventory, by rung
  verify:
    method: review checklist
    detail: "an audit of all buffer map/read calls finds only the reduction readback and the two sanctioned pulls"
  rulings: []
  milestone: M5
- id: REQ-SYS-037
  area: SYS
  statement: "The averaged pixel colour must never be read back as data, and Benettin shadows must never be coloured or enter the resolve pool."
  source:
  - file: docs/notes/principia_sampling_msaa_note.md
    section: The two firewalls (unchanged, restated for this model)
  - file: docs/notes/principia_sampling_msaa_note.md
    section: "Uniform samples: every sample is a full, normal SimState"
  verify:
    method: review checklist
    detail: "resolve pool indices exclude shadow state; no data reads of resolve output"
  rulings: []
  milestone: M5
- id: REQ-SYS-038
  area: SYS
  statement: "Decode mode (full vs linearised) must be a per-quad workgroup-uniform flag, not a baked variant, unless measurement shows the dead full-decode path hurts deep-quad occupancy, in which case it becomes a baked variant."
  source:
  - file: docs/contracts/principia_lowering_contract.md
    section: Compute side
  - file: docs/contracts/principia_lowering_contract.md
    section: Part 5 — The resolution function (the "switch", concretely)
  - file: decisions.md
    section: R-90 — The decoder switchover trigger *(closes RQ-41)*
  - file: docs/design/principia_deep_zoom.md
    section: 2. Linearised decoder — IC precision
  verify:
    method: benchmark
    detail: "measure deep-quad occupancy/register pressure with the two-path branch vs a baked linearised variant; record the result"
  rulings:
  - R-90
  milestone: M6
  note: "The switch fires when the full decoder's adjacent samples give bitwise-identical ICs, or past SWITCH = ℓ_switch = 20 (an upper bound), whichever comes first (R-90)."
- id: REQ-SYS-039
  area: SYS
  statement: "The whole frame loop (WebGPU device, scheduler, cache, quadtree, compute and render) must run in the wasm engine in a Web Worker via `OffscreenCanvas`, while the main thread is a separate TS binary that owns no simulation state."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: review checklist
    detail: "build produces two binaries; main-thread bundle contains no engine state"
  rulings: []
  milestone: M8
- id: REQ-SYS-040
  area: SYS
  statement: "Input deltas must reach the engine through a SharedArrayBuffer written with `Atomics` and read at the top of every frame (pointer events coalesced), and discrete settings through `set_field(path, value)`."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: unit test
    detail: "burst of pointermoves produces one SAB state, not queued messages"
  rulings:
  - R-52
  milestone: M8
- id: REQ-SYS-041
  area: SYS
  statement: "The input backend must be chosen once at boot — SAB when cross-origin isolated, otherwise one coalesced `postMessage` per main-thread rAF tick — behind the same engine interface."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: unit test
    detail: "boot without isolation selects postMessage; engine code path identical"
  rulings: []
  milestone: M8
- id: REQ-SYS-042
  area: SYS
  statement: "Cross-origin isolation on GitHub Pages must come from a self-hosted, same-origin `coi-serviceworker` beside `index.html`."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: review checklist
    detail: deployment contains the script; no CDN
  rulings: []
  milestone: M8
- id: REQ-SYS-043
  area: SYS
  statement: "The engine must post only a GUI-sized state snapshot to the main thread, throttled to about 10 Hz, never per frame and never engine-sized data, and the GUI (egui) must redraw at frame rate from the latest snapshot."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  - file: decisions.md
    section: R-94 — The GUI snapshot is ~10 Hz *(closes RQ-45)*
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  verify:
    method: unit test
    detail: "message rate ≤ ~10 Hz; payload contains no quad tree or payload data; the GUI repaints every frame between snapshots from the last one received"
  rulings:
  - R-94
  milestone: M8
- id: REQ-SYS-044
  area: SYS
  statement: "Click-to-inspect must send cursor coordinates to the engine, which runs the f64 re-integration fire-and-forget in the inspector worker (a second wasm context) and posts the result back."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: review checklist
    detail: "inspector runs in a separate worker; main thread never blocks"
  rulings: []
  milestone: M8
- id: REQ-SYS-045
  area: SYS
  statement: "Resize must be observed on the main thread (`ResizeObserver`), sent as one message, and reconfigure the swapchain in the worker."
  source:
  - file: docs/contracts/principia_caching_contract.md
    section: "Part 6a — The threading model: the render loop lives in a worker (and that worker is the wasm engine)"
  verify:
    method: unit test
    detail: resize event yields one message and one reconfigure
  rulings: []
  milestone: M8
- id: REQ-SYS-046
  area: SYS
  statement: "Principia must be a browser-based WebGPU application, publicly released via GitHub Pages, that renders the planar three-body problem's 8-dimensional symmetry-reduced IC manifold itself as the image, each sample being one IC coloured by the fate/diagnostics of its trajectory."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 0. What Principia is
  verify:
    method: review checklist
    detail: "the release build loads from the GitHub Pages site in a WebGPU browser and renders an IC-manifold slice whose samples are ICs coloured by outcome"
  rulings: []
  milestone: M8
- id: REQ-SYS-047
  area: SYS
  statement: "The whole CPU engine (scheduler, cache, quadtree, allocation ring, physics reference) must be Rust compiled to wasm, and the GUI must be a thin TypeScript shell owning only DOM and view-scratch state."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  verify:
    method: review checklist
    detail: "the engine crate builds to wasm and contains all CPU-side state; the TS shell holds no scheduler/cache/physics state (code review of the TS package)"
  rulings: []
  milestone: M8
- id: REQ-SYS-048
  area: SYS
  statement: "The native build must be able to run the survey headless from the same kernel as the browser build, producing the Paper-2 numbers."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: 1. The substrate (the defining decision)
  verify:
    method: unit test
    detail: "a native headless survey run completes without a window and writes its outputs"
  rulings: []
  milestone: M8
- id: REQ-SYS-049
  area: SYS
  statement: "The entire frame loop — playhead, barrier, scheduler, physics and all sim state — must run in the wasm engine inside a Web Worker via OffscreenCanvas, with the main thread only a TS input pump and DOM-GUI host, and a second wasm context as the f64 inspector worker."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  - file: docs/design/principia_temporal_architecture_note.md
    section: "The frame loop — the one genuinely new object (and what makes lockstep clean)"
  verify:
    method: review checklist
    detail: "the main-thread bundle contains no engine state; two worker wasm instances exist (engine, inspector)"
  rulings: []
  milestone: M8
- id: REQ-SYS-050
  area: SYS
  statement: "Field edits must reach the engine worker via SharedArrayBuffer when cross-origin-isolated and via postMessage otherwise, and snapshots back must be throttled to about 10 Hz."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: decisions.md
    section: R-94 — The GUI snapshot is ~10 Hz *(closes RQ-45)*
  verify:
    method: unit test
    detail: "both transports deliver the same edit sequence; snapshot rate is capped"
  rulings:
  - R-94
  milestone: M8
- id: REQ-SYS-051
  area: SYS
  statement: "Big data must never cross either membrane: the payload, quad tree and reductions are only summarised across GPU↔CPU and wasm↔JS, and each is a data boundary — the GUI holds no wasm handles, only the last snapshot."
  source:
  - file: docs/contracts/principia_canonical_spec.md
    section: "6. Memory & deployment model *(authoritative: `memory_tiers`, `caching_contract`, `deep_zoom`, `systems_architecture`)*"
  - file: docs/contracts/principia_canonical_spec.md
    section: "9. The load-bearing invariants (the walls — the primary comparison checklist)"
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/design/principia_systems_architecture.md
    section: 6. Cross-cutting invariants (the load-bearing walls)
  verify:
    method: review checklist
    detail: "no wasm-bindgen object handle is exported to JS; snapshot payload size is GUI-sized"
  rulings: []
  milestone: M8
- id: REQ-SYS-052
  area: SYS
  statement: "The engine crate must mark only the state surface (the SimConfig / RenderState / ViewUI schema, set_field and the snapshot) pub, keeping the scheduler, cache, kernel and payload buffers crate-private so the GUI crate cannot reach past it."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  - file: decisions.md
    section: R-146 — The crate layout is confirmed *(closes RQ-76)*
  verify:
    method: unit test
    detail: "compile-fail test (trybuild): a gui-crate snippet naming the scheduler, cache, kernel or a payload buffer fails to compile"
  rulings:
  - R-146
  milestone: M8
- id: REQ-SYS-053
  area: SYS
  statement: "The engine must never import the GUI: the dependency arrow must point GUI → state → engine only."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  verify:
    method: review checklist
    detail: "cargo tree for the engine crate lists no gui crate and no egui dependency"
  rulings: []
  milestone: M8
- id: REQ-SYS-054
  area: SYS
  statement: "The entire engine must be drivable from a JSON state file with no GUI attached."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  verify:
    method: unit test
    detail: "headless test loads a SimConfig + RenderState JSON, runs frames and produces an image with no GUI crate linked"
  rulings: []
  milestone: M8
- id: REQ-SYS-055
  area: SYS
  statement: "The GUI↔engine boundary must be a data boundary under both bindings: set_field(path, value) of plain serialised data in, a serialised snapshot out, with the TS binding holding no Rust handles, only the last snapshot."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  verify:
    method: review checklist
    detail: "review the wasm-bindgen layer: exported functions take/return serialised data only; no exported struct with callable methods reaches the scheduler or other internals"
  rulings: []
  milestone: M8
- id: REQ-SYS-056
  area: SYS
  statement: "The canvas must be the only non-data crossing: under the TS binding the OffscreenCanvas is transferred to the engine worker once at startup and never again, and no state may ride on the canvas under either binding."
  source:
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  verify:
    method: review checklist
    detail: "review the TS startup path: one transferControlToOffscreen / postMessage transfer, no later canvas messages carrying state"
  rulings: []
  milestone: M8
- id: REQ-SYS-057
  area: SYS
  statement: "With no suitable GPU the application must fall back to the CPU path, indicate it loudly and permanently in the UI, and record the reason (no adapter / adapter lacks required features / backend init failed) in telemetry."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.1 No suitable GPU, or no GPU at all
  verify:
    method: unit test
    detail: "simulate each failure: CPU path runs, UI banner shows, telemetry reason distinguishes the three"
  rulings: []
  milestone: M8
- id: REQ-SYS-058
  area: SYS
  statement: "On device loss the application must reinitialise, rebuild from the cache metadata and say what happened."
  source:
  - file: docs/design/principia_dd_telemetry_and_tiers.md
    section: 6.3 Device loss, timeouts, driver resets
  verify:
    method: property test
    detail: "inject device loss: payloads rebuilt are byte-identical and a notice is shown"
  rulings: []
  milestone: M8
- id: REQ-SYS-059
  area: SYS
  statement: "The wasm↔JS membrane must have exactly three crossings: set_field(path, value) as the GUI's only write, a GUI-sized (never engine-sized) state snapshot throttled to about 10 Hz, and the OffscreenCanvas handle transferred once at startup."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: "3. The membrane — the deployment view (demoted, not diminished)"
  - file: docs/design/principia_systems_architecture.md
    section: 5. The seam catalogue
  - file: decisions.md
    section: R-94 — The GUI snapshot is ~10 Hz *(closes RQ-45)*
  verify:
    method: review checklist
    detail: "the wasm exports are set_field, snapshot and the one-time canvas init"
  rulings:
  - R-94
  milestone: M8
- id: REQ-SYS-060
  area: SYS
  statement: "Trajectory integration and sonification must run in the dedicated inspector worker, separate from the render-loop worker, and never block the GPU survey, the frame loop or the main thread."
  source:
  - file: docs/design/principia_trajectory_viewing.md
    section: 1. The single mechanism
  verify:
    method: benchmark
    detail: "frame times with continuous hover + listen equal the no-hover baseline within noise; the work runs on the inspector worker"
  rulings: []
  milestone: M8
- id: REQ-SYS-061
  area: SYS
  statement: "The interactive atlas must state its predictability horizon, and nothing downstream may treat an atlas pixel past the horizon as a result."
  source:
  - file: docs/read_first/principia_00_philosophy.md
    section: The interactive atlas — a hypothesis generator
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.1 The instrument must say where it stops knowing
  - file: docs/read_first/principia_00_philosophy.md
    section: 3. The consequence people get wrong
  verify:
    method: review checklist
    detail: "Atlas outputs carry the horizon (field) with them; any export or statistic built from atlas data is labelled as exploratory."
  rulings: []
  milestone: M8
- id: REQ-SYS-062
  area: SYS
  statement: "Every GUI control must read the state Snapshot and emit a typed SetField, and no GUI element may touch simulation internals, hold a &mut into engine state, hold sim logic, cache sim data or compute anything the engine needs."
  source:
  - file: docs/gui/principia_render_gui_spec.md
    section: G1. Rules that hold everywhere
  - file: docs/gui/design/GUI_DESIGN_NOTES.md
    section: Rules that hold everywhere
  - file: docs/contracts/principia_gui_state_contract.md
    section: 1. The one-way dependency rule
  - file: docs/contracts/principia_gui_state_contract.md
    section: "7. What a replacement GUI must honour (the teardown contract)"
  verify:
    method: review checklist
    detail: "grep the gui crate: no engine type other than the state surface is named; no &mut engine borrow; every edit path ends in set_field"
  rulings: []
  milestone: M8
- id: REQ-SYS-063
  area: SYS
  kind: definition
  statement: "The constants register — where every settled constant is declared with its value, its citation (corpus section or prin-rs FINDINGS/results path) and its admissibility class (philosophy §4.2), and whether the register is part of the ledger hashed into the schema version (R-36) — must be defined in dd_generation_root §3.8 by the task that needs it and reviewed by the physics reviewer before merge."
  source:
  - file: docs/design/principia_dd_generation_root.md
    section: 3.8 Metadata schema (what every entry must carry)
  - file: docs/read_first/principia_INDEX.md
    section: The evidence base — where settled defaults were measured
  - file: docs/read_first/principia_00_philosophy.md
    section: 4.2 Every constant must be derived or absent
  - file: decisions.md
    section: R-36 — Schema version = content hash of the ledger *(PL-1)*
  - file: decisions.md
    section: "R-72 — A missing definition is written by the task that needs it *(closes RQ-46 to RQ-55, definitions)*"
  verify:
    method: review checklist
    detail: "the section gives the register's entry fields (value, citation, admissibility class) and states whether it is hashed with the ledger; physics reviewer approved"
  rulings:
  - R-36
  - R-72
  milestone: M0
- id: REQ-SYS-064
  area: SYS
  kind: definition
  statement: "The crate map (systems_architecture §7.1) must assign every node of the §7 build DAG to one crate of the R-146 layout and list the allowed workspace edges, each with the §7 arrow it realises, and must be confirmed by the human before TASK-M0-01 starts."
  source:
  - file: docs/design/principia_systems_architecture.md
    section: 7.1 Crate map
  - file: docs/design/principia_systems_architecture.md
    section: 7. Hierarchy and dependency (the build DAG, abstract)
  - file: decisions.md
    section: R-170 — The crate map *(closes G2)*
  - file: decisions.md
    section: "R-185 — The crate map is confirmed; kernel → ledger is a build-dependency only *(closes TASK-M0-00)*"
  verify:
    method: review checklist
    detail: "every §7 node appears in the §7.1 table with one crate; every allowed edge names its §7 arrow; the human's confirmation is recorded in decisions.md"
  rulings:
  - R-146
  - R-170
  - R-172
  - R-185
  milestone: M0
- id: REQ-SYS-065
  area: SYS
  statement: "GPU CI must run on two backends on every commit, on GitHub-hosted runners (R-186): macos-15 (Apple silicon, paravirtual Metal) for the Metal correctness suites and ubuntu-latest with Mesa lavapipe (Vulkan), the harness taking its backend from PRIN_GPU_BACKEND=metal|vulkan; the first check on macos-15 must show a Metal adapter and the M0 fixtures within tolerance, or the build stops for a REVIEW_QUEUE entry proposing a self-hosted runner. R-174's same-repository guard is dormant while no self-hosted runner exists."
  source:
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: "R-169 — The GPU CI jobs, and an install step for every toolchain *(closes G1, H5)*"
  - file: decisions.md
    section: "R-174 — The self-hosted runner runs only this repository's code *(closes H1)*"
  - file: decisions.md
    section: "R-186 — GitHub-hosted runners first; no self-hosted runner *(amends R-110, R-169, R-174)*"
  verify:
    method: review checklist
    detail: "the CI log shows gpu_harness and metal_hosted_probe green on macos-15 and gpu_harness green on lavapipe, with the named backends; no job uses a self-hosted runner; an unknown PRIN_GPU_BACKEND fails naming the variable"
  rulings:
  - R-110
  - R-169
  - R-174
  - R-186
  milestone: M0
- id: REQ-SYS-066
  area: SYS
  statement: "Every reviewer role a task file names must record its verdict as a PR review headed `VERDICT: APPROVE <role>` or `VERDICT: CHANGES <role>`, and the `reviews-complete` CI check must pass only when every named role has approved on the PR's latest commit with no later CHANGES verdict."
  source:
  - file: decisions.md
    section: "R-175 — The reviewers are agents, and a CI check counts their verdicts *(closes H3)*"
  verify:
    method: unit test
    detail: "reviews-check fixtures: all roles approved on head passes; an approval on an older commit, an APPROVE superseded by CHANGES, and a missing role each fail naming the role"
  rulings:
  - R-175
  milestone: M0
- id: REQ-SYS-067
  area: SYS
  statement: "CI must run at the cadence R-110, R-177 and R-186 set: ci.yml runs `cargo xtask ci` (plan-check included) on every push; nightly.yml, scheduled, runs the CPU and lavapipe suites (and the survey from M8), not benchmarks; screenshot.yml runs screenshots on GUI PRs (those touching crates/gui/** or docs/gui/**); gate.yml, dispatched with a milestone, runs every hosted suite and writes the gate report. Benchmarks and performance gates run only on the human's own Mac via prin profile, at gates and on demand."
  source:
  - file: decisions.md
    section: "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
  - file: decisions.md
    section: R-177 — Cadence *(closes G4, C6)*
  - file: decisions.md
    section: "R-186 — GitHub-hosted runners first; no self-hosted runner *(amends R-110, R-169, R-174)*"
  verify:
    method: unit test
    detail: "the gate-report fixture lists every requirement of the milestone's and earlier gate blocks, fails on a missing one, and marks benchmark requirements awaiting the human's run until their prin profile files are supplied; review: no workflow runs bench, nightly.yml is scheduled, gate.yml takes a milestone input"
  rulings:
  - R-110
  - R-134
  - R-177
  - R-186
  milestone: M0
